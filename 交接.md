# LenBot V3 计划 — Production Closure, Social Evaluation & Extension Foundation

> 撰写时间:2026-09-04
> 基线:commit `f4fbf63`(V2 全量交付,**91 passed / 0 failed**)
> 状态:**计划已定稿,尚未开始实施**(此前一次已开工的改动已全部撤回,工作区干净)

---

## 一、V3 定位

V2 已完成主体架构(Immutable Events / SceneActor / Attention / Pi cognition / Atomic Proposal Commit / Task / OpenLoop / Ambient / Typed Memory / Reflection / Provider Registry / Plugin Host / Trace / Replay / Shadow Mode / Vue Control Plane)。V3 **不允许重新设计这些模块**,目标是:

> 把 Feature Complete 的 V2 收敛成可进入真实 QQ 群 Shadow Testing 的稳定 Runtime,并建立下一阶段外部感知与 Agentic Tool 扩展的最小正确基础。

V3 第一步是**做减法**:先删除 V2 中错误或重复的抽象,禁止先开发新功能。

### 核心原则(全文适用)

1. **禁止兼容性补丁**:旧抽象错误直接替换;禁止双轨逻辑/legacy fallback/dead API;允许删除无必要能力。
2. **禁止过度设计**:复用既有 Event/SceneActor/RuntimeGate/SQLite/PluginHost/ProviderRegistry;禁止 Supervisor/Planner Agent、Social Graph、Workflow Engine、Message Broker、Vector DB、第二个 Runtime 等。
3. **Runtime State 唯一 authority**:Event/SceneState→SceneActor;Episode Proposal→RuntimeGate;Task→Gate/Scheduler;OpenLoop→Gate/OpenLoop authority;Memory→Gate/Atomic Commit;Provider→Registry;Plugin lifecycle→PluginHost。
4. **LLM 只产生 Proposal**:不能发消息/建 Task/改 SceneState/写 Memory/resolve OpenLoop/跨 Scene 授权/操作 Plugin lifecycle。
5. **外部作用可观察可追踪可阻止**:每个真实副作用必须能回答——为什么发生?来自哪个 Event/Episode?哪个 authority 批准?产生什么效果?

---

## 二、缺陷核实结论(探索已逐项确认,含行级证据)

V3 计划文档的所有缺陷断言核实**全部属实**,另有 4 项新发现。此为实施依据:

| # | 缺陷 | 关键证据 |
|---|------|---------|
| 1 | Memory `visibility` 双 authority,LLM 可逃逸 scope | `memory/store.py` 检索 SQL `OR visibility='global'`;`memory/reflector.py` prompt 提供 `"visibility": "scene|global"` 且原样采纳模型输出;`commit_proposal_transaction` 只强制 scope 从不触碰 visibility |
| 2 | OpenLoop 跨 scene resolve fallback | `events/store.py` 事务 step 3 的 `UPDATE ... WHERE id=?`(无 scene_id),模型给任意 loop id 即可越权 resolve |
| 3 | `state_annotations: dict[str, Any]` 任意入 reducer | reducer 只懂 topic/close_thread/thread_status 三个键;非法 `thread_status` 字符串会让 `ThreadStatus()` 抛 ValueError 打穿 actor |
| 4 | Reflection cursor 跳批 | `get_events_since` 默认取 200 → reflector 只消费末 30 → cursor 推进到 200,**最老 170 条永不反思** |
| 5 | Reflection 非原子 | episode 先 INSERT;提案 gate 拒绝仍推进 cursor;中途异常 cursor 不动但 episode 已存 → 重试产生重复 episode(无去重) |
| 6 | **Reflector 从未在启动时接线**(比预想严重) | `has_live_provider` 探测发生在空 registry 的 `apply_update` 之前,永远 False → 生产一直走确定性 fallback |
| 7 | Quiet window 永久阻塞 | postpone 条件 `bot_engagement=="active"`,而 engagement 只在新消息到达时衰减 → 安静场景永久卡 active;从不检查 `SceneActor.has_active_episode()` |
| 8 | Memory 校验双实现不一致 | MemoryGate(反射路径,多 commit 非原子,信任调用方 scope)vs 事务内联(强制 scope);gate 的 evidence 合并被 `save_memory` upsert **静默丢弃**(DO UPDATE 不含 evidence) |
| 9 | ParticipationThread 不可达分支 + 无时间衰减 | reducer 先更新 `last_relevant_at` 再判状态,`elif >300s: CLOSED` 永不可达;无任何定时衰减 |
| 10 | participant 单独即 WAKE | `attention/engine.py` OR 链:`is_participant or has_topic_match or is_immediate_adjacency` |
| 11 | **带 thread 的 Scene 页面 AttributeError** | `query_service` 读不存在的 `ParticipationThread.bot_consecutive_messages` 字段 |
| 12 | 插件工具 last-write-wins 覆盖核心工具名 | `register_plugin_tool` 无重复检查;plugin 优先于 builtin(execute 先查 plugin_host) |
| 13 | 插件生命周期缺陷 | poll task 在 `on_load` 启动(与 enabled 无关);`disable` 不取消 task;disabled 仍可 emit |
| 14 | read_page 无 SSRF 防护 | 仅 scheme 检查;`follow_redirects=True` 盲随;无内网/环回/DNS 校验;无字节上限 |
| 15 | Provider 缺校验 | routing model 可为空;target 可 disabled;禁用在用 provider 无保护 |
| 16 | Shadow 期 Task 切回 live 后**物理发送** | origin 无处标注(`TaskItem/ActionItem` 无 origin_mode);ActionQueue 只看实时 probe |
| 17 | `human_messages` 按 stimulus 计数 | burst 3 条消息 → 1 stimulus → +1(应为 +3) |
| 18 | Mailbox 收一切事件 | Bot 自己的 MESSAGE_SENT 含"停"字会**自我取消** episode;TASK_DUE 描述含取消词同样误触 |
| 19 | `bot_cooldown_seconds` 死配置 | 全仓无 runtime 读者;SpeakingBudget 的 recency 惩罚(60s/180s)是实际生效语义 |
| 20 | 认证 Bearer+localStorage | login 返回 JSON token,前端 localStorage 存放,XSS 可窃取 |
| 21 | OneBot reply 靠 CQ 串猜测、无自回声过滤、无 reply message id 捕获 | `[CQ:reply` + `qq=<bot_qq>` 子串匹配;adapter 层无 self 过滤 |
| 22 | Condition task 无 durable claim | matching event 提交后 crash → 任务仍 pending → 匹配条件被遗忘(仅剩 7 天 deadline);周期 sync 还会重发 TASK_DUE |
| 23 | Control Plane 读接口创建 actor | `scene_detail` 用 `get_or_create_actor`,查历史 scene 会启动新 worker |

**已定决策**:
- `bot_cooldown_seconds` **删除**(选项 B:死配置,保留即重复 authority;SpeakingBudget recency 已覆盖语义)。
- **BilibiliContentPlugin 本轮包含**(作为扩展架构验证,Tool 型,公开 API,mock 测试)。
- NapCat fixture 为 OneBot v11 规范推导样本;真实 NapCat 样本验证列入 Shadow 期清单(如实声明)。

---

## 三、实施阶段(每阶段 pytest 全绿后推进;ADR 先行;测试全部确定性——Event/barrier/受控 future/mock DB failure,禁 sleep 证明 race)

### Stage 1 — 减法(§2/§3/§4/§6/§19.2/§23)

1. **删除 memory visibility(§3)**:models 删字段;`query_memories` SQL 严格 `scope IN allowed_scopes`,删 `OR visibility='global'`;gate/事务/reflector(prompt 移除 "scene|global")/query_service 全去 visibility;旧库 `ALTER TABLE memories DROP COLUMN visibility`(幂等);相关测试重写为纯 scope 语义。
2. **PromoteMemory(§3.3)**:`MemoryStore.promote_memory(memory_id)` —— **新建** `scope='global-safe'` 记录(subject/kind/key/value/certainty/evidence 保留,assertion 前缀 `promoted_from:<id>:`),原记录不动(绝不原地改 scope);`POST /api/cockpit/memories/{id}/promote` + Memory Explorer 按钮。Reflection/cognition 无晋升权限。
3. **删除 Plugin SCHEDULE_TASK(§4)**:`PluginPermission.SCHEDULE_TASK`、`PluginContext.schedule_task` 删除(已核实无使用者)。Plugin 能力只余 Sensor(只发 Event)/Tool(被动执行)/Interceptor(allow/modify/block)三类。
4. **删除 bot_cooldown_seconds(§19.2B)**:config 字段、settings 路由与请求模型、持久化键、恢复行、replay allowlist、测试。
5. **thought → decision_reason(§6)**:字段改名;assembler schema 给出行为理由示例("peer already answered…"风格);pi_core 9 处合成、gate 理由、trace 键名;**Trace 只存 decision_reason,不存任何 chain-of-thought**;~17 个测试文件机械更新。
6. **Cookie-only 认证(§23)**:`get_current_user` 删 Bearer;login 不再返回 token;cookie `secure=config.dashboard_cookie_secure`(新配置,默认 False);`api.js` 删 localStorage;8 处 Bearer 测试改 cookie 会话(httpx 自动携带)。

### Stage 2 — Authority 闭合(§5/§7/§13)

7. **SocialStateProposal(§5)**:`ThreadTransition(StrEnum: KEEP/FADE/CLOSE)` + `SocialStateProposal(topic, thread_transition)`;`EpisodeOutcome.social_state_proposal` 替换 `state_annotations: dict`(删除);assembler prompt schema 显式列出(§9.4);actor 在 gate **accepted** 后发 STATE_ANNOTATION(metadata typed `social_state`),reducer 只消费两个键——非法字符串被 enum 在模型层拒绝。`GateDecision` 增 `accepted: bool`;拒绝(stale/cancelled/transaction 失败)→ **零 Task/Memory/loop/social-state/Action**(§5.1)。
8. **OpenLoop authority(§7)**:删 id-only fallback;`UPDATE ... WHERE id=? AND scene_id=? AND status='active'`,rowcount==0 → raise → 整事务回滚。
9. **统一 Memory 校验(§13)**:新 `memory/writes.py` —— `validate_memory_proposal(db, mp, scene_id)`(evidence 非空/去重/存在于本 scene events∪episodes/kind∈MemoryKind/key 非空)+ `commit_memory_proposal_core`(槽位冲突 resolve 纯 SQL,不 commit);三条路径(事件事务/MemoryGate/Stage5 batch)共用且**都强制 `mp.scope=scene_id`**;修 `save_memory` upsert 丢 evidence 的 bug。

### Stage 3 — Semantic Staleness 闭合(§8/§20)

10. **Mailbox**:`post()` 只收 GROUP/PRIVATE_MESSAGE_RECEIVED(根治自取消/TASK_DUE 误取消/回声);新增非破坏性 `has_unseen_interim()`。
11. **pi_core 末步 fail-closed(§8.1)**:late interim 在最终 step → discard → SILENCE(镜像 follow-up 处理;现状是 stale outcome 原样返回,且 cursor 已推进导致事件"被看过却没用")。
12. **Gate 最后窗口(§8.2)**:cancel/follow-up 后增 `has_unseen_interim()` → stale 拒绝 + `stale_outcomes_rejected++`。**不使用 SceneVersion equality**(§8.3,system event/annotation/plugin fact 都会改 version;base_version 仅保留给 lease)。
13. **Metrics 修正(§20)**:`human_messages` 按 committed human message Event 计数(burst 3→+3,stimuli 计数独立);去重 cancellations_honored(只留 gate);新增 `followups_incorporated`/`openloops_resolved`/`obligations_fulfilled`/`unsolicited_visible_messages`/`stale_outcomes_rejected`。

### Stage 4 — Participation 修正(§9/§22.1)

14. **Reducer 生命周期**:显式 FADE_AFTER=120s/CLOSE_AFTER=300s 常量;先判 CLOSE 再判 FADING;`last_relevant_at` 仅在真相关时更新(mention/reply/private/bot 消息/topic 词面命中),participant-only 计入 intervening。
15. **Attention 语义(§9.2/9.3)**:continuation WAKE 需 `topic_match OR immediate_adjacency`;participant 单独 → OBSERVE(`participant_off_topic`)——membership 只是置信证据,不独立证明连续性。
16. **时间衰减**:maintenance loop 扫描已加载 scene 的 active thread,超阈值 → FADING/CLOSED(注入时钟测试)。
17. **控制面修复(§22)**:`scene_detail` 改读 `event_store.load_scene_state`(纯读,不 `get_or_create_actor`);修复 `bot_consecutive_messages` AttributeError;schema 级 API 测试覆盖 empty/thread/active/historical 四形态。

### Stage 5 — Reflection 生产闭合(§10/§11/§12)

18. **启动顺序(§10.1)**:EventStore → MemoryStore → **Provider Registry 加载/迁移** → Reflector → ReflectionEngine → 启动;生产+有 provider 时断言 `llm_reflector is not None`。
19. **批量游标(§11)**:`REFLECTION_BATCH_SIZE=30`;循环取 `get_events_since(cursor, limit=30)`,reflector 消费恰为整批(删内部切片),batch 原子提交成功后 cursor 推进到本批最大 rowid;满批继续(单次 fire 上限 10 批防 LLM 成本失控);空批停。
20. **原子性(§12)**:`MemoryStore.commit_reflection_batch(record, proposals, scene_id, last_rowid)` —— 先逐条 validate(非法剔除属 authority 决定,记录日志),同一事务(write_lock)内写全部提案(resolve 冲突)→ INSERT episode → upsert cursor → commit;任何 DB 失败整体回滚 cursor 不动;"cursor advanced but episode missing" 不可能存在。
21. **Quiet window(§10.2)**:postpone 条件改 `SceneActor.has_active_episode()`;Bot 最后发言后安静即反射。

### Stage 6 — Obligation v2.1 与 Shadow origin(§14/§21)

22. **wake_match(§14.1)**:`TaskItem/TaskProposal.wake_match: dict`(如 `{"room_id": 123}`);tasks 表列;匹配 = type + scene + payload 字段全等;**无 DSL/脚本/任意谓词**。
23. **Durable claim(§14.2)**:tasks 加 `trigger_event_id`;`scheduler.on_event` 在 write_lock 下 `UPDATE tasks SET status='claimed', trigger_event_id=? WHERE id=? AND status='pending'` → rowcount=1 才 emit TASK_DUE;`_sync_from_db` 启动时对未发射 claimed 重发(内存 set 防重);TASK_DUE commit → triggered;crash → 重启恢复完成义务。
24. **Shadow origin(§21)**:`TaskItem.origin_mode`('live'|'shadow')列,gate 经 `origin_mode_provider` 注入;`ActionItem.origin_mode`;TASK_DUE payload → `Stimulus.origin_mode` → ProposalCommit → actions 全链传递;ActionQueue:`shadow_probe()` **或** `origin_mode=='shadow'` → would-send(切回 live 也不会意外发送);`POST /api/cockpit/tasks/{id}/promote`(scheduler authority)→ origin=live + 前端按钮;Memory 在 shadow 期照常沉淀(正常 scene scope)。

### Stage 7 — Plugin & Provider 闭合(§15/§16/§17/§18)

25. **生命周期(§15)**:`load_plugin` on_load 后若 enabled 调 on_enable;BilibiliLiveSensor poll 移入 on_enable、on_disable 取消、on_unload 保证停止+关 client;`emit_event` 拒绝 disabled 插件(disabled = zero poll/zero event/zero background work)。
26. **工具命名空间(§16)**:`RESERVED_CORE_TOOLS = {search_messages, read_context, query_timeline, query_person_history, query_memory, inspect_episode}`;撞保留名或跨插件同名 → ValueError → load 失败进 error 状态(禁 last-write-wins);补 `inspect_episode` 核心工具。
27. **网络沙箱(§17)**:新 `plugins/net_policy.py` —— scheme 白名单 + `getaddrinfo` 全地址校验(loopback/private/link-local/multicast/unspecified/reserved,含 IPv4-mapped IPv6,任一非法即拒);`follow_redirects=False` + 手动重定向循环(每跳重新校验,≤3 跳);流式读取字节上限(2MB)+ 提取截断;web_search/read_page 全走策略。
28. **Provider 闭合(§18)**:apply_update 增 routing model 非空、target enabled(同时覆盖禁用在用保护——除非同次 update 已切走);hot swap 语义不变(下一 ReAct step/下一次 reflection 调用即生效)。

### Stage 8 — 控制面/运维/Shadow 评估(§19.1/§28/§29/§34)

29. **monitored_keywords 真同步(§19.1)**:`AttentionEngine.set_monitored_keywords()`(写 topic_keywords["monitored"] + topics 权重);settings 路由与启动恢复都调用;restart 语义一致。
30. **Shadow 标注数据集(§28)**:表 `shadow_annotations(id, target_type('would_send'|'trace'), target_id, label, note, created_at)`;would-send 用 action.id 作稳定 target;`POST /api/cockpit/shadow/annotate`(枚举 GOOD/UNNECESSARY/STALE/TOO_EARLY/TOO_LATE/WRONG_THREAD/WRONG_PERSON/STYLE_BAD;trace 目标支持 SHOULD_HAVE_SPOKEN);GET 联表;前端最小标注按钮。一张表,不做复杂标注系统。
31. **Behavioral Trace(§29)**:episode trace 增情境使用计数(ambient/memories/openloops)与 decision_reason;Event→Stimulus→Scene snapshot→Attention→Memory/Loop/Ambient→Model route→Tools→decision_reason→Gate→would-send/actual→human evaluation 全链可看;不存私有推理流。
32. **NapCat 闭合(§34)**:adapter 解析 `[CQ:reply,id=X]`/段数组 reply id → `reply_to_message_id`;发送路径维护 `recent_bot_outbound_ids` ring;`reply_bot` = reply_to_message_id ∈ ring(**删除 CQ 串猜测**);adapter 层丢弃 `user_id == bot_qq` 入站(防 MESSAGE_SENT+echo 双社交事实);`tests/fixtures/onebot_samples.py` 规范样本 + 集成测试;真实 NapCat 样本验证列入 Shadow 期。

### Stage 9 — Scenario M、分层、Layer 3 评估、BilibiliContentPlugin、文档

33. **Scenario M(§24,Goal 11 正式进验收)**:seed group:X 的 organizer/group_norm/bot-in-group social_pattern,group:Y 无;同 actor 同句两 scene 唤醒 → X 的 prompt 含 social memories、Y 不含且零跨组泄漏。
34. **基准分层(§26)**:AGENTS.md 增三层分类——Layer 1 不变量测试(rollback/scope/single-writer/plugin disable/SSRF/claim recovery/stale gate)、Layer 2 行为管道(mock cognition,A–M Scenario 属此)、Layer 3 模型评估(真实模型,不进 CI);测试文件头标注归属。
35. **Layer 3 评估(§26)**:`scripts/eval_transcripts.py`:固定 transcript(JSONL)→ ReplayLab + 真实 provider(无 mock,需 key)→ 输出每条 Attention/Prompt/Outcome/Would-send/decision_reason JSON 供人工标注;手动命令,不进 CI。
36. **BilibiliContentPlugin(§31)**:`plugins/builtin/bilibili_content.py`,Tool 型:get_video_info(公开 view API)、search_bilibili(公开搜索端点,需凭据优雅报错)、get_dynamic_feed(需 cookie 配置,缺省报错);仅 cognition 主动调用、走 net_policy 与沙箱;NEW_BILIBILI_DYNAMIC Sensor 留待后续(§31.2 可选);不与 LiveSensor 混成万能插件;测试 mock httpx。
37. **文档**:AGENTS.md 新模式与目录;交接.md 刷新为 V3 交付状态;前端 rebuild(cookie 认证 + Shadow 标注 UI + PromoteMemory/promote 按钮)。

---

## 四、必须新增的测试(§36,全部确定性)

**Authority**:`test_open_loop_cannot_resolve_cross_scene`、`test_memory_cannot_escape_private_scope`、`test_reflection_cannot_create_global_safe_memory`(§25.1 恶意 case:模型输出 scope=global-safe/visibility=global 一律被 runtime 覆盖)、`test_rejected_proposal_cannot_apply_social_state`。
**Staleness**:`test_final_step_late_interim_discards_outcome`、`test_interim_between_pi_return_and_gate_invalidates_outcome`。
**Participation**:`test_same_participant_different_topic_does_not_force_wake`、`test_thread_closes_after_timeout`。
**Reflection**:`test_provider_loaded_before_reflector_wiring`、`test_bot_last_message_then_quiet_still_reflects`、`test_reflection_cursor_advances_only_consumed_batch`、`test_reflection_failure_does_not_advance_cursor`。
**Condition obligation**:`test_live_task_matches_room_id`、`test_wrong_room_does_not_trigger`、`test_condition_claim_recovers_after_restart`。
**Plugin**:`test_disabled_sensor_never_polls`、`test_disable_stops_poll_task`、`test_plugin_tool_name_collision_rejected`、`test_ssrf_loopback_rejected`、`test_ssrf_private_dns_rejected`、`test_ssrf_redirect_to_private_rejected`。
**Metrics**:`test_three_messages_one_burst_counts_three_human_messages`。
**Control Plane**:`test_scene_with_thread_serializes_without_500`、`test_read_historical_scene_does_not_create_actor`。
**Shadow**:`test_shadow_created_task_cannot_live_send_after_mode_switch`。
**Social**:`test_scenario_m_group_social_position`。
**Privacy 恶意重测(§25)**:private→group(含提案伪造 global)、cross-group、cross-scene OpenLoop——现有 Scenario L 扩展。

---

## 五、全链路验收 Scenario(§37)

1. Normal quiet group:20+ 消息 → mostly OBSERVE/TRACK → 无 cognition spam → 零可见消息
2. Natural participation:@ 进入 → 无 @ 续接 → 话题漂移自然退出
3. Semantic stale:B 作答 + A 确认 → 旧 outcome 丢弃 → 零 stale send
4. Promise:room 123 开播叫我 → room 456 不触发 → room 123 durable claim → TASK_DUE → 履行;**模拟 claim 后 crash 重启仍完成**
5. Reflection:Bot 末句 → 安静 → L1 Episode + L2 typed memory → cursor 精确推进
6. Group identity:同 actor 同句,X/Y 两群不同 scoped context,零泄漏
7. Plugin failure:tool 超时 / sensor 报错 / disabled / SSRF URL → Runtime 无恙
8. Shadow:真实 NapCat 群 → 全链运行 → would-send → **零物理发送**

## 六、完成定义(§38)

- **Runtime**:无 cross-scene OpenLoop mutation;无 visibility scope bypass;rejected outcome 零 partial effect;末窗 stale 不发送;condition task crash 可恢复。
- **Social**:participant≠topic;quiet 正常 Reflection;Goal 11 有真实 Scenario;Goal 12 恶意 privacy 全过。
- **Plugin**:disabled 真禁;无 Task authority;命名空间不冲突;无明显 SSRF;Sensor/Tool 失败不伤 Runtime。
- **Observability**:human message 计数正确;Trace 无 chain-of-thought;Scene/Trace/Task/Memory/Plugin 页可用;would-send 可人工标注。
- **Operations**:可连真实 NapCat;Shadow 连续运行;restart 后 Task/Memory/Scene/Provider/Plugin config 正确恢复。

## 七、V3 明确不做(§32)

Tieba Plugin、直播视频理解、vision pipeline、视频下载、autonomous browser agent、generic crawler、vector RAG、推荐引擎、social graph DB、multi-agent 委派、自修改插件、marketplace、subprocess 沙箱框架、分布式 runtime、Redis/Kafka、K8s;**Durable Action Outbox 仅当 Shadow 数据证明其为真实 blocker**(届时语义只允许 PENDING/SENDING/SENT/FAILED,不假装 exactly-once)。

## 八、V3 完成后(§39)

**FREEZE CORE RUNTIME** → 至少一轮真实 QQ Shadow Evaluation → 输出 Shadow Evaluation Report(human message 数、WAKE ratio、WAKE→SILENCE、would-send ratio、unnecessary speech rate、stale rate、missed participation、cancellation compliance、常见 wake reasons、escalation rate、plugin error rate、典型 GOOD/BAD trace)→ 由真实数据决定 V4 方向(外部媒体感知 / 更多内容插件 / durable outbox / social policy),**不许提前猜**。

最终形态:Plugins provide capability. Models provide judgement. Runtime provides authority. Events provide truth. SceneActor provides ordering. Memory provides continuity. Shadow provides safety. Trace provides accountability.

---

## 附:执行备注

- 实施顺序严格按 Stage 1→9;每阶段 `uv run pytest` 全绿再进下一阶段;每阶段先写对应 ADR(0024 memory scope、0025 proposal contract、0026 staleness/openloop、0027 participation、0028 reflection、0029 obligation/origin、0030 plugin/sandbox、0031 控制面/认证/指标/NapCat)。
- 基线:`f4fbf63`。实施前工作区必须干净(本次交接稿写入本身除外)。
- `thought → decision_reason` 涉及 ~17 个测试文件机械改名,用 `thought=` → `decision_reason=`、`"thought"` → `"decision_reason"`、`.thought` → `.decision_reason` 模式批量替换后逐文件核对。
- 前端改动(Cookie 认证、promote/annotate 按钮)统一在 Stage 9 rebuild,避免中间多次构建。
