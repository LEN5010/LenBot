# LenBot V2 全面演进 — 交付交接文档

> 更新时间：2026-09-04  
> 仓库：`/Users/len5010/code/len_bot`  
> 当前全量测试：**91 passed / 0 failed**（`uv run pytest`）

---

## 一、任务背景

本轮在既有 V1 Runtime 与前五份 ADR（0013~0017）基础上，完成了 V2 计划的**全部剩余代码面**（原计划 Phase 3~8）：

- 条件绑定 obligation 与 Ambient Item（社交持续性收尾）
- Quiet-window 反射、reflection cursor、LLM reflector 接线、Person Card
- Provider Registry 与 Routing Metrics
- 两个真实插件（Bilibili Live Sensor / Web Search Tool）与插件生命周期健康
- Control Plane 完整化（QueryService、Trace、Metrics、Logs、Replay Lab、Vue 3 前端）
- Shadow Mode
- Scenario A–L 基准套件（12 个 Goal 全部有命名 Scenario）

**重要澄清**：此前交接文档的"Phase 3/4/5"编号与 V2 计划的九阶段不一致——它实际只覆盖了原计划 Phase 4/6/7 的一部分。本轮交付后，九阶段中所有**代码面**工作均已完成；Phase 8（真实群 Shadow 采集）与 Phase 9（受控上线）为运营动作。

---

## 二、本轮六份新 ADR

| ADR | 主题 | 测试 |
| :--- | :--- | :--- |
| [0018](docs/adr/0018-condition-bound-obligations-and-ambient-items.md) | 条件绑定 obligation（`TaskItem.wake_event_type`）、插件事实事件 OBSERVE-only、`AmbientStore`（内存 TTL soft state）、OpenLoop resolve 溯源修复 | `tests/test_v2_phase6_continuity.py` |
| [0019](docs/adr/0019-quiet-window-reflection-cursor-and-typed-social-memory.md) | Quiet-window 反射（替换 ==5 计数触发）、`reflection_cursors` 行级游标、LLM reflector 接线、`MemoryKind` 规范化（pattern→social_pattern 一次性迁移）、sender 快照 + Person Card、decay 接入 maintenance | `tests/test_v2_phase7_reflection.py` |
| [0020](docs/adr/0020-provider-registry-and-routing-metrics.md) | `ProviderRegistry`（多 provider、热更新、一次性迁移）、`RuntimeMetrics`（tier/provider/model 维度 calls/errors/tokens/latency p50/p95/escalation + social 计数）、升级原因化、`[COMPLEXITY: HIGH]` 真实生产者 | `tests/test_v2_phase8_providers.py` |
| [0021](docs/adr/0021-plugin-discovery-lifecycle-health-and-real-plugins.md) | Manifest 声明面（config_schema/emitted_events/registered_tools）、插件健康状态机、内置注册表发现与装载、删除 mock 注册表、两个真实插件 | `tests/test_v2_phase9_builtin_plugins.py` |
| [0022](docs/adr/0022-control-plane-query-service-trace-and-replay-lab.md) | `RuntimeQueryService` 只读门面（路由不再扒 `_db/_actors/_plugins`）、loop resolve 改走 authority（修复裸 SQL 违规）、`traces` 表（attention 行 + episode 全链路行）、Replay Lab、Events/Logs、Vue 3 前端、CORS 收紧 | `tests/test_v2_phase10_control_plane_shadow.py` |
| [0023](docs/adr/0023-shadow-mode.md) | Shadow Mode：拦截器照跑、跳过物理发送、不发 MESSAGE_SENT（零社交事实）、would-send 记录、热切换持久化 | `tests/test_v2_phase10_control_plane_shadow.py` |

---

## 三、全量测试矩阵（91 passed）

| 测试文件 | 覆盖 |
| :--- | :--- |
| `test_v1a~v1e_*`、`test_attention`、`test_p0_*`、`test_p1_*`、`test_runtime_*`、`test_event_store` 等 | V1 基线 + 不变量（全部保持通过） |
| `test_v2_phase1/2/3/4/5_*` | 前一轮交付：原子提交、社交核心、记忆存储层、插件沙箱、Cockpit API |
| `test_v2_phase6_continuity.py` | E/F Scenario、裸事实不唤醒、G Scenario（retain/scope/TTL）、retained 提案、loop 溯源 |
| `test_v2_phase7_reflection.py` | quiet window 单 episode、cursor 增量、LLM reflector→gate、kind 迁移、Person Card、decay 接线、J Scenario |
| `test_v2_phase8_providers.py` | 一次性迁移、K Scenario（人格连续 + 指标）、复杂度标记生产者、LLM 错误计数 |
| `test_v2_phase9_builtin_plugins.py` | 内置装载/持久化、传感器事件入总线、web_search 沙箱执行、未配置惰性 |
| `test_v2_phase10_control_plane_shadow.py` | Trace 全因果链、信念链/事件过滤、Replay Lab 双策略对比、Shadow 模式与持久化、CORS 断言 |
| `test_v2_scenarios_a_to_l.py` | **Scenario 基准 A–L**：12 个 Goal 的命名验收层（确定性、离线） |

---

## 四、主要变更文件

### 新增
`state/ambient.py`、`memory/reflector.py`、`cognition/providers.py`、`runtime/metrics.py`、`plugins/builtin/{bilibili_live,web_search}.py`、`web/query_service.py`、`web/log_ring.py`、`web/routes/replay.py`、`testing/replay.py`、`web/frontend/`（Vue 3 + Vite 工程，构建产物 `web/static/dist/`）

### 核心改动
| 文件 | 变更 |
| :--- | :--- |
| `scheduler/models.py` + `engine.py` | `wake_event_type` 条件任务；`on_event()` 匹配触发走 TASK_DUE 权威路径 |
| `events/models.py` / `builder.py` / `store.py` | `LIVE_STARTED/LIVE_ENDED`、`PLUGIN_FACT` 刺激、tasks 表条件列、`get_events_since`（rowid 游标）、`traces` 表 |
| `runtime/gate.py` | ambient 副作用（事务后）、取消指标 |
| `runtime/agent_runtime.py` | AmbientStore/shadow/QueryService 挂载、quiet-window 防抖反射、provider 一次性迁移、metrics 接线、内置插件装载 |
| `cognition/pi_core.py` | registry 逐步解析、metrics 记录、`(outcome, step_trace)` 返回 |
| `cognition/router.py` | `should_escalate` 返回原因；模型选择权移交 registry |
| `cognition/assembler.py` | 【RELEVANT AMBIENT ITEMS】、【CURRENT ACTOR】、规则 6/7/8 |
| `adapters/onebot.py` | sender 快照（nickname/card/role） |
| `memory/models.py` / `store.py` | `MemoryKind` 枚举、pattern 迁移、`reflection_cursors` 表 |
| `tools/retrieval.py` | kind 描述、`_with_complexity_signal` |
| `plugins/*` | Manifest 扩展、健康状态机、`unload_all`、builtin 注册表 |
| `web/routes/*`、`app.py` | 全部走 QueryService、真实插件注册表、provider/routing API、`/api/logs`、`/api/replay`、SPA 托管、CORS 收紧 |
| `AGENTS.md` | 新目录 + ADR-0018~0023 模式描述 |

---

## 五、完成定义对照（V2 计划 §四十六）

| 维度 | 状态 |
| :--- | :--- |
| Runtime：ProposalCommit all-or-nothing | ✅（ADR-0013，既有） |
| Social：12 Scenario 全过 | ✅ `tests/test_v2_scenarios_a_to_l.py`（A–L 12/12） |
| Persistence：OpenLoop/Task/Memory 重启保持 | ✅（既有测试 + provider/plugin/shadow 配置持久化） |
| Privacy：跨 scope leak = 0 | ✅ Scenario L + P0.3 + Scenario D |
| Reflection：自动形成稳定社会记忆 | ✅ quiet window + cursor + LLM reflector（生产模式接线） |
| Plugin：一个 Sensor + 一个 Tool 真实工作 | ✅ bilibili_live_sensor + web_search_tool |
| Control Plane：单消息全因果链可视 | ✅ `traces` + Trace 视图（attention/episode 行） |
| CI | ✅ `.github/workflows/ci.yml` 跑 pytest |

---

## 六、下一步（运营动作，非代码）

1. **前端构建**：`cd src/len_bot/web/frontend && npm install && npm run build`（dist 已随本次构建生成；改前端后需重新 build）。
2. **Phase 8 Shadow 采集**：真实群开启 Shadow Mode（Settings 页或 `POST /api/cockpit/shadow/toggle`），观察 would-send 与 Visible Speech Ratio，调 monitored_keywords / speaking budget。
3. **Phase 9 渐进开放**：按计划 §三十五 顺序：mention/reply → active participation → initiative/ambient；每步观察 `/api/overview/stats` 的 social metrics。
4. Provider 配置（base_url + api_key）建议先在控制面 Models & Routing 页配置并 Test 通过再上线。
5. 未做 git commit——工作区变更是否入库、如何拆分 commit 由仓库所有者决定。

---

> **维护原则**：所有架构变更先建 `docs/adr/NNNN-*.md` 并同步 `AGENTS.md`；严守 §109 十条不变性；禁止补丁式设计。
