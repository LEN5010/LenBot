# 审查修复与验收状态

2026-09-08，北京时间。供维护者核对三个审查缺陷的修复证据及恢复运行前仍缺少的验收。本次以 master 的 `ad770951c14fc0a3bef0ca285d941394c2c4902e` 为基线，现行契约见[架构](../architecture.md)与[ADR-0045](../adr/0045-attention-maintenance-and-durable-work.md)。

## 代码与检查

| 审查问题 | 修复与已有链路验证 |
|---|---|
| 已认领但未投递的任务重启丢唤醒 | 恢复前按任务、场景、本次触发匹配待投递 TASK_DUE，保留有效 claimed；事务更新也校验触发，旧事件不能抢占新认领。原失败用例覆盖普通与条件触发、重复恢复及重复投递，保持一次处理。无关 pending 不抑制核对，unknown 不自动重发。 |
| 工作压缩后不能回读图片，六张变成累计限制 | 从实际轨迹同步当前像素集合；压缩后回读重新装入，新增第七张换出旧像素且保留来源与覆盖范围。现有 work/media 夹具用七张可区分 PNG，解码下一次模型请求的实际像素，验证压缩、回读、换入及换出后再次回读。原始工具正文、附件与检查点仍保留。 |
| 积压目录撑满上下文、两套原文预算冲突 | Context 按实际消息、工具 Schema、图片估算及输出预留统一装配；先装原文，目录与摘要按余量加入。目录分页最多 20 条，仅授予定位。1,000 条确定唤醒的原生运行时夹具可建立有界请求、确认已读部分，并在下次真实唤醒继续推进；未读取消仍阻止旧工作提交。 |
| 修复集成中的引用与关注 | 持久目录保存真实事件 ID，跨轮重新绑定 M 引用，不借目录授予原文证据；通过工具实际补读的待处理来源也纳入本轮发言人关注，保留连续追问的观察机会。 |

基线 [GitHub Actions 运行 34147176736](https://github.com/LEN5010/LenBot/actions/runs/34147176736)的真实结果为 **1 failed / 241 passed**，失败项为 `test_claim_and_due_event_survive_crash`，不是依赖安装失败。

本机 Python 3.13.13 使用既有 CI 的 `pytest -v` 命令，通过 uv 离线使用已安装依赖，最终结果为 **246 passed in 10.91s**，包括工具补读后的连续追问修复。本地日志为 `.runtime/review-fix-pytest.log`。11 个 Python 改动文件语法解析、3 份 Markdown 的 24 个本地引用及 git diff --check 通过。这证明本地测试链通过；尚未推送的修复没有新 GitHub CI 结果，不能把该旧失败运行改称通过。新增检查复用现有任务、工作和运行时夹具，没有建设新测试平台。前端没有改动。

## 停机与已存在的实机证据

修复前服务仍在运行。正常停止后，01:51 备份至本地 `.backups/review-fix-20260908-015106`，包含数据库、324 个媒体文件和修改前源码。备份有 5,623 条事件，SQLite integrity_check 为 ok；manifest 记录仓库提交 ad77095。修复期间未执行 Reset、启动生产或发出真实模型请求。

收尾时只读比较生产库与备份的全部 27 张业务/配置表，内容逐项一致；5,623 条事件、人工样例、保存配置与媒体记录保持，324 个媒体文件逐文件哈希相同。生产数据库完整性为 ok，面板端口已无监听。保全结果保存在本地 `.runtime/review-fix-preservation.json`。

只读备份确认 ADR-0045 的离线结构迁移于 **01:23:28** 完成，新注意力字段已存在。迁移后有 125 条群消息输入、8 条对话提交、4 条真实 MESSAGE_SENT；送达时间为 **01:33:09–01:43:03**，均标记 live、非模拟、sent，并具有协议回执。另有 1 条发送失败，以及两个工作结局 failed、partial。

因此，主体链路此前已经实际运行与发送，“尚未部署”只适用于早先实现记录的当时状态。调用账没有逐调用代码版本字段，不能仅凭备份 manifest 把每条历史行为精确归因到某个提交。这些记录也不证明本次修复已经上线，或真实群聊的自然度、工作修改/取消与最终交付全部通过。

## Usage 与费用

备份中 9 月 8 日调用账覆盖 **01:23:33–01:46:32**，包含全部已记录用途，未只统计主对话。

| 用途 | 成功且 usage 已知 | 失败且 usage 未知 |
|---|---:|---:|
| conversation | 18 | 3 |
| work | 11 | 1 |
| capability_probe | 6 | 5 |
| history_maintenance | 0 | 1 |
| 合计 | **35** | **10** |

35 次已知 usage 合计输入 **270,968**、输出 **16,530**、总计 **287,498 tokens**。其中缓存输入 64,640、推理输出 14,074 已属于对应子项，不重复相加。10 次失败请求的 usage 未知，不能按零用量或零费用计算。历史维护唯一尝试超时；没有工作压缩或技能维护的实机调用记录，不能用零次记录声称它们已验证成功。

此前[七次隔离供应商验证](20260908-attention-work-validation.md)使用独立数据库，与上述生产聚合分开。其长文本压缩成功和 45,519 输入 / 5,107 输出 tokens 仍是有效证据，但没有真实图片、完整实群行为或费用对照。

## 当前结论

| 验收项 | 状态 |
|---|---|
| 三处代码缺陷 | 已修复，隔离业务边界通过 |
| CI | 本地现有测试通过；新提交的远端 CI 待取得 |
| 主体部署 | 已存在迁移、运行和真实送达证据 |
| 本次修复部署 | 尚未启动生产，当前保持停机 |
| 实群行为 | 仍需以本次修复后的原群原话、工具和回执核对明确呼唤、连续追问、修改/取消及结果送达 |
| Usage | 已核对备份中的全部用途与已知聚合；10 次未知量保留 |
| 完整费用与节省比例 | 未验收；缺少未知请求的账单用量、价格及等口径前后基线 |

恢复运行后按具体真实输入继续核对上述缺口，不预设固定消息数量、评分或批量模型对照。本次结果不表述为全部业务与成本验收完成。
