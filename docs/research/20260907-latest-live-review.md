# 最新实机运行审查

2026-09-07 00:03 截止。当前代码 `d8ec5a0`；最近一次 Reset 是 2026-09-06 19:24:57，搜索修复部署于 20:21:38。本次重点统计部署之后，19:36 的 Qwen/Astra 失败属于部署前历史，不重复计为新版失败。

只读快照位于 `.runtime/live-review-20260907-000308/snapshot.db`，截至事件 rowid 1504；同目录保留脱离运行库的聊天整理、错误记录、统计和公开搜索复现。未停止或修改生产程序，未调用模型、修改配置或发送消息。分析中的时间均为北京时间。

## 实际运行概况

| 项目 | 部署后至快照截点 |
|---|---:|
| 两个实发群收到消息 | 398 |
| 成功对话轮次 | 317 |
| 沉默 / 参与 | 284 / 33 |
| 真实 MESSAGE_SENT | 33 |
| MESSAGE_SEND_FAILED | 0 |
| 对话失败 | 6，均为严格新鲜度冲突 |
| 新工作 | 2，均最终提交结果并实际交付 |
| 成功 Reflection / 失败 | 31 / 1 |
| 普通对话模型耗时中位数 / P90 | 5.97 / 10.97 秒 |
| 最慢一次普通对话模型调用 | 56.749 秒 |

主群 367 条输入、299 次成功对话，其中 266 次沉默；另一个群 31 条输入、18 次成功对话全部沉默。33 条实发均为文字加图片，没有纯文字或单图。没有自动更换模型、Shadow 或名单：仍是 BotCF / gemini-3.8-flash，conversation low、work high，Shadow=false，两个已批准群。

以上不是消息级响应率或固定效果评分：多个输入可合并为一轮，工作与反思也可唤醒对话。耗时是 Trace 中的模型耗时，不包含完整排队与实际发送时间。

## 1. 工作结果等了半小时，最后插入无关话题

23:18:48 群友问自抽号里的券用途。23:19:17 建立工作 `job_e994b42a60905ca586ed`；23:21:04 工作完成（`trc_bc88987293df`，107.19 秒）。直到 **23:51:49** 才真正交付补充说明，距离结果完成约 **30 分 44 秒**。

中间不是工作一直在搜索。相关过程为：

- 23:21:34 首次交付被未读新消息拒绝；随后 23:21:50 同批的游戏交付和 Tibo 新建提案一起回滚，23:22:15 Tibo 建单再次被新输入拒绝。
- 23:22:41 正式提交 Tibo 工作和少回应表情包的认识；游戏结果仍在待交付状态。Tibo 于 23:23:25 正常交付，说明发送链路并非整体阻塞。
- 23:27:56、23:30:36、23:37:56 又尝试补充同一游戏结果，均被新鲜度检查拒绝。其余多轮选择旁听，结果一直留在运行事实里。
- 23:51:28 群友已经在问另一人的妹妹年龄；23:51:49 Bot 忽然补发游戏购买提醒，23:52:32 收到“？”，随后自认反射弧太长。23:57:21 群友直接评价“列你的机器人反射弧还能再长点吗”。

关键 Trace：`trc_f3a8b2b986f5`、`trc_a5bffc052974`、`trc_6dcb86af8835`、`trc_2ecd0782fc7a`、`trc_8f5632d38257`、`trc_45ab16d64307`；最终交付 `trc_0c5866f986c9`，MESSAGE_SENT rowid 1448。

Actor 确实保住了原子提交和未读输入；当前认知在直接终结时不再吸收新输入，严格履约容易被连续聊天打断。失败之后，待交付结果又没有根据最新语境明确收束，造成“反复尝试—旁听—很晚才补发”。下一步应解决交付前的新输入处理和过时结果的处理方式，而不是把这半小时归给模型或搜索耗时。

## 2. Bing 返回可解析结果，但很多内容与查询无关

部署后的两项工作共 **13 次 web_search，全部状态为 ok**。其中至少 8 次返回主题明显无关：

| 实际查询 | 保存的结果主题 |
|---|---|
| 战双帕弥什 自抽号 券 是什么券 | 日本当前时间 |
| “战双帕弥什” “研发券” | Chrome 下载 |
| Punishing Gray Raven gacha tickets black cards | 巴哈马 Freeport 旅游 |
| “banked reset” keyboard OR keycaps OR tibo | 瑞士披萨店 |
| “banked reset” | 台湾百科 |
| site:x.com/thsottiaux banked | Microsoft 首页与账号 |
| site:x.com/thsottiaux “banked reset” OR “bank” | Google Sheets QUERY 函数 |
| site:codex-reset.com “banked reset” OR “可储备” OR “reset bank” | Toyota 论坛 |

部分包含 `site:` 的结果没有遵守域名限制。另有三次游戏查询基本只返回游戏首页，未取得所问的商品券细节；Tibo 的两个较短查询取得了相关线索。

用生产相同 URL、参数和 User-Agent 只读复现前三类代表查询：HTTP 200，RSS channel 标题正确包含原查询，items 却分别变成 Binance、印度时间和 Outlook 帮助。原始 XML 已保留；目前没有证据表明是本地解析或工具结果引用串线。准确结论是“当前公开 RSS 返回不可靠”，其上游机制尚不能确定。

上轮 Qwen/Astra 两例查到正确来源，不能证明这一入口对任意查询都稳定。本次暴露出仅检查 HTTP、RSS 结构和存在结果的不足。

## 3. 有来源不等于结论有依据，前台还在查证完成前直接回答

游戏工作唯一成功读取的正文是 BWIKI 首页，内容主要为网站说明、活动列表和角色图。它不包含“自抽号几万券绝大多数是常驻蓝票”的商品构成证据。

工作第一次 `finish_work(result_ids=[], unresolved=[])` 被新的来源检查拦住；后来引用该首页后便通过，并把以上概括当作已核实结论。说明引用可用性检查起作用了，但无法判断来源是否支持具体结论。不能据此断言结论一定错；可以确定的是本次资料不足以证明它。

在后台完成前，前台 23:19:19 的建单确认已经给出完整解释，并声称标题中的券通常为各种券加总；23:20:14 又作详细解释。23:21 后工作结论却变成绝大部分为常驻券。确认消息仍会夹带尚未核实的答案，“已创建工作”没有保证前台等到证据再作结论。

Tibo 工作虽然最终从网站读出了正确的讨论对象，创建目标却先带入了“发售、补货、GB”，首个查询甚至加了 keyboard/keycaps。最后主要依据 `codex-reset.com` 的第三方文章，实际未读到官方公告正文，却使用“官方完全没公布”等绝对语气。此处只能确认来源范围不足，不据这次审查判断是否真的存在下一次预告。

## 4. 群聊整体能旁听，局部连续反应仍显得吵

成功轮次中约九成沉默，不能说它一直抢话。但看到角色图片时会连续反应：20:25–20:27 从要求撤回照片一路加戏；22:22 看到图后连续回应多个问号；23:21:28 将别人发图理解成“听不懂自己的解释”，发出“明明解释得超认真超清楚”。这些与普通群友间话题形成明显落差。

33 条实发全部为图文混排，说明单图和纯文字能力虽然已经开放，实际选择仍趋同。没有必要用硬性配图比例修正；应看每次回应是否需要解释图片、是否误把群友表情当成针对自己的评价。

23:22:06 运营明确要求“以后看别人发你表情包别大惊小怪，不用回复”。23:22:41 已原子保存为 `mem_875d3b405a664425b77c552673d2d3ec`，随后有多条沉默 Trace 明确引用该要求；例如 00:01:40 按 B1 保持旁听。这个反馈闭环已真实生效。

23:25:55 群友评价“这个戛然有点吵啊”，Bot 随后表示闭麦。之后确有长段旁听，但仍被旧工作交付打断。这一段“吵”既有角色反应过密，也有工作返回时机不当，两者需要分别处理。角色扮演目标本身继续保留。

## 5. 分段输入会产生过时的中间回复

23:03:55 群友发“我每次看這個機器人”，23:03:58 补完“都以為然然出現了”。Bot 23:04:08 仍问“话怎么只说一半”，十秒后再回应完整意思。

`trc_6593189c6d35` 只读取到第一句 rowid 1143，模型调用耗时 11.073 秒；第二句已在调用期间进入，但这一轮是直接终结，普通聊天允许按旧截点提交。`trc_28f47afe3eb7` 才读取第二句。这是已知读取语义在真实聊天中的表现，不是漏存第二句，也不是用户真的停在半句。

## 6. 上下文与反思开销，以及一个协议失败

主群的估算文本上下文从约 11K 逐渐增长到 22K。提供商回报的成功对话输入累计约 611 万 tokens，其中沉默轮次约 518 万；缓存命中回报为零。这是服务方 usage 统计，未核算实际账单。普通轮次大多仍一次模型调用完成，但个别会出现 20–56 秒等待，放大分句迟到和严格提交冲突。

31 次成功 Reflection 使用 61 次模型调用。23:26:02 还有一次 `trc_e1ba163363b0` 协议失败：模型使用了 `kind=add`、`certainty/object/predicate/source_event_ids` 等不符合当前认识结构的字段。运行记录仅保存异常，缺少该失败反思的完整步骤，进一步定位修复过程不够方便。

这不代表之前少回复表情包的要求没有保存：该认识已由前台在 23:22:41 成功提交。当前人工表达样例仍为零；它是已有运营状态，不自动恢复或覆盖。

## 后续优先级

1. 工作结果在活跃群聊中的提交、延迟与过时处理，避免半小时后插话。
2. 搜索结果与查询的匹配、来源对结论的支持，以及前台提前作答。
3. 分段输入的过时回复和角色图片引发的连续反应。
4. 反思协议错误的完整记录与上下文开销。

本轮只做诊断，不放宽事务、场景隔离或发送约束，不改动模型与人格，也未重跑场景集。
