# 18:06 Reset 后的工作链路审查

2026-09-06，上海时间。只读审查最新 Reset `c4507dc7-7576-4cb3-ba35-03db797997b0`（18:06:51），结合真实事件、Trace、工作资料与回执。快照保存在本地 `.runtime/reset-review-20260906-181519/snapshot.db`，读取截点包含 18:15:03 的工作完成通知；原始记录不改写。

## 已改善的部分

主群 18:07 的文件分享、游戏通关及晒图对应三次沉默提交，未主动插话。已发送的工作确认与结果也能够使用图文混排。本轮看到的主要失败不在昵称检测或图片字段：出错轮次均有正确的 @/“小然”线索，`start_work` 出现在开放工具中，新 text/image 片段通过了结构解析。

## 工作启动协议连续失败

主群在 18:11:40、18:12:16、18:12:27、18:12:48、18:13:25、18:13:44 共六次认知失败。对应 Trace：

- `trc_a8cc85eb3440`
- `trc_775afc4fc566`
- `trc_f0580e4bed45`
- `trc_9ad6b75ad393`
- `trc_658285787268`
- `trc_956de952c3a1`

共同起点是模型只调用 `finish_turn`，消息带有自行填写的 `ack_ref`，却没有调用 `start_work` 创建暂存提案。运行时拒绝没有对应事项的确认，因而既没有建单，也没有向群中发送“正在计算”。多数参数修正再次提交同样的确认；其中一次修正终于暂存工作，但最后一步没有原生终结调用，仍未提交。

这不是按群配置的故障：同一配置下，另一个群 18:09:15 的 `trc_09064ab6f35a` 正确先建工作再提交确认。主群直到 18:14:25 的 `trc_9fd381e7e97d` 才在三个步骤中完成“错误确认 → start_work → finish_turn”，18:14:27 真正发出确认。相对于 18:11:28 的最初请求，已接近三分钟。

事务拒绝与保留未读输入是正确行为，不能通过放过无效 ack_ref 或清空输入来消除报错。问题在于建单和确认的模型接口仍然容易被误解，而且新一轮未看到前一轮的协议失败：当前 `uncommitted_job_attempts` 只回传有工作提案的提交冲突，不回传这类未建单的协议失败。新消息到来后，仍未处理的请求会再次走同一种错误路径。

## Qwen 查询也被同一问题截断

18:09:16 收到 Qwen3Guard 性能问题，`trc_dd4fa6d82691` 的步骤为：

1. 读取已有糖果工作的 `query_jobs(J1)`。
2. 填写 `ack_ref=qwen3guard_info`，打算说会去查询，但没有创建查询工作，被拒绝。
3. 达到最后一步，只剩终结工具；改为说手头没有确切数据，请群友提供评测资料。

最终回复于 18:09:32 送达，但实际没有 Qwen 工作，也没有对 Qwen 调用外部搜索。它比无依据断言用户记错版本更诚实，仍没有执行用户的查询请求。这里不是搜索工具返回空结果，而是查询根本没启动。

## 工作完成并不等于数学结论已验证

另一个群的糖果工作 `trc_920d7edbae70` 确实运行了八个模型步骤和七次工具调用，进入配置为 high 的 work 链路。错误已存在于工作结果中，对话主要复述了它：

- 声称“10 个圆形 + 11 个五角星形”能保证配对，实际不成立。
- 同一结果的推导已经意识到五角星形可恰好取到“7 苹果 + 4 西瓜”，但没有解决反例，仍把 10+11 写成最优方案，并将 `unresolved` 留空。
- 纯盲摸的最小保证数被含糊写成“32 个或以上”；主群随后另一次工作 `trc_14818d0666f4` 又将其写成 38。

独立的本地枚举检查保存在同目录 `independent-math-check.json`，枚举所有符合六类数量上限的取出组成。结果：

- 10+11 的反例：圆形取 2 苹果、8 西瓜；五角星形取 7 苹果、4 西瓜。共 21 颗却没有桃子，不能配对。
- 固定按形状取数时，9 圆形 + 12 五角星形可以保证；枚举得到固定配额下最小总数为 21，唯一对应配额为 9+12。该枚举不声称穷尽其他自适应挑选策略。
- 完全盲摸的最大失败组成是全部 24 个圆形加 4 个五角星西瓜，共 28 颗；最小保证数为 29。

工作模型实际使用的计算器只算了总数与几个加法，并没有执行它所称的“穷举最坏情况校验”。检索同款题得到搜索摘要，其中一次论坛正文读取为 403，另有无结果或搜索不可用；这些都不构成独立的数学验证。`completed` 只证明工作执行按协议结束，不证明自由文本结论正确。

工作提示原本已要求边界反例、完整保证及矛盾写入未决项。因此不能把再加一句“认真验证”当作可靠解决方案；当前数值算术工具也不能执行有限状态枚举。

## 最后一条工作结果尚未交付

主群工作于 18:15:03 完成，任务仍为 `result_ready`，认知游标为 row288，观察游标到 row320。没有该结果的真实发送回执。审查时未发现 LenBot 主进程，11307 也无监听，因此目前不能将此条待处理结果单独归因为投递故障；恢复时仍应根据真实状态处理，不能假装已经交付。

## 后续修复重点

优先收紧“创建工作—取得引用—确认”的接口，让确认引用来自实际建单回执；同时让同一未处理请求能看到相关的上一轮失败事实。保留事务、送达和实际读取边界，不自动替模型建工作，不通过扩张日常轮次预算或重试来掩盖。

随后补足工作中的可执行验证能力，区分给定数据的本地推导与需要外部信息的查询。不能把正确的几次加法、搜索同款题或完成状态当作整个证明已经成立。

此前三项真实模型合成核对只覆盖直接文字、沉默与单图；建单/确认仅有确定性协议检查。这次实群暴露了此前没有覆盖的模型实际工作发起失败，不能用先前检查通过来否定本次失败。

本轮完成定位和独立数学核对，未修改运行代码或启动程序。现有代码与操作约束见[架构](../architecture.md)和[实施记录](../implementation.md)。
