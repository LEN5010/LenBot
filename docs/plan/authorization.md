# 2026-09-21 推进授权记录

本文件记录维护者对「持续推进 S0—S6」给出的当次授权。它约束自动推进过程中能做什么、
不能做什么；**不扩大任何生产、发送、模型调用或配置变更的授权**。

记录时间：2026-09-21。基线分支 `feat/s0-product-contract`。以下保留当时决定；后续具体动作服从当前明确授权，本批操作见[当前任务](../iteration.md)。

## 一、推进范围

| 决定 | 内容 |
|---|---|
| 范围 | **S0 收口 + S1/S2/S3 代码与文档 + S5/S6 文档与发行纪律** |
| 不做 | S7 全部（招募外部使用者、外部插件作者闭环、案例文章）——无法真正完成，只出准备材料 |
| 验收口径 | 需要真实环境的验收项**只做源码部分并明确标未确认**，绝不把编译或构建写成实测通过 |

## 二、Git 授权

| 动作 | 授权状态 |
|---|---|
| 在 `feat/s0-product-contract` 上提交 | **允许** |
| 推送该分支到 `origin` | **允许** |
| 开 PR / 合并到 master | **不允许**（合并由维护者决定） |
| 改动 `master`、重写历史、force push | **不允许** |

## 三、需要维护者拍板的事项：出对比稿后暂停

遇到下列决定时，**产出选项对比稿并停下等待**，不自行选择、不写进仓库生效：

1. 代码许可证选择；AGPL 插件与同 wheel 打包的关系。
2. `font.ttf`（24.4MB，上游无独立许可）的处置。
3. 素材与人设原文的授权记录方式。
4. 数据保留政策（事件、认识、摘要、聊天媒体的保留期限）。
5. 公共结构的**所有权边界**（会话段、插件 API 世代）——涉及新增表或公共符号时。
6. 是否修改「不运行测试」的工程约束。

## 四、始终不变的工程禁限（沿用 AGENTS.md）

- 不新增、修改或运行测试、夹具、断言式探针、自动截图、覆盖率任务、回放、故障注入、压力任务。
- 不启动生产、不调用真实模型或平台、不向真实群发送、不改真实配置/数据库/人格/群名单。
- 不新增通用 checksum、内容指纹、迁移哈希清单、配置签名、备份校验或自愈降级系统。
- `web/static/dist` 不进 Git；根配置 `lenbot.config.json` 是唯一运行配置来源。
- 提交信息、PR 正文、代码注释与文档中不出现任何工具、模型、Agent 或 CLI 的名字；
  不加协作者或生成署名，作者只有仓库所有者。

## 五、每个任务的完成口径

按路线书第 9.5 节：代码写完 ≠ 实现完成 ≠ 该组合已支持 ≠ 已部署。
任务关闭前明确：用户结果是否达成、所需数据是否可追溯、样例与文档是否同步、
当前验证证据是什么、未确认范围是什么。

实际观察、失败原文与未确认项写入 `docs/iteration.md`；任务状态更新到
[README](README.md)。没有运行证据的项不得写成实测通过。
