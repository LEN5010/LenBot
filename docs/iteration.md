# 当前任务

更新于 2026-09-23。任务状态只见[产品路线](plan/README.md)，稳定合同见[文档入口](README.md)，此前过程见[历史入口](history/README.md)。

## 当前批次

- 分支 `feat/s0-product-contract`，基线 `7488f73`，开始时工作区干净；上一批原话直达历史维护来源批次已本地提交，实际查询与页面待同版人工核对。
- S1-06：按任务卡复核单条群消息审计入口。原 `MessageProgress` 已沿明确来源与行动身份收窄六阶段、轮次调用、逐次请求材料、工作、提交与回执；`SceneInspector` 和事件详情保留原始记录；`EventDiagnostics` 从原鉴权路由读取并预览同份只读材料后下载，不再次执行。缺失关联不反推无执行。

## 本批交付与核对

补齐两个源头消息可读性缺口：若已取得关联但没有精确以该原话为来源的历史维护批次，明确提示“本次关联未找到”，不把空列表解释成全群无维护；关联截断提示将新增批次类别显示为“历史批次”，不泄露内部键名。复用原查询与组件，没有新状态、接口、持久化或业务副作用。

源码核对原 `/events/{event_id}/diagnostics` 路由的登录约束和 `no-store`、查询服务的显式字段投影、前端预览与下载同一份记录，以及群消息／回执的身份收窄。按 S1-06 自身源码交付转入待复核，不宣称依赖的 S1-02 请求材料清单与 S1-04 阶段计时已完成，也不将 S1-03／05 的静态状态判读当作真实发送验收。

- 前端目录 `npm run build` 退出 0，491 个模块、1.68 秒，日志 `/private/tmp/lenbot-s1-timeline-review-build.log`；`git diff --check` 退出 0。只改前端和文档，未重复 Python 编译，构建产物不入 Git。
- 首次补丁因行内空格上下文与实际文件不同，返回 `apply_patch verification failed: Failed to find expected lines in /Users/len5010/code/len_bot/src/len_bot/web/frontend/src/components/SceneInspector.vue`；读取真实行后补丁成功。不是业务运行失败。整理文档时一次同文件删除与新建合并补丁因工具不接受重复目标而失败，改为单次文件写入，无业务影响。
- 没有获准同版服务与面板，实际群消息、调用归属、缺档提示、截断、鉴权、诊断下载和窄屏布局仍待人工正常操作；源码与构建不当作业务通过。未运行测试、夹具、断言探针、自动截图、回放、故障注入、覆盖率、依赖安装、服务、模型／平台调用或实发；未读取真实配置和业务库。

## 待决定与接续

1. S1-06 待同版人工从一条群消息核对读取机会、处理、调用／依据、工作、提交／发布、回执和诊断文件；未获记录前不标已验收。S1-02 动态定义／材料及 S1-04 其他等待仍各自实施中，S1-03／05 的状态分支仍待人工复核。
2. S2 仍为 `source_window_only` 增量；原生交换、活动摘要基线与压缩交接未完成，额外原生响应与必要回复片段的保存边界待维护者答复。S3／S4／S5 现场尤其已选 Linux 容器群报告和文件交付仍待人工证据。
3. S5-03 分类期限、许可证／素材授权、精确支持版本、S6 候选与远端 CI／升级、S7 外部使用者／作者闭环仍未完成；没有生产或发布授权。
4. 仅阶段性本地提交，不推送、合并、部署或实发，总体目标继续。
