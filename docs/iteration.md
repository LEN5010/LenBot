# 当前任务

更新于 2026-09-23。任务状态只见[产品路线](plan/README.md)，稳定合同见[文档入口](README.md)，此前过程见[历史入口](history/README.md)。

## 当前批次

- 分支 `feat/s0-product-contract`，本批基线 `8730431`，开始时工作区干净；上一批已本地提交。
- S3-06：按真实描述符整理同版兼容与迁移清单，并同步原面板缺少的注册声明字段。未执行目录发现或装载，不全量启用插件，不修改业务配置或存量数据。

## 本批实现与核对

新增[插件兼容与迁移](plugin-compatibility.md)，覆盖 13 个内置描述符和 local_clock：明确真实 ID／目录、自身版本、共同世代适配、哪些业务文件无须额外修改、哪些路径已迁到公共入口、仍保留的内部依赖，以及资源和现场未确认项。共同 ordered 默认变化、Hook／取消、专用工作回调与 restart_plugin 默认列在前面，避免把“业务文件未动”解释成“运行无变化”。

保留 workspace／python_workspace 的既有兼容身份、同一实现及各自存量归属，写明唯一启用／配置选择和同名工具冲突；没有删除旧 ID、猜执行后端、移动数据目录或恢复旧工作。清单链接原描述符及稳定合同，不覆盖旧基线盘点；任务状态仍只在产品路线维护。

PluginHost.status_snapshot 从原已注册工具追加 ordered、deferred、timeout_seconds、page_chars。PluginsView 的原详情页显示执行顺序、发现展开、超时和展示页长，并展示 handler 原注册的 refresh_deferred 标志；缺失值不猜 False 或零。权限标题改成“声明的资源权限”，空声明不再解释成尚未装载；提示注册不授予当前执行资格、允许并行非保证、页长不代表总长。没有新增配置项、表、接口或执行路径。

- 阅读全部 13 个内置 __init__.py、业务时钟、PluginSpec 默认和 PluginCatalog.discover；描述符全部声明字面世代 2，自身版本按原值列出。目录名不同于 ID 的直播／网页两项已明确。
- 用 Git 文件差异核对 ad41a5a 至本批基线的插件修改范围；群报告、直播、动态、链接和时钟的额外业务适配与只改描述符的插件分开。不是通过导入或运行插件取得清单。
- 源码核对配置模型、资源路径及 configured_workspace；全部当前描述符未显式选择 config_apply，沿 PluginSpec 默认 restart_plugin。对 config_apply 的 rg 无匹配是源码事实，不是业务失败。本批未遇到命令执行错误。
- uv compileall 编译 plugins/host.py，退出 0；前端两次 npm run build 均退出 0，最终为 491 modules、1.76s，日志 `/private/tmp/lenbot-s3-plugin-contract-build.log`；git diff --check 无格式错误。构建产物不进 Git。
- 没有同版开发面板，实际修改的插件详情页、外部目录发现／配置／启停及各插件现场结果均待人工核对；没有另起服务或自动截图替代验收。构建通过不表示页面和业务运行通过。
- 未新增、修改或运行测试、夹具、断言式探针、回放、故障注入、压力或覆盖率任务；未读业务库、保存根配置、下载依赖、调用模型／平台或真实发送。

## 待决定与接续

1. S3 的六项源码与文档交付已进入复核范围，均未标为已验收或已发布；现场记录、资源授权、公开支持组合仍待维护者确认。其他内部集成没有因清单存在而变成公共 API。
2. 下一步回到 S2-04 的新输入、恢复与撤销边界，先沿实际 Actor／原工作版本核对不依赖额外片段保存的路径，再进入有此依赖的 S4-01；不把 S2 完整原生续接尚缺的事实跳过。
3. S2 活动段中不可由引用还原的原生响应字段／必要回复片段保存边界仍待维护者回答；已确认的宿主所有权和原事件权威不是额外片段保存授权。本批不扩大保存，不重复提问。
4. S1 其他提示／动态材料和等待／失败计时仍待补；S2 尚缺完整交换、其他编号／提案句柄、窗外范围、基础摘要及压缩交接。S4—S7 仍需逐项实施或核对，不宣称整体完成。
5. 许可证、字体／素材授权、长期数据保留、公开支持承诺仍待维护者决定；人物资料、五张常服与 19 张表情待人工采用，真实成果复用与文件交付仍待业务记录。
6. 300k／128k 仍为候选，根配置未改；既往令牌轮换与部署条件未复验。仅授权阶段性本地提交，不推送、合并、部署或真实发送。总体目标保持进行中。
