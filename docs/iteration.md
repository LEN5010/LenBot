# 当前任务：A/B 改进计划

2026-09-10。用户要求实施 A 后完成 B、阶段性本地提交，随后明确跳过 A 验收，先完成 B 全部计划。起点为 `8061035`。现行契约见[产品](product.md)、[架构](architecture.md)、[运行手册](operations.md)与[插件开发](plugins.md)。

## 已完成的实现

| 阶段 | 行为变化 | 本地提交 |
|---|---|---|
| A0/A1 | 原话与运行资料分区，按容量装配并保留实际阅读范围 | `62ceca0` |
| A2/A3 | 分开请求、回应、引用与等待对象，开放短工具与真实成员提及 | `d3698b9` |
| A4 | respond 分阶段提交、累计预算与真实送达后的等待恢复 | `8a70310` |
| A5 | 显式继续 partial 原工作，保留旧资料和交付记录 | `226fc06` |
| A6/A7 实现 | 直播请求标识、行为协议及工作回执关联收口；实群验收跳过 | `9878be4` |
| B0/B1 | 目录描述符、类型化配置、公共调用与消息匹配消费 | `5cfbac2`、`8f7f723`、`58d51c5` |
| B2 | 模型、工具、提交及真实回执六种钩子 | `ddf9e47` |
| B3 | 插件选择输入与结果模式，复用原 Agent、预算、Ledger 和等待 | `0c733f1` |
| B5 及工作迁移 | 工作归属、版本、业务参数和覆盖迁入插件；停用取消所属运行 | `3b60168` |
| B4/B6 收口 | 通用插件配置与 Trace 页面、剩余中央业务分支清理、独立时钟插件、开发示例 | 本提交 |

六个内置插件通过同一公共宿主注册；群总结仍使用原工作运行器、数据库和发送链。新[业务时钟](../local_plugins/local_clock/README.md)只在自己的目录定义工具、“现在几点”确定性命令和“时间简报”Agent 命令。没有新增数据库表列、第二套模型客户端或发送队列。

## 实际配置与核对

09-09 21:21 停机检查未见 11307 监听或当前数据库句柄，普通备份保留于 `.backups/20260909-212148-before-A-start/`。09-10 00:25 再确认无监听及数据库句柄后，另存实际配置、SQLite、完整媒体及 A 代码到 `.backups/20260910-002519-before-B-config/`，执行一次明确的离线配置转换。

实际根文件已改为 B 的 plugin_directories、群插件 enabled/config 和插件相对资源路径；日历命令、六项直播订阅及 mention_all 原值保留，公告明确保留 conversation 路由。新增 local_plugins 目录及业务时钟全局配置，仅在原业务群 `126300994` 开放两个时钟命令。原有两个群、chat 开关、模型、嘉然人格、人工样例和 Shadow 均未更换。原数据库与媒体没有结构转换或重编号。

转换时实际发现 7 个描述符，并由同一 ConfigStore 入口解析、保存实际根配置。最新正常 Python 编译（src/len_bot 和 local_plugins）、前端构建（441 modules）、`git diff --check` 及受影响 Markdown 引用核对完成。没有新增、修改或运行测试、夹具、断言式探针、自动截图或覆盖率。

生产尚未启动，实际页面交互、命令消费、插件 Agent、模型用量、停用效果及新发送回执均未运行确认。B 的最终运行验收仍待当次生产启动授权及正常人工使用；不能以编译、构建或配置解析代替。未推送。

## 保留的真实观察与未确认项

- 修改前 09-09 17:44:20 的“评价一下你的创作者”来源 `b958ed89-ed17-4fc9-aa5f-afd941edd0b6`，Trace `trc_50d948c14bdb`；送达 `85f8011a-600b-41b3-910d-92bf4a9d0c5d` 把“待处理来源：M1”说成群友措辞。
- 修改前 17:44:14 的“我操你妈我不是旅行者”来源 `a672d9c6-6d35-4763-b724-a4ab69cb7df4` 后，17:44:53 送达 `1e88c997-5ea0-47f9-a784-31d5f8b516ea` 仍提出“难道你是漂泊者呀”；对应 Trace `trc_0bcc2de675e6` 的直接来源是另一条 `734d7daf-308e-4858-872e-7f6d7c38d33b`。A 新效果未核对。
- 工作 `job_7fd2432a0551568e86ca` 的 250/515 条部分总结已有真实送达 `0c844ef0-3117-4fef-a2a4-e7c521fd6a16`；使用模型 19 次、工具 15 次。本轮未恢复该旧工作，部分完成与送达事实保留。
- 09-09 20:59，使用 LenBot/0.1 的实际直播请求中，六个已配置房间均返回 200 并通过类型解析；此前默认标识返回 412。只确认该次来源读取，没有确认持续采集、公告生成或发送。
- ICS 原来源仍返回 403；21:14 显式经 Clash 127.0.0.1:7897 请求同一来源也返回 403 和 Cloudflare 验证 HTML，未取得可解析 ICS。Clash 开启 TUN，不能把未传显式代理认定为另一个出口。没有改变来源或代理设置；历史动态部分成功保留，本轮未重新核对。

B 不把来源失败或 A 未验证的效果改写为通过。实际起停与原群使用完成后，只在此更新观察结果。
