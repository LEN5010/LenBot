# 当前任务

更新于 2026-09-23。任务状态只见[产品路线](plan/README.md)，稳定合同见[文档入口](README.md)，此前过程见[历史入口](history/README.md)。

## 当前批次

- 分支 `feat/s0-product-contract`，本批基线 `24d3ea8`，开始时工作区干净；上一批已本地提交。
- 回到 S2-04，不依赖额外原生片段保存的普通对话租约取消边界。实际 acquire_episode_lease 在取消后仍 shield 等待 Actor Future，收尾再次取消可能留下已授租约；等待取消也会一直依赖 Actor 队列推进。本批只修这一条内存租约路径。

## 本批实现与核对

acquire_episode_lease 改为直接等待 Future；取消时标记当前 mailbox，并仅在 Actor 持有相同对象时同步释放租约，无异步二次等待。Actor 消费 EpisodeLeaseCommand 时要求 Future 和 mailbox 均未取消，才可在空闲时授租约。取消发生在排队阶段不会留下无人等待的占用；授租约后但调用方尚未恢复时取消，也在原方法内同步释放。仍用既有 _active_mailbox、Future 和 idle 信号，没有新状态、锁、队列或重试。

保留真正持久提交的处理：commit_turn 继续 shield 等待同一 Actor 决定，已提交则记录发布中断而不是撤销事务或补发。未修改 _commit_turn 的租约、知识版本、截点、已读／已处理及相关新输入检查；重复已提交轮次仍返回原结果与 not_repeated，不重复发布。没有改活动段保存范围、逐来源唤醒、工作修订或预算。

- 源码核对 _run_conversation 在 acquire_episode_lease 之后才进入原快照／执行 try/finally；取得期间的取消必须由租约方法自身收尾，不能依赖尚未进入的 finally。正常运行仍在原 finally 按 episode_id 释放。
- 阅读 Actor 工作循环、stop 的 Future 取消与队列 task_done；取消命令仍被正常消费／排空，不把撤销等待当作未完成队列项。同步清理用 mailbox 对象身份，不因恢复运行复用 episode_id 而释放别人的对象。
- 阅读 _commit_turn 已处理来源必须属于真实已读集合及当前待处理／先前已处理集合的核对，以及已有提交的重读分支；本批未用局部核对声称所有恢复／撤销情形已完成。
- uv compileall 编译 scenes/actor.py，退出 0；git diff --check 无格式错误。未改前端，没有运行前端构建；本批未遇到命令执行错误。
- 没有运行取消、重复取消或并发竞态，也没有同版运行环境；租约授出、取消、场景停止和后续正常对话均待人工验收。以上是源码路径结论，不是实测结果。
- 未新增、修改或运行测试、夹具、断言式探针、自动截图、回放、故障注入、压力或覆盖率任务；未启动服务、读业务库、修改根配置、调用模型／平台或真实发送。

## 待决定与接续

1. S2-04 继续核对相关／无关新输入、原等待恢复、工作取消／修订、权限撤销及压缩期间输入；租约修复不等于整项完成。S4-01 仍需这些实际依赖，不提前声称群聊体验已验收。
2. S2 活动段中不可由引用还原的原生响应字段／必要回复片段保存边界仍待维护者回答；已确认的宿主所有权和原事件权威不是额外片段保存授权。本批不扩大保存，不重复提问。
3. S1 其他提示／动态材料和等待／失败计时仍待补；S2 尚缺完整交换、其他编号／提案句柄、窗外范围、基础摘要及压缩交接。S3 源码／文档已进入复核范围，但未验收或发布；S4—S7 仍需逐项实施或核对。
4. 许可证、字体／素材授权、长期数据保留、公开支持承诺仍待维护者决定；人物资料、五张常服与 19 张表情待人工采用，真实成果复用与文件交付仍待业务记录。
5. 300k／128k 仍为候选，根配置未改；既往令牌轮换与部署条件未复验。仅授权阶段性本地提交，不推送、合并、部署或真实发送。总体目标保持进行中。
