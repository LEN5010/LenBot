# 当前任务

更新于 2026-09-23。任务状态只见[产品路线](plan/README.md)，稳定合同见[文档入口](README.md)，此前过程见[历史入口](history/README.md)。

## 当前批次

- 分支 feat/s0-product-contract，基线 1302795，开始时工作区干净；上一阶段系统设置修复已提交，属于实际进展。
- S5-02：模型配置的未知结果与原对象核对；不修改真实模型、绑定、供应商或密钥。

## 本批交付与核对

ModelsView 原本已处理 config_saved=true 的应用失败与成功后读回，但网络／响应解析失败时仍可直接重交原密钥替换、删除或目录草稿。本批在原五类配置写入入口登记“请求已提交”，JSON／本地字段处理失败不算提交。供应商新增编辑、删除、目录、职责、检索五个成功接口增加 config_saved=true；原读回入口要求本次 success 与写入确认，不把任意成功 HTTP 响应当作配置落盘。

未知结果保留操作种类和原供应商 ID，原页面及三个编辑弹窗可读取当前保存／运行投影，再明确采用当前值。读回本身不清除未知或重建编辑基线；采用时关闭对应原编辑、清除其密钥／目录草稿，后续新操作重新进入当前对象。不存在的供应商不恢复成新建，未知删除不直接再删。沿原 readback 写入控制延伸未知分支，不新增后端状态或重试平台；已保存但应用失败仍走原已保存读回路径。

未知提示跨当前页面的标签切换保留，不跟随迟到响应变成另一对象的结果；页面卸载不持久化，不取消服务器动作。明确冲突只采用 config_saved=false，其他不明确错误不按 409 等状态码猜测是否未写入。原目录失效规则、模型绑定整项合并、凭据世代和已开始工作的模型快照均未改变。

- 阅读模型路由 _edit_models 与五类保存、ModelsView 的读回／目录／弹窗与请求边界，以及 model_settings_draft。当前读取投影将保存及运行密钥都映射为“已设置”，未知结果预览只复用该公开响应，不读取私有导出或真实根配置。
- 源码复核修正编辑过程中的 writeHeld 自引用：批量替换误改了派生值定义，现明确只由原 readbackPending 与当前未知结果决定。修正在编译构建前完成，未运行探针、模拟或测试，也无本批现场异常原文。
- uv compileall 编译 web/routes/models.py 退出 0；npm run build 退出 0，491 个模块，1.67s；git diff --check 无格式错误。构建产物不进 Git，前后端须同版，无数据转换。
- 没有获准同版开发面板，实际保存、未知后读回／采用、弹窗和标签切换仍待人工验收。未访问平台、获取远端模型目录、做模型能力检查、调用模型或嵌入；没有为验收启动另一服务或制造输入。
- 未新增、修改或运行测试、夹具、断言式探针、自动截图、故障注入、回放、压力或覆盖率任务；未读取业务库、安装依赖、改真实配置或实发。人格、人工样例、素材保持原样。

## 待决定与接续

1. S5-02 保持实施中；继续系统保存后的运行应用／管理事件阶段，以及其他配置页面的现有合同。S5-01 安装现场、S4-06 群观察待获准环境，不由静态核对代替。
2. S3 与 S4-01—05 待复核；S1 材料／耗时、S2 完整活动段与未登记依赖仍有缺口，额外原生字段／必要回复片段保存边界待决定。S5—S7 按原目标继续。
3. 许可证、字体／素材授权、长期保留、公开支持与精确候选镜像由维护者确定；原直播参考已更正插件身份，不因此取得启动或发送授权。
4. 仅阶段性本地提交，不推送、合并、部署或实发。源码、编译构建、人工验收、发布分别记录；总体目标保持进行中。
