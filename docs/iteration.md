# 当前任务：A/B 改进计划

2026-09-09，按用户要求先实施《LenBot 改进建议报告：计划 A 与后续计划 B》，阶段性本地提交。起点 `8061035c10795a6c886f202fbef8578c74e49527`，工作区原本干净。现行契约见[产品](product.md)、[架构](architecture.md)、[运行手册](operations.md)；后续目标不当作已有能力。

## 基线与实际观察

- 只读本地根配置与数据库：conversation/work/maintenance 均绑定提供商 `G`、`gemini-3.8-flash-high`，reasoning_effort 为 null；对话预算 8/16，工作预算 24/48/600 秒。根文件值不能证明此前进程实际生效版本，运行提交与完整模型请求仍待现场确认。
- 根配置人格为嘉然，现有相处要求已包含分清交谈对象、被纠正后收住；本地 voice_exemplars 为空。未更换模型、人格、样例、Shadow 或群设置。
- 本地最新入站事件截至 17:59:11。工作 `job_7fd2432a0551568e86ca` 仍为 partial，19 次模型／15 次工具；已有交付行动 `2dc57792-dd6f-4d06-a59d-4170e574df38` 和送达事件 `0c844ef0-3117-4fef-a2a4-e7c521fd6a16`。此前 250/515 条的部分总结后来送达，不能继续沿用旧任务文档中的“尚未交付”。
- 日历 403/连接失败、凌晨直播 412 与动态部分成功是报告已区分的来源现象。本轮尚未作外部业务查询，不推断根因或宣布恢复。11307 未见监听；未启动生产。

## 已提交：A0/A1（62ceca0）

- 群友原话改为含作者、正文、提及、引用与阅读范围的 chat_message；待处理来源和预算独立投影，含外部文字的资料不提升为指令。
- 首次上下文按业务优先选取、再按保存顺序展示当前和关联原话；后续原生工具调用与回执不排序。小工具结果不再无条件释放历史；按实际容量逐步移出可选内容。
- 现有 Trace 记录请求中各段的角色、来源、范围与省略情况，不保存全量请求或图片数据。

正常 Python 编译与 `git diff --check` 已完成，受影响 Markdown 引用已核对；未涉及前端。本轮未运行测试、夹具、断言探针、截图或覆盖率。尚未观察到新代码下的启动、模型效果或群聊送达。

## 本阶段：A2/A3 与 A4 成员提及

- source、addressed_to、reply_to、expect_reply 分开解析；普通表达支持真实 QQ 成员提及。当前群人物定位返回账号、名称与认识来源；不把 U 当人名检索。
- 普通回应和认识修改纳入相关新输入检查；本人纠正可在原事务中撤销关注，该次确认不重新续期。
- 短计算、公开网页/B 站读取开放给 conversation；长工作按工作量选用。发送已登记获准图片不再强制重复读像素，视觉分析仍须实际读取。
- 消息和 Trace 页面展示结构化提及及回应对象。

本阶段正常 Python 编译、前端构建（432 modules）和 `git diff --check` 完成；受影响 Markdown 引用已核对。页面交互与真实提及送达仍待正常启动后的人工核对，不以构建替代。

## 后续与未确认项

继续 A4 多阶段提交/等待、A5 逐来源结果及 partial 续做、A6 来源排查和 A7 面板与真实验收。B 的公共插件接口、路由、配置和迁移尚未实施；按报告的 A→B 边界，在 A 正常运行与人工核对后进入。生产起停、实群发送、推送和实际配置转换均未执行；用户本轮已授权阶段性本地提交。
