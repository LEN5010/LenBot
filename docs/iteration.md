# 当前任务：修复真实会话提交失败

用户提供 2026-09-08 12:52–12:59 日志，要求定位、修复并提交。本轮只修改源码与现行文档，不启动生产或推送。

## 实际原因与改动

| 原请求与实际记录 | 原因 | 修复 |
|---|---|---|
| 12:54:38，群 `126300994`：`[CQ:at,qq=3684366985] 总结一下今天群里发生了什么`。事件 `6776dede-6a9b-47c2-9d05-7e83110900be`，失败 trace `trc_9d5c4eff1404` | 第一步暂存 `S1`，后两步都正确提交确认，但 Actor 把生成期间所有旁听消息都列为必须补读；新来的“好好好”“神了”等没有注意力原因，仍两次阻止提交，最后被记成模型步数耗尽 | 删除全量新增输入屏障及其专用查询、mailbox 字段。控制与履约继续要求完整读取所有待处理确定唤醒；实际请求人追加要求仍由在途关注识别。末步若确有未读确定输入，保留具体冲突原因为本次失败 |
| 12:55:50，群 `1014123451`：`[CQ:at,qq=3684366985] 这周有什么直播`。事件 `9705bef1-bd19-428c-a835-efe14807c94b`，失败 trace `trc_0183367fc7cf` | 先读取两张真实图片，再暂存日程查证 `S1`；最后把 `S1` 填成 `work_ref`，遗漏 `ack_ref`。旧 Schema 将确认引用列为可选，又在没有正式工作时提供自由字符串的工作引用字段 | 有新建事项时 `ack_ref` 为每条消息必填且枚举当前回执；工作与履约引用仅按本轮真实对象展开。缺字段的错误准确说明引用错位，不把参数自动转换，不追加格式修正请求 |

两轮的模型请求均为 HTTP 200；问题发生在本地提交链。真实 trace 的 Gate 回执均为空，数据库没有这两个 episode 的 `CONVERSATION_COMMITTED` 或新建任务；两轮暂存工作和确认没有提交。第二轮图片工具观察 `3538e370ff784b6dbcd07198041e5cc0`、`66a1865ad8244361aa2f4b4046f0d09a` 保留在原数据库，失败原文与候选没有删除或重写。

## 实际配置与开放状态

此前按用户授权完成根配置补齐和插件参数填写：原插件参数来自 `ssh lbot` 的 AstrBot Docker，B 站凭证来自本机 `dynamic_asoul/.env` 的一个指定账号，仅写入被 Git 忽略的根文件。配置加载成功，已保存六个实际成员身份；“枝江娱乐的小黑”没有取得房间号，公开查询 HTTP 412 后未猜填。没有复制旧 Bot 的群列表。

本次只读检查时六个插件已经全局启用，但两个现有群的 `plugins` 仍只有 `web_search_tool`、`bilibili_content`，命令、公告和订阅为空。因此这些真实轮次没有取得日程及群总结插件，实际使用的是普通 `start_work`。全局启用与本群开放的操作路径已写入[运行手册](operations.md)；本轮未改群开放、订阅、Shadow、模型或人格。

## 核对范围与待确认项

- 只读关联原始事件、注意力标记、逐步工具参数、工具观察、暂存提案和提交记录，确认上述两个实际阻碍。
- 五个受影响 Python 文件正常编译通过；没有前端改动。`git diff --check` 与受影响 Markdown 引用核对通过。
- 未运行测试、断言探针、自动截图或模型请求；数据库、媒体和实际配置未改。没有代为启动、重放失败轮次或发送群消息。
- 修复后的模型字段选择、正常工作创建及真实送达仍待下一次获准运行中回到同群人工核对，不能据编译或源码阅读宣布通过。插件来源与 Cookie 有效性也未在本轮重新请求。

交付包含本批代码及现行文档，授权范围为本地提交，不含推送或生产启动。
