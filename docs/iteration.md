# 当前任务

更新于 2026-09-23。任务状态只见[产品路线](plan/README.md)，稳定合同见[文档入口](README.md)，此前过程见[历史入口](history/README.md)。

## 当前批次

- 分支 `feat/s0-product-contract`，本批基线 `24a8b95`，开始时工作区干净；上一批已本地提交。
- S4-05：通用群助手与个人参考入口。现有 lenbot.config.example.json 已是通用配置，不需要再复制一份完整 JSON；当前只移动明确的人格预览耦合点，保留原嘉然模板、人工样例和素材关系。

## 本批实现与核对

原 EventStore.preview_diana_persona 会由通用存储导入具体人格材料。将同一预览实现移动到现有 RuntimeQueryService.preview_diana_persona，存储只提供 list_palette。GET 设置预览入口、fields／examples／missing_media 返回形状、素材标签和人工选择／保存流程保持不变；没有兼容转发、第二角色引擎、配置来源或自动模板采用。

README 与运行手册明确两条入口：通用群助手使用已有根样例；嘉然个人参考在同一基础配置上，显式预览并选择既有模板字段，再分别处理人工样例和素材。最终都只运行 lenbot.config.json，不提供两个互相覆盖的运行文件，不把多个预设装进每次请求。

- 阅读样例的非秘密配置字段：identity_name 为 Len，identity_persona 为群聊助手，character_context 为空，members 与 scenes 为空；character_reference_assets 未填写时沿原字段默认空列表。角色业务插件停用，只有 web_search_tool 启用；供应商和连接仍需运营者填写，样例不是可直接收发的部署。
- 阅读 RootConfig 的描述符解析、关闭插件的所属校验以及人物参考字段，确认通用聊天／工作不要求填 A-SOUL 成员或导入人物素材；未运行配置加载或启动来制造验收。图片、转写和工具资料的原覆盖合同未变，不因预设选择授予已读。
- 阅读设置路由、原查询代理和存储预览的唯一源码调用关系，移动后仅预览查询按需导入 cognition.diana；该文件内容、docs/persona 原文、角色图片、表情与真实人工样例均未修改。旧具名存储方法移除，公开设置接口未改，无数据转换。
- uv compileall 编译 events/store.py、web/query_service.py，退出 0；git diff --check 无格式错误。未修改前端，不运行前端构建；预览返回及两条初始化路径的正常启动仍待同版人工验收。
- 本批源码路径查找未出现新的不存在路径错误；没有新增、修改或运行测试、夹具、断言式探针、自动截图、回放、故障注入、压力或覆盖率任务。未启动服务、读业务库、修改真实配置、调用模型／平台或真实发送。

## 上批配置判读更正

上批用 bilibili_live 查询真实配置，误把目录名当成插件 ID，随后提出“直播没有启用目标”的选择题。实际描述符是 bilibili_live_sensor。本批只读正确 ID 的启用布尔和数量，全局 enabled=True，7 个场景同时满足场景／插件启用、live_started 公告及非空订阅；没有输出场景标识、订阅成员或凭据。

已向用户更正，无需按错误前提重新开启业务；沿已有直播参考路径满足 S4-04 的已配置业务入口，其源码策略与准入修复进入待复核。没有启动、发送或证明平台连通，不将已配置说成已运行。原选择题仅用于偏好选择，用户若另选公共兴趣分享仍须另行确认启用和实际目标，不据原问题擅自改配置。错误记录在历史批次旁明确保留更正。

## 待决定与接续

1. S4-05 进入待复核：两条配置入口和具名预览归属已收口，正常初始化、个人模板采用与媒体覆盖仍待人工验收；字体、素材和公开分发授权没有因此确定。
2. S4-06 的真实群聊观察依赖获准同版环境，不新增人工输入或模拟记录充数。继续 S5 的安装、配置、保留／备份和运营文档源码工作，现场步骤保持待验收。
3. S2 完整活动段额外原生字段／必要回复片段保存边界仍待维护者回答，未登记自由文本依赖不作自动推断。S1 仍有材料／耗时口径待补；S3 与 S4-01—05 待复核，未验收或发布；S5—S7 仍有计划工作。
4. 许可证、字体／素材授权、长期数据保留、公开支持承诺仍待维护者决定；人物资料、五张常服与 19 张表情待人工采用，真实成果复用与文件交付仍待业务记录。
5. 300k／128k 仍为候选，根配置未改；既往令牌轮换与部署条件未复验。仅授权阶段性本地提交，不推送、合并、部署或真实发送。总体目标保持进行中。
