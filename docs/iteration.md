# 当前任务

更新于 2026-09-23。任务状态只见[产品路线](plan/README.md)，稳定合同见[文档入口](README.md)，此前过程见[历史入口](history/README.md)。

## 当前批次

- 分支 `feat/s0-product-contract`，本批基线 `dde1e57`，开始时工作区干净。
- 继续 S3-02：链接插件下载媒体时直接读取 Runtime、存储与文件上限配置。本批将这一条路径移回已有宿主媒体服务，不新增表、持久字段或通用下载框架。

## 本批实现与核对

新增当前调用的 download_public_media 窄方法，先核对实例归属并复用 Host.validate_call，再由宿主绑定场景和真实来源。媒体服务承接原同场景缓存查找、真实文件读取、预期类型检查、公开地址下载和资产保存；链接插件保留原资料／播放地址解析及已有客户端，不改 B 站协议或客户端生命周期。框架类型改从现有公共入口导入，网络策略和共享 B 站客户端仍是内部依赖，不宣称已独立打包。

接口世代保持 2，没有改旧方法签名或插件自身版本。资产编号、缓存及文件上限不再由链接插件访问；下载结果仍交回原工具观察登记路径，发送仍由既有提交、Gate 和回执负责。缓存不可读／类型不符不重新下载；下载失败、地址拦截和缓存错误保持原分类。新增宿主准入失败原样抛出，取消不转成普通下载错误，不重试或继续业务。

- 源码核对 media_by_locator 只选当前场景启用且已有路径的资产；get_file_bytes 沿原路径／体积／格式检查，save_downloaded 原签名和调用保持不变，媒体分段调用不受此次移动影响。
- 源码核对原 fetch_public 逐跳地址策略和流式字节上限，未访问外网、调用 B 站或下载真实媒体；ToolResult 的字段、来源与缓存标记保持原含义。
- uv compileall 编译 media/service.py、plugins/base.py、plugins/models.py、plugins/builtin/link_parser/plugin.py，退出 0；git diff --check 无格式错误。本批未改前端，没有运行前端构建；未遇到命令执行错误。
- 尚无同版开发环境；实际缓存命中、损坏文件、下载取消、准入失败、工具登记与发送均待人工验收。编译与源码阅读不等同运行通过。
- 未新增、修改或运行测试、夹具、断言式探针、自动截图、回放、故障注入或压力任务；未启动服务、读业务库、保存配置、调用模型／平台或发送。

## 待决定与接续

1. S2 活动段中不可由引用还原的原生响应字段／必要回复片段保存边界仍待维护者回答；已确认的宿主所有权和原事件权威不是额外片段保存授权。本批不扩大保存，不重复提问。
2. S1 关联诊断、请求材料、输入缓存分组及槽位等待已有阶段交付；其他提示／动态材料和等待／失败计时仍待补，同版记录及页面待人工验收。
3. S2 目前只有原话窗口引用和 R/J 名称预留，不含完整交换、其他编号／提案句柄、窗外范围、基础摘要或压缩交接。S3 已有世代 2 和部分窄入口，继续收窄其他插件执行／媒体内部依赖；群报告仍依赖 cards 版式。
4. 许可证、字体／素材授权、长期数据保留、公开支持承诺仍待维护者决定；人物资料、五张常服与 19 张表情待人工采用。真实图片关联、成果复用、文件交付及纠正后的采用待业务记录；S4—S7 仍需逐项实施或核对，不宣称整体完成。
5. 300k／128k 仍为候选，根配置未改；既往令牌轮换与部署条件未复验。仅授权阶段性本地提交，不推送、合并、部署或真实发送。总体目标保持进行中。
