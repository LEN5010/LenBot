# 当前任务：计划 A 运行后修复

2026-09-09 基于 `518afd6977d9c7147795a0521f0f4cf5dc0715ff`，阅读用户提供的 01:26—01:36 运行日志和该版本审查，并只读关联实际数据库。本批源码、面板与文档按用户随后授权纳入同批本地提交，未推送或启动生产；计划 A 的完整运行验收仍未完成。现行契约只维护在[产品行为](product.md)、[架构](architecture.md)和[运行手册](operations.md)。

## 本批改动

- 首次上下文装配计入执行预算说明；配置只允许一步时也计入终结提示。工具交换优先保留当前原话、运行事实及匹配回执，释放旧目录、参考和已提供过的历史正文；必要内容仍超量时记录准确阶段、输入、上限和分项。
- 工作目录缩为控制定位，长详情沿原始 JSON 的字符位置续读。目录不算详情已读；共享装配无法采用正文时保存带原资料位置的 `presentation_capacity_error`，不返回原地续读指令。认识、素材与待处理目录继续履行完整记录的读取授权。
- 对话事务返回真实提交结果，Actor 先采用已提交 Session，再发布 Scheduler 和完整行动列表。取消与发布异常保留 accepted、提交事件、实际结果、发布阶段和逐条入队状态；不重复事务或补发。创建确认在事务内绑定工作版本，面板可回查尚未入队的已提交行动。
- 媒体错误保留异常类型、阶段、HTTP 状态和脱敏来源地址；HTTP 请求日志不再输出 URL 查询凭据。实际来源、地址、请求参数及重试方式未改变。

## 实际观察

- 新记录已经证明预算生效：对话 Trace 为 **8 次模型／16 次工具**，总结执行段为 **24／48／600 秒**。此前“仅保存、有效值未确认”的结论已被这次真实运行记录更新。
- 01:27:38，群 `126300994` 原话“看看明天有什么直播”（事件 `a639f533-3132-4dce-baaf-75941faa6d07`，`trc_5b48b35f2358`）在 4 次模型与 4 次工具后发生输入容量失败；最后成功请求估算为 19904，等于 24000 扣除 4096 输出预留。旧目录与历史正文仍占空间。另有 `trc_8c649d595a3e`、`trc_b585b3ae0fb3`、`trc_ca569b7127ec` 在首次模型调用前即失败；其中 01:35:29 原话为“你到底还要多久”（事件 `7dbf38dc-c44d-4dcd-852b-17c6f42f93a7`）。源码确认首次装配后额外追加预算说明的缺口。
- 01:31:13 原话“那你现在总结一下昨天晚上群里发生了什么”（事件 `740c2835-49cb-478e-a938-0bbe559006fc`）经 `trc_e16faece6b0a` 成功创建 `job_7fd2432a0551568e86ca`，日期入口已走通。范围为 9 月 8 日 18:00 至 9 日 00:00（Asia/Shanghai）。创建确认 action `1a1d57d0-56ec-42ea-8b2a-704aee0dcce3` 于 01:31:29 真实送达，回执事件 `78bb8e7b-5930-4416-b43b-83b8a1188088`。
- 该总结匹配 515 条、25 人，实际读取 250 条；`trc_04c6b484120b` 于 01:32:48 形成 **partial**，保留其余 265 条及图片、转发详情等未决项。执行实际使用 19 次模型（含 3 次压缩）、15 次工具、约 79.43 秒；主动终结时仍余 5 次模型，并非硬预算耗尽。首次结果交付对话即上述 `trc_8c649d595a3e`，在模型前失败；当前工作 `result_ready`、没有 delivery_action_id，不能把创建确认当作最终总结送达。
- 日志中六个 B 站直播房间共 60 次 HTTP 412，ICS 三次 HTTP 403，QQ 原图一次 HTTP 400。ICS 错误已有明确执行阶段与状态，访问失败早于解析；B 站请求已有必要 room_id，不能凭状态码断言具体风控原因。QQ 资产 `image_36b63a1cbd845ed98bdcac98ed27d3c1` 对应 9 月 8 日 23:47:17 原图，01:28:50 读取失败；不能据此证明链接过期，也未在有关 Trace 中找到该 URL 凭据传入模型的记录。
- 本地实际工作资料中未发现超过 12000 字符的单记录；附件的长记录结论来自其构造核验，本轮没有重跑。提交后发布异常也是源码确认路径，本次没有生产发生证据。

## 核对与未确认项

正常 Python 编译、前端 `npm run build`（432 modules）及 `git diff --check` 已完成；受影响 Markdown 引用已核对。遵循[工程约束](../AGENTS.md)，未新增、修改或运行测试、夹具、断言探针、自动截图或覆盖率，也没有通过修改约束采用附件中的回归要求。没有新增表或执行数据转换。

用户日志显示 01:36:09 正常停止，本轮只读检查未见 11307 监听。没有执行生产起停、模型请求、外部业务查询或 QQ 发送；新面板实际页面、本批容量修复、长工作续读和发布异常仍待获准环境中的正常运行与人工核对。外部 HTTP 来源尚未恢复，未改模型、人格、人工样例、Shadow、群名单或根配置。前一批停机备份仍在 `.backups/plan-a-20260909-010644`，不代表本次运行后的新备份。
