# 当前任务

更新于 2026-09-24。任务状态只见[产品路线](plan/README.md)，稳定合同见[文档入口](README.md)，此前过程见[历史入口](history/README.md)。

## 当前批次

- 分支 `feat/s0-product-contract`，基线 `ab724c7`；上批 S2 必要原生片段方向已确认并归[历史记录](history/iteration-20260924-s2.md#当前段必要原生片段边界)，尚无交换保存／恢复代码，无 `result_id` 回执方式待维护者答复。
- S5-02／S5-06：原群文件配置强制填写 SnowLuma 精确版本、设置页不填版本不能标部署核验，实际形成按发行版本准入的前置；维护者已要求按 OneBot 协议和文件动作判读。本批沿原字段将版本改为可选现场记录，不修改真实配置或平台上传动作。

## 本批交付与核对

- `FileUploadConfig.version` 由必填改为可选现场备注；旧有字符串仍可读取。设置页版本输入、部署核验开关、连接页只读判读和能力卡阻断文案同版调整；换实现仍撤销原核验，改版本备注不撤销。原配置／回执标签、`upload_group_file` 动作、授权、资产审查与真实 `data.file_id` 成功条件未变。
- [运行手册](operations.md#配置-onebot-文件上传)、[支持边界](support.md#当前入口矩阵)、[Linux 前置](../deploy/linux/README.md#onebot-文件与可选项)及发布模板同步可选版本口径；路线状态仍为待复核／实施中，不把代码或构建算现场验收。
- 前端 `npm run build` 退出 0（491 模块，1.72 秒）。首次从前端目录误用仓库相对编译路径，输出 `Can't list 'src/len_bot/…'`，虽退出 0 但目标文件**未编译**；随后在仓库根目录编译三个改动模块退出 0；`git diff --check` 退出 0。页面、配置保存、目标平台、协议与上传未现场验收。详细记录归[本批历史](history/iteration-20260924-s5.md#文件上传现场版本改为可选记录)。
- 未新增、修改或运行测试、夹具、断言式探针、自动截图、回放、故障注入、压力或覆盖率任务；未读取真实根配置／业务库、启动服务、调用模型／平台或实发。没有本批业务运行失败原文。

## 待决定与接续

1. `start_work` 等暂存提案与 `respond` 的工具回执可能没有可引用的已存 observation；这些原生组的最小回执保存或明确换段方式已另请维护者确认。答复前不把任意工具正文写入段，也不把 S2 设计当已实现。
2. S2 仍为 `source_window_only`；固定材料持久版本、跨轮完整交换、容量压缩与未完事项交接都未实现。文件设置／能力卡同版页面、首个 Linux／SnowLuma 报告与文件回执、S1／S3／S4 正常操作仍待现场；数据期限、许可证／素材授权、公开承诺、S6 候选与 S7 外部闭环未完成。
3. 仅阶段性本地提交，不推送、合并、部署或实发，总体目标继续。
