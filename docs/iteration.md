# 当前任务

更新于 2026-09-23。任务状态只见[产品路线](plan/README.md)，稳定合同见[文档入口](README.md)，此前过程见[历史入口](history/README.md)。

## 当前批次

- 分支 `feat/s0-product-contract`，本批基线 `bab9f1e`，开始时工作区干净。
- 继续 S2 的编号连续性：保存资料 R 与工作 J 的身份对应，但不恢复旧阅读资格或可操作工作对象。当前仍是 source_window_only，不包含跨轮原生工具交换。

## 本批实现与核对

原 R/J 注册表同时承担分号与本轮解析，直接恢复旧表会让旧工作对象混入本轮资格；不恢复又可能把旧号发给新对象。本批在原 TurnReferences 中分开编号预留表和 active results/jobs：只有再次实际登记的对象才进入本轮解析表。资料正文、工作状态／修订、已读范围和权限都不从预留表恢复。

原 ConversationSegment 增加两个可缺省身份映射，随原 Actor 命令和原事务保存。本轮只输出当前登记的 R/J，不把所有退出上下文的旧号永久堆积；下一轮保留的同号不可改指其他身份。历史工作页不得覆盖已有当前工作快照的原路径同步保持预留映射一致。其他引用和完整交换仍未接通，不把本批当作完整跨轮轨迹恢复。

- 源码核对注册、引用解析、试装快照深复制／恢复，以及保留较新工作事实的 jobs.update 路径；后者同步维护身份预留。没有向 active jobs 注入仅有 ID 的不完整工作，也未调用 adopt_presentations。
- 首次 rg 搜索直接下标赋值未匹配，退出 1、无输出；后续按赋值及 update 真实路径找到并核对 jobs.update。它是只读搜索结果，不是运行失败。
- uv compileall 编译 scenes/models.py、actor.py、cognition/context.py、social_core.py，退出 0；git diff --check 无格式错误。未运行导入、断言、序列化或 SQLite 探针。
- 无前端修改，不重复构建；此前同版前端记录为 491 modules、2.16s。旧段缺新字段按空预留读取；旧程序对新增嵌套字段的严格解析回退影响已补运行手册。
- 未新增、修改或运行测试、夹具、断言式探针、自动截图、回放、故障注入或压力任务；未启动服务、读业务库、保存配置、调用模型／平台或发送。跨轮登记、旧号拒绝、工作修订及试装回退均待人工验收。

## 接续与未确认

1. S2-01／02 仍需窗外原话片段、基础摘要、其他编号、提案句柄和完整原生交换的保存与恢复，再接唯一压缩交接。当前身份预留只支持相邻保留上下文，不是永久全群编号目录，也不恢复人格／Schema。
2. S3-02 继续其他插件媒体／工作／运行时内部依赖；群报告渲染仍用 cards 资源。接口世代 2、正常工作回调、公共错误和调用读取待人工验收。
3. S1-02 其他提示、插件动态定义及部分专用入口仍有缺档；S1-04 首次／后续分类和部分等待计时待补。S3-01 与 S1-03／05 保持待复核，各同版页面和真实调用持久化待人工验收。
4. 许可证、字体／素材授权、数据保留、公开支持承诺仍待维护者决定；人物资料、五张常服与 19 张表情待人工采用。真实图片关联、成果复用、文件交付及纠正后的采用待业务记录。
5. 300k／128k 仍为候选，根配置未改；既往令牌轮换与部署条件未复验。仅授权阶段性本地提交，不推送、合并、部署或真实发送。总体目标保持进行中。
