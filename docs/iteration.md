# 当前任务：补齐工作提交修复

用户要求提交已有修复，模型接口超时不继续处理，提供商由用户之后更换。本轮不改实际配置、不启动生产、不推送。

## 已修复

此前 `aab5ff1` 已修复 Actor 对普通旁听消息的过宽检查及暂存回执字段。13:12:59 的真实 trace `trc_2f30d66e1b60` 暴露了遗漏：群 `126300994` 对原请求“总结一下今天群里发生了什么”已经正确提出工作和确认，但后续 Gate 又按 mailbox 的全量未读消息队列拒绝，行动列表为空。

本次删除 Gate 的重复输入检查、无人写入的 follow-up 检查，以及只为这些检查保留的 mailbox 队列、游标、确认方法和对应调用。新输入仍由事件存储进入 Context；Actor 继续核验确定唤醒是否完整读取、轮次执行权、认识版本、证据与场景，Gate 继续处理显式取消、请求者资格、工作与提醒校验、原子提交和发送。

## 实际观察

- 13:13:01 的 `trc_202b6dd5adfe` 是另一种情况：用户于 13:12:57 再次 @“这周有什么直播”（事件 `d38221df-da81-4886-ad06-23895f88bba8`），发生在最后一次模型请求期间，旧候选没有读取这条确定唤醒，因而未提交。系统随后开启新一轮；13:13:53 的 Ctrl+C 取消后续调用。未清除这些未读请求，也未绕过检查。
- 用户于 13:18 再次启动后，群总结的 trace `trc_7a1541361f58` 已通过 Gate，创建 `job_643dc526e7a652f7bd0f`，并于 13:18:39 开始工作调用。这证明该次工作创建成功，不代表总结已完成或实际送达。
- 群 `1014123451` 的 `trc_291c2e918ffe` 则在模型请求阶段失败：调用 `d957313b42ce4c30b66351e6d59579eb` 从 13:18:34.241 等待至 13:19:34.254，约 60 秒后产生 `APITimeoutError`，尚未提交。当前提供商读取超时为 60 秒；现有记录无法区分服务端排队与网络停滞。用户决定暂不处理，之后自行更换提供商。

## 核对与交付

四个改动 Python 文件正常编译、`git diff --check` 与受影响 Markdown 引用核对通过。已沿真实调用账、原始事件、工具参数和 Gate 回执阅读；未运行测试、断言探针、自动截图或额外模型请求，未写数据库、媒体及实际配置。

本轮仅提交源码及现行文档。后续工作完成、真实发送与新提供商表现仍待正常使用确认；不将工作创建成功扩大为全链路通过。插件全局启用与本群开放仍按[运行手册](operations.md)分别设置。
