# 当前任务

更新时间：2026-09-16。前批 `541b24b` 已提交 F01—F12 与 C14—C20 源码链路，`b7f9a48` 已提交 C21。当前按用户目标继续到 C29，并按阶段提交；C21—C28 已按阶段提交；C29 部署材料已补齐，实际发布证据仍待获准环境。保护计划 `7a4152d` 正文不改。C13/C14 **未放行**，代码提交不代表部署验收。

## 本批行为

- 历史维护按事件列表接收提交结果；对话租约获取串行化，维护候选在活动对话期间等待，不重复调用模型，提交后投影错误不改写完成事实。
- 日历未知成员返回可纠正结果；工具异常在宿主边界记一次。worker 面板读取参数统一。工作恢复使用原预算快照，明确无限维度与历史缺失分开处理。
- B 站公共原语使用独立匿名客户端；校验 cid 和字幕时间范围，区分需要登录与匿名空轨道；保留评论分页、字幕完整匹配片段和来源，下载字节有上限。
- Gateway 增加 browser 执行、持久命令与有界 stdio；容器镜像和 seccomp 部署说明已补。固定观察正文随 R 保存，截图经原产物接口回收；失联不重放点击，重启失效原页面。Gateway 模式不创建宿主 Chromium。
- 动作请求保存具体目标、参数、工作修订和原生调用；一次无工具审查使用原 work 绑定与预算，公共研究启动和联网 Python 已接入。网络 Python 另有默认关闭开关，当前授权仍单独核对。
- 公共兴趣候选经原工作完成事务核对匿名来源、实际读取范围与修订后采用，修订/撤回保留原因和前后状态。system 场景名不证明公开。
- 心跳由真实槽事件建立 SystemInitiator 工作，种子为根配置主题与有效公共兴趣；保留人类容量，最多原额度/1800 秒，实际工作与外部执行共同决定占用。只保存研究和兴趣，不做 C21 发布。
- 睡眠只豁免明确注册的确定性日程卡片；自然语言确认限五分钟，同一请求者在提问实际送达后确认才叫醒本群 30 分钟。Shadow 不作送达证据。
- 延期 action 单一意图、持久领取与发送尝试；原任务和延期任务同回执终结，unknown 不重发。保留原到期时间和迟到量，旧闲聊过期、成果复用、直播重取原场次。
- 面板补充心跳主题、叫醒确认、来源范围、审查用途与事件标签；前端产物同批提交。

## 实际核对

- `uv --cache-dir /private/tmp/lenbot-review-uv-cache run --offline --no-sync python -m compileall -q src/len_bot`：正常编译通过。
- 在 `src/len_bot/web/frontend` 执行 `npm run build`：通过，Vite 6.4.3，444 个模块；构建产物已生成。
- `git diff --check`：通过。只做源码阅读、正常编译/构建及监听状态读取；没有新增、修改或运行测试、夹具、断言式探针、自动截图或覆盖率任务。
- 根配置面板端口 11307 的 `lsof -nP -iTCP:11307 -sTCP:LISTEN` 未返回监听记录；没有因此启动生产，实际改动页面未做在线人工观察。
- 真实根配置、模型/人格/人工样例/Shadow/群名单与保护计划未改；没有 Reset、生产启动、实发或推送。

## 未确认与放行边界

- C13：Linux 网络隔离、公共代理转发、CONNECT 字节、卷/UID 和取消终止等真实部署证据仍未取得。
- C14：镜像未在目标环境构建/启动，seccomp 安装、真实页面和进程重启/未知命令等运行路径未观察。现有 worker 配置保留试运行入口，不作为正式隔离。
- C15—C20：真实模型审查、B 站响应、心跳完成、公共兴趣采用、自然叫醒与延期重启/早醒/实际发送回执均未做运行验收。
- 面板在线、键盘与窄屏观察待获准环境；编译和构建不写成运行通过。后续源码阶段已推进到 C29；下文按各批保留当时观察，实际部署与发布仍未完成。
- 旧 P0 计划文档仍保留；本批不以源码提交替代其未完成运行核对，也不擅自删除。

## C21 增量

- 新增默认未配置的公共兴趣分享插件，复用原 Scheduler、SocialCognitionCore、Gate 与 ActionQueue；逐群独立 grant、话题、每日次数和冷却，候选可沉默。
- 候选版本及公共资源 URL 随真实发送尝试保存，在原写事务内复核额度；unknown 保留占用，Shadow 不当送达。睡眠和连续消息上限生效，重启不补跑已领取机会。
- 场景配置说明与模型/Trace 用途标签同步；根实际配置及群名单未改。
- 正常 Python 编译通过；前端 `npm run build` 通过（Vite 6.4.3，444 个模块）；`git diff --check` 通过。没有运行测试或启动生产。真实候选模型表达、发送额度并发、延期及平台送达尚未人工验收。

## C22 增量

- 新增默认未配置的 media_analysis 插件与固定媒体容器入口。bvid/cid 和明确毫秒区间经过匿名元信息核对与原动作审查；临时 CDN URL 不进入模型。ffmpeg 在 Gateway 媒体容器解码，真实响应体字节含索引/关键帧超取，超过上限返回不足。
- 片段、执行、工作修订、采样帧时间点与音轨范围共同回收；相同原生调用不重启容器，取消沿原 Gateway 终止回执。宿主只接受本次 execution_id 的清单，失败进程不拿旧工作区产物冒充本次完成。
- 模型绑定新增默认 false 的 supports_vision，随工作冻结，缺少确认不装配视频采样帧；模型配置表单同步。可选转写绑定默认 null，使用原 ProviderRegistry 和工作账，原生 duration usage 与 token 估算分开，结果保留 derived 和原片段来源。
- 本批正常 Python 编译通过；前端 `npm run build` 通过（Vite 6.4.3，444 个模块），`git diff --check` 通过。没有运行测试、启动生产或改实际根配置。
- 未确认：媒体镜像尚未在 Linux 目标环境构建运行，真实 B站 CDN 响应与帧时间点、字节中止、取消回执、已配置供应商的视觉/转写协议与费用均未运行验收。页面未在线人工观察，静态构建不作运行通过。C23—C29 继续推进。

## C23：文件资产、ZIP 与提案

已增加持久文件资产、限定格式及 ZIP 检查、工作归属/修订/有效期、文件提案和 Gate 复核、原队列的原子上传额度及独立文件回执类型。工作页列出资产下载、尝试和回执。当前适配器对文件明确返回 capability_missing；实际 OneBot 上传由 C24 接入。

实际核对：Python 正常编译通过；前端构建通过。未运行测试、探针、自动截图或生产启动；未人工观察运行页面，未验证真实文件生成/上传、ZIP 拒绝分支或重启恢复，不能记为运行验收通过。

## C24：OneBot 文件上传适配

已接入显式 NapCat 上传协议、专用只读资产路径映射、HTTP/WS 原传输、严格 file_id 回执和 unknown 不重放。新增根配置模型与面板说明，默认 null/未验证，不改现场配置。已读取 NapCat 官方接口表与其链接的上传文档（2026-09-16），文档给出路径参数及 data.file_id；这是协议依据，不是实际 OneBot 版本的联调证据。已询问实际实现和版本，未收到确认时保持待核对。

实际核对：正常编译与前端构建通过。未运行测试/探针/截图，未上传真实文件、未启动生产；实际版本、只读卷权限、真实 file_id 和超时/重启行为仍未确认。

## C25：B 站凭据与专用登录态读取

已收窄现有动态工具为人类工作账号 connector，新增明确账号 UID/开关、当前 grant/工作修订/期限复核及审查，固定目的地与 2MB 响应上限、真实登录 UID 核对、账号 provenance、分页正文和服务器端凭据遮蔽沿原配置出口。根配置非 0600 时停止账号请求，不自动修改文件权限。默认关闭，未改现有 Cookie/根配置。

正常 Python 编译与差异格式核对通过；没有前端源码变化，插件表单使用新增 schema。未调用真实账号、没有测试/探针/截图或生产启动；实际登录读取、权限撤销及平台错误分支待人工核对。协议依据为 bilibili-api-collect 本地动态 space/nav 文档；文档不等于在线行为证据。

## C26：明确点赞/收藏状态与持久回执

已接入仅当前人类工作可用的点赞/收藏提案、独立 grant/额度/收藏夹配置、原工作审查、专用账号固定端点、同资源串行和持久尝试身份。新增 platform_actions 保存 unknown 占用，平台回执与 QQ 回执分离；当前状态核对不能倒推未知请求未执行，点赞近期状态 0 不用作取消成功。工作页展示账号、资源、目标状态、修订、尝试与结果。buvid3 为文档要求的运营输入，默认空，不自动生成。

协议只按已阅读的 bilibili-api-collect 本地 video/action 与 fav/info 快照实现；没有真实账号请求、点赞、收藏或生产启动。实际账号、收藏夹、每日限额与登录错误/重启核对仍未确认。根配置不变；C25 连接器路径打包遗漏已由补充提交 0e4ee58 修正。

本批实际核对：正常 Python 编译通过；前端 `npm run build` 通过（Vite 6.4.3，444 个模块）；差异格式检查通过。未运行测试/探针/截图；实际页面未在线观察，构建不代表平台运行验收。

## C27：可选 Core 桥接收口

已保留现有连接器，增加原命令/逐帧/回传的持久尝试身份，实际 OneBot 身份复核、精确前缀与空闲连接保持；回执关联改从原持久事件读取。首版只接受带 echo 的群文字/at/图片帧，其他消息类型及无 echo 帧明确 unsupported，支持矩阵在插件诊断页展示。地址不允许内嵌凭据，日志只记连接异常类型。

2026-09-16 读取 Core 与上游适配器当前 models.py，核对 echo 是可选的逐帧回执令牌；无 echo 拒绝属于明确的兼容范围限制，不是上游都支持的假设。没有实际 Core 端点/版本、首批游戏命令或真实逐帧回执证据，运行联调仍待完成；没有连接 Core 或启动生产。

本批实际核对：正常 Python 编译、前端构建（Vite 6.4.3，444 个模块）与差异格式检查通过。未新增或运行测试/探针/自动截图；面板未在线人工观察，未把静态核对写成 Core 运行验收。


## C28：面板跨模块事实

已补公共兴趣分页/来源证明/修订详情、独立系统周期列表、工作发起主体与实际预占账户、原预算快照、模型实际呈现范围、外部执行占用与停止回执，以及系统能力入口和独立音频秒数。普通提醒控制接口拒绝系统调度槽。以上读取原数据库事实，不创建新的调度、权限或回执体系。

实际核对：正常 Python 编译与前端构建通过（Vite 6.4.3，446 模块）；差异格式检查通过。11307 端口无监听，未启动生产，因此未在线人工核对页面、键盘与窄屏；没有测试、探针、自动截图或真实平台调用。根配置、保护计划和群名单不变。C28 运行验收仍待获准环境。


## C29：Linux 部署、恢复与发布材料

新增 LenBot Dockerfile、严格缩小构建上下文的 .dockerignore、无 Docker socket 的 Compose、独立 Gateway systemd 服务与离线配置样例。配置挂父目录保证原子保存，LenBot 数据/外部插件/Gateway 控制卷/OneBot 只读文件导出分别映射。部署文档包含 UID、两类网络、真实根配置对照、起停、完整目录加 SQLite 普通备份、保留事实的恢复与可选能力限制；附本地发布记录模板。交叉核对补正系统场景的来源链接，转到原运行记录页，避免依赖并不存在的普通会话页面；前端已重新构建。首页已移除“心跳/社会能力源码尚未实现”的过时表述。

实际核对：C28 正常 Python 编译与 446 模块前端构建通过；C29 `docker compose -f deploy/linux/compose.yaml config --quiet` 解析通过，`uv --cache-dir /private/tmp/lenbot-review-uv-cache build --offline --out-dir /private/tmp/lenbot-c29-release` 成功生成 sdist 与 wheel。未运行测试、探针或自动截图。Docker 初次只读版本查询在沙箱内返回 `permission denied while trying to connect to the docker API at unix:///Users/len5010/.docker/run/docker.sock`；随后仅为正常镜像构建取得工具升级权限，没有启动服务。首个镜像构建停留在 `load metadata for ghcr.io/astral-sh/uv:0.12.13` 数分钟无进展，由本轮主动 SIGINT 结束（`#3 CANCELED`、`exit status 130`），不是上游返回失败；配方改为在 Python 构建阶段通过 PyPI 安装同一 uv 版本，随后正常构建成功，输出 `lenbot Built`，镜像 `lenbot:release` 的原生 ID 为 `sha256:fea112d69e00ca3c23f78cb2555e0e1fd1b0684b7d4bcdc94b86a3f4f7f38657`；这是本机 Docker Desktop 的 Linux/arm64 构建，不是目标 VPS 运行验收。

C29 仍缺目标 Linux 规格/用户映射、防火墙与 DNS 隔离、实际 OneBot 版本及一个聊天群和一个播报群的当次授权与真实证据。已询问目标环境与版本，未收到确认；没有以本地源码/构建替代目标部署。Core、转写、B 站账号/收藏夹等可选项按各自缺失保持未配置/未放行。实际根配置、模型/人格/人工样例/Shadow/群名单与保护计划未变，没有 Reset、生产启动、实发或推送。

当前结论：实现已推进至 C29 的代码与发布材料；“部署就绪”和“实际链路通过”尚不能确认。C13/C14 隔离、C24 上传版本和全部未运行项保留，后续需要在具体授权环境按原身份完成正常业务观察与恢复记录。
