# 当前任务

更新时间：2026-09-16。纠偏提交 `507d559`。C13 源码合同已接：未核验部署拒绝入场，样例 `gateway.config.example.json` 不参与运行合并。C13 **不可放行**。未运行容器或真实路由。保护计划基线 `7a4152d`。

## 三个状态

| 项 | 代码 | 运行 | 放行 |
|---|---|---|---|
| P0-C01 后端入口 | 源码闭合 | 未观察 | 等待环境 |
| P0-C02 凭据 | 源码闭合 | 未观察 | 等待环境 |
| P0-C03 取消/对账 | 源码闭合 | 未观察 | 等待环境 |
| P0-C04 产物身份 | 源码闭合 | 未观察 | 等待环境 |
| P0-C05/C08 预算 | 源码闭合 | 未观察 | 等待环境 |
| P0-C07 草稿/互斥后端 | 源码闭合 | 未观察 | 等待环境 |
| P1-U 面板 | 源码闭合 | 未观察 | 不允许称 UI 已验收 |
| C11 Gateway 接线 | 源码闭合 | 未观察 | 真实根配置仍是 worker |
| C12 输入/产物 | 源码闭合 | 仅解释器 | 无真实容器 |
| C13 出口 | 有已知部署缺口 | 未观察路由 | **不允许** |
| C14—C29 | 未开始 | — | 不允许 |

## 本批合同（相对 `9cd46ad`）

- **RC-01** `deployment_verified` 默认 false，未核验的 proxy 拒绝入场。
- **RC-02** 联网只允许无 `source_event_ids`、无文本/图片导入的 SystemInitiator，且当前有 `network_python`。
- **RC-03** 响应写出头部空行；凭据绑定策略+当前执行；撤销保留累计用量；`HTTP_PROXY` 带 Basic；异步 DNS；负长度拒绝；同端口冲突启动前拒绝。
- **RC-04** 互斥后端未选分支提交 `null`。
- **RC-05** 嵌套凭据完整路径；列表按 item 类型；焦点走子表单。
- **RC-06** `capabilities[]`；新建 grant_id 由服务端生成；主体存 QQ UID。
- **RC-07** 活动状态占用；404 仅 ACCEPTED 未 start 可记未登记。
- **RC-08** 入场前再核 revision；`deadline_at` 为上界；确认启动受期限约束；同终态仍可补 stdout/stderr。
- **RC-09** 当前快照=最新已确认执行；导出/下载带同一 `execution_id`；文本页不累积前缀。
- **RC-10** 输入上限分层写在运行手册；manifest 不授予外发。
- **RC-11** `tokens=0, limit_tokens=None` 写入 NULL 上限；启动时只回填 `reserved_tokens>0` 的旧 held 行。
- **RC-12** JSON 视图只读且不重置草稿；非法数字不截断成无限。
- **RC-13** 授权错误按条目定位；成员页改名主播与订阅对象；运行参数常用预算有数字框。

`uv run python -m py_compile` 与 `npm run build` 须在提交前再跑一遍。

## 明确未完成

- C13 实际网络隔离、公网转发、CONNECT 计字节、Linux 卷/UID：部署待验，代码拒绝未核验入场。
- 面板键盘/窄屏：待人工观察。
- 文档体积：非保护工程 Markdown 当前约 99.9 KiB（architecture 43.3、operations 28.0、plugins 18.9、iteration 3.1、README 3.8、AGENTS 3.0）。保护计划与人格原文不计入。
- `docs/LenBot_P0文档收敛与修复_P1配置面板改进计划_141ec2e.md` 仍在工作树，任务关闭后再 `git rm`。
- 系统工作没有生产者；`execution_runs` 无面板消费者。

## 下一步

提交本批后，获准环境再做离线执行与面板人工核对。C14 独立浏览器不得在 C13 放行前启用。
