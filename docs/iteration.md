# 当前任务：C01—C10 基础修复（FX01—FX04 / FX12）

更新时间：2026-09-15。本批提交相对 `social-agent-m0-foundation@8f31458`，计划比较基线 `7a4152d`。本文件只维护最新状态。

## 当前结论与授权范围

用户要求阅读项目与 C01—C10 fix 交接后继续工作。工作树上已有未提交的预算改动，但类声明丢失、`job_measured_tokens` 被删、准入闭包不能 await，不能运行。本轮在该草稿上把 FX01—FX04 与 FX12 修到源码路径闭合；不实施 FX05/FX11 授权面板，不实施 FX06—FX10 Gateway，不改根配置、不启动服务、不真实发送。

| 范围 | 当前状态 |
|---|---|
| FX01 每次调用统一预留 | 源码已接：工作/压缩/技能维护/插件工作子调用经 ModelGateway 在写入调用行的同一事务准入 |
| FX02 结果完成与预算关闭 | 源码已接：`settling`、按 call 记录收口、维护结束后再结算；未运行真实维护 |
| FX03 绝对期限 | 源码已接：首次开始写 `deadline_at`，对话恢复写回已存窗口并包围 AgentLoop；旧行无该字段时仍按累计时长 |
| FX04 原上限/空策略/热更新 | 源码已接：`limit_tokens` + `budget_json` 快照；空策略不再套 10M/30M；热更新同步 `budget_config` |
| FX12 日账显示 | 源码已接：账户全日聚合，明细仍 100 条；具名策略主体仍显示默认上限 |
| FX05 / FX11 | 未做 |
| FX06—FX10 Gateway | 未做；C11 切换仍关闭 |
| FX13 认识余量 | 未做 |

## 本批代码与文档

- 工作创建写入不可变快照和 `limit_tokens`；结算不再毁掉原上限。
- 模型请求按本次估算+输出上限占用；在途调用计入后续准入。
- 空 `ReservationPolicy` 各维度默认为 null。有限日额度且无法预占时拒绝创建。
- 模型页账户合计改为 SQL 全日聚合；前端标明明细截断，并显示 `settling`。
- 产品/架构/运维正文已按上述源码改写，不再把 FX01—FX04 的旧缺口写成当前行为。

## 实际核对与限制

已做：源码阅读、定向修改、`python -m compileall` 通过（uv `--no-sync --no-dev`，缓存 `/tmp/lenbot-docs-uv-cache`）。`src/len_bot/web/frontend` 执行 `npm run build` 成功（vite 1.39s）。未打开浏览器核对模型页。

未做：不运行测试、夹具、模型、容器或服务；无真实工作观察。HTTP 客户端、晚到 usage、跨进程恢复、超过 100 条日账的真实面板、空策略升级对既有部署的影响均未运行。D05/D06 数值仍未确认，本轮未改 `job_max_seconds` 等根配置。

## 下一步与未确认项

1. 获准环境中用正常短时工作核对 A19—A21、A24：准入记录、维护入账、原上限恢复、全日余额。
2. FX05/FX11 授权闭合与面板新建 Grant。
3. 未接线状态下修 FX06—FX10，完成前不切 C11。
4. FX13 认识余量可独立推进。

旧已结算且没有 `limit_tokens` 的行不能自动恢复。空策略从隐式 10M/30M 变为不设上限，是升级行为，需运营确认是否要在根配置里写出原数字。
