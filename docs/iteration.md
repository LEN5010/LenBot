# 当前任务

更新于 2026-09-23。任务状态只见[产品路线](plan/README.md)，稳定合同见[文档入口](README.md)，此前过程见[历史入口](history/README.md)。

## 当前批次

- 分支 `feat/s0-product-contract`，本批基线 `8e03629`，开始时工作区干净；上一批已本地提交。
- S4-04：事件触发、心跳公共研究及主动分享。现有业务事件与群表达链继续保留；本批修复心跳槽触发检查到后台创建工作之间的当前准入缺口，并记录明确开启的参考业务仍待选择。

## 本批实现与核对

Heartbeat.run_slot 已在槽触发时检查启用、睡眠、占用和公共研究权限，但 _create_work 异步排队之后才进入原工作事务。admit_cycle_in_transaction 现在在查询原占用与工作数量后、写占用和工作关联前，从原能力授权的当前根配置再次核对 heartbeat_enabled、jobs_enabled、睡眠窗口及 PUBLIC_RESEARCH 权限。原槽时刻检查也移到这些读取之后，与当前检查时钟一致；容量仍要求为人类工作保留一个位置，使用同一当前根配置的 job_max_concurrent，不按旧局部配置继续采用。

缺少授权、关闭、睡眠、已错槽或权限拒绝都明确结束本次采用；原工作事务回滚，沿已有拒绝／失败记录结清该槽，不换到后来槽、不重建种子、不补跑。已开始的工作仍按原工作、预算及终止合同处理，本批不增加在途强制取消系统。

补充架构的主动业务策略，分别说明 Scheduler 槽、公共研究、公共兴趣候选、目标群表达和直播新场次。资料保存不表示要发群，研究不借人类主体或私群来源；目标群的睡眠、额度、话题和回执核对仍留在原分享／发送路径。

- 阅读 ensure_next、owns_event、run_slot、admit_cycle_in_transaction、occupied、verify_public_job 及公共工具目录。无种子的槽可 completed_empty，非当前槽 missed；真正工作由真实系统来源、原占用和预算关联，不从 system 场景名或缺少 QQ 身份推断授权。
- 阅读 interest_share.run_slot、validate_candidate、publication_for、check_publication，确认候选修订、公开来源、主题、当前群权限、睡眠、发送次数与未知回执分别核对；没有把公共研究直接接成群广播。
- 阅读直播 poller 的首次采样不补告、新场次事件保存、当前场次校验和延期重核。没有修改或调用 B 站协议；仅读取相关本地技能约束，未同步外部文档或访问平台。
- 只读根配置并输出相关启用布尔与启用场景数量，不输出凭据、模型参数或群标识：heartbeat_enabled=False，interest_share 全局关闭且启用场景为 0，bilibili_live 全局启用标志未提供且启用场景为 0。SceneSettings／ScenePluginSettings.enabled 是必填字段，没有按遗漏字段猜默认开启。该读取不是部署健康检查。
- 已异步询问以“直播开播邀请”还是“公共兴趣分享”作为验收参考；问题明确只选择业务，不更改开关、目标群或真实发送。尚未收到回答，不将任何业务写成已启用，也不因此停下其他源码工作。
- uv compileall 编译 runtime/heartbeat.py，退出 0；git diff --check 无格式错误。未修改前端，不运行前端构建；根配置、主体授权和主动实发名单未改。
- 源码查找出现 `rg: src/len_bot/runtime/action_review.py: No such file or directory (os error 2)` 与 `rg: src/len_bot/configuration.py: No such file or directory (os error 2)`；随后沿实际 capabilities.py、config_store.py 和运行时绑定读取。均为定位错误，不是业务运行失败。
- 未触发心跳、变更开关、撤权、模拟跨槽、创建研究或发送候选；准入拒绝、原事务回滚、零成果和目标群表达均待同版人工验收。编译与源码阅读不等于运行通过。
- 未新增、修改或运行测试、夹具、断言式探针、自动截图、回放、故障注入、压力或覆盖率任务；未启动服务、读业务库、调用模型／平台或真实发送。

## 待决定与接续

1. S4-04 保持实施中：源码策略与本批准入修复已交付，明确选择／开启的业务示例和真实记录仍待维护者决定及另行操作授权。不替用户启用主动能力。
2. 接续 S4-05 的通用预设与个人参考发行版边界，只先读取现有配置、人物材料引用和发行入口，不替换真实人格、模型或人工样例。
3. S2 完整活动段额外原生字段／必要回复片段保存边界仍待回答，未登记自由文本依赖不作自动推断。S1 仍有材料／耗时口径待补；S3 与 S4-01—03 待复核，未验收或发布；S4 其他项和 S5—S7 仍有计划工作。
4. 许可证、字体／素材授权、长期数据保留、公开支持承诺仍待维护者决定；人物资料、五张常服与 19 张表情待人工采用，真实成果复用与文件交付仍待业务记录。
5. 300k／128k 仍为候选，根配置未改；既往令牌轮换与部署条件未复验。仅授权阶段性本地提交，不推送、合并、部署或真实发送。总体目标保持进行中。
