# 当前任务

更新于 2026-09-23。任务状态只见[产品路线](plan/README.md)，稳定合同见[文档入口](README.md)，此前过程见[历史入口](history/README.md)。

## 当前批次

- 分支 feat/s0-product-contract，基线 3a822ea，开始时工作区干净；上一阶段 S1-01 身份／阶段关系合同已本地转待复核。
- S2-02／S2-05：上一段 R 定位虽按本群查询保存观察，但原目录与 `read_tool_result` 不复核插件当下启用、版本和入口归属。插件停用或换版后，旧观察可能再次进入新请求。沿原观察和 PluginHost 边界定向收口，不删除原始事件或观察。

## 本批交付与核对

- 原观察表只读查询新增名称及插件归属的定位投影，不加载旧正文到目录；不新增表、列或配置。段目录初装与每次最终请求前重新核对同群保存位置和当前归属，不可用项不展示旧工具名，也不激活 R。请求记录 v4 仍只登记最终保留的目录位置，不补造此前请求。
- `read_tool_result` 在旧正文进入本次模型请求前复核当前归属；被停用、退出场景、换版或原入口换所有者时返回 `saved_source_unavailable`。无归属的旧插件结果不推测所有者；宿主核心读取及工作 `find_skills`／`read_skill` 保持原读取入口。原观察留供审计，不撤回以前真实已读。
- 已在普通对话、插件 Agent 和工作轨迹中的旧工具页／插件资料，在后续请求装配时同样替换为明确失败，不把旧正文继续提供；按原 result_id 关联的媒体消息也撤下。新工作媒体清单保留该来源 ID；旧工作检查点缺关联且当前轨迹已有失效资料时，保守撤下不能归属的旧工具媒体，不触动独立原话图片。工作压缩来源及成果采用的原资料核对补当前归属。当前可解析的失效 R 从普通对话引用表移除，不改变原工具执行、预算、工作版本、Gate 或发送回执。
- 源码阅读核对原观察保存字段、普通段目录、最终请求装配、工作压缩／成果事务与 PluginHost 生命周期。`uv --cache-dir /private/tmp/lenbot-uv-cache run --no-sync python -m compileall -q` 编译本批六个 Python 文件退出 0；`git diff --check` 退出 0。未改前端，不重复构建；没有同版服务，未对权限撤销、旧版观察、压缩或文件交付做人工运行验收。编译不当业务通过。
- 编辑当前任务文档时直接调用 `python` 返回 `zsh:1: command not found: python`，改用项目规定的 uv Python 后完成；不是业务运行失败。本批无其他源码定位、编译或业务运行失败原文。未运行测试、夹具、断言探针、自动截图、回放、故障注入、覆盖率、依赖安装、服务、模型／平台调用或实发；未读取真实配置和业务库。

## 待决定与接续

1. S2-02／05 仍仅为 `source_window_only` 增量；跨轮原生交换、其他编号、旧摘要未登记依赖及完整压缩交接未完成。额外原生字段及必要回复片段保存边界待维护者答复；本批未替代它们。
2. 旧无归属插件观察的不可用判读和停用／换版后同版请求、工作压缩及结果采用待获准人工复核。S1-01／02／04／06、S3／S4／S5 的其他运行与页面项仍待人工证据。
3. S5-03 分类期限、许可证／素材授权、精确支持版本、S6 候选及远端 CI／升级、S7 外部使用者／作者闭环仍未完成；没有生产或发布授权。
4. 仅阶段性本地提交，不推送、合并、部署或实发，总体目标继续。
