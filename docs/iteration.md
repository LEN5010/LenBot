# 当前任务：方案 B 调用链收尾

审查基线为已推送的 `d87b648511dedd575ce4a4ba12c3b999cfc3c9a3`，其中包含方案 A、B 的同批改动。本轮只修复明确断点和剩余参数入口，保留原框架、数据库、前端、`ack_action_id` 及整组工具页装配；不继续扩展业务能力。

用户此前决定跳过 A6，并明确“目前具体插件的配置先不用写”。实际根配置继续保留原运营值，本轮不启动生产、不填写插件参数或启用新群。实现状态不等于实际使用验收。

## 本轮改动

| 问题 | 已修改的源码行为 |
|---|---|
| 公告用途在 Gateway 被拒绝 | `announcement` 成为正式记账用途，仍用 conversation 模型绑定。原调用账与用途统计已支持，无需新表；公告调用详情按实际 episode_id 查关联记录，不把源事件 ID 当作轮次 ID |
| 面板修改／恢复被空 QQ UID 阻断 | 可信 `operator_control` 允许无发言的工作和提醒管理，不能携带消息；原请求者继续保存在工作／提醒中，实际执行、插件调用与交付仍受当前业务条件约束 |
| 拦截器异常后继续发送 | 六个内建插件没有实际注册者，已删除 Host/Base 的行动拦截入口、权限和类型；队列只保留媒体准备，失败沿原发送失败链结束 |
| 时间和读取页量留在源码 | 工作进展冷却、等待回应存续、检索／邻居／待处理目录页量、工具发现和候选条数统一来自 runtime。原话与工具资料复用既有字符页参数；Schema、handler、SQL 不再分别保留旧默认或自动裁切 |
| 失效注释和状态记录 | 涉及文件中的旧 ADR／ReAct 说明改成当前职责；已更正上轮提交的推送状态，没有恢复历史文档树 |

12 项新增 runtime 字段在样例中保留原业务起点；样例仍只供人工初始化，不与实际配置合并。原话字符页改用已有 `tool_result_page_chars`／`tool_result_max_chars`，不再单独保留源码中的 4000／8000。

## 核对范围

- 已沿公告用途、调用账与界面关联、面板工作／提醒控制、原请求者继承和发送准备逐段阅读。
- 本轮前端 `npm run build` 成功；全部源码集成后的 `uv run --offline python -m compileall -q src/len_bot`、`git diff --check` 和受影响 Markdown 引用核对完成。
- 未新增、修改或运行测试、夹具、断言式探针、自动截图或 checksum；没有调用实际模型、重新请求外部源、写生产数据或修改实际配置。
- 没有实机复现或真实群消息送达记录，不能把源码核对或编译写成运行通过。

## 配置与既有数据

实际 `lenbot.config.json` 尚未对齐 B 的根结构，也未填写本轮新增运行字段，不能直接用新代码启动。时间、成员、QQ 白名单、群字段与具体插件参数继续等运营决定；正常启用方法见[运行手册](operations.md)。

上轮指定数据库已完成 A 的结构转换和 B 的旧工作字段补齐，10 项原信息工作没有明确记录请求者，保持 `requester_qq_uid=null`，不能从最后一条证据猜 QQ 身份。上轮普通备份在 `.backups/plan-b-20260908-104708`，包含数据库、媒体、实际根配置和源码；本轮未改数据库，不重复转换。

此前日程源请求返回 HTTP 403，动态站的一次正常查询成功；这些只证明当时的源响应。本轮没有重新取源，日程重复规则仍需成功取得实际源后确认，不为假设需求扩建日历平台。

## 待运营决定后的正常使用

1. 整理实际根配置并补齐运行字段，确定首批聊天群／仅播报群、QQ 白名单、成员、命令、订阅、全体提及和旧 Bot 同类入口停用安排。
2. 单独获准启动后操作实际修改页面，在仅播报群管理白名单用户的既有工作／提醒，核对保存、修改、恢复及原请求者执行资格。
3. 正常使用日程与引用评论、动态查询及跨页时间范围总结；遇到真实新开播时，核对“源事件 → announcement 调用 → 文案提交 → 真实发送回执”。未发生的路径继续标未确认。

本轮按用户要求将修复、构建产物和对应文档提交到本地 master，未推送，生产未由本轮启动；不将编译成功或旧阶段记录当成这些真实使用路径的验收结果。
