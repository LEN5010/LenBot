# 当前任务：方案 B

方案 B 在方案 A 的本地改动上继续，仓库原提交为 `9fde49425d8e52c8e1d27c8f4e55e8ab926f77e0`；A 未单独提交，本次按用户要求将 A、B 同批提交，不能把该旧提交称为 B 的完整实现基线。A 的根文件、ToolResult／ObservationPage、统一发送片段、文档与旧入口清理均保留；`ack_action_id` 和完整工具组容量装配没有回退。

A 已完成 Python 编译和前端构建，A6 的正常启动、页面及原群操作未执行。用户明确要求不自动启动生产，并要求“目前具体插件的配置先不用写”。本轮因此实现配置结构与功能入口，保持生产停止，不填写实际插件参数，不启用新群、订阅或插件。

## 当前进度

| 阶段 | 已做与未确认 |
|---|---|
| B0 接受 A 与运营对象 | 保留 A 全部改动。新配置允许插件 `config=null` 且必须停用；业务时间可明确未配置，成员和 QQ 白名单可为空。首批群、成员、源参数、命令词、@全体及旧 Bot 停用安排尚未填写 |
| B1 原生插件调用 | 已按显式注册表整理插件目录，具体工具沿既有 Host、ToolResult、观察分页与工作提案接入；源码集成已完成，未用实际模型请求核对 |
| B2 分群运行 | 根配置类型、场景 API 和本群设置入口已实现；Runtime 已贯通聊天资格、命令归属、工作请求者与发送类别，运行效果未确认。已移除独立实发名单的配置与页面权威，不存第二个 mode；既有私聊保留观察与原 Shadow 语义 |
| B3 日程与静默 | 已有 ICS 读取、来源身份、范围和确定性图片实现，日程命令／引用评论归属接入当前链路。实际源请求返回 HTTP 403，当前重复规则尚无实时样本，不以源码实现判源可用 |
| B4 动态与扩展查询 | 已接入唯一动态站的最新、搜索、详情、历史同日与二创查询；一次正常源查询成功，不等于模型调用、分页与群交付全链路通过 |
| B5 当前群总结 | 固定本群与时间范围、数据截点、统计、原话分页及实际读取覆盖进入原工作链。工作详情已展示范围与覆盖；预算、确认关联、修订和最终交付仍待真实使用核对 |
| B6 开播邀请 | 真实房间采样、新场次订阅事项、窄公告模式和原发送链已接齐；无真实新开播或 @全体送达验证，不补发旧场次 |
| B7 面板与文档 | 已增加本群设置、QQ／时间／成员表单、插件源状态与开放群、总结范围及原生交互记录；最终前端构建和本组 Python 编译通过。实际桌面／手机页面未打开 |

## 配置与数据准备

实际 `lenbot.config.json` 仍保留 A 的运营值，没有写入 B 的具体插件配置。它尚未对齐 B 新根结构，不能直接用新代码启动。结构样例仅展示 `access`、`scenes`、`time`、`members` 与未配置插件条目，不在运行中补默认值或合并。

根配置以 `scenes[group:id]` 保存启用、chat、插件、命令、公告、订阅和 @全体；`delivery` 仅保存全局 Shadow。未配置插件不实例化网络客户端，启用时才要求必要的时间、成员及该插件参数。QQ 回复资格与 B 站 UID／房间号分别填写。

A 的普通备份位于 `.backups/plan-a-20260908-091421`。B 已在确认面板无监听、指定数据库无打开句柄后，普通备份至 `.backups/plan-b-20260908-104708`，包括 SQLite、完整媒体、实际根文件和源码归档。

指定数据库的离线转换已提交：只为 10 项既有信息工作的 payload 补齐 `work_operation=information`、明确未知的 `requester_qq_uid=null` 及空总结范围／覆盖，提醒更新为 0。原工作没有记录请求者，不猜 QQ 归属。转换后普通读取为 6,309 条事件、776 条媒体资产和 10 项工作，未重算身份；没有在每次读取或启动时保留旧形状猜测，也没有修改实际根配置或启动服务。

## 已取得的核对

- 全部源码集成后的 `uv run --offline python -m compileall -q src/len_bot` 成功，最后一轮 `npm run build` 成功；`git diff --check` 与受影响 Markdown 引用核对完成。既有 tests 与 src/len_bot/testing 没有改动。
- 日程源实际请求为 HTTP 403，未更换来源或用旧缓存冒充新鲜数据。动态站一次正常查询返回成功，只证明该次源响应。
- 未运行测试、测试夹具、断言式探针、自动截图或 checksum；没有启动生产、调用模型批量核对或发送群消息。

## 剩余工作

1. 按用户后续运营安排填写新根结构与具体插件参数，确定首批聊天群／仅播报群、QQ 白名单、成员、命令、订阅及 @全体。当前不要求立即填写或启用。
2. 用户单独授权启动后，查看实际修改页面，正常保存配置并重启；在指定群使用日程与引用评论、动态查询和跨页时段总结，核对来源、范围、工作确认和原群交付。
3. 遇到真实新开播时，确认正确主播、每订阅群一次邀请、未订阅群无通知及真实提及回执。未发生的使用路径继续标未确认。

本次本地提交包含 A、B 的代码、构建产物和对应文档，未推送；生产保持停止。源码完成与编译成功不代替以上运行验收。

原始失败和旧阶段记录保留在 Git 历史及生产事件中，不改写为成功。仅维护本页当前状态，不新增 ADR、研究目录、阶段大日志或全量评测流程。
