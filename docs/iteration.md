# 当前任务

更新于 2026-09-22。这里只保留当前批次的交付、核对与未确认项；任务状态见[产品路线](plan/README.md)，稳定行为见[文档入口](README.md)，详细过程见[历史记录](history/README.md)。

## 当前批次

- 分支：`feat/s0-product-contract`；本批开始时 HEAD 为 `75acb53`，业务基线为 `master/9178c34`。
- 本次交付：S1 单条消息关联调用、调用登记材料，以及文档入口与历史归档。
- 用户授权本次整理并提交，代码与所属文档同批；实际提交身份以 Git 为准。本次没有推送、部署或启动服务。
- 用户已明确没有可用开发面板；同版页面与真实业务验收保持未确认。

## 本批实现

| 用户可见结果 | 实现范围与限制 |
|---|---|
| 从一条消息查看关联调用 | 沿原已读提交、行动和来源轨迹中的轮次身份，筛选同场景、非独立工作的调用，按 call_id 去重。整轮可能处理多条消息，不把整轮消耗归为单条消息独占。 |
| 查看调用用量与耗时 | 分别显示供应商输入／输出／缓存、本地输入估算和调用账起止；缺失字段保留未记录，不计算未经核实的金额或消息总延迟。 |
| 失败调用也可查登记材料 | model_call_request 与原调用准入、预占同事务保存，绑定原 scene_id/call_id。登记失败回滚，不继续传输；有登记不证明已发送或模型已接收。 |
| 查看最终装配位置 | 调用详情显示消息顺序、已有原话范围、省略、图像资产与工具顺序，按 25 个位置展开。没有来源的字段保持缺失，不从正文猜测。 |

没有新增表或结构转换。消息正文、动态提示、工具参数、完整工具定义、媒体字节、地址和凭据没有写入新清单。格式 v1 不是提示或插件版本；提示／工具定义版本留存、工具资料页完整定位和逐字还原仍未完成。直接检索／转写调用未接入新清单，旧记录不回填。

机制分别见[架构的面板与记录](architecture.md#面板与记录)、[上下文装配](context.md)和[运行手册](operations.md)。两批实现的细节与当时核对记录见[S1 实施明细](history/iteration-20260922-s1.md)。

## 实际核对与失败

| 核对 | 已取得的结果 | 不能据此确认 |
|---|---|---|
| Python 编译 | 使用 uv 对 request_record、gateway、call_store、context、query_service 五个修改模块执行 compileall，退出 0 | 实际事务、取消／中断、客户端协议接受 |
| 前端构建 | 最后一次 npm run build 退出 0，485 个模块，1.68 秒；日志 `/private/tmp/lenbot-s1-request-build.log` | 同版页面、窄屏、详情展开与返回位置 |
| 补丁格式 | 源码批次 git diff --check 无错误；新增文件对照空文件无空白错误输出 | 业务正确性 |
| 面板环境 | 常用 11307 端口没有监听，用户确认没有可用面板 | 任何实际页面验收 |

接管时错误使用 main 分支和不存在的 MessageTimeline.vue、cognition/call_service.py 进行定位，分别返回 unknown revision 和 No such file or directory；随后沿 master、MessageProgress.vue、call_store.py 阅读。失败原文保留在[S1 明细](history/iteration-20260922-s1.md)，没有创建替代路径或把定位失败记为业务故障。

本批未运行测试、夹具、断言式探针、自动截图、回放、故障注入或压力任务；未读业务数据库、调用模型／平台、保存真实配置或发送消息。构建产物不入 Git。文档整理没有再改源码或前端，因此沿用同一未变源码的编译和构建结果。

## 接续顺序

1. S1-02：继续确定提示／工具定义版本留存与动态材料的可还原范围；现有位置清单不能代替这些工作。
2. S1-04／S1-06：补完整阶段耗时口径和只读诊断材料，沿既有查询与身份推进。
3. 有同版面板后，人工核对多来源轮次、失败调用、缺档展示、窄屏及返回位置；取得证据前不关闭验收项。
4. 会话段、插件 API 世代涉及公共结构所有权时，先完成[三项决策草案](plan/s0-02-product-positioning-and-decisions.md)的选择，再实施；许可证、字体素材与保留政策分别见[授权与支持底稿](plan/s0-04-06-license-and-support.md)。

## 仍需保留的既有交接项

- 人物资料与五张常服、19 张表情仍待人工预览、入库或采用；见[素材交接](persona/asoul/README.md)，不自动修改人格、绑定和表情频率。
- 真实图片关联、研究成果复用、文件生成／登记／上传，以及群聊纠正后采用仍待同版业务记录。平台上传须核对版本、挂载与授权，以真实 file_id 确认。
- 对话 300k、工作 128k 是候选容量；本批没有改根配置。缓存、费用与延迟继续分别判读，见[缓存问题交接](serious-issue.md)。
- 诊断令牌此前被记录为明文暴露，轮换尚未确认。当前部署、公开出口、浏览器、媒体与可选能力状态未复验；历史事实见[2026-09-19—21 记录](history/iteration-20260919-21.md)，不当作当前许可或就绪状态。

## 本次文档整理

新增文档总入口；路线状态只由 plan/README.md 维护。路线书与任务卡说明目标和交付合同，旧 P／N 计划保留为原设计与验收依据；S0 盘点标注基线，委派方案标明历史用途。原 iteration 中的千余行过程按日期归档，失败、真实观察和未确认项保留。路线表中的已归档证据直接链接日期明细，避免下批更新当前任务后丢失入口；已逐项阅读归档内的相对链接并修正层级。

本次提交包含此前两批 S1 代码与所属文档、文档整理；不包含构建产物、运行配置、凭据、数据库或媒体数据。没有把源码交付改写成已验收或已发布。

提交前暂存首次因沙箱拒绝创建 `.git/index.lock`（`Operation not permitted`），经写入授权后继续；暂存差异检查发现 S1 归档末尾多余空行（`new blank line at EOF`），已移除。
