# 当前任务：改进计划 A

2026-09-09 从干净的 `master`、`540ce22e96c5d076d79ad87d24a5fda3dedab0bd` 开始实施。实际依赖为 Python 3.13.13、Pydantic 2.13.5，业务时区 Asia/Shanghai。代码、面板、构建产物和现行文档已更新，按用户随后授权纳入本批本地提交；未推送，运行验收尚未完成，因此未标记“计划 A 完成”。

## 已实现

- 五项预算随新对话或工作执行段固定，原工作累计账目不清零；请求及 Trace 提供调用位置、剩余模型／工具次数和工作时间余量，压缩后重新核对预算与工具定义。
- 总结日期在严格插件入口转换一次为带时区对象；参数、引用、HTTP 和最终展示错误保存唯一观察及脱敏正文、具体字段和调用 ID。模型错误回执与面板读取同一事实。
- 操作确认使用本轮真实 operation_ref，事务先验旧版本再应用，只有对应确认绑定实际新版本；提交事件、返回结果和 Action 保持一致，外部版本变化继续拒绝。认识操作也保存实际提交依据。
- 已回滚的候选字段、图片或资料范围错误可以在原预算中纠正，保留原生调用／回执配对、实际允许范围和续读位置。提交后的发布失败单独记录，不谎报候选未提交。
- 当前能力与明确委托、失败原因优先呈现；昵称撤销与停止采用当前称呼分别说明。人工模板增加日程失败和单图参考，仅供现有预览／保存入口选择，没有替换实际人格或人工样例。
- 关注集合内的普通发言与弱关注带来的 Bot 接话不无条件续期；目录与检索候选展示本群最近真实发送窗口中的素材使用。面板可查看预算、字段错误、范围及操作／行动关系。

父任务负责预算、终结纠正、上下文、共享接口与文档；工具协作负责日期、观察与能力事实；事务协作负责操作确认；关注与素材协作负责续期、素材窗口和面板，并交叉审阅循环与事务。现行契约见[产品行为](product.md)、[架构](architecture.md)和[运行手册](operations.md)。没有新增表或执行数据转换。旧版未标明版本的迁移 SQL 已退出运行手册；人格素材说明合并到来源 README，删除重复的 dialogue-and-stickers.md，更新旧模板数量、查询路径和终结错误说明。来源原文、许可证、生产记录与 Git 历史保留。

## 实际核对

- 已只读关联 2026-09-08 23:25—23:47 的真实记录。23:39 原话“那总结一下今天上午”（事件 `3c121c10-93c4-4285-a120-9b0ecca9174d`）的 `trc_270477b9f27f` 参数齐全、含时区，旧调用仅保留 invalid_arguments，缺少错误正文和 result ID。
- `trc_c9954a137db1` 对 `job_693e399c9d8b5649a22e` 恢复后确认因旧版本回滚；它当次只用系统完成事件作控制证据，也不符合本轮要求的人类请求依据。`trc_936c41642825` 中资料 `59282825bc0a4ef89fbb084219f89086` 实际展示 [0,6000)，总长 10842，错误全长引用被拒。历史未保存代码 SHA 的部分不按时间补写。
- 日程命令 `trc_1ee026c997bd` 明确记录唯一 ICS 来源返回 403；自然查询 `trc_010a1af36ac1` 关联错误观察 `29543af483904586bb76187c7bd6c421`。这些只能证明当次访问失败。最近发送窗口也实际读到仅由 continuing_interaction／in_flight_follow_up 引出的 Bot 回复与重复素材记录，没有重放它们。
- 正常 `uv --cache-dir /private/tmp/lenbot-uv-cache run --offline python -m compileall -q src/len_bot`、前端 `npm run build`（432 modules）及 `git diff --check` 通过；受影响 Markdown 引用已核对。依照本次明确提供的[工程约束](../AGENTS.md)，未新增、修改或运行测试、夹具、断言探针、自动截图或覆盖率；附件要求改禁测规则未采用。AGENTS、CI、依赖锁与算术／有限枚举算法未改。

## 配置与待运行核对

开始时 PID 53279 监听 `127.0.0.1:11307`，实际预算为对话 3/6、工作 16/24/300。已通过管理页面保存并回读根文件确认对话 **8/16**、工作 **24/48/600**；旧服务明确返回“重启后生效”，没有把保存值当成有效值。刷新新页面时出现 ERR_CONNECTION_REFUSED，随后已确认该端口及数据库均无进程占用；本轮没有执行停机、重启或启动生产。

当前停止状态下已普通备份数据库、完整 media、实际根配置及基线源码到 `.backups/plan-a-20260909-010644`，未做校验和流水线或结构转换。实际模型、人格、人工样例、Shadow、群名单及五项之外配置未改。

待获准启动本批代码后回读有效预算，人工核对管理详情，并沿真实人类请求验证参数回执、恢复／修订／取消确认、长资料纠正、昵称处理、图片送达与关注行为。未发起模型请求、外部业务查询或 QQ 消息；构建和源码审阅不能代替这些运行结果。
