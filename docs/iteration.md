# 当前任务

更新于 2026-09-23。任务状态只见[产品路线](plan/README.md)，稳定合同见[文档入口](README.md)，此前过程见[历史入口](history/README.md)。

## 当前批次

- 分支 `feat/s0-product-contract`，本批基线 `b322508`，开始时工作区干净；上一批已本地提交。
- S2-05 的已存认识查询再次进入请求。原新查及翻页已有修订复核，但留在后续原生工具交换或插件资料中的旧页不会再次执行展示复核。本批复用原读取和失效错误，不新增表、版本、存储片段或跨群可见范围。

## 本批实现与核对

认识页修订／可读范围／查询后到期核对抽为同一内部方法，新查和翻页继续使用。普通对话、独立插件和普通工作的最终请求在预算取舍及实际资料页清单前，逐条识别原工具页或插件资料中可对应已保存 query_memory 的正文；同一次装配的同份查询只读原表一次。有效正文保持原样；失效正文改为原 stale_memory_result，认识账本不可用改为 memory_unavailable。保留消息角色、原生 tool_call_id 配对和资料编号，错误不带旧正文、展示范围、页证据引用或自动续读。

原已保存观察不覆盖，历史已读事实不撤销；最终请求不为失效页新增阅读／证据采用。工作在两处压缩准备前也执行此核对，防止已在核对时失效的保留认识页被新压缩直接概括；压缩完成后的最终请求仍核对保留下来的原始页。没有自动发起新查询、嵌入或重排，不从错误生成替代认识。

- 阅读原词法查询、语义候选同范围／同源修订连接、有效状态和主体／类别／时间前置过滤；这些过滤已在排名及 top-k 前，不重复实现。查询期间的候选修订再读取仍保留。
- 阅读 read_presentations 的原正文／宿主投影匹配、资料编号解析与实际采用合同；只核对能定位到当前工具箱中已保存认识结果的实际正文页，目录定位和没有关联原结果的自由文字不作为认识页猜测处理。
- 阅读最终装配在 before_model 钩子之后、预算采用与调用之前的顺序；三类入口的实际阅读清单均在替换后计算。工作原先保存的原生交换及原始观察仍可审计，不把展示失败写成工具执行失败或查无结果。
- uv compileall 编译 tools/retrieval.py、cognition/social_core.py、runtime/job_runner.py、runtime/plugin_interactions.py，退出 0；git diff --check 无格式错误。未改前端，没有运行前端构建。
- 源码定位曾出现 `rg: src/len_bot/events/observations.py: IO error for operation on src/len_bot/events/observations.py: No such file or directory (os error 2)`、`zsh:1: no matches found: src/len_bot/llm/agent*` 和 `rg: src/len_bot/cognition/loop.py: IO error for operation on src/len_bot/cognition/loop.py: No such file or directory (os error 2)`；随后定位 tools/results.py、tools/retrieval.py 与 cognition/agent_loop.py。均为路径查找错误，不是业务运行失败。
- 未实际修改认识、等待到期、恢复工作或发送请求；三类入口的后续请求、旧页替换、原生配对、压缩与原审计回读仍待同版人工验收。编译与源码阅读不等于运行通过。
- 未新增、修改或运行测试、夹具、断言式探针、自动截图、回放、故障注入、压力或覆盖率任务；未启动服务、读业务库、修改根配置、调用模型／平台或真实发送。

## 待决定与接续

1. S2-05 只接通了可定位认识旧页的后续请求复核，不能标成整项完成。旧生成摘要、模型自由表达、工作状态与其他衍生材料的修订联系仍待核对；本批不声称能撤回先前送达模型的内容，也不保证核对后到调用期间状态不再变化。
2. S2 活动段中不可由引用还原的原生响应字段／必要回复片段保存边界仍待维护者回答；宿主所有权和原事件权威不是额外片段保存授权。本批不扩大保存，不重复提问。
3. S1 其他提示／动态材料和等待／失败计时仍待补；S2 尚缺完整交换、其他编号／提案句柄、窗外范围、基础摘要及压缩交接。S3 已进入复核范围但未验收或发布；S4—S7 仍需逐项实施或核对。
4. 许可证、字体／素材授权、长期数据保留、公开支持承诺仍待维护者决定；人物资料、五张常服与 19 张表情待人工采用，真实成果复用与文件交付仍待业务记录。
5. 300k／128k 仍为候选，根配置未改；既往令牌轮换与部署条件未复验。仅授权阶段性本地提交，不推送、合并、部署或真实发送。总体目标保持进行中。
