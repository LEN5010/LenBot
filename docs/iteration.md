# 当前任务

更新于 2026-09-23。任务状态只见[产品路线](plan/README.md)，稳定合同见[文档入口](README.md)，此前过程见[历史入口](history/README.md)。

## 当前批次

- 分支 `feat/s0-product-contract`，本批基线 `30e1a9a`，开始时工作区干净；上一批已本地提交。
- S3-02／05 收口所选三条参考路径。直播的最后依赖是绘图组件、HTML 渲染和已导出的 B 站业务卡片；简图字体还假定调用插件旁边有日历目录。采用现有包入口与同一随包资源，不复制实现或字体。

## 本批实现与核对

在原 cards 包显式导出 THEME、card、HtmlCardRenderer 和 BUNDLED_CARD_FONT，原绘图函数、主题对象与渲染类直接复用。直播简图从包入口取得组件，富卡仍使用原 cards.bilibili 的既有显式导出清单。公共合同分别说明核心插件入口、可选绘图入口和可选 B 站业务卡片入口；没有把联网卡片模块导入所有插件的核心入口，也没有新建渲染层、客户端或状态。

BUNDLED_CARD_FONT 定位宿主当前发行包的原日历字体文件，直播不再按本插件目录猜相邻资源。内置路径下使用的仍为同一文件；外部插件目录不必复制日历包，字体缺失仍报原错误。群报告自己的 render_font_path 不变，未改根配置、图片、模板、品牌图或字体文件，未作素材授权决定。

三条教程已同步实际依赖与边界：业务时钟、群报告使用 plugins.api；直播使用 plugins.api、cards 和可选 cards.bilibili。源码核对没有所选三例直接访问私有 Runtime、Host、EventStore 或媒体服务的调用。S3-02／05 进入待复核，而不是已验收；其他复杂内置集成仍保留内部依赖，S3-06 逐项列出，不把全部内置插件改造当作这三例已经完成的事实。

- 阅读 cards 包初始化、components、tokens、html_render 以及 bilibili 的模型、资料获取、模板上下文和渲染调用；构造导出不建立网络连接或启动浏览器，模板／品牌图在实际业务构造时读取。
- 明确 HtmlCardRenderer 的 HTML 渲染阶段不导航网页，原 http／https 拦截、截图参数和字节上限未改；富卡的资料与图片预取仍联网，不能称整条链离线。缺项、富卡错误后的既有简图路径均保留并记录边界，没有新增降级或重试。
- 文件元数据确认当前字体存在，大小 25,631,744 字节；没有读取像素、生成卡片或实际调用浏览器。该文件存在不证明授权、字体渲染效果或安装产物已验收。
- uv compileall 编译 cards/__init__.py、plugins/builtin/bilibili_live/plugin.py、plugins/builtin/bilibili_live/render.py，退出 0；git diff --check 无格式错误。未改前端，没有运行前端构建。
- 首次按单文件查找出现 `head: src/len_bot/cards/bilibili.py: No such file or directory`，随后定位 cards/bilibili/ 包。字体变量搜索无匹配时 rg 退出 1，之后沿直播调用处找到原路径；均为源码定位结果，不是业务运行失败。
- 三条真实使用链、外部目录装载、浏览器依赖、字体／模板授权、正常渲染与图片送达仍待同版人工确认；编译与源码阅读不等同运行通过。
- 未新增、修改或运行测试、夹具、断言式探针、自动截图、回放、故障注入、压力或覆盖率任务；未启动服务、读业务库、调用模型／平台、下载资料或真实发送。

## 待决定与接续

1. 下一步 S3-06：按真实目录和当前描述符整理内置插件兼容清单、迁移范围、启停／资源／依赖与现场待确认项；保留 workspace／python_workspace 等旧兼容身份，不全量启用插件或修改真实配置。
2. S2 活动段中不可由引用还原的原生响应字段／必要回复片段保存边界仍待维护者回答；已确认的宿主所有权和原事件权威不是额外片段保存授权。本批不扩大保存，不重复提问。
3. S1 其他提示／动态材料和等待／失败计时仍待补；S2 尚缺完整交换、其他编号／提案句柄、窗外范围、基础摘要及压缩交接。S4—S7 仍需逐项实施或核对，不宣称整体完成。
4. 许可证、字体／素材授权、长期数据保留、公开支持承诺仍待维护者决定；人物资料、五张常服与 19 张表情待人工采用，真实成果复用与文件交付仍待业务记录。
5. 300k／128k 仍为候选，根配置未改；既往令牌轮换与部署条件未复验。仅授权阶段性本地提交，不推送、合并、部署或真实发送。总体目标保持进行中。
