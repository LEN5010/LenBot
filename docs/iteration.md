# 当前任务

更新于 2026-09-23。任务状态只见[产品路线](plan/README.md)，稳定合同见[文档入口](README.md)，此前过程见[历史入口](history/README.md)。

## 当前批次

- 分支 feat/s0-product-contract，基线 350ce39，开始时工作区干净；上一阶段工作控制已提交，属于实际进展。
- S5-04 收口：认识撤销结果与诊断原对象，保留现有群工作台与只读投影，不新建运营平台。

## 本批交付与核对

认识撤销原路由在管理事件之后执行 Actor／Gate 提交发布，接受或拒绝之外的异常无法确认结果；页面仍可直接重交依据。现在响应携带原认识与范围、管理来源和原提交身份，以 control_accepted 区分明确接受、拒绝和未确认。提交／发布异常记录经过原脱敏的错误并结束，不重试、不从最近全局决策推断结果、不伪造提交身份，取消仍传播。

MemoryView 的未知结果保留原认识与依据，原表单和撤销入口暂停重交；读取原修订链后明确结束原确认，再从当前状态决定。明确拒绝不当成已撤销，普通读回不追认原请求。成功后的读回现在必须为 refuted，不能把 superseded 等其他非有效状态当作已撤销一致。页面提供原管理来源与提交事件链接；原认识写入、修订链、作用域和权限不变。

EventDiagnostics 原先已输出受限投影并下载当前预览，本批只补原 eventId／sceneId 读取身份核对与同步切换失效。诊断不属于原目标时不开放下载，旧预览不跨对象使用；没有新增匿名化引擎、完整数据库导出或自动上传。

S5-04 转待复核：日常手册及现有关键页面源码已收口，但真实操作、窄屏／键盘与返回定位仍待同版人工验收，不把静态阅读或构建写成现场通过。

- 阅读 MemoryView、认识撤销路由、memory/writes 的原目标与运营依据合同；没有改变真实认识、人格、人工样例、素材或根配置。
- 阅读 ScenesView／SceneInspector 的待处理来源和关联入口、查询服务 event_diagnostics 的显式字段投影及截断说明；导出不含正文、工具参数、错误原文或请求快照，仍保留业务编号且不是原子快照或单句独占用量。
- 使用本地界面资料检索表单提交反馈，命中“提交反馈／错误后续操作”建议；只采用明确状态与下一步，不采用直接重试建议。保留当前 Vue/Vuetify、语义按钮和文字提示，未创建新视觉系统。实际布局与交互检查因无获准同版页面保持待验收。
- uv compileall 编译 web/routes/cockpit.py 退出 0；最终 npm run build 退出 0，491 个模块，1.67s；git diff --check 无格式错误，构建产物不进 Git。前后端需同版，无表或数据转换。
- 源码查找出现 `rg: src/len_bot/web/frontend/src/components/CallDetails.vue: No such file or directory (os error 2)`，随后按真实文件定位 EventDiagnostics.vue 与查询服务。未据不存在路径新增组件。
- 没有获准同版开发面板或本批实际业务失败原文；未新增、修改或运行测试、夹具、断言式探针、自动截图、故障注入、回放、压力或覆盖率任务；未访问业务库或真实配置、调用模型／平台、启动服务或实发。

## 待决定与接续

1. 接续 S5-05 四类读者文档，沿已有 README、运行手册、插件合同和架构去重，不把同版现场演示或外部插件经验编造成已完成。
2. S5-03 分类保留期限仍待维护者答复；S5-02／04、S3、S4-01—05 待复核，S5-01 空环境安装与 S4-06 真实群观察待获准环境。
3. S1 材料／耗时、S2 完整活动段与未登记依赖仍有缺口；额外原生字段／必要回复片段保存边界、许可证、素材授权、公开支持和候选镜像仍待决定。S5—S7 按完整目标继续。
4. 仅阶段性本地提交，不推送、合并、部署或实发；源码、构建、验收与发布分别记录，总体目标保持进行中。
