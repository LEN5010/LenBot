# 当前任务

更新于 2026-09-23。任务状态只见[产品路线](plan/README.md)，稳定合同见[文档入口](README.md)，此前过程见[历史入口](history/README.md)。

## 当前批次

- 分支 feat/s0-product-contract，基线 432db5a，开始时工作区干净；上一阶段 S5-02 已提交并转待复核，属于实际进展。
- S5-03：数据位置、过期／删除和成套恢复；本批仅源码阅读与文档交付。

## 本批交付与核对

新增数据生命周期文档，承担原话、认识、摘要、工作、资料、方法、媒体、文件、调用、登录和独立网关数据的职责表；运行手册继续独占起停／备份命令，不重复创建运维系统。该文档明确文件卷、原身份、派生资料和不可逆外部事实的区别，并从文档入口链接。

原备份命令只列主库和 media，虽正文提到其他目录，容易遗漏。示例补齐实际使用的 file_assets、plugins，并显式列出网关日志库、workspaces_root、controls 和实际出口日志；命令仅写在文档中，未执行任何备份或复制真实数据。

- 读取 EventStore 及各 Mixin、MemoryStore、HistoryStore、MemoryIndex、技能存储、媒体服务、FileAssetService、插件数据目录、网关入口和日志存储。清点主库表与外置路径，不连接主库，不读取实际配置、凭据、消息或文件。
- 持久文件默认 retention_seconds=604800 来自类型声明，不是本部署值。创建固定 expires_at，上传拒绝过期，列表标记 expired；工作下载仍按归属读取现有文件，不因该时间删除行或字节。已明确区分有效期和保留政策，不修改此行为。
- 认识到期影响有效查询，索引重建清理的是派生行；主链窗口裁剪、授予／工作期限和 Reset 不等于长期物理删除。完整恢复不能只留摘要或重建相似正文，也不能丢掉新的调用、费用和回执。
- 已向维护者询问原话、工具／文件、调用与发送审计、备份副本的分类或统一保留期限；未收到决定前，S5-03 保持实施中，不设永久保留或自动清理，不执行数据操作。政策待答不阻塞其余 S5 源码与文档工作。
- 源码查找出现 `rg: src/len_bot/storage.py: No such file or directory (os error 2)` 和 `rg: src/len_bot/cognition/skill_store.py: No such file or directory (os error 2)`；后按真实文件定位 events/store.py 与 skills/store.py。未据错误路径推断存储不存在。
- 本批仅文档，未运行编译、构建、测试或服务；git diff --check 无格式错误，不等于备份可恢复。未运行迁移、Reset、自动截图、探针、故障注入或回放，未修改数据、人格、样例、素材或真实配置。

## 待决定与接续

1. S5-03 的保留期限与同版成套恢复现场待确认；继续不依赖政策决定的 S5-04 日常运营入口核对，不新建备份校验、自动清理或兼容平台。
2. S5-02、S3、S4-01—05 待复核；S5-01 空环境安装与 S4-06 真实群观察待获准环境。S1 材料／耗时、S2 完整活动段与未登记依赖仍有缺口，额外原生字段／必要回复片段保存边界待决定。
3. 许可证、字体／素材授权、公开支持与精确候选镜像由维护者确定；S5—S7 按完整目标继续。既往运行条件不继承为本批授权。
4. 仅阶段性本地提交，不推送、合并、部署或实发；源码、编译构建、人工验收、发布分别记录，总体目标保持进行中。
