# 当前任务

更新于 2026-09-23。任务状态只见[产品路线](plan/README.md)，稳定合同见[文档入口](README.md)，此前过程见[历史入口](history/README.md)。

## 当前批次

- 分支 `feat/s0-product-contract`，本批基线 `0af628a`，开始时工作区干净；上一批已本地提交。
- 继续 S3-05：任务卡明确要求修正时钟的 silent/incomplete 合同，并提供读取命令、业务事件表达和后台工作产物三条教程。复用原代码，不另造同功能插件或人工事件。

## 本批实现与核对

LocalClock.on_brief 仍要求 sources.status=replied，与当前 SourceResolution 的 silent/incomplete 字面值冲突。本批修正为消息的 source 关联真实已读命令 M，next=end，成功回应由宿主派生；没有改主模型、人格、命令、配置、额度、真实消息或来源身份。确定性“现在几点”路径不变，仍无需模型。

群报告仅剩 render.py 两处框架内部导入：冻结主题和纯语义分页函数。本批由既有 plugins.api 直接导出 CARD_THEME／split_card_pages，渲染端改用公共入口，复用同一对象和函数，不复制主题、分页或绘图代码，也不新增渲染服务／类型／存储。所有群报告框架 import 现从公共入口取得，业务模型仍在原插件；渲染算法、字体选择和资产登记流程不变。接口世代仍为 2，不产生素材授权结论。

新增[三条参考路径](plugin-examples.md)，归文档入口导航：按真实符号描述配置、读取、发布、handler、表达、暂存工作、批次进度、渲染及交付，链接原代码而不是复制实现。时钟和群报告已使用公共框架导入；直播事件的公共发布／表达部分可按教程阅读，但完整传感器仍有延期刷新、存储查询和卡片内部依赖，明确不能整包当作独立公共接口范例。S3-05 因此继续实施，不以文档齐全冒充全部三例完成。

- 源码核对 proposals.py 中 SourceResolution／Respond／RESPOND；input_mode=source 由宿主提供实际 M，不在时钟指令中固定编号；result_only 的开播 Invitation 不混用 respond 参数。
- 阅读群报告 __init__、work、service、analysis、render 与配置，核对原固定范围、父预算、进度、JobResult.delivery 和 PreparedWorkDelivery。阅读 cards 的包初始化、tokens、layout、models：新导出不建立客户端或任务，原主题冻结，分页不拆超高单项；没有新增资源读取。
- uv compileall 编译 local_plugins/local_clock/__init__.py、plugins/api.py、plugins/builtin/group_summary/render.py，退出 0；git diff --check 无格式错误。未改前端，没有运行前端构建；没有实际绘图、网络请求或外部来源取样。
- 初次按假定路径查找出现 `rg: src/len_bot/cognition/respond.py: No such file or directory (os error 2)`，随后定位实际 cognition/proposals.py；这是源码定位错误，不是运行失败。
- 没有同版运行环境；时钟简报的实际参数、三条业务来源、报告批次与渲染、图片交付均待人工验收。教程不创建假事件、测试群、自动截图或服务，编译不等同运行通过。
- 未新增、修改或运行测试、夹具、断言式探针、回放、故障注入、压力或覆盖率任务；未启动服务、读业务库、保存根配置、调用模型／平台或真实发送。

## 待决定与接续

1. 继续 S3-05 的事件参考路径：沿真实开播身份和延期处理收窄必要宿主依赖，不复制传感器、不扩成通用事件平台；完整卡片与共享业务模块依赖需明确归属。S3-06 仍需逐插件兼容与迁移清单。
2. S2 活动段中不可由引用还原的原生响应字段／必要回复片段保存边界仍待维护者回答；已确认的宿主所有权和原事件权威不是额外片段保存授权。本批不扩大保存，不重复提问。
3. S1 其他提示／动态材料和等待／失败计时仍待补；S2 尚缺完整交换、其他编号／提案句柄、窗外范围、基础摘要及压缩交接。S3-02 专用依赖随业务收窄；S4—S7 仍需逐项实施或核对，不宣称整体完成。
4. 许可证、字体／素材授权、长期数据保留、公开支持承诺仍待维护者决定；人物资料、五张常服与 19 张表情待人工采用，真实成果复用与文件交付仍待业务记录。
5. 300k／128k 仍为候选，根配置未改；既往令牌轮换与部署条件未复验。仅授权阶段性本地提交，不推送、合并、部署或真实发送。总体目标保持进行中。
