# 当前任务：C01—C10 基础修复收口

更新时间：2026-09-15。本批相对 `f4115e4`（FX01—FX04/FX12）。计划比较基线 `7a4152d`。本文件只维护最新状态。

## 当前结论与授权范围

用户要求安全提交上一批后，继续完成剩余 FIX 并再提交。本轮实施 FX05、FX11、FX06—FX10、FX13；不改根配置、不启动生产、不真实发送、不把 Gateway 接到 `run_python`。

| 范围 | 当前状态 |
|---|---|
| FX01—FX04 / FX12 | 已提交 `f4115e4`；未运行真实工作 |
| FX05 | 源码已接：create/revise/resume 与执行期 grant；策略绑定 public_research；失效策略拒绝；并发准入；系统工作只读 global-safe |
| FX11 | 源码已接：可编辑授予请求、认证后绑定签发者、只改白名单保留原授予 |
| FX06 | 源码已接：短执行记录进程结果、取消后不启动、sweep 走停止边、单条失败不拖垮服务 |
| FX07 | 源码已接：工作区归属与占用（含未知终止） |
| FX08 | 源码已接：JSON 枚举解析；拒绝 / 冲突 / 结果未知三类 |
| FX09 | 源码已接：按 worker GID 设置控制目录/脚本/工作目录权限；未在 Linux 容器上运行 |
| FX10 | 源码已接：产物复制到不可变目录后按描述符打开；目录字节/文件上限 |
| FX13 | 源码已接：认识页按余量装完整记录 |
| C11 Gateway 切换 | 仍关闭 |

## 本批代码与文档

- 非人类工作的授权、计费策略和执行期复核走同一条 Grant。
- 面板不再把空 `operator_id` 交给完整 Grant 模型。
- 未接线 Gateway 的状态机、占用、HTTP 分类、UID 和产物边界按合同修订。
- 历史维护认识读取不再整页硬塞。

## 实际核对与限制

已做：源码阅读与定向修改、`python -m compileall`、前端 `npm run build`。未打开浏览器核对设置页；未运行 Gateway 容器、模型或真实授予保存。

未确认：Linux UID/卷映射、短执行与取消竞态、产物替换窗口、超过余量的真实维护批次、面板登录保存授予。D05/D06 数值仍未改根配置。

## 下一步

获准环境中按 A08/A22/A23/A24 做正常短时工作、面板保存授予和（若授权）离线 Gateway 最小执行。C11 切换另需当次授权。
