# 当前任务

更新于 2026-09-23。任务状态只见[产品路线](plan/README.md)，稳定合同见[文档入口](README.md)，此前过程见[历史入口](history/README.md)。

## 当前批次

- 分支 `feat/s0-product-contract`，本批基线 `7e0901b`，开始时工作区干净；上一批已本地提交。
- S2-05 的工作压缩摘要来源。原 WorkSegment.result_ids 由生成响应挑选，不能覆盖输入中未被选中的认识；另有整个 work_state 作为压缩输入，却不属于给定交换的来源清单。本批沿原压缩条目、工具观察及请求装配修复，不新增依赖表、完整原生回复保存或跨群读取。

## 本批实现与核对

压缩输出合同只让生成响应填写 summary 和 unresolved；新条目的 result_ids 改由宿主从实际所选区间的成功工具回执／结构化插件资料收集，仍使用原字段，保存前要求属于原工作资料集合。不为模型选中的少量引用冒充完整输入来源。失败展示说明不含旧事实正文，不把其中保留的旧 result_id 重新当作有效输入依赖；错误和原生交换仍在原归档中。

压缩请求不再附带全局 work_state，只保留原目标／约束、给定交换和保留尾部数量。工作运行请求本身仍有原 work_state，本批没有擅自删除或重写工作状态。源资料在维护调用计费前以及响应生成后采用前分别复核：对已登记的认识查询使用原修订／到期检查，对历史摘要使用原批次及来源可用性检查；来源失效沿原压缩失败记录结束，不保存新摘要，不自动重做或换模型。

WorkCompressor 接入当前工作工具箱，在每次 prepare 的阈值判断之前、最终工作请求的钩子之后复核保留摘要。只按原压缩条目精确生成的载体定位，不从自由文字猜摘要身份；已登记来源不可用时，下一请求用区间、资料编号及失败说明替换摘要和 unresolved。原 compression 条目、原观察、此前真实阅读和原生归档不回写，也不新增证据。下次原检查点保存可记录这个不可用说明，但不会修改先前已保存的摘要审计。

- 阅读原 job_exchanges 的每个完整交换保存／页精化、压缩条目区间与 checkpoint 恢复；现有结果列表足以登记当前输入中的可定位成功资料，不需要新增来源表。旧摘要来自模型选取的列表可能不全，本批只复核旧列表中的实际引用，不从旧交换或当时未保存的全局状态猜补全部依赖。
- 阅读唯一 WorkCompressor 构造处、两处 prepare 及最终请求，确认低于压缩阈值也会复核旧摘要；请求预算仍在摘要撤下后按原路径计算，不因引用变多绕过容量约束。
- 阅读原保存压缩的同工作／场景／修订及 processing 事务；生成期间来源失效会保留已发生调用费用，按原 compression.status=failed 与上下文中断路径记录具体错误，未重置次数或期限。已有权限和预算异常继续按原类型传播。
- uv compileall 编译 runtime/work_context.py、runtime/job_runner.py，退出 0；git diff --check 无格式错误。未修改前端，不运行前端构建。本批源码定位未出现新的不存在路径错误。
- 未实际压缩、改变认识、等待到期、删除来源或恢复工作；新条目来源列表、旧摘要撤下、生成期间失效、预算计费及检查点回读均待同版人工验收。编译与源码阅读不等于运行通过。
- 未新增、修改或运行测试、夹具、断言式探针、自动截图、回放、故障注入、压力或覆盖率任务；未启动服务、读业务库、修改根配置、调用模型／平台或真实发送。

## 待决定与接续

1. S2-05 已接通知识旧页、历史批次来源及工作压缩条目已登记来源的后续复核。旧压缩条目未列出的依赖、自由表达和全局工作状态里的认识依赖仍待核对；不把原始来源存在当作当前断言正确，不宣称能追溯全部推理依赖。
2. S2 活动段中不可由引用还原的原生响应字段／必要回复片段保存边界仍待维护者回答；已确认的宿主所有权不是额外保存授权。本批不扩大保存，不重复提问；完整段交接仍待完成。
3. S1 其他提示／动态材料和等待／失败计时仍待补；S3 已进入复核范围但未验收或发布；S4—S7 仍需逐项实施或核对。接续核对工作状态中已有资料依据与新请求投影，不再给自由文本另造猜测型依赖系统。
4. 许可证、字体／素材授权、长期数据保留、公开支持承诺仍待维护者决定；人物资料、五张常服与 19 张表情待人工采用，真实成果复用与文件交付仍待业务记录。
5. 300k／128k 仍为候选，根配置未改；既往令牌轮换与部署条件未复验。仅授权阶段性本地提交，不推送、合并、部署或真实发送。总体目标保持进行中。
