# 当前任务

更新于 2026-09-24。任务状态只见[产品路线](plan/README.md)，稳定合同见[文档入口](README.md)，此前过程见[历史入口](history/README.md)。

## 当前批次

- 分支 `feat/s0-product-contract`，基线 `cb63714`；上批已收口支持文档的当前打包事实，仍未创建候选或发布。
- 本日实际运行后，群 1078114081 的历史维护批次在第二次请求前超出输入容量而失败，该群摘要停在 2026-09-18。本批修切批次容量与位置记录重复；按用户要求更新嘉然表达方式、反例清单和群聊样例。

## 本批交付与核对

- 历史批次切分与面板重试改用扣除认识读取预留后的容量；完整片段的位置记录不再重复正文已有的事件类型和说话人，维护固定提示为修订 2。见[架构](architecture.md)、[上下文参数](context.md#相互影响的参数)、[运行手册](operations.md#历史维护阻塞)，失败原文与数据见[本批历史](history/iteration-20260924-s6.md#历史维护批次占满输入容量)。
- 只读查询业务库并离线重算估算：失败批次首个请求由 111769 降为 75450，新容量 107712。未启动服务、调用模型或实际重试；两个改动文件 `py_compile` 通过；未新增、修改或运行测试。
- 嘉然预设改为 `diana-v5`；根配置 `conversation_style` 与 47 条全局文字样例已在停机状态下写入，备份在仓库外 `~/lenbot-backups/`。按用户当次要求直接生效，未经面板逐条预览；详见[本批历史](history/iteration-20260924-s6.md#嘉然预设-v5-与群聊样例)。

## 待决定与接续

1. 2026-09-25 维护者已决定：没有 result_id 的工具回执（暂存提案、终结工具 `committed=false`、`fresh_input_conflict`、成功终结后的 `after_finish`）只保存宿主生成、结构固定的最小回执——暂存句柄、committed 真假、拒绝或冲突原因码、工具调用 ID；不存模型正文与 Hook 任意改写，不从 trace 或提交结果倒推；Hook 改写且无法由权威状态重建的组标为不可续接并由宿主写明原因换段。见 [S2-01 设计](plan/s2-01-conversation-segment.md#回执定位不等于原生回复可还原)。
2. S2 仍为 `source_window_only`；固定材料持久版本、跨轮完整交换、容量压缩与未完事项交接都未实现。文件设置／能力卡同版页面、首个 Linux／SnowLuma 报告与文件回执、S1／S3／S4 正常操作仍待现场；数据期限、许可证／素材授权、公开承诺、S6 候选与 S7 外部闭环未完成。
3. 待人工：在群 1078114081 历史页重试批次 `9945c84347394da0aced898e8b08ae94`，核对完成状态和上游实际输入；维护估算约低 23% 仍未处理。重启后对多日前 @ 消息补回复是否需要过期上限，待维护者决定。新表达方式与样例的实际效果待下一次正常运行观察。
4. 仅阶段性本地提交，不推送、合并、部署或实发，总体目标继续。
