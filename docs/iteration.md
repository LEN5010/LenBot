# 当前任务

更新于 2026-09-23。任务状态只见[产品路线](plan/README.md)，稳定合同见[文档入口](README.md)，此前过程见[历史入口](history/README.md)。

## 当前批次

- 分支 `feat/s0-product-contract`，基线 `f45b151`，开始时工作区干净；上一批 S1-06 单条消息审计源码已转待复核，尚无同版页面或业务链验收。
- S1-04：原 `timings_ms.publication` 包含本群按提交顺序发布前等待与准备，却不能解释具体是否卡在发布串行锁。沿原 `RuntimeGate.publish_committed` 和持久提交身份，单列申请该锁至取得／取消／失败的已耗时间；不借此补造发送状态或总延迟。

## 本批交付与核对

`PublicationRecord` 在原每群发布串行锁申请开始后计时；取得后记 `acquired`，取得前取消或失败记相应状态和已耗毫秒，原异常、已提交行动身份、队列和发布状态不变。已取得后发生的准备／调度／入队问题仍由原发布阶段与回执解释，不倒写成锁等待失败。即时取得也可能记录正值，不能据此断言实际争用。

只读轨迹投影从原对话 checkpoint 或插件执行 `commits` 的各次持久提交记录逐项提取等待，旧记录返回空列表；阶段页在原段落内列出每次等待与对应提交，不与外层 `publication` 累计或发送时长相加。未在业务库新增列、表、trace 类型或配置。适用原直接和分阶段提交路径，不对未形成提交的候选生成锁计时。

- 源码核对 `publish_committed` 的等待／异常传播、对话 checkpoint 发布后刷新、插件提交审计保存、查询服务原轨迹分段与详情页 `TraceTimings` 的使用路径。`uv --cache-dir /private/tmp/lenbot-uv-cache run --no-sync python -m compileall -q` 编译 `gate.py`、`query_service.py` 退出 0；前端目录 `npm run build` 退出 0，491 个模块、1.67 秒，日志 `/private/tmp/lenbot-publication-lock-build.log`；构建产物不入 Git。
- 无同版获准服务与面板，实际锁争用、取得前取消、轨迹投影及页面显示待人工正常操作；编译、构建和源码核对不当作业务通过。本批没有命令或业务失败原文。未运行测试、夹具、断言探针、自动截图、回放、故障注入、覆盖率、依赖安装、服务、模型／平台调用或实发；未读取真实配置和业务库。

## 待决定与接续

1. S1-04 仍未形成完整无重叠总延迟：其他等待、未选定候选和同版用量／计时仍待复核。原缓存率、覆盖、请求分组、工作与维护计时继续按各自已存事实解释，不把本批锁计时当任务完成。
2. S1-02 动态定义／材料仍缺；S1-06 已交付源码待同版实链与页面复核。S2 仅 `source_window_only`，原生交换、摘要基线与压缩交接未完成，额外字段保存边界待维护者答复。
3. 已选 Linux 容器群报告和文件交付仍需正常现场回执；S5-03 分类期限、许可证／素材授权、精确支持版本、S6 远端 CI／升级、S7 外部使用者／作者闭环仍未完成。没有生产或发布授权。
4. 仅阶段性本地提交，不推送、合并、部署或实发，总体目标继续。
