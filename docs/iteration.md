# 当前任务

更新于 2026-09-23。任务状态只见[产品路线](plan/README.md)，稳定合同见[文档入口](README.md)，此前过程见[历史入口](history/README.md)。

## 当前批次

- 分支 `feat/s0-product-contract`，本批基线 `0b4f0e7`，开始时工作区干净；上一批已本地提交。
- S2-05 的工作状态投影与已完成步骤依据。原状态仅在进入工作循环时装入；update_work_state 已保存新状态，后续请求却仍带旧初始投影。原事务会核对提交的阅读范围，但允许完成步骤只填资料 ID 而不填范围。本批沿原状态、资料和请求载体修改，不增加状态表、版本或新存储机制。

## 本批实现与核对

_validate_work_state 要求每个完成步骤列出的资料 ID 都有该步骤的 evidence_spans，再由原阅读账本核对坐标、总长及真实已提供范围。该入口同时约束 update_work_state 和成果携带的状态；关键资料目录仍可作为后续定位，不把所有 key_result_ids 都改成必须已读，不以取得资料 ID 代替完成步骤的阅读依据。

初始工作事实标记为唯一 work_facts 载体。两处工作压缩准备前、最终模型请求的钩子之后，重新读取当前同工作／修订／processing 状态，只刷新载体中的 work_state、state_needs_revision 及工作状态错误，不重新装入原话、媒体或整个资料目录。最新版状态在原窗口预算计算之前进入请求；缺失或重复宿主载体直接结束，不猜位置或另建替代载体。

旧状态若缺少完成步骤的逐项阅读范围，返回 work_state_evidence_missing；已登记来源中的认识查询或历史摘要失效时沿原错误说明。只将当前请求中的 work_state 置空，携带资料编号和原因；数据库状态及先前提案、原生交换仍保留审计。state_needs_revision 仍只说明目标版本变化，不与资料失效混成另一状态。原系统合同明确历史提案参数不是当前状态，需明确补读／核对后更新，不自动重算计划。

工作压缩与状态投影共用工具箱的 saved_knowledge_failure，只核对已登记资料中的认识查询和历史摘要，保留原作用范围；没有新增通用来源检查平台，没有检查无关外部页面的“实时真伪”。

- 阅读 WorkState、CompletedWorkStep、_validate_evidence_spans、update_work_state、complete_job，确认本批新增的只是完成步骤资料与其实际阅读范围的关联；原同群、工作资料归属、事务与结果拒绝仍保留。
- 阅读初始事实的第一个文本块、图片清单同步、检查点恢复时重新建立前两条消息、两处压缩准备及最终装配；仅修改已标记的宿主事实，不改变图片位置、原目标、预算或原生调用配对。
- 阅读钩子的材料追加与预算装配顺序，状态刷新发生在原容量核对前。状态中的未登记自由文本依赖不能凭文本推断；本批未改终结成果其他来源的采用规则。
- uv compileall 编译 runtime/job_runner.py、runtime/job_store.py、runtime/work_context.py、tools/retrieval.py，退出 0；git diff --check 无格式错误。未修改前端，不运行前端构建。
- 源码定位出现 `rg: src/len_bot/cognition/evidence.py: No such file or directory (os error 2)`；随后沿 runtime/job_store.py 的 _validate_evidence_spans 阅读。属于路径查找错误，不是运行失败。
- 未实际保存状态、读取资料、纠正认识、恢复工作或调用模型；同轮状态刷新、旧状态撤下、完成步骤缺少依据拒绝及预算取舍均待同版人工验收。编译与源码阅读不等于运行通过。
- 未新增、修改或运行测试、夹具、断言式探针、自动截图、回放、故障注入、压力或覆盖率任务；未启动服务、读业务库、修改根配置、调用模型／平台或真实发送。

## 待决定与接续

1. S2-05 已接通状态的已登记认识／摘要来源投影核对，但不是全文依赖追溯。旧压缩条目漏记的来源、自由表达和没有关联资料的计划文字仍不作有效性猜测；最终成果采用时的来源复核还应沿原提交边界继续检查。
2. S2 活动段中不可由引用还原的原生响应字段／必要回复片段保存边界仍待维护者回答；已确认的宿主所有权不是额外保存授权。本批不扩大此保存，不重复提问；完整段交接仍待完成。
3. S1 其他提示／动态材料和等待／失败计时仍待补；S3 已进入复核范围但未验收或发布；S4—S7 仍需逐项实施或核对。继续不依赖保存决定和现场验收的源码任务，不把当前小批完成当成总目标完成。
4. 许可证、字体／素材授权、长期数据保留、公开支持承诺仍待维护者决定；人物资料、五张常服与 19 张表情待人工采用，真实成果复用与文件交付仍待业务记录。
5. 300k／128k 仍为候选，根配置未改；既往令牌轮换与部署条件未复验。仅授权阶段性本地提交，不推送、合并、部署或真实发送。总体目标保持进行中。
