# 当前任务：社会 Agent 文档整理与基础修复准备

更新时间：2026-09-13。代码基线 `social-agent-m0-foundation@2c862c6`，计划比较基线 `7a4152d`。本文件只维护最新状态；既有阶段、运行失败原文与配置操作完整保存在 [历史任务记录](archive/iteration-through-2c862c6.md)。

## 当前结论与授权范围

用户依次要求审查分支、修订计划并新增 C01—C10 fix、整理遗留文档，随后授权将本批文档提交到当前分支。本批只改文档，不实施代码修复、改根配置/数据库、启动服务或真实发送；此次仅作本地 Git 提交，不推送。开始整理时已有上一轮未提交的文档修改，本次在其上继续整理。

分支保留并定向修复，不整体重写。C01/C03/C04/C05 的代码保留；C02 的认识分页尚未完成请求余量适配；C06—C10 的预算、恢复、授予与 Gateway 相关条目仍有已知缺口。开发状态和启用资格不能从历史“实现完成 / 未运行”直接继承。

| 范围 | 当前状态 |
|---|---|
| 完整计划与 C01—C10 fix | 文档已修订；保留原 C00—C29，新增 FX01—FX13 和 A19—A24 核对要求 |
| 产品/架构/运维/插件/执行边界 | 已按当前源码修正正文，目标只链接计划，不再靠页首提示覆盖正文中的强保证 |
| FX01—FX13 代码修复 | 全部待实施；本轮没有修改运行代码 |
| Worker Gateway | 代码存在但未接入 run_python；相关基础修复前不进行 C11 正式切换 |
| 历史文档 | 旧计划、readiness、实施日志停止维护当前状态；旧任务与运行手册已保存快照，统一从历史索引进入 |

## 本批文档改动

- README 分别指向当前行为、架构、操作、插件、目标、缺陷、进度和历史资料；人格与来源原文保留。
- 产品与架构正文如实描述调用预留/结算缺口、累计活动时长、恢复重算上限、空策略默认值和局部 grant 检查；不将目标写成已具备。
- 运行手册修正预算热更新、授予新增、余额显示和当前新增表说明；旧 A/B 搬移表及过时环境记录移入历史快照。回退保留上线后消息与回执，不再笼统要求恢复旧数据库。
- 插件文档更新日历 source_unavailable 状态卡分支、取消传播及共享预算限制；执行边界分清宿主 Python、同进程浏览器和未接线 Gateway，修正截图登记与像素已读的含义。
- 补充只读核对发现：reset_conversation_data 的清理表列表尚未包含 usage_reservations、execution_runs、execution_events。已在运行手册说明，不能把 Reset 当作修复额度或停止容器的手段；未操作 Reset，未扩大为本轮代码任务。

## 实际核对与限制

本批只做源码和文档阅读、Markdown 编辑、相对链接整理与差异核对；没有运行测试、临时库/断言式探针、自动截图、模型、容器或服务。无代码/前端变化，不重跑编译或构建。前一轮编译和前端构建成功的记录保存在历史任务中，不作为业务验收。

文档归档脚本首次执行被本机 uv 缓存权限阻止，原错误为：`Failed to initialize cache at /Users/len5010/.cache/uv`；`failed to open file /Users/len5010/.cache/uv/sdists-v9/.git: Operation not permitted (os error 1)`。该次未执行归档脚本。随后使用 `uv run --no-sync --no-dev --cache-dir /tmp/lenbot-docs-uv-cache python` 完成文档编辑，不修改项目依赖或运行配置。

最终核对：`git diff --check` 无输出、退出码0；对 fix 文档和三个新增归档文件分别作 `git diff --no-index --check -- /dev/null <文档>`，均无空白错误输出，退出码1表示新增差异。人工核对 README、当前任务、历史索引和两份快照的相对链接，以及当前正文中的期限、预占、授予和迁移措辞；工作树变化仅为 Markdown。所有触发链仍是静态分析，没有运行复现；特别是 HTTP 客户端、UID/卷、异步取消和晚到 usage 的真实表现未验证。

## 下一步与未确认项

1. 按 [fix 文档](social-agent-c01-c10-fix.md) 先修 FX01—FX04 预算事实、公共请求入口及恢复，联动 FX12 的完整日账显示。
2. 补 FX05/FX11 的真实主体、当前授权和正常面板保存，再修未接线 Gateway 的 FX06—FX10。
3. FX13 的维护请求余量适配可独立推进；文档正文整理完成不代表该代码缺口已解决。

D05/D06 数值的逐项采纳、旧记录的原额度/首次开始事实、目标 Linux UID/卷映射和真实业务核对条件仍未确认。部署、生产启动和真实发送按用户当次授权处理。

入口：[完整实施计划](LenBot_社会Agent_完整实施计划_7a4152d.md) · [C01—C10 fix](social-agent-c01-c10-fix.md) · [历史文档索引](archive/README.md)。
