# 2026-09-23 S0 基线与文档入口复核

任务状态只见[产品路线](../plan/README.md)，当前交接见[当前任务](../iteration.md)。以下事实仅属于对应批次。

## 依赖边界交付转入复核

阶段提交：`30357d3`。

### 当前批次

- 分支 feat/s0-product-contract，基线 c282533，开始时工作区干净；上一阶段上段资料定位的逐请求清单已本地提交。
- S0-03：基线依赖测绘与非 A-SOUL 配置草案已有，但任务表仍列实施中。本批只按当前源码复核实际边界及后续改动，区分已交付的边界选择和仍存在的具体业务耦合，不重排目录或增加通用策略层。

### 本批交付与核对

基线报告中的具名人格预览反向依赖已在 c65ba84 移出 EventStore：当前预览由 QueryService 调用 diana 模板和原运营素材目录，只返回草稿，不保存配置；对话仍从根配置读取身份字段。通用配置样例的 identity_name 为 Len、character_context 为空，bilibili_live_sensor、asoul_calendar 和 asoul_dynamics 默认关闭；插件发现仍导入内置描述符，停用不是恶意代码隔离。直播插件的 members/time 校验仅在自身全局启用时生效；延期重核已由 30e1a9a 绑定到原 handler 声明，Runtime 调 PluginHost 的通用入口，不按直播插件 ID 或访问私有字典分派。这些是静态源码事实，不证明某个真实安装已成功换人格或关闭直播。

S0-03 的依赖边界表、三层归属与非 A-SOUL 发行草案已具备；本阶段以报告的候选 2（具名预览移出存储）作为已经实施的最小拆分，延期重核是另一个已发生的定向收窄，故交付可转入复核。原报告按 ad41a5a 基线写的旧行号和异常点仍保留为历史盘点，当前任务状态仅在路线表改为待复核，不把旧报告改成当前源码清单。interest_share 的 Gate／Store 特判、平台能力名与卡片品牌等仍存在；它们不因本批状态变化而视为已拆除。候选 1 的更大提交语义改造未做，后续只有出现明确业务缺口和兼容路径时才进入单独实施。

- 阅读 PluginCatalog.discover、bilibili_live 的启用校验和 handler 声明、PluginHost.refresh_deferred、Runtime 延期入口、QueryService.preview_diana_persona、通用样例与两次历史提交范围；没有读真实根配置或业务数据。
- 本批只有任务状态与交接文档变化，git diff --check 无格式错误；未修改代码和前端，不重复编译或构建。此前 c282533 的编译／构建结果仍仅属于此前源码批次。
- 无同版通用安装或真实群观察，不标已验收。未运行测试、夹具、断言探针、自动截图、回放、故障注入、覆盖率、依赖安装、服务、模型／平台调用或实发。
- 本批无源码定位、编译构建或实际业务失败原文。

### 待决定与接续

1. S0-03 交付待维护者源码及实际通用发行复核；S0-04 许可证、字体和素材授权未决定，S0-06 精确 Linux 容器／模型／OneBot 组合和现场仍缺，不把方向当成公开支持。
2. S1-02 动态定义与其余动态材料仍有缺档，S1-04 未登记等待；S2 完整原生交换与压缩交接还需要必要字段保存边界的答复。已交付源码项保留人工验收状态。
3. S3／S4／S5 现场、S6 候选／远端 CI／升级和 S7 外部使用者／作者闭环未完成。分类保留期限和发布范围仍待维护者决定。
4. 仅阶段性本地提交，不推送、合并、部署或实发，总体目标继续。

## 基线索引与文档入口转入复核

阶段提交：`9d9daf1`。

### 当前批次

- 分支 feat/s0-product-contract，基线 30357d3，开始时工作区干净；上一阶段 S0-03 依赖边界交付已本地转入待复核。
- S0-01／S0-05：既有四份基线盘点、剩余工作索引、按读者入口、贡献流程和任务模板仍在任务表列实施中。本批只核对交付物及唯一状态边界，转待复核，不补造真实验收或重写旧盘点。

### 本批交付与核对

S0-01 的剩余工作索引按 S1、S2、S3、S4—S7 四份只读报告对照旧 P／M 与路线任务，将保留、定向修改、待维护者决定和后置分开；每份注明原 commit 与当时读不到的范围。当前实施后的真实状态归 docs/plan/README.md，旧报告的源码行号、当时“未实现”和建议顺序仍限其基线，不改成当前 Bug 清单。任务表转待复核，不表示后续所有 S1—S7 已交付。

S0-05 的 docs/README.md 已按四类读者给入口，当前合同分别归 architecture/context/plugins/operations，路线入口唯一维护任务状态；docs/iteration.md 只记当前实际核对，docs/history/README.md 索引已归档批次。CONTRIBUTING.md 连接真实工程约束与最小交付步骤；原 .github 任务、缺陷和变更模板都有用户结果、版本、范围、证据与未确认项的位置。旧两份计划仍作为目标与历史引用，不当当前能力表；未建立并行状态看板或第二文档平台。

- 阅读 S0-01 总索引及 S4—S7 盘点的基线限制、docs/README.md、docs/plan/README.md、docs/history/README.md、CONTRIBUTING.md 与三份原 .github 模板；仅按文本和仓库文件核对，不执行独立读者流程。
- 本批只有文档与状态整理，git diff --check 无格式错误；没有代码或前端改动，不重复编译构建。先前批次的构建结果不能代替本批整体文档验收。
- 无独立读者实际使用、同版安装或现场记录；S0-01／05 只列待复核。未运行测试、夹具、断言探针、自动截图、回放、故障注入、覆盖率、依赖安装、服务、模型／平台调用或实发；未读取真实配置和业务库。
- 本批无源码定位、编译构建或实际业务失败原文。

### 待决定与接续

1. S0-01／03／05 待维护者复核；S0-02 的保留和公开承诺、S0-04 许可证与素材、S0-06 精确支持组合仍未完成。选择的 Linux 容器群报告与文件交付方向不等于现场通过。
2. S1-02 动态定义与其他材料、S1-04 未记录等待，S2 完整原生交换／压缩交接仍有源码缺口；额外原生字段及必要回复片段的保存边界待维护者答复，不代替其决定。
3. S3／S4／S5 待同版人工复核，S5-03 分类期限、S6 候选／远端 CI／升级和 S7 外部使用者／作者闭环仍未完成。无生产或公开发布授权。
4. 仅阶段性本地提交，不推送、合并、部署或实发，总体目标继续。


## 动态简图公共字体定位

阶段提交：`b7ce7f4`。

## 当前批次

- 分支 `feat/s0-product-contract`，基线 `7e8a07c`，开始时工作区干净；上一批锁定 workspace worker 的直接／间接 Python 包，并在本机完成主镜像与 worker 的 arm64、交叉 amd64 构建，未运行服务或群业务。
- S0-03／动态简图资源边界：原 `asoul_dynamics` 在富卡失败后直接找相邻 `asoul_calendar/resources/font.ttf`，找不到再找自身并不存在的 `resources/font.ttf`。现有 `len_bot.cards.BUNDLED_CARD_FONT` 已为直播卡片提供同一随包文件的公共定位；本批只复用它，不移动或复制字体、不新增配置和兼容路径。

## 本批交付与核对

`AsoulDynamicsPlugin._render_source_card` 的简图分支改用 `BUNDLED_CARD_FONT`。文件缺失时在此边界明确报“动态卡片字体文件不存在”，不扫描机器字体或改用其他资源。富卡、来源查询、图片登记与发送链均不变；当前公共常量仍指向内置日历目录下同一原件，路径抽离不等于字体授权或打包边界已决定。

同步[插件开发](../plugins.md#可选卡片公共入口)、[插件兼容清单](../plugin-compatibility.md)和路线 S0-03 的当前证据；基线[依赖测绘](retired-plans.md#dependency-boundary)仍按旧提交保留，不把当时的静态发现涂改成当前事实。群报告仍要求运营明确 `render_font_path`；代码和素材许可待维护者决定，本批不动样例配置或真实文件。

- `uv --cache-dir /private/tmp/lenbot-uv-cache run --no-sync python -m compileall -q src/len_bot/plugins/builtin/asoul_dynamics/plugin.py` 退出 0；`git diff --check` 退出 0。仅证语法与差异格式，未获准同版页面、插件装载或真实卡片现场。
- 未新增、修改或运行测试、夹具、断言式探针、自动截图、回放、故障注入、压力或覆盖率任务；未读取真实配置／业务库、启动服务、调用模型／平台或实发。没有本批实际业务失败原文。

## 待决定与接续

1. S0-03 其他已盘点耦合和群报告字体明确路径／素材授权仍未收口；本批只移除动态简图的一条相邻插件路径，不把 S0-03 标为已验收。
2. S5-01 双架构本机构建已完成；本机 SnowLuma 以协议而非固定发行版本名单判读。目标机器正常安装、模型职责、报告／文件真实回执及 S5-06 公开承诺仍待授权和证据。
3. S2 仍仅 `source_window_only`，原生交换及必要回复片段保存边界待维护者答复；数据分类期限、许可证／素材授权、S6 候选和 S7 外部闭环仍待决定或现场记录。
4. 仅阶段性本地提交，不推送、合并、部署或实发，总体目标继续。
