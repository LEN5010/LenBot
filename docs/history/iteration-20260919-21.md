# 2026-09-19—21 实施与运行记录

历史归档；整理于 2026-09-22。以下保留当时的批次顺序、真实观察、失败原文与未确认项，不表示当前部署状态或新的操作授权。当前交接见[当前任务](../iteration.md)，后续任务见[路线状态](../plan/README.md)。

原文件可用 `git show 75acb53:docs/iteration.md` 回查；旧盘点中的文件行号属于其核对基线，不能用于当前文件。

## 2026-09-21：收尾提交与交接

按当前要求停止扩展改动，将基线 1ce1657 之后本批 N00—N05 的源码、资料审阅稿和所属文档一并提交。仅提交，不推送、不部署、不启动服务；具体提交身份以本次 Git 回执为准。此前各节“未提交”保留为当时状态，不是本次交付状态。

### 已实现与本次最后收口

- 提交结构统一、普通多来源 covers 与内部来源结果派生；相对时间锚定；对话与普通工作采用已读页 evidence_ref，保留原阅读与事务边界。
- 五张用户常服原图、当前人物资料候选及 19 张表情审阅稿已归档。人物参考配置／用途分离、媒体管理入口、同群真实图片与引用关系带回原话已实现；没有把原图或资料稿写入运行配置。
- 普通研究成果按版本固定后复用于新整理／导出工作，工作页可追溯关系；MD 按纯文本格式登记，工作事实提供现有文件交付前置。旧工作阅读资格、预算和发送资格不复制到新工作。
- 最后一批补齐混合插件的对话／工作用途、条件型工作用途、原入口缺项和工作开关／模型前置。适用性判断沿原顺序，没有假造工作或提前运行条件回调。tool_search 在排名和 LIMIT 前应用原 tool_names／allowed_tools／公共工具交集；嵌套目录与父入口隔离，权限、阅读账、预算及最后一步策略未改。能力卡复用原投影列表，无新增 Vue 修改。
- event_packing_detail 沿真实装填路径记录候选数、文字投影、快照／回退、估算、媒体准备及可选释放；媒体细分当前资产查询、源准备、失败及局部复用。计时包含试装和等待，层级嵌套；接受候选数不是最终已读数，也不是整个工作链路耗时。移除同候选未变化时的一次重复估算，释放后仍重算。自动媒体装配复用前若当前资产不可用，则丢弃该局部准备项并省略像素；没有新增跨轮缓存，不能与 cached_tokens 混称。

本轮只读定位曾访问不存在的 plugins/builtin/web_read 与 plugins/builtin/bilibili_content/account_actions.py，返回 No such file or directory；没有因此创建替代路径。这是源码定位失败，没有本轮业务故障观察。

### 交接剩余项

| 剩余工作 | 下一步与完成证据 |
|---|---|
| 人物资料采用 | 用户预览 character-context-20260921.md，经原 character_context 保存入口采用；保留人工人格和表达样例。 |
| 素材入库与绑定 | 五张常服、19 张新表情均未上传；沿原素材入口登记，常服绑定人物／服装，反应表情追加并保留原频率。动画完整呈现仍待人工查看。 |
| 前端实际页面 | 已构建，尚未在同版真实服务查看人物绑定／用途筛选、工作关系、能力卡及保存冲突。不得把构建当页面通过。计划其余页面优先沿既有投影验收，有具体缺口再改。 |
| 同版业务验收 N06 | 逐项核对计划 A01—A14，重点为提交返工、来源覆盖、相对时间、页引用／工作恢复、图片纠正、短答及时提交、文件与提醒。没有正常运行证据的项保持未覆盖。 |
| 文件交付 | 工作生成、MD 登记、下载、平台上传均未实际执行。原上传部署核验仍未具备，需核对实现、版本、挂载、申请者及授权，再以真实 file_id 确认；不直接翻转标记。 |
| 容量与性能 N05 | 对话 300k、工作 128k 只是候选，根配置未改。先记录同版装配细分、压缩和调用结果，再分批采用；没有加速或缓存稳定结论。 |
| 发布 | 本次只有本地提交授权；推送、停机备份、运行采用和真实群发送分别按后续当次授权。 |

真实图片关联 SQL、复用事务、页引用恢复、模型对新结构的接受、正常工具发现、页面操作和缓存／时延均未验收。保留原框架，不接入另一套执行循环或子工作树，不继续无具体证据的结构扩展。交接入口为本节与增补计划第 12—14 节，运行前先读 operations 的同版升级边界。

没有新增、修改或运行测试、夹具、断言式探针、自动截图、模拟后端、压力任务或故障注入；没有访问业务库、调用模型／平台、上传素材、保存配置或实发消息。

收尾重新执行全目录语法编译、入口／插件交互／插件宿主／图片关联／工作空间插件导入及 git diff --check，组合命令退出 0。所属前端上一批 npm run build 退出 0（1.66 秒，日志 /private/tmp/lenbot-n04-work-reuse-build.log），其后未改前端源码，不重复构建；web/static/dist 不入 Git。这里只确认编译、导入、构建与补丁格式，源码与运行效果分别确认。

首次暂存被文件权限边界拒绝，原文为 `fatal: Unable to create '/Users/len5010/code/len_bot/.git/index.lock': Operation not permitted`；未删除锁或改权限，按本次提交授权申请执行 Git 暂存和提交。

首次暂存后检查发现新增计划中的 Markdown 行末双空格，返回 trailing whitespace；已改为显式引用段落分隔，没有调整检查规则。此前未暂存检查未覆盖这些新增文件，交付前以完整暂存差异重新核对。

## 2026-09-21：N04 普通研究成果复用与文件输入源码实施

上一批新增了图片原话关联路径，属于实际进展。本批核对当前工作区及原 start_work、JobStore、文件登记和工作页面后，针对“完成研究后另要文件”的来源关系缺口继续实现；原 result_refs 能传资料，但没有明确保存所选研究成果版本，不能从资料相同就推定两项工作的关系。

start_work 新增可选 reuse_work_ref。仅本轮已实际提供完整／部分成果的普通研究 J 可用于此入口，宿主生成内部 ReusedWorkResult，固定原 job/revision、目标、委托来源位置、summary、unresolved、result_ids 与 evidence_spans；原成果资料身份合并到新工作的 result_ids。新工作仍以本次已读人类 request_source 确定请求者和计费，沿原创建、准入与预占执行。继续原研究的未完成部分仍用 resume_work；专用插件结果保持所属入口，不把停用或版本约束变成普通工作可以绕过的路径。

原 Gate 核对 Mailbox.provided_work_results；原提交事务再核对同场景真实原结果与所选固定输入、提交记录里的已读版本。原结果在暂存后改变时拒绝本次提交，不自动采用新版本。固定输入保存在新工作的 tasks.payload.reused_work，没有新增表；重复创建身份也核对该输入。新工作 observation_reads 仍从空开始，原预算、发送片段、文件资产和上传资格没有复制；原摘要只作为整理输入，原资料范围只作回读定位。原工作后续修订不改写已保存的新工作输入。

普通工作运行事实带入 reused_work；有人类申请者的工作同时采用现有 file_delivery_facts，避免给系统研究套用人类文件授予判断。工作合同明确仅排版／导出时复用既有材料，缺失内容保留，外部事实按原 result_ids 实际回读取得本工作页引用。原始文本可沿已有 input_result_ids 导入当前工作空间。生成、普通产物下载、持久资产登记和平台上传分别按实际回执处理；没有额外查询部署连接、开放工具或翻转核验标记。这些源码提示尚未证明模型实际减少了重复研究。

工作详情增加固定输入与原工作当前详情入口，原工作可查看最近 20 项明确保存的后续复用关系及总数；同场景筛选在查询内执行，不按相似目标回填旧关系。对话工作事实只显示简短来源关系与原结果版本，正文阅读资格保持原合同。没有新增工作控制按钮或重复执行入口，实际页面尚未检查。

按本批实施前明确的范围，将独立 MD 加入持久文件登记：MIME 为 text/markdown，沿原 UTF-8、控制字符及字节上限检查，不渲染或执行正文；工作空间登记工具的说明与发现关键词同步。普通产物、Gateway 不可变快照、原工作修订、send_file、动作审查、资产与队列回执边界未改。没有实际运行文件生成、登记、下载或上传，因此不声称 MD 真实交付已通过。

已同步架构、上下文、插件、运行手册和计划进度。全目录语法编译、入口／插件模块／工作空间插件导入及 git diff --check 最终退出 0；前端 npm run build 退出 0，构建 1.66 秒，日志在 /private/tmp/lenbot-n04-work-reuse-build.log，web/static/dist 未进入 Git。本批未运行测试、夹具、断言式探针、自动截图、压力任务或模拟后端。构建不是页面／业务验收；真实事务、来源变化拒绝、重启后的固定输入、模型选择、文件链路及页面体验均待同版正常业务核对。

只读定位曾使用不存在的 workspace/descriptor.py，返回 No such file or directory；未加引号的 Jobs* 路径产生 zsh 的 no matches found，随后沿真实 plugin.py 和 JobsView.vue 核对。一份组合补丁因上下文合同的目标行不完整而整体拒绝，未部分采用，按实际代码重新应用后完成。这些是定位／编辑失败，不是运行故障。

未访问业务数据库、启动服务、调用模型或平台、上传素材、保存配置、更改部署核验、扩大上下文或实发消息；未提交、未推送、未部署。N04 已有上述源码进展，其余能力调度核对和实际业务门槛仍未完成；N05—N06 仍待推进。人物资料采用及新增素材入库继续保留确认边界，不以本批源码替代运行采用。

## 2026-09-21：N03 图片讨论与纠正对象关联源码实施

上批新增了人物参考配置、媒体用途与页面入口，属于实际进展。本批核对当前工作区后，继续实现同一图片及其原话关系的带回，不重复做资料清点，也没有将上批构建结果当作业务通过。

新增 events/image_context.py 的定向只读投影，由原 EventStore 调用。当前真实人类来源带图片或明确 reply 时，从本场景及读取截点内的原事件，沿真实平台引用、Bot 已送达回执的 origin_event_id 和 covered_source_event_ids 找图片；限定指向更早的记录，并排除模拟、Shadow、未知发送及 conversation_excluded。图片只按已登记 asset_id 关联，并保留本群／global-safe 范围。后续候选先筛对象与关系，再取最近人类回复，必要时带回被引用回答和原来源；不先拿一页 Bot 回答把人类纠正挤出去，也不把正文含“这是某某”直接判断为正确事实。

向前祖先和后续人类回复分别使用原 retrieval_default_limit；每批回复共用三倍条数的补充祖先／桥接回执额度，未完整展开或达到后续额度时保留 limited。实现的是有界对象关联，不是全历史穷尽查询。当前来源保持原片段，相关原话先尝试较新人类说法、后尝试旧 Bot 回答，最后仍由原 pack_events 按时间呈现、核对文字与像素容量及实际阅读；窗外原话维持 related_original。初始装配和续轮吸收新输入都已连接，来源／材料专用插件不扩展读取范围。

对象关系放在独立 image_discussion 定位目录及原 context_plan，不修改原话正文、不追加提案或认识、不改变真实发送回执。静态核对时移除了一个会按本轮关系给旧消息补更丰富 quote_context 的中间实现；最终统一使用原 project_reply_context，避免相同历史在被选为关联来源时改变窗口内固定投影。关系目录与装配清单、原话已读范围、媒体 manifest 分开；目录存在不证明原文或像素已读。查找耗时追加到原 timings_ms.image_related_source_reads，属于关联读取阶段，不独立相加为总时延。

为新路径在原事件表声明 scene＋message_id、scene＋reply_to_message_id 两个定向表达式索引；后续查询先形成具体目标集合，再按引用 ID 找人类原话。没有新增表、永久纠正库、图片相似度或识别模型，没有写入当前业务库或实际建立索引。普通重传取得新 asset_id 的图片不自动承接旧图纠正；未明确引用的说法继续依赖当前群聊语境，不补造永久关系。对这些边界已同步架构、上下文、运行手册及计划进度。

最终全目录语法编译、入口／插件模块／新增图片关联模块导入及 git diff --check 均退出 0。只读取得本机 SQLite 版本为 3.50.4；查询所用 MATERIALIZED CTE 要求 3.35 及以上，已写入运行手册。没有运行关联 SQL、模型调用或实际装配案例，因此 SQL 运行结果、查询耗时、缓存变化、下一句采用和同对象再次出现的效果都未验收。本批未改前端，保留上批构建记录及实际页面待验收状态；未运行测试、夹具、断言式探针、自动截图或压力任务。

一次文档组合补丁因运行手册目标行不完整而整体拒绝；未应用该补丁，按真实段落重新修改后完成。本批没有业务失败观察，因为未启动业务。未上传素材、保存配置、扩大额度、替换人格、启动生产、真实发送、提交、推送或部署。N03 源码已具备人物参考和对象关联路径，资料采用与业务门槛仍待确认；下一批继续 N04 的能力前置、成果复用和文件交付，不因此将整份计划标为完成。

## 2026-09-21：N03 人物参考资产与管理入口源码实施

继续保留 N00—N02 的未提交修改。本批实现人物与服装参考的配置、素材用途、同场景目录及管理入口；尚未实施图片纠正的专项对象关联，不把 N03 标为完成。

根配置类型增加默认空的 character_reference_assets，仅保存人物键、服装和真实 asset_id，最多 40 项，人物／服装组合与资产各自唯一。配置保存在原配置锁内合并基线并核对素材，再写原根文件；要求已登记、启用、有本地路径元数据的运营图、独立“人物参考”标签和空 palette_order。已绑定图允许停用，修改用途前须解除绑定。素材编辑沿同一配置锁后进入原数据库事务，合并实际字段后核对用途互斥；事件登记与读回仍在锁外走原回执路径。这里检查登记元数据，没有用元数据冒充实际文件或像素已读。

初始对话在历史摘要后装配人物参考短目录，只查同场景及 global-safe，按本轮配置的绑定提供 character_key、outfit、image_ref、短说明和 catalog_only，逐项计入请求容量；省略时恢复引用。图片仍按需使用 read_media、原图片数量／字节限制及实际已读确认，不每轮塞入五张像素。完整对话插件沿原人格包含规则采用目录，仅来源／材料入口不扩展。固定反应表情目录在 LIMIT 前排除人物参考，原频率、顺序、额度和窗口锚定未改。

媒体页增加用途筛选和独立“人物与服装参考”卡片。筛选在 SQL 分页前执行；详情可把已保存的参考素材加入绑定草稿并返回卡片，填写人物键／服装后单独保存。收起、同页筛选及详情导航保留绑定草稿，离页沿原未保存提示；原人格页只提供链接，不新建重复保存入口。配置读接口一起返回 saved、baseline、effective 与已配置资产状态；保存沿原 ConfigEdit，冲突重读后由运营选择，保存成功待读回或请求结果未知时暂停重复提交。上传、配置绑定、浏览器预览与模型读取始终分开，不改变人工人格字段。

已同步架构、上下文、运行手册、资料采用说明与计划状态。全目录语法编译、入口及插件模块导入、git diff --check 退出 0；前端两次 npm run build 退出 0，最终构建 1.83 秒，日志在 /private/tmp/lenbot-n03-reference-build.log，生成的 web/static/dist 未进入 Git。本批没有运行测试、夹具、断言式探针、自动截图或故障注入。编译构建不是业务通过；没有启动同版服务进行实际页面核对，故页面布局、保存操作、实际请求装配、图片辨认和回执链仍待正常业务验收，未用模拟后端代替。

本批只读定位出现 `cat: src/len_bot/web/frontend/src/composables/useConfigDraft.js: No such file or directory`，随后沿现有 useConfigConflicts 与 lib/configDraft 查明实现；此前人物绑定定位曾使用不存在的 web/configuration.py。一次包含不匹配架构文档行的多文件补丁被整体拒绝，确认未应用后按真实行重新提交；此前空补丁 hunk 也未应用。这些是编辑／定位失败，不是业务运行故障。

没有上传五张常服或 19 张表情，没有保存运行配置、扩大上下文、替换人格、启动生产、调用模型／平台或实发消息；未提交、未推送、未部署。下一步继续沿真实 reply、Bot 回执 origin_event_id 和图片 asset_id 实现对象关联，保留原事件、场景与阅读范围，不新增纠正知识库、相似识别或识别模型。N04—N06 仍待实施，资料采用与同版人工验收另行确认。

## 2026-09-21：N02 已读资料页引用源码实施

上批已修改相对时间召回并形成真实资料审阅稿，属于实际进展。本批核对工作区后继续实现计划中的 evidence_ref，没有改变其他未提交批次的授权范围。

资料页投影现在分配 E 开头的不透明页引用，在原资料展示工具包内映射到实际 ResultSpan，解决模型手填 result_id／单位／起止范围的负担。页名是业务定位身份，不是内容指纹、校验和或权限凭据。公开 respond.answer_basis 使用 evidence_refs，普通工作 finish_work 也以 evidence_refs 替代手填顶层坐标；宿主解析后仍用原 AnswerBasis.result_spans／JobResult.evidence_spans 进入既有校验、事务和回执链。工作状态、公共兴趣候选和专用工作的类型保持原范围合同，页面 evidence_span 继续供这些结构和诊断使用。

页名与已读资格分开保存。试装和被裁掉的页可以有暂定名字，但只有最终请求保留原正文或合法宿主投影、并在模型响应后由原 adopt_presentations 确认，才进入可引用集合。原生页恢复时可从重新保留的真实正文找回其名字，但仍须等本次模型响应；仅凭检查点中的名字不补已读。正文外置会清除当前页展示范围与页引用，已经确认的历史读取仍沿原账本保留。保存原始工具结果时清空外来 evidence_ref，避免把插件提供的字段当作宿主资格。

工作原 observation_reads 的资料／坐标单位记录追加 evidence_refs 映射，随原 record_job_presentations 事务保存精确范围。合并 ranges 不丢页名，同一个名字不能绑定另一资料或范围。同页多个已展示名字分别保留，解析同次重复引用同一页时仍拒绝。恢复先按场景装入原资料，再核对页名落在保存的实际阅读区间内；旧范围记录没有页名时不回填，按原读取工具取得需要的页。没有新增表、迁移任务或第二套读取状态服务。

静态核对了普通对话、初始插件材料、工作分页、独立插件读取、嵌套表达、正文外置和恢复路径。独立插件有自己的局部工具包时，已确认的页名通过原工作 record_presentations 回调进入原事务与父工作范围记录；父工作不以插件摘要替代真实读取。页引用仍只说明实际提供的位置：目录、摘要、群原话序列化记录与模型产出继续通过所属资料类别校验，不能借 E 引用改成人类或外部事实证据。内部保存的结果仍是原坐标，现有单条依据页面和已读范围投影不需要新增状态，因此本批未改前端。

已同步架构、上下文、插件、运行手册和计划进度。执行全目录 `uv --cache-dir /private/tmp/lenbot-uv-cache run --no-sync python -m compileall -q -f src/len_bot`、入口与插件模块导入，以及 `git diff --check`，组合命令退出 0。仅语法编译、导入及静态路径核对，不是实际模型接受新 Schema、分页引用、工作恢复或事务运行通过的证明。本批未遇到业务运行失败，也没有运行测试、夹具、断言式探针、自动截图或故障注入。

未启动服务、访问业务库、调用模型／平台、保存运行配置、上传素材或实发消息；未提交、未推送、未部署。N02 源码已覆盖相对时间、精确召回与页引用，实际业务门槛仍待同版验收；N00 的采用确认和 N03—N06 尚未完成。下一批继续人物参考资产与对象关联的实现，不把这次编译结果当作生产效果结论。

## 2026-09-21：N00 资料审阅与 N02 相对时间召回

上一批实际改变了提交接口及原提交／发送链路，属于源码进展。本批核对工作区后保留这些修改，继续沿同一计划推进资料整理和已经观察到的时间漂移问题，没有重做生产采样。

recall_chat 的时间输入改为 time_range 下的两种明确形状：absolute 使用 start_time／end_time，relative 使用 request_source／lookback_seconds；省略时不筛事件时间。对话的相对锚点经 TurnReferences 核对整条原话已读，再查询本群与截点内的真实人类来源。工作入口只接受本工作原始 request_source_event_id，工作事实补充该现有来源字段，不新增请求身份。宿主计算 `[原请求timestamp-回看秒数, 原请求timestamp)`，保留右端不含的语义；同一锚点不因执行或续轮时间改变。没有通过正文猜日期，也不自动把错误参数改成另一个问题。

实际区间、人物、截点与 limit 写在原工具观察的 message_filters，输入形状回显于 time_range，来源 ID／timestamp 写在 time_anchor；相对输入回显没有混入不可用于原参数的扩展字段。精确原话查询不再附带未经人物和时间筛选的摘要，也不以摘要命中把原话空集变为 ok；主题摘要仍由现有 search_history_summaries 单独提供。SQL 的场景、截点、关键词、人物、时间、排序、LIMIT 及 `%`／`_`／`!` 转义路径未改。此实现固定了选择 relative 时的计算规则，模型是否正确选原来源、空集回答及实际续轮效果仍待同版正常业务验收；不宣称自然语言的所有时间要求都已被自动理解。

已阅读六份桌面知识原稿并整理[当前人物资料稿](../persona/asoul/character-context-20260921.md)，采用用户确认的当前名单、基础外观、本套常服、生日、粉丝称呼、常用昵称和按语境分开的社群梗。未复制冲突名单、发展史、健康／身体描述和未经确认推测；原稿与已有历史源档未动。来源、取舍及按既有 character_context 入口预览保存的流程写在[资料说明](../persona/asoul/README.md)。没有替换运行人格或其他人工字段。

已逐项查看 19 个表情文件的静态内容或动画代表帧，形成[新增表情审阅](../persona/asoul/sticker-review-20260921.md)。7 JPG、1 PNG 及 `不敢看.gif` 为单帧；另外 10 GIF 查看了各自第 0、约 1/3、约 2/3 帧，记录实际帧数／时长。`那咋啦你管我.jpg` 的画面只写“那咋啦”；`被敲脑门.gif`、`跪地.gif` 原文件有透明索引，角落 RGBA 为 `[0,255,0,0]`，没有据预览中的绿色底值擅自改图。临时解码 PNG 只放在 `/private/tmp/lenbot-sticker-review-crzd9cq1` 供查看，原图未裁剪、重绘或重编码为运行素材；这不是应用截图。完整动画节奏及平台呈现未验收，全部素材尚未上传。

本批实际资料读取的初次脚本对 JPEG 使用 n_frames，返回 `AttributeError: 'JpegImageFile' object has no attribute 'n_frames'`；随后按实际格式区分静态图与 GIF，重新读取成功。只读路径定位另出现 `sed: src/len_bot/cognition/work_context.py: No such file or directory` 与 `zsh:1: no matches found: src/len_bot/time*`，后续采用实际 runtime/work_context.py 与明确目录。这些属于本次资料阅读／定位失败，不是服务运行故障。

执行全目录语法编译、入口及插件模块导入，以及 git diff --check，组合命令退出 0。没有运行测试、夹具、断言式探针、自动回放、自动截图或压测；未启动服务、访问业务库、调用模型／平台或实发消息。没有前端改动，不重复构建页面。提示文字和工具 Schema 变更会改变请求前缀，尚未测出新的缓存、延迟或返工率。

N02 的已读资料页 evidence_ref 尚未实现；N03—N06 仍未完成。人物稿及素材说明待用户预览，后续配置生效、资产登记和业务验收分别保留授权与回执边界。源码与文档未提交、未推送、未部署；未改根配置、模型、窗口额度、人工人格、表情频率或群名单。

## 2026-09-21：N01 提交接口源码实施，待同版业务验收

按持续推进授权，本批针对前述真实记录中的依据形状和来源对应返工修改源码，不再仅追加提示文字。公开 respond Schema 从同一组输入模型生成；去除与唯一业务句柄重复的 intent，普通回复填写 segments／source，工作、操作及文件保留互斥关系。answer_basis 按类别拆分，social/general 仅接受类别，observed 需要实际直接依据，work_result／mixed 保留原工作成果校验，unverified 需要具体缺口。参数拒绝包含 messages 索引、字段及原因，不自动删字段、换类别或部分提交。

增加 covers，仅用于一条普通回复明确覆盖其他已读、同群且待处理或本轮继续处理的人类原话；主来源、请求者及工作权限不变。内部 MessageProposal／ActionItem 增加 covered_source_event_ids，复用原提交 JSON 与实际发送回执，不新增表。Actor 保留原已读与待处理检查，Gate 核对每个覆盖来源的回复资格及消息索引，事务再次核对真实原话和关联，发送前检查这些来源的当前群资格；插件表达、工作控制与文件不会借覆盖列表更换授权主体。

模型只在 sources 声明 silent／incomplete 及具体原因或未完成部分；Ledger 从消息来源、覆盖列表、实际创建提案及等待关系派生其余处理结果。已读运行来源仍按真实事项／origin 关系关联；仅仅读到的其他人类原话保持待处理。显式 incomplete 保留未完成范围和已有行动关系。next=wait 的恢复来源改沿实际 expect_reply 消息关联取得，使含未完成部分的等待仍可保留原请求；等待生效继续要求真实送达，预算与三条消息额度不变。插件提案式 submit_message 同步去除旧 replied 输入，直接确定性入口没有换协议。

静态核对 QueryService.relations、SourceOutcomes 与 messageProgress：已有 source_outcomes.action_ids 和 message_indices 投影能够让多条来源关联同一行动，不需要为本批改 Vue 或新建前端状态。它展示已组织回应和实际送达的不同阶段，不把覆盖原话自动加为 answer_basis，也不回填旧历史的关联。实际页面与合并发送仍待人工业务验收。

已同步架构、上下文、插件合同与计划状态。执行全目录 `uv --cache-dir /private/tmp/lenbot-uv-cache run --no-sync python -m compileall -q -f src/len_bot` 及入口／插件模块导入，均退出 0；`git diff --check` 退出 0。仅编译、导入和差异检查，没有运行测试、夹具、断言式探针或截图，没有访问业务库、模型或真实群。编辑过程中两次补丁因目标行不匹配被整体拒绝，核对实际行后重新应用；只读定位曾使用不存在的 cognition/references.py，随后沿实际 context.py 核对 TurnReferences，这些不是业务运行失败。

本批接口不接受旧 intent 或 sources 的 replied/delegated/waiting 输入，不提供猜测式兼容转换；历史提交和回执仍按已保存事实读取。发布前须按运行手册处理在途执行、停机备份，并整体采用同版代码和定义。尚未确认实际接口对新 Schema 的接受、55 次原问题的减少幅度、合并答复、含 unfinished 的等待、正常创建／控制／交付及插件提交的运行效果，不能称为已解决生产返工。

N00 的五张常服已原样保存，当前角色稿与 19 张表情逐项预览仍待做；N02—N06 未完成。没有更改根配置、模型、人工人格、样例、表情频率或群名单；没有导入运行素材、启动服务或实发消息。源码与文档留在工作区，未提交、未推送、未部署。

## 2026-09-21：运行复盘后的改进计划与人物参考附件

用户要求本轮先完成分析与下一步计划，待其验收后实施。已经明确：当前表情频率可以保留，追加素材而不强制降频；人物日常资料采用贝拉、嘉然、乃琳与枝江二期心宜、思诺，旧历史保留原档而不进入本批常驻角色资料；提示文字以确定事实和正向动作表述。

只读清点 `/Users/len5010/file/Diana/表情包1`：19 个有效图片文件，11 GIF、7 JPG、1 PNG，22,534,793 字节；尚未逐个查看动图或登记到素材库。只读查看 `/Users/len5010/Desktop/ASOUL/知识` 的六份 Markdown，共 33,955 字节，采用用户确认口径整理未来资料稿，不把原稿中的冲突、历史与过期状态原样装入请求。

已查看用户提供的三视图集首页与贝拉分类，实际读取一张贝拉出道服参考图；网站说明包含录播截图与生成式补绘、免费非商用，部分细节存在差异，故计划作为补充参照而非自动替换来源。浏览器会话随后中断，未继续视觉核验其他分类；没有整站导入或新增图库爬取。用户随后直接提供五张常服图，已按乃琳、贝拉、心宜、思诺、嘉然的顺序原样复制至 [参考图附件](../persona/asoul/reference-images/20260921/README.md)，记录当前服装特征。附件不等于业务媒体资产，也未进入表情目录或模型请求。

已撰写[改进计划](retired-plans.md#natural-chat)，包括实际问题优先级、确定事实资料、按需视觉参照、对象化纠正、提交形状与多来源关系、资料页引用、相对时间锚点、能力调度、文件交付、对话 300k／工作 128k 候选值、前端对应入口、N00—N06 批次及人工验收。总计划仅补链接，不替换已确认架构合同；新增字段与接口只为本批具体缺口提出，不宣称现有实现已经支持。

本批只修改文档并保存五张用户图片副本，源码和运行配置未改，未访问业务库写入口、导入运行素材、启动服务、调用模型或发送群消息。没有测试、探针、自动截图、图像生成或图片编辑；无源码及前端变化，不重复编译构建。只读源码定位曾包含不存在的 web/api.py 与 frontend/src/pages，返回 `No such file or directory`，后续使用实际 routes 与 views 路径。计划与图片归属待用户验收；未提交、未推送。

## 2026-09-21：本机正常运行记录的只读复核

### 范围与证据口径

按本次授权，以 SQLite `mode=ro` 连接并设置连接级 `query_only=ON`，只读本机 len_bot.db 的事件、调用、Trace、工作及资料；未重新运行案例。固定样本为北京时间 2026-09-21 01:36:57—02:01:55，事件 rowid 43018—44035，共 1018 条，其中 511 条群输入涉及六个群。源码对照基线是本地 master/1ce1657；数据库没有本次实例的提交号，不能只凭当前 HEAD 证明全部请求使用这一提交的完整请求体。01:45:48 存在一次 scene_settings 操作，因此也不能把整段样本称为所有运行条件完全固定。

样本含 217 条逻辑模型调用：196 条完成的对话调用、11 条工作调用、8 条维护调用（其中 6 条工作压缩）、1 条嵌入调用，以及 1 条取消的对话调用。对话有 127 次成功 checkpoint 提交、1 份提交前失败 Trace；成功提交中 80 次产生行动，47 次无行动。消息取得 84 条真实 sent 回执，另有 2 条 unknown，不将后者算明确拒绝或成功。用户已确认 02:01:55 主动停止服务，该时点 ConnectionError 与模型调用取消不列为新增运行故障；01:57:58 的 TimeoutError 未由此次停机解释，仍是未确认送达，不重发。

### 提交协议返工是本批首要交付问题

对话 Trace 记录 55 次 respond/invalid_arguments，影响 42 个 episode；另有 4 次 fresh_input_conflict，单独统计。55 次中，25 次是 social/general 携带实际依据，21 次是 sources 标记 replied 却没有对应消息来源，9 次为其他来源、句柄或范围错误。这些拒绝保留了原事务与阅读边界，但模型反复修改结构仍不能及时提交，不能把“校验正确”当成用户任务顺利完成。这 55 次对应已上报输入 token 合计 7,471,389、累计调用墙钟约 662 秒；输入包含缓存，调用跨群并行，不等于新增账单费用或单个用户等待时间。

关键 episode 为 `conversation:005c6202c0cd4f00b9bf370b5efb1920` 与 `conversation:565776325d7947c491366b139f4cde32`。前者 8 次模型调用、4 次提交参数拒绝，最后以 final_step_invalid_arguments 结束；后者另用 8 次调用、5 次参数拒绝后才提交。Python 查询在 01:39:31 提出，01:51:52 才真实送达，延迟约 741 秒。01:39:59 的 uv/Poetry 委托直到 01:51:51 才创建，01:52:02 才确认。前轮 start_work 仅暂存，随整轮失败没有成为真实工作；样本只有一次实际创建，不能把两次工具执行计成重复后台工作。

源码定位为 cognition/proposals.py 的 `_message_schema()`、`ProposalLedger.finish()` 与 cognition/models.py 的 `AnswerBasis.evidence_shape()`。模型输入的组合表达、重复来源填写与宿主要求之间存在明显操作负担；下一批应先统一模型可选业务形状与实际合法组合，明确同一答复与多个请求来源的关系，并改善可定位到字段的纠错说明。不能通过删除阅读校验、静默丢弃参数或把未完成请求标 silent 来掩盖失败，也不以增加轮数替代处理。

### 下划线召回：SQL 命中正确，重跑时问题范围被改写

来源 `008cf710-f466-4af8-9b84-e015f6d60555` 在 01:39:37 请求查本人最近十分钟含下划线的原话。首次资料 `9e419938c245448e9380396740de885d` 使用 01:29:43—01:39:43，准确返回这条请求本身，候选只有一条。第二轮资料 `524c9f8855224d47aed7d0e9caf21b4c` 却改成 01:40:00—01:50:00，排除了原请求所在时间，随后回答“没有”。这不是 `%`／`_` 转义回归，而是相对时间条件没有沿原请求保持：重跑不应把同一问题悄悄改成新的时间范围。两次附带的摘要线索均为 10 条且标注未确认人物／时间；不能拿这些摘要证明精确命中或空集。

Python 版本答复有实际保存页面支持，并非纯凭印象：资料 `e55a57c341324dfc9025aae2a99188cf` 的 [0,6000) 已实际提供，约字符 2945 起包含答复所述版本，约 3008 起包含日期。但最终 answer_basis 声明 [1100,2500)，不覆盖对应文字。阅读范围合法与引用语义正确仍是不同结论；不能宣称目前的范围校验验证了答案事实。

### 缓存与延迟分开判断

196 次完成对话调用中，176 次返回正数 cached_tokens，20 次字段缺失，显式零为 0。只在明确返回缓存字段的调用中，累计 cached_tokens / prompt_tokens 为 19,602,611 / 22,571,642，约 86.85%；这不是计费比例，也不把缺失字段当成零。初始装配记录中，六个群的窗口头保持不变或向后移动，未观察到退回；目标查询群 11 个 episode 的窗口头均为 M37000。上述支持保留窗口修复，但没有完整出站正文，不能据此证明所有前缀相同或解释全部 20 次字段缺失。工作调用只有 1 次返回正数、10 次缺失，也不能把缺失都认定未命中。

216 份客户端传输记录中，207 份只有一次尝试、9 份有两次尝试；重试前错误为 ReadError 或 RemoteProtocolError，均没有可靠响应头。前述首次查询 episode 的四次首尝试失败累计约 227 秒，是其长等待的重要组成部分。当前证据只能定位到客户端至接口的读取／协议失败，不能判断中转内部上游尝试次数、实际处理次数或额外费用。

对话逻辑调用墙钟中位数 8.26 秒、P95 36.83 秒；已送达消息从来源至回执中位数 26.38 秒、P95 89.75 秒，最长约 1099 秒为后台工作结果交付。初始装配中位数 2.69 秒、P95 12.76 秒、最大 39.56 秒；最慢 episode `conversation:bc026fdbafc846e9bc914cc0efa9ffc5` 首步 event_packing 为 38.24 秒，而模型前请求准备中位数约 89 毫秒。event_packing 包含 await 图片准备，不能直接称为纯 CPU、深拷贝或数据库开销；已有记录足以定位装配阶段，不足以确定其内部唯一根因。

### 工作与文件没有形成同一个完成结论

工作 `job_f9d76965d610535ab29a` 在约 322 秒内形成 completed 结果，使用 11 次工作调用、6 次压缩调用；8 次 read_page、8 次 read_tool_result 实际完成，说明正常工作分页可推进。6 次压缩约占 120 秒累计调用墙钟，其中一次约 70 秒；这是已经观察到的压缩成本，不再只是静态猜测，但也不据此直接调整额度或更换框架。成果交付另经历两次结构／范围拒绝、一次成果正文读取，最后取得真实消息回执。

最初委托只要求对照表与来源，01:58:51 才明确要求文件。该工作没有 execution_runs、没有 file_assets，也没有文件上传尝试或回执；最终仅送达群内文字表格。当前保存配置虽然启用 file_delivery，但 onebot_file_upload.deployment_verified=false，因此“能配置文件功能”不等于上传链路已具备前置。源码 media/files.py 的 `file_delivery_facts()` 会明确投影该阻断。后续应先人工核对实际实现、版本、资产只读挂载与授权，再按真实条件处理文件请求；不能仅把标志改成 true 或靠提示词要求成功。对用户只说“复制一下也很方便”且把事项标 replied，没有交代具体阻断和尚未满足的文件要求，属于交付解释不足，不证明上传器已执行失败。

### 社交表现与尚未覆盖项

84 条真实发言中 76 条带图片；这只描述当前表达频率，不据此断言全部配图不当。01:59:11 的消息 `42f7a4fd-1782-498a-877a-2c236d7c193a` 声称“悄悄循环了好几遍”，其 episode 只有 respond，没有相应播放／收听依据，仍存在把角色表达写成真实经历的问题。

本轮至少两次收到“这是贝拉”的明确纠正，均有当轮认错回应。第一次纠正 `3c6066ef-754f-4a53-86c4-0148826407d4` 在后一次误认相关 episode `conversation:2fb9f8c691d84141bfb6a509299b88ca` 中仍以 M43085 完整保留 [0,49)，未被省略。因此不能把再次认错直接归因于上下文丢失。两次是不同图片，本次未额外视觉核验，不宣称已证明同一图像识别错误；应继续核对对象关联与纠正采用，而不是先把所有纠正自动写成长期认识。

样本没有验证人工偏好的创建／修订／撤销、普通提醒创建／改期／取消、真正文件交付、百分号和感叹号的独立查询、故障后的工作恢复，不能宣布全功能通过。TASK_DUE 在这里是后台工作触发，不是普通提醒验收。合法插件说明与阅读资格的边界也未单独取得同版案例。

本次只新增本节分析，没有修改源码、配置或业务数据，未启动服务、重发消息、运行测试、夹具、断言式探针、截图或压测；没有重复编译，也未提交或推送。读取进程列表被沙箱拒绝，原文 `zsh:1: operation not permitted: ps`；少量源码定位使用了不存在的 history、conversation、scene_actor、presentation 路径并返回 `No such file or directory`，后续沿实际文件读取，不将这些定位失败混为业务错误。原始数据库保留，本节只记录必要统计和定位 ID，不复制群图片下载凭据或整份群原话。

## 2026-09-21：提示词与工具说明消歧

本批从干净的 master/019e1de 开始，只处理上一轮阅读确认的提示文字不一致，不重写人格或扩大架构。旧聊天检索明确按原话关键词使用 recall_chat，按主题或摘要线索使用 search_history_summaries；后者实际以词面查询为基础，只有当前开关、绑定与索引具备条件时补语义候选，不再无条件描述为语义检索。关键词字段提醒将人物、时间与检索要求分开。

read_tool_result 与初始资料定位回执统一要求沿 next_call 保留完整参数及坐标单位，不只复制 next_offset，继续区分源端下一批 source_next_call。respond 的工具 Schema 与本地来源模型统一描述已读来源M，包含人类原话和系统来源，但不把系统来源提升为人类证据，也不混淆 messages[].source。系统合同和 start_work 说明都明确：当前循环及预算内的短查询、计算与必要续读直接处理；较长执行、跨轮进度或仅工作可用能力才按需创建工作，分页本身不是触发条件。无效 ack_ref 的原错误提示仍写“需要查询时先调用start_work”，本批同步改为新建事项先取对应回执，普通短查询不填创建句柄。

已同步上下文文档。上述修改只涉及系统合同、工具／字段说明和既有回执提示，不改工具名称、参数类型、必填项、默认值、工具可见性、执行分支、权限、事务、预算或阅读校验。没有移动系统消息或动态切换工具目录，也不重写工作合同、人工人格或人工样例。

执行 `uv --cache-dir /private/tmp/lenbot-uv-cache run --no-sync python -m compileall -q -f src/len_bot` 退出 0；随后执行 `uv --cache-dir /private/tmp/lenbot-uv-cache run --no-sync python -c 'import len_bot.main; import len_bot.runtime.plugin_interactions'` 退出 0。仅语法编译与模块导入，不调用业务入口，不代表实际召回、工具选择或群聊效果通过。只读定位曾包含不存在的目录，返回 `rg: src/len_bot/history: No such file or directory (os error 2)`；随后沿 tools/retrieval.py 的实际摘要查询分支核对，无业务运行失败记录。

没有运行测试、夹具、断言式探针、自动截图、覆盖率或压测；无前端改动，未构建前端。未启动服务、访问业务库、调用模型／平台或真实群发送，未改根配置、模型绑定或网络链路。提示词和工具定义变化会改变请求前缀，不能把新旧版本的缓存差异直接归因于窗口修复；实际误选工具、响应质量、耗时与缓存影响仍待同版正常业务观察。

按本次授权将本批源码与文档一并提交，身份沿仓库所有者配置，实际提交结果以 Git 回执为准；本次不推送、不部署。019e1de 的 R1、R2 代码结论保持不变，本批不新增“生产缓存已稳定”或“提示词效果已验证”的结论。

## 2026-09-21：边界修复提交与直连推送

按本次授权，将下述 R1、R2 修复与架构、上下文、当前任务文档一并提交，随后直连推送 origin/master。提交仅使用仓库所有者身份；推送清除本次进程代理环境并覆盖 Git 代理设置，不修改持久配置、不强制推送，实际完成状态以 Git 回执为准。下文“未提交、未推送”保留为当时的实施记录。

本次只整理交付状态并执行 Git 操作，不新增业务修改或重复编译。上一轮全目录语法编译、入口与插件模块导入退出 0 的范围不变；真实业务、性能与缓存效果仍未验收。未部署、未启动服务、未调用模型／平台或真实群发送，未修改运行配置和业务数据。

## 2026-09-21：锚定边界与短词检索修复

本批对照 master/992582a 的审查报告与源码，只处理 R1、R2。两处代码条件成立；报告中的窗口回退示例是条件推导，不是本轮生产复现，也不证明它解释了全部缓存异常。

R1 原因是锚定候选中只要出现必需原话，就取消整个锚定。本批先区分边界前必需原话与普通历史，仅检查待删除普通历史的本次摘要完整覆盖；覆盖不足时保持原窗口，不提前移动必需原话。允许锚定时，必需原话保留到窗口之后的 related_original，正文、引用和阅读贡献不变，普通历史才按原规则删除。初始排序与锚定共用同一顺序，仍是人格、窗口、带回原话、易变材料；不用于重排原生工具交换。步长关闭、没有边界前原话或边界将清空整个窗口时，继续保留原早退规则。

R2 原因是短关键词直接用于 LIKE 模式，参数绑定不会让 `%`、`_` 变成字面字符。本批在短于三个字符的分支指定 `ESCAPE '!'`，先转义 `!` 再转义 `%`、`_`。原参数绑定、三个字符及以上的全文检索、场景／截点／人物／时间筛选与 LIMIT 顺序不变，不扩大召回数量。

已同步架构与上下文合同。对 src/len_bot 全目录执行 `uv --cache-dir /private/tmp/lenbot-uv-cache run --no-sync python -m compileall -q -f src/len_bot`，退出 0；随后执行 `uv --cache-dir /private/tmp/lenbot-uv-cache run --no-sync python -c 'import len_bot.main; import len_bot.runtime.plugin_interactions'`，退出 0。这里只完成语法编译与模块导入，不调用业务入口，不证明数据库检索、真实窗口或缓存复用已经通过。

未运行测试、夹具、断言式探针、自动截图、覆盖率或压测；无前端修改，未执行前端构建。未启动服务、访问业务数据库、调用模型／平台或真实群发送，未改运行配置、窗口额度、人格或链路。尚需在获准同版环境确认边界前旧委托仍实际提供且不单独退回窗口、阅读范围保持，以及含通配字符的短词按字面召回；实际性能与上游缓存结果仍待观察。本批源码与文档留在工作区，未提交、未推送、未部署。

## 2026-09-20：审查修复提交与直连推送

按当前授权，将下述 F1—F6 修复、窗口身份／调用列表补漏及所属文档一并提交，随后直连推送 origin/master。推送命令清除代理环境并覆盖本次 Git 代理设置，不修改持久代理配置、不强制推送；实际完成状态以 Git 回执为准。下文“未提交／未推送”保留为当时的实施记录，本节说明新的交付范围。

本次只整理交付状态并执行 Git 操作，不修改业务源码，不重复编译或构建。核对证据仍是上一批 214 个 Python 文件语法编译、启动入口与插件运行模块导入退出 0；完整服务、真实模型／工具调用、群发送、性能与缓存效果仍未验收。提交和推送不代表部署或运行通过，也未改变运行配置与业务数据。

## 2026-09-20：补齐近期窗口身份与调用列表诊断

继续沿上一批未提交修复核对实际入口，HEAD 仍为 fa5e636。源码复核发现两处遗漏，不能只重复上批编译结果就把修复视为全部核对完毕：

- **窗口身份的入口缺口**：普通对话的 _read_initial_window 和插件的 _source_events 都会把近期消息与较早的待处理／恢复／插件来源合并。上一批只按合并后 events 中的连续完整消息分类，在候选列表没有中间消息时，仍可能把窗外旧来源当成窗口开头。本批由实际 get_recent_events 查询产生独立 frozenset 身份集合，沿普通对话、插件表达及嵌套装配传入 build；邻居与窗口成员只从该集合选取。当前及关联来源照常优先读取，窗外原话留在尾部；source/materials 模式不因此额外读取群历史。不增加数据库字段、持久状态或新的读取请求。
- **调用列表与详情不一致**：上一批详情读取了关联传输 Trace，但 model_usage 分页未读取诊断，公用投影却统一填 transport=null。本批让模型调用分页按原 call_id 与 scene_id 读取同一诊断，计数与 usage 汇总保持原 model_calls 口径。其他未加载诊断的关系简表省略 transport，只有明确查过且没有记录才返回 null，不把未加载冒充不存在。

已静态核对仓库源码与脚本中的相关入口调用点、普通等待恢复的来源合并、插件 respond 与指定来源分支；没有为缺少窗口参数猜测全体 events 都属于近期窗口。上下文、架构与运行手册已同步。

本轮在最后源码修改后重新执行全部 214 个 Python 文件的 `uv --cache-dir /private/tmp/lenbot-uv-cache run --no-sync python -m compileall -q -f src/len_bot`，退出 0；随后执行 `uv --cache-dir /private/tmp/lenbot-uv-cache run --no-sync python -c 'import len_bot.main; import len_bot.runtime.plugin_interactions'`，退出 0。仅语法编译与模块导入，不调用业务入口，不代表实际窗口、数据库查询、插件运行或模型请求已经通过。

没有前端修改，未重复前端构建；未运行测试、夹具、断言式探针、自动截图、覆盖率或故障注入。未启动服务、访问业务数据库、改配置、调用模型／平台或真实群发送。全部修复仍在原工作区，未提交、未推送、未部署；正常同版业务观察与实际性能／缓存证据仍未完成。

## 2026-09-20：审查问题的定向修复

本批从干净的 master/fa5e636 开始，逐项对照本次审查报告与实际源码。F1—F4 的筛选／阅读／窗口问题成立；F5 的重复全量处理成立，但实际延迟与优化幅度没有测量；F6 的逻辑调用与传输尝试记录缺口成立，不等于已证明重复处理或计费。因此当前工作不能只表述为等待验收，确有需要先修正的代码不一致。前端职责集中属于维护建议，没有取得界面竞态证据，本批不做组件化重写。

### 源码处理

| 项目 | 本批修复与保留边界 |
|---|---|
| F1 召回漏筛 | search_messages 在 SQL 中先应用人物与起止时间，再排序和 LIMIT；recall_chat 不再截断后筛选，也不靠扩大 limit 缓解。场景与读取截点继续生效。返回保留筛选条件，摘要辅助线索明确未确认符合人物／时间条件。 |
| F2 注释使原话失去已读资格 | after_tool 保持原工具投影字符串不变，将 notes/view 作为独立 plugin_tool_view 追加在完整原生工具组之后。仍拒绝改写原观察；原话继续按 tool_call_id 与宿主投影匹配，不因说明重序列化而失去范围。 |
| F3 自报范围冒充原文 | characters 页核对已保存正文的确切片段；本地记录及带定位前缀的工作页核对宿主采用的投影正文与坐标。插件摘要复制旧坐标不能因此登记原文完整已读。工作恢复的原始记录数组按既有序列化规则确认；只有实际请求保留且模型返回后才采用范围，不改写历史已保存阅读账。 |
| F4 窗内原话迁移 | 将当前／关联来源优先级与窗口归属分开，完整原话及引用采用固定投影。原近期候选中连续保留的完整窗口按原时序放置，窗内关联原话只保留一次；窗外来源、片段与当前像素仍在尾部。本轮必需原话不能因转入窗口而被锚定或普通可选上下文释放删除。 |
| F5 重复装配开销 | 可选历史复用同次基础请求与工具定义估算，只累加新消息；估算不再深拷贝整份请求。试装失败只恢复本条及其引用的投影状态，不逐条复制全部引用与历史。真实出站副本、当前输入与工具页的原回退、完整容量检查不省略。沿原 Trace 增加资料读取、装配、逐步请求准备、工具执行／呈现、提交／发布耗时，实际发送仍查原 queue_ms/send_ms；计时有嵌套，不能相加或宣称纯 CPU 开销。 |
| F6 调用与尝试难区分 | 保留原 call_id、usage 与 max_retries=2，在同一调用结算事务中写入关联的 model_call_transport Trace，记录传输尝试／重定向阶段、状态码、请求 ID、异常类型、耗时及最终返回型号／响应 ID。复用原 traces 表与查询，不新增表／列或费用账；不保存请求正文、图片、URL 查询参数或鉴权头，不把尝试数乘成费用。 |

核对共用分页器时另发现：WorkToolPresentation 复用了对话 pack_tool_pages，但没有 refs 与 tool_original_presentations；原分页器在选中正文时直接访问这两个对话专属属性，工作读取因此存在运行时属性错误路径。现由调用方在采用页时登记所属阅读投影，共用容量／图片分页逻辑不再访问对话专属字段。此项同样是源码确认，不是本轮真实长工作复现。

稳定合同已同步架构、上下文、插件及运行手册。没有调整模型、人格、原窗口额度、摘要参数、权限、根配置、网络链路或原重试次数；没有结构转换、历史清理、新的通用缓存／校验平台或额外模型调用。调用诊断结束前进程退出、旧调用或非该网关入口没有记录时，保留未记录，不能补认一次请求；返回型号只是上游标签，也不证明实际后端身份。

### 实际核对与未确认

- 对当前 **214 个 Python 源文件**执行 `uv --cache-dir /private/tmp/lenbot-uv-cache run --no-sync python -m compileall -q -f src/len_bot`，退出 0。
- 随后执行 `uv --cache-dir /private/tmp/lenbot-uv-cache run --no-sync python -c 'import len_bot.main'`，退出 0。仅导入入口依赖，不调用 main/run_app，不启动数据库、Bot 或平台连接；不能据此宣称完整启动或业务通过。
- 已按调用点静态回读工具结果、插件注释、工作恢复、最终容量与模型调用结算路径。没有前端源码修改，未重复构建前端。
- 只读定位曾使用不存在的 cognition/metrics.py、tools/retrieval_models.py 和 transport 目录，返回 `No such file or directory`，随后按真实路径定位。公开请求关联文档的一次读取返回 `curl: (56) The requested URL returned error: 403`，没有把该页面当作支持证据；客户端发送接口与重试计数头按当前安装依赖源码核对。

尚需在获准同版环境正常观察：限定人物／时间的召回、首次原话读取带合法插件说明、部分资料与完整依据的区别、相同边界下窗内关联来源的位置、正常工作分页及恢复、自然发生重试时的调用关联、实际阶段耗时与缓存返回。未制造数据、故障、重试或延迟，也未运行测试、夹具、断言式探针、自动截图、覆盖率或压测。没有重新解释历史五次缓存异常的根因，没有清洗旧阅读记录或证明答复语义正确。

本批源码与文档留在工作区，未提交、未推送、未部署；未修改运行配置和业务数据，未启动服务、调用模型或真实群发送。完整产品目标仍处于集成稳定化与真实体验验收阶段。

## 2026-09-20：启动入口的不存在枚举引用修复

用户在阶段提交 cd816e9 后正常执行 `uv run len-bot`，提供了启动导入失败原文：`AttributeError: type object 'EventType' has no attribute 'USER_LEFT'`，位置为 memory/writes.py 的模块级 `_EXTERNAL_EVENTS`。这确认了一个实际启动回归，不是配置、依赖安装或模型连接问题。

原因是上一批将原字符串集合改为 EventType 成员引用时，保留了枚举中从未定义的 USER_LEFT；字符串比较不会触发属性查找，模块级枚举引用会在导入时立即求值。现只移除该不存在的成员，保留实际定义的 USER_JOINED、LIVE_STARTED 与 LIVE_ENDED，不新增退群事件协议、兼容猜测或迁移。既有真实来源／模拟排除及认识事务不变。

已执行 `uv --cache-dir /private/tmp/lenbot-uv-cache run --no-sync python -c 'import len_bot.main'`，退出 0：仅加载同一启动入口的模块依赖，不调用 main/run_app，不读取根运行配置或启动数据库、Bot、平台连接。此结果只证明本次入口导入链通过，不代表服务完整启动或业务验收。此前 214 文件的 compileall 只做语法编译、不执行模块级表达式，因此未发现该错误，不能用它证明可启动。

本次修复与本节说明按当前授权一并提交，随后清除本次进程的代理环境并覆盖 Git 代理设置，以直连方式推送 origin/master；实际提交和推送结果以 Git 回执为准，不修改持久代理配置。未部署，未执行测试、夹具、断言式探针或真实模型／群发送，也未修改运行配置和业务数据。完整启动和 P11 真实业务验收仍须正常运行记录确认。

## 2026-09-20：阶段实现提交与当前交付位置

按本次明确授权，将本轮源码、完整改造计划和所属合同一并纳入阶段提交；提交身份沿仓库所有者配置，不附加协作者或生成署名。范围包括唯一群工作台与页面操作、答复依据、认识／方法的来源与时效、工作／执行／文件回执及配置冲突处理。两份旧计划的仓库删除与新计划替换同批提交，原副本保留在既有系统回收站位置。

当前处于 **P11 的整体验收与发布准备**。P01—P10 及 P11 已有逐批实现记录，但不表示对应人工验收全部通过；下面的 U01—U10 对照继续有效。现有证据是 214 个 Python 源文件语法编译退出 0，以及最后一次前端改动后的 481 模块构建退出 0。此次只有提交与状态文档整理，没有新增行为代码或再次构建，也没有执行测试或运行验收。

本次提交不包含根运行配置、凭据、数据库、媒体／文件数据、依赖或 dist 产物；不推送，不部署，不启动生产，不调用模型／平台或真实群发送。此前各批“未提交”是当时记录，本节说明新的交付位置，实际提交身份以 Git 历史为准。完整目标仍缺同版页面、正常群聊查证与纠正后采用、长工作和文件真实交付，以及停机备份／发布前置；提交授权不等于这些操作的授权。

## 2026-09-20：P11 整体交付核对与现场前置

### 当前证据的范围

上一轮有偏好到期与查询采样时间的源码／编译进展。本轮重新读取 HEAD（6b115aa）、工作树、B01—B12 边界、M01—M24 模块合同、U01—U10 与 A01—A30 对应、发布记录模板和停机备份要求。保留所有未提交修改；源码尚未形成新的发布提交，也没有已确认运行这些修改的同版实例。没有存活命令或任务句柄可供继续等待。

本次先读取源目录，未发现 test*.py 或 tests 目录，再执行 `uv --cache-dir /private/tmp/lenbot-uv-cache run --no-sync python -m compileall -q -f src/len_bot`，退出 0。范围是当前 src/len_bot 中的 **214 个 Python 文件**，只编译、不导入或启动业务。前端在上一相关修改批次完成 481 模块构建；此后未改前端，本轮不重复构建。上述结果只能说明源码语法／打包范围，不能证明运行导入、界面或业务链路正确。

### 目标到剩余交付的对照

下表是当前工作树的实现入口与所缺证据，不是新的通过清单，也不声称已经排除所有源码缺陷。计划的完整模块和边界合同继续有效。

| 目标 | 现有实现入口／记录范围 | 下一项所需的真实证据 |
|---|---|---|
| U01 导航与配置 | M22／M23；群工作台、router、EntityLink、配置草稿及并发保存 | 同版已登录面板的 A01/A02/A30，实际宽窄屏、键盘、离开草稿及返回位置 |
| U02 状态解释 | M01／M02／M05／M22；提交、来源处理、查询关系与阶段组件 | A04/A05；自然出现失败／未知时按 A21 读原记录，不制造故障 |
| U03 接话与潜水 | M03／M04；attention、context、social_core、原睡眠与观察边界 | 获准真实原话、模型与回应范围内的 A05—A08，不能用发言率或提示词代替 |
| U04 按需查证 | M04／M05／M07；固定定义、资料展示范围、answer_basis 与原 Gate | A09—A11/A28 的正确对象、实际正文／像素、原调用及单条回答依据 |
| U05 回忆与偏好 | M08／M09；查询／原表复核、时效、认识链、历史与索引 | A12—A14 的原话定位和后续有效偏好；不强制触发失败、重建或维护 |
| U06 纠正与后续采用 | M08／M10；refute/supersede、来源资格、工作方法候选 | A07/A13，以及实际涉及方法时的 A15；不自动重写人工资料 |
| U07 工作连续性 | M11／M12；原工作版本、checkpoint、压缩和累计账 | A16/A17 的正常长工作、压缩、明确修订／取消／恢复；A18 仅在自然不足时记录 |
| U08 成果及文件交付 | M13—M16；执行输入、产物快照、file_asset、原发送回执 | A19/A20 的当前部署、共享路径及真实平台 file_id；面板可下载不代替上传 |
| U09 用量与错误归属 | M06／M12；调用账、预占、用途和上下文统计 | A28/A29 当前协议实际调用与原账户账；旧缓存调查不能证明本版工具定义表现 |
| U10 可选能力 | M17—M21；提醒、兴趣、账号、插件与 Core 的逐项处置记录 | 先确定本次启用范围，再观察对应 A22—A27；未启用仍保持关闭，不以全部开启验收 |

M24 的提交／同版产物／正常停机备份／获准启动仍有独立前置；发布模板是待填写的工具，不是现场证据。外部循环适配和普通短对话自动发布方法仍非本计划默认交付，不为填验收缺口临时引入。

### 接续条件与操作边界

下一项有意义的交付工作是取得同版、已获准的正常运行环境并进行人工观察，而不是继续无具体缺口地新增抽象。当前没有新提供的已登录开发面板、允许调用／发送的具体范围、当前 OneBot 文件协议及可选部署范围；此前所询问信息尚未补齐，本轮不重复索问或查询浏览器库存。

现场条件补齐后，先只读查看本版页面与已有记录，再按当次授权做正常群聊、资料查证、纠正后再次互动、长工作与小文件交付；失败沿原 event/call/job/action 身份定位。提交、推送、停机、备份、启动、模型／平台调用和真实发送仍分别确认，不继承旧缓存调查的授权。未获得这些条件时，不能伪造数据、登录、模拟回执或用编译代替实际效果。

本轮没有新增业务源码、测试、夹具、断言探针、自动截图、覆盖率或故障注入；未修改运行配置、业务库、人格、人工样例、Shadow、名单与凭据，未提交／推送或启动生产／Gateway。完整目标未达到完成条件；本次全目录编译补足语法范围后，现场前置仍是接续验收的实际阻塞。

接续复核（相同现场前置第 2 次）：HEAD 仍为 6b115aa；发布文件仍是待填写模板，未收到同版面板和本次业务范围的新信息。本次没有读取或改写根运行配置，Git 状态不能证明被忽略的运行文件是否变化。上一轮全目录编译属于语法证据进展，本次只确认同一前置未满足，不计作新的实现或运行进展。没有运行中的工具句柄，不能记为等待服务完成；不重复编译、生成验收数据或继续无依据扩大源码修改。目标尚未完成，现场信息补齐前保留原工作树。

接续复核（相同现场前置第 3 次）：重新读取当前 HEAD、剩余交付要求与发布模板后，仍未取得可执行真实验收的同版环境和当次业务范围；上一轮是无新进展，不是等待中的运行任务。目标已标记 blocked，完整范围与未完成项不变，不标完成，也不以继续静态修改绕过现场缺口。恢复需要提供同版开发面板／环境及当次允许的操作范围；文件交付同时需当前平台与路径前置。全部源码和文档保留在原工作树，未提交、推送或部署。

## 2026-09-20：持续实施 P07 对话偏好到期与查询采样时间

### 本批定位与源码

上一轮认识来源修改及四文件编译是实际进展。本批重新读取 HEAD（6b115aa）、工作树、对话 checkpoint／Actor／AgentLoop 与认识查询展示路径，保留全部未提交修改，没有仍在运行的接续命令。首先排除了“知识版本冲突会一直拿旧提案重试”的疑点：现有 SceneCommitConflict／CommitConflict 会结束当前轮，运行记录保留失败；成功提交才采用返回的知识版本，未改成自动恢复或重买预算。

确认的源码缺口有两处。互动偏好只在初次装配、新输入或成功 checkpoint 后更新，而 expires_at 自然经过不会增加知识版本；没有新输入的长工具循环可能继续保留已经到期的临时偏好。现每次最终模型请求前复用 install_preferences，仍只读取原本群和相关人物的本地有效偏好，并走最终容量核对；指定资料模式的插件不因此读入群记忆。没有新增自动语义检索、查询工具调用、状态层或模型轮次。

query_memory 在最终原表复核后还会等待写 Trace，原 ToolResult 的默认 fetched_at 却在这些等待之后生成；在此期间到期的记录可能绕过“查询后到期”的展示判断。本批把既有 fetched_at 固定为原表复核开始时的认识账本时钟。保存与展示继续沿原结果身份、范围、状态和 stale_memory_result；没有重查、历史回填或改变已经保存的工具正文。

架构与上下文合同已同步。刷新偏好只表示请求装配时的采样，不证明模型推理期间未变，也不更新提交所用的 Actor 知识版本；并发修订仍按原边界拒绝。这不是新的认识有效性平台或语义真假判定。

### 编译与未确认

两个 Python 文件（cognition/social_core、tools/retrieval）以 `uv --cache-dir /private/tmp/lenbot-uv-cache run --no-sync python -m compileall -q` 编译退出 0；本批无前端改动，未重复构建。这里只取得源码与语法证据，没有运行长工具循环、到期等待或真实查询，未通过改时钟、注入延迟或构造数据制造结果。

没有新的已登录开发面板信息，未重复询问、查浏览器库存或伪造登录。未运行测试、夹具、断言探针、截图、覆盖率、真实模型／平台或群发送；未启动生产／Gateway，未改运行配置、业务库、人格、人工样例、Shadow、名单或凭据，未提交／推送。

本批有源码与编译进展，目标继续 active。A05—A14 的真实互动／纠正后采用、长工作与文件交付、实际页面、停机备份和发布前置依旧缺完整证据；仍不能以代码与编译宣布完整计划完成。

## 2026-09-20：持续实施 P07 认识来源与真实互动边界

### 本批定位与源码

上一轮完成素材／方法回执源码、文档与构建，属于实际进展。本批重新读取 HEAD（6b115aa）、工作树、完整计划 5.1—5.7、对话输入／提案／Actor／原事务以及认识／历史维护入口，保留全部既有未提交改动。没有存活命令需要继续，也没有重新启动服务。

静态阅读发现 validate_memory_proposal 只用消息类型及“不是 Bot”判断人类来源，未读取模拟标记；MESSAGE_SENT 用于关系语境时也没有核对真实送达。认识新主体的在场查询同样可由模拟事件满足。这些是源码缺口，并不证明实际数据库已存在此类错误认识；本批没有查询业务库或制造模拟输入。

本批沿既有写入边界将原事件解析为 Event，使用现有 human_event_uid 判定人类身份，拒绝模拟标记／simulated 来源；主体创建／替代排除只有模拟记录的在场依据。工具原观察要求明确布尔独立来源；Bot 关系语境复用 receipt_delivery_status，必须是 live 且有真实送达身份，并继续要求人类原话，不能独立支持事实。撤销仍核同场景有效目标和本次纠正／具名运营事件，不要求重新证明待撤销的错误主体或分类；创建／替代的原规则没有放宽。没有更换 Gate、认识修订号、权限或原事务。

对话原话投影补模拟标记，Bot 发言补原回执分类／模式，避免仅凭 assistant 角色误认已发生互动。历史维护在片段位置外层投影 source_context，保留原事件类型、说话人、模拟与送达事实；新批次和加载原批次共用。history_source_text 未修改，旧正文坐标和完整覆盖身份不重算；附加事实计入原请求容量。维护终结及原提交事务拒绝将模拟来源变成当前 review_items，认识继续走同一写入边界；不新建事实平台、补购模型调用或自动重做历史。

稳定行为同步架构、上下文与运行手册。没有新增数据库表／列、转换脚本、状态版本或工具 Schema；原历史与认识保留，可能存在的旧问题须按原事件及人工纠正处理，不能由本次源码修正宣称已清理。

### 编译与未确认

四个 Python 文件（memory/writes、memory/history、memory/reflector、cognition/context）以 `uv --cache-dir /private/tmp/lenbot-uv-cache run --no-sync python -m compileall -q` 编译退出 0。没有本批前端变更，未重复运行构建；没有执行模块导入、数据库事务或模型调用，编译不证明真实记忆行为、摘要覆盖或缓存效果。

没有新取得的已登录开发面板，未重复索问、查询浏览器库存或制造数据。未实际撤销／替代认识、重做维护、改人格／人工样例／Shadow／名单／凭据，未修改根配置或业务库，未启动生产／Gateway／模型／平台或群发送。未新增、修改或运行测试、夹具、探针、自动截图、覆盖率或故障注入；未提交／推送。

本批有新的源码与编译进展，完整目标继续 active。真实原话的回忆、当轮纠正及后续采用（A05—A14），资料性答复、完整工作与 A19/A20 文件交付、实际页面以及停机备份／发布前置仍缺完整人工证据；不能以更严格的来源校验替代“能自然搭话、按需解决问题且记得正确”的实际效果。

## 2026-09-20：持续实施 P11 素材未知结果与方法公开回执

### 本批定位与源码

接续上一轮素材实现，重读工程约束、HEAD（6b115aa）、工作树与原素材／方法公开路径；保留前批所有未提交修改，没有存活命令需要接续，没有重复归档旧计划。以下缺口来自静态源码阅读，没有上传、公开、并发或故障复现。

素材上传错误的 asset_saved=true 身份若不匹配，旧分支没有设置待核对状态，仍能再次上传；编辑请求的网络／响应异常也只显示错误而开放原草稿保存。本轮沿页面已有请求生命周期增加本次未知结果展示：只接受原范围／对象、事件身份和已知阶段的入库回执；不符仍未知。编辑成功及已保存读回继续核原来源／创建身份。未知时保留并冻结原基线和提交值，重读只作对照，须明确放弃旧草稿并采用当前值才能重新编辑；不把相同当前值追认为旧请求回执，不取消或重放旧请求。没有改变媒体 API、权限、生成式产物或持久恢复路径。

方法公开原接口写入指定版本后读回“当前最新”，在后来出现新版本时可能返回另一版；读回异常也不能区分已经公开。本轮将读回固定为 expected_version，保留 success／skill 成功格式；409 区分 skill_published=false 的事务冲突与 skill_published=true 的已公开后读取失败，均带原 ID／场景／版本。原 skills 与 skill_versions 两项更新在写锁／BEGIN IMMEDIATE 内核命中并一起提交，异常回滚，避免后续语句失败留下未结束事务。原运营事件仍是请求记录，不改为成功依据；正文、来源、其他版本及可读范围准入不变。

方法页复用共享请求生命周期；确认窗口固定对象及版本，公开反馈位于当前详情，不被后续列表错误替代。确认成功的同版本响应直接作为新采样；已写入待读回或未知结果保留独立回执，当前详情不再次提交。后续刷新固定原版本，不用新版或当前公开状态补认未知请求。离页／切换后不采用旧回调，也不宣称服务器已取消。

稳定合同同步架构与运行手册，方法公开 API 须与前端同版；无数据库结构变化、历史回填、新公开队列、权限平台或模型调用。

### 编译与未确认

两次 `npm run build` 均退出 0、481 个模块，分别 **1.65 秒／1.62 秒**；末次包含方法页和最终素材页改动，日志位于 `/private/tmp/lenbot-media-skill-build-20260920.log`。两个 Python 文件（skills/store、web/routes/cockpit）以 `uv --cache-dir /private/tmp/lenbot-uv-cache run --no-sync python -m compileall -q` 编译退出 0。均只证明语法／构建，不证明事务或页面交互已经运行通过。

没有新的已登录开发面板信息，未重复索问或查询浏览器库存，没有伪造登录、资料或回执。未实际上传／编辑素材、公开方法、运行模型／平台或群发送，未启动生产／Gateway，未修改根配置、业务库、人格、人工样例、Shadow、名单或凭据。未新增、修改或运行测试、夹具、探针、自动截图、覆盖率或故障注入；未提交／推送，dist 未列入 Git 改动。

本批有源码、文档与编译进展，目标继续 active。剩余运营入口及全路径仍需收口，素材／方法的真实页面、键盘／窄屏与正常操作未验收；核心群聊 A01/A02/A04—A13/A16/A17/A28—A30、A19/A20 文件交付和正常停机备份／发布前置依然缺完整证据，不以这批构建替代全部计划完成。

## 2026-09-20：持续实施 P11 素材原值与登记回执

### 本批定位与源码

上一目标轮有提醒控制源码、文档与编译进展。本轮重新读取 HEAD（6b115aa）、工作树、素材页和原 media 路由／服务／存储及事件登记路径，保留全部未提交修改，没有存活命令或需要重启的进程。静态路径确认：原素材编辑覆盖全部可编辑字段，没有编辑原值；原上传／编辑入库后，事件登记或读回异常仍按普通失败返回，无法区分已存在的资产。未做并发或上传故障复现。

- **原值到事务**：新增 CuratedMediaBaseline 只承载原公开身份、来源、创建时间及四个编辑字段，解决旧表单覆盖他人修改的缺口，不新增表／列或通用版本。原写锁内以 BEGIN IMMEDIATE 核同范围运营素材，并按字段合并实际改动；冲突回滚，标签整组。原事件队列与写入同事务，MEDIA_UPDATED 记录实际采用值，不把旧草稿中未编辑的字段冒充本次写入。消息／工具媒体仍只读，不移动范围或来源。
- **入库与后续阶段**：原 curated 保存事务返回后，事件登记／读回失败单列已入库、asset_id、scope、event_id 和阶段，不返回文件路径、字节或异常中的原始内容。事件可能尚不可读，读回保存值不等于事件已恢复；没有再登记、重放、清理或新恢复队列。未得到入库确认的网络／取消结局继续未知，不补成功身份。生成式工作产物路径未改。
- **编辑界面**：素材页复用请求生命周期和原冲突对照组件；同对象刷新保留草稿，显式选择后才重建编辑意图，原来源变化不能接用旧草稿。保存／读取错误与当前弹窗反馈分开，已入库待读回暂停再次提交；无实际修改不能由表单回车产生空写。目录顺序保持非负整数或 null，公共启用／顺序确认明确其影响范围。
- **上传界面**：原 ScopeSelect 的 disabled=false 会覆盖父表单禁用，现对上传范围显式传递禁用；提交按钮也单独核等待状态。成功后保留登记回执，不自动跳走或让导航失败变成上传失败；进入新素材须明确点击。结果未知时当前表单不再次发送，只能核对原范围，不能按相似记录推断成功。列表、目录、详情及上传响应各守所属页面／范围；关闭／离页不撤销服务器处理。

架构与运行手册已同步。素材编辑接口及原服务方法现在要求 baseline，前后端须同版；旧请求缺原值明确拒绝，不为兼容而补当前值。没有数据库迁移或历史回写，没有新增模型可见定义或模型调用。

### 编译与未确认

本批四个 Python 文件（media/models、media/store、media/service、web/routes/media）以 `uv --cache-dir /private/tmp/lenbot-uv-cache run --no-sync python -m compileall -q` 编译退出 0。两次 `npm run build` 均退出 0、481 个模块，分别 **3.15 秒／2.94 秒**；末次包含最终上传范围核对导航与防空写。仅为语法／打包证据，不是实际上传、并发事务、事件登记或页面验收。

没有新取得的已登录开发面板，未重复索问或查询浏览器库存，没有伪造登录／素材／样例。未实际上传、编辑或停用素材，未调用模型、平台或群发送；未启动生产／Gateway，未改根配置、业务库、人格、人工样例、Shadow、名单或凭据。未新增、修改或运行测试、夹具、探针、自动截图、覆盖率或故障注入；未提交／推送，dist 未列入 Git 改动。

本批有真实源码和编译进展，目标继续 active。接续收口剩余运营入口及全路径边界；素材页面的宽窄屏／键盘、正常登记回执和实际启用链路仍待人工确认。核心群聊、A19/A20 文件交付、正常停机备份与发布前置尚未取得完整证据，不能以本批完成全部计划。

## 2026-09-20：持续实施 P11 提醒控制原值与等待读回

### 本批定位与源码

上一目标轮有认识／工作页面源码、文档和构建进展。本轮重新读取工作树、工程约束和提醒控制 → 运营事件 → Actor/Gate → 原事务的实际路径，保留所有未提交改动；没有存活的接续命令，没有重启服务。源码确认提醒事务原来只查当前是否可编辑，未核对编辑开始时的事项、时间和状态；只在 HTTP 路由提前比较也不能覆盖等待 Actor／事务期间的变化。

- **同一事务的原值**：新增 ReminderControlSnapshot 仅类型化现有 id／场景／事项／时间／状态／创建及来源／触发条件与实际触发身份，解决旧编辑覆盖已变提醒的缺口，不新增数据库列、修订序号、指纹或权限源。修改／取消的 TaskProposal.expected 在原写锁及 BEGIN IMMEDIATE 内与当前 TaskItem 比较，缺失或变化就回滚，原终态、范围、系统槽／延期拒绝和绝对时间检查继续保留。
- **面板与对话共用**：面板 update／cancel／trigger_now 均要求原值 baseline，先固定同一 ID 和场景，完整原值连同显式 null 写入原运营事件；没有接受失败后补当前值。scene_tasks 补取原列，TurnReferences 登记同一事项原值，ProposalLedger 暂存控制时复制它，后续目录更新不改已暂存提案。工具定义没有增加模型参数，目录原值不作为正文已读证明，没有新增模型调用。
- **冲突与读回**：提醒页复用请求生命周期，原编辑、确认对象和操作不因离页再返回而串用。冲突重读成功后明确保留实际编辑字段或采用现值，再另行保存；已终结／更换业务身份的原草稿不自动恢复。成功回执与对象读回分开，待读回时暂停再次提交，错误／失去详情时仍保留可查看的草稿。等待结束仍走原 active／review_required 条件更新；没有把结束等待当作收到答复，也没有建立另一套等待基线协议。
- **时间取舍**：没有编辑时间时保留原 Unix 秒精度；冲突重建保存独立的日期文本／实际时间对应，避免用户改过的整数秒在同秒显示相同后被换成他人的小数秒。非法日期、跳过的本地时间不自动规范成其他时刻，超出日期控件范围仍显示原绝对值；确认窗口可核对绝对时间与触发条件。

兼容边界已写入架构、上下文、运行手册和插件合同：前后端须同版，旧面板或调用方缺 baseline 会被拒绝并要求重新读取；旧已提交事件保留可读，旧未提交修改／取消不能自动补原值或重放。没有数据库结构转换或历史回写。原值相同不是新的单调修订号，也不证明期间没有变化后还原。

### 编译、定位失败与未确认

六个 Python 文件（scheduler/models、cognition/models/context/proposals、events/store、web/routes/cockpit）两次以 `uv --cache-dir /private/tmp/lenbot-uv-cache run --no-sync python -m compileall -q` 编译退出 0，末次含完整运营原值记录。前端 `npm run build` 退出 0，**481 个模块、1.66 秒**，包含本批最终页面修改。仅为语法／打包证据，没有运行接口、模型或提交事务。

只读定位时三处假定路径不存在，原文分别为 `sed: src/len_bot/state/tasks.py: No such file or directory`、`rg: src/len_bot/cognition/refs.py: No such file or directory (os error 2)`、`cat: src/len_bot/scheduler/__init__.py: No such file or directory`。随后按实际 scheduler/models.py、cognition/context.py 和目录内容继续阅读，没有补造这些文件，也未改检查。

没有新的已登录开发面板信息，未重复索问、查浏览器库存或伪造登录／资料。没有真实编辑／触发／取消提醒、结束等待、并发控制、页面／键盘／窄屏或时间控件观察。未新增、修改或运行测试、夹具、探针、截图、覆盖率或故障注入；未启动生产／Gateway、模型、平台或群发送，未改根配置、业务库、人格、人工样例、Shadow、名单或凭据，未提交／推送。构建产物不入 Git。

目标继续 active。本轮完成一条原值到事务的源码贯通，不宣布 A22 或 P11 验收完成；接续收口其余素材／运营入口的对象归属和未确认结果。核心群聊、A19/A20 文件交付、实际启用可选分支、正常停机备份及发布前置仍需真实证据。

## 2026-09-20：持续实施 P11 认识撤销与工作修订草稿

### 本批定位与源码

上一目标轮有模型配置源码、文档与构建进展。本轮重新读取工作树、HEAD（6b115aa）、完整计划的页面／控制合同与现有认识、工作、提醒、方法界面和原提交端点；保留所有未提交修改。没有存活的接续命令，不重启服务。以下为源码核对与修改，不是现场竞态观察。

- **认识撤销归属**：原回调仅判断当前 ID／范围，离开后再返回同一认识仍可能接受旧响应，finally 也会清掉后来页面的 saving。现列表／详情／撤销复用请求生命周期，切换身份同步失效，只有同一次操作能更新反馈和后续读回。服务端继续按原认识 ID、运营事件、Actor 和 Gate 提交，没有更改权限或撤销协议。
- **回执与链读回**：原实现先就地改 memory.status，再读链；读链失败会使同屏认识和修订链属于不同状态。本轮改为单列同一认识的撤销回执，链与详情仍一起读取。读回失败或仍与回执矛盾时显示旧采样并暂停再次撤销；不自动重发。未取得成功回执的错误不冒充已保存。对象不可读时，错误、撤销理由和工作修订草稿仍可查看并明确放弃，不消失或改投其他对象。
- **工作修订意图**：原“改为基于当前版本”只换基线，保留的旧要求文本可能把别人新增的要求误算成删除。现以原基线计算自己真实增删，再重建到刚读取的要求；未改目标取现值。提供明确保留／放弃两个入口，读取失败不能使用旧样本更新基线。选新基线不保存，提交继续携带原 expected_revision。
- **业务参数与确认**：工作草稿固定开始编辑时的修订接口、归属及参数；接口改变后不能悄悄忽略旧参数或套入新 Schema，可以只放弃业务参数而保留目标／要求。嵌套参数编辑同步保存禁用，确认窗口固定对象、版本和目标，不随后台刷新换成另一个目标。确认后若已读到版本变化，返回核对而非迁移旧确认；操作及拒绝后的重读绑定原页面世代。取消反馈改为请求已提交，实际停止仍看执行记录。

方法公开路径本轮保留：界面已有独立发布世代和 id／场景／版本选择；原 publish_skill 在既有写锁按 current_version 与 scene 限定，只改该版本可见范围。没有公开任何方法。提醒编辑尚需下一步沿原提交路径继续核对：当前 TaskUpdateRequest 只有 description／due_at，不能仅凭工作页的 expected_revision 修复就宣称提醒也具备同样的编辑期并发保护；本轮没有为提醒增加版本平台或修改业务数据。

### 构建与未确认

两次 `npm run build` 均退出 0，481 个模块，分别 **2.34 秒／3.01 秒**；末次包含冻结确认目标、不可读对象的草稿展示和操作反馈位置。没有新增 Python 行为，不引用上一批 Python 编译作为本批验证；未运行测试、夹具、断言探针、自动截图、覆盖率或故障注入。源码及文档已保存，dist 未列入 Git 改动；未提交、未推送。

没有新的已登录开发面板信息，未重复索问、查浏览器库存或伪造登录／业务资料。未实际撤销认识、控制工作、改提醒、公开方法、调用模型或发送；未启动生产／Gateway，未改根配置、业务库、人格、样例、Shadow、名单或凭据。页面、键盘、窄屏、真实撤销读回、并发修订及外部停止回执均未人工确认。

架构与运行手册已同步；目标保持 active。接续核对提醒／等待控制的原值和状态边界，再收口剩余运营页面。核心业务、A19/A20 文件交付、实际启用可选分支以及停机备份／发布前置仍未完成，本批构建不能替代整计划验收。

## 2026-09-20：持续实施 P11 模型配置冲突与绑定归属

### 本批源码与定位

接续上一批系统／人格设置，重新读取工作树、模型页、保存路由、原三方合并及配置投影；保留全部未提交改动。旧计划已归档，本轮没有再次移动文件，没有尚在运行的接续命令。下述问题来自静态路径核对，未做并发运行复现。

- **分对象冲突**：供应商编辑、职责、检索与每份供应商目录接入原临时冲突记录和对照组件。冲突后重读保存值，失败不能选择；保留改动只重建当前项编辑意图，不更新其他草稿或自动提交。已保存但运行应用失败仍走 config_saved=true／待读回分支。没有业务错误正文的 409 不再被该 helper 标为已确认未保存。
- **供应商身份与凭据**：原 upsert 的 desired 不含 credential_revision，三方合并却含该只读基线，会将其视作删除意图，让普通 keep 编辑随他人换密钥发生冲突。现将修订留给原 replace／clear 核对，keep 仍取锁内当前秘密。非空基线必须同名；已删除目标拒绝旧编辑。新建改名清除旧名冲突；重名须明确采用现值或另取名称。页面已看到删除后，旧编辑不能接到后来出现的同名项。新建名称控件显式继承等待禁用，避免它的 disabled=false 覆盖整个表单的锁。
- **绑定参数归属**：原逐叶合并可能把一方针对旧模型修改的参数接到另一方刚保存的新模型。现职责／检索在原配置锁内先核同项 provider_id／model 关系，交叉换绑与参数编辑返回原冲突，不新增表或版本源。前端改选绑定清除旧推理／视觉确认或维度／协议；明确换绑按整项保留，不自动拼另一供应商的参数。检索半填、0／非法维度不再被序列化为关闭或留空；处理冲突时可先采用未编辑项的新绑定，不因旧供应商已删除而丢掉其他项的编辑。
- **目录与删除**：目录保留真实原列表基线、未保存选择和获取时公开接口事实。获取结束后重读配置，已观察到接口、凭据修订、启用变化或删除的草稿只供核对，明确取消后才能另行获取；消失对象另列，不能隐藏后再自动恢复。删除冲突提供只读当前值，重新删除仍需确认，不自动重试。目录查取只在点击入口发生，没有实际查目录或模型请求。

### 编译、失败原文与未确认

两次 `npm run build` 均退出 0、481 个模块，分别 **2.46 秒／1.94 秒**；末次包含最终冲突识别、绑定表单和已观察删除状态。`web/routes/models.py` 两次使用 `uv --cache-dir /private/tmp/lenbot-uv-cache run --no-sync python -m compileall -q` 编译退出 0，末次包含锁内绑定核对和供应商保存修正。构建／编译只说明语法与打包，不是接口、凭据并发或页面验收。一次只读定位的 glob 没有匹配，原文为 `zsh:1: no matches found: src/len_bot/web/config*.py`；随后按实际 config_edit.py 和 query_service.py 定位，没有修改检查或凭空补文件。

未取得新的已登录开发面板信息，未重复索要或查询浏览器库存；没有实际页面、窄屏、键盘、保存／删除／冲突或调用观察。未运行测试、夹具、断言探针、截图或故障注入；未启动生产、Gateway、模型、平台或群发送，未改根配置、业务库、人格、人工样例、Shadow、名单或凭据，未提交／推送。构建产物仍不入 Git。

目录来源判断只覆盖已读到的样本，不能识别原 ID 下未被观察到的删除再建或接口改变后还原；没有以临时前端状态冒充服务端身份世代。稳定合同已同步架构与运行手册。本批有源码和编译进展，目标继续 active；接续收口 P11 其余运营页面的对象归属与失败态，A03 实际交互、核心业务、A19/A20 文件交付、启用的可选分支和停机备份／发布前置仍未完成，不能用本批编译宣布全计划完成。

## 2026-09-20：持续实施 P11 系统／人格设置的冲突处置

### 本批定位与源码

前一目标轮中断前的只读工作取得了改变下一步的证据：访问保存除了整组基线，还逐条核对授予 revision；简单更换总基线仍会失败，也不能把已删除的 ID 当成新授予。该轮属于定位进展，不是已完成实现。本轮重新读取工作树、工程约束与相关接口；没有需要继续等待的命令句柄，保留全部前批修改。文件访问权限变化没有转成生产启动或真实保存授权。

- **八个配置标签**：连接、访问、额度、订阅对象、运行参数及人格／参与／时间接入原冲突提示和草稿比较。新增 useConfigConflicts 仅共用按标签的临时拒绝记录、重读值与错误；请求生命周期仍由原 helper 控制，不发起自动保存。跨标签保留原草稿和冲突标记，返回重新读取，错误时不能选择。现值可展开核对，明确保留／采用只调整当前标签的编辑基线。
- **授予身份**：比较可编辑内容，未改授予不随白名单编辑重传。明确保留修改后才按当前同一 grant_id 取得 revision；缺失 ID 停止并要求明确处理，不清空 ID 重新签发。实际请求仍提交版本并由后端认证、原锁和原版本校验决定结果，没有修改权限规则。原 serializer 将并发 0 转为 null 的行为已去掉；有效期 0、格式无效和日期控件无法表示分别显示，未改到期值继续保留原精度。
- **保存与运行区分**：连接冲突只保留当前草稿的 keep／replace／clear 意图，不将新令牌放入保存值对照区。运行 JSON 与额度按语义值重新装配，不把格式变化当成对象覆盖；授予和主播列表仍是整组编辑。连接状态文案采用 active_connection，业务时间／订阅对象的全局重启标记不冒充具体组件状态。能力选项读取失败不再抹掉已取得的访问配置，原参考错误仍显示。
- **静态回读修正**：共享冲突条增加后端说明后曾将 else 绑定到该说明，普通设置会同时显示“已读入”和“尚未取得”；本轮读模板时已改为明确 !current。该项没有现场复现，说明构建成功仍不能证明状态文案与流程正确。

### 编译与未确认

三次 `npm run build` 均退出 0：第一次 480 个模块、1.59 秒，后两次 481 个模块、1.55／**1.67 秒**；末次包含最终冲突对照、授予时效与运行事实文案。没有新增 Python 行为、测试、夹具、断言探针、自动截图或故障注入。新 helper 已显示为待纳入源码的文件，dist 未列入 Git 改动；没有提交或推送。

没有新取得的已登录开发面板，未重复查询库存或制造登录／业务数据。未实际保存连接、授予、额度、人格、时间、样例或 Shadow，未改根配置和业务库；未调用模型、插件、平台或群发送，未启动生产。接口字段与分支的静态核对不等于实际并发、凭据冲突或页面验收。

稳定合同同步架构与运行手册。目标保持 active：模型弹窗的冲突处理与对象切换仍需专项收口，八标签的实际交互、当前值对照、授予删除／修订及窄屏键盘仍待正常人工确认。全计划的核心业务链、文件交付、已启用可选分支、停机备份与发布前置尚未完成，不能由本批构建宣布完成。

## 2026-09-20：持续实施 P11 本群／插件配置的冲突闭合

### 本批定位与源码

上一目标轮有源码、文档和构建进展。本轮重读当前工作树与完整计划的 4.3／4.4、M20／M22／M23 合同；没有仍在等待的运行句柄，没有重新启动服务。静态核对发现本群保存仍只比较场景 ID，冲突重读还有空 catch；“保留我的改动”只换基线、没有重建改动，会把未编辑字段的旧值也变成下一次提交意图。未做运行竞态复现。

- **冲突与草稿**：本群读取、保存、冲突回读和字段定位接入已有请求生命周期；换群后返回、换页或卸载不采用旧响应。冲突后另持有真正读到的当前值，失败保留原错且不可选择。新增仅供明确冲突选择使用的 configDraft 窄函数，对照原始草稿、当前草稿和新值，保留实际编辑项，其余采用新值；数组仍按整组。插件 JSON 字段先按 Schema 转为实际值，排版变化不导致整对象覆盖。互斥后端冲突保留自己编辑的分支，不由属性先后顺序决定。没有新保存入口、持久状态、签名或自动提交。
- **保存结果与编辑锁**：全局插件写入后不提前将原草稿标成新基线，须读回同插件；失败保持待读回状态，不能再次提交。原配置锁和凭据版本继续核对。嵌套插件字段及列表／分支按钮显式接父表单禁用状态，避免等待期间仍能更改已提交内容。冲突选择只改变本页，保存、启停和重新装载仍各自明确执行。
- **唯一群配置入口**：固定能力卡中的重复插件 ID 只显示一份参数；卡片外的 catalog 插件补入“其他已声明插件”，仍用原接口和群 Schema。缺全局参数链接到原插件详情，缺 Schema 明确失败，不写空对象替代原参数；全局未配置时，本群已有启用项仍能改为停用。字段错误可展开并定位实际插件表单，尚未现场核对键盘和布局。
- **文件授予与继承**：原表单每次都提交文件申请者，初次填写群规则还会清空原选择。现只在选择实际变化时传 send_file_principals，未改时沿已有 None 合同跳过授予修改；新群草稿保留读到的申请者。旁听 null 覆盖按全局值预览，已保存的有效值另列，不用旧本群覆盖解释“继承”。没有实际保存群参数、签发或撤销授予。

### 编译、失败原文与未确认

首轮前端构建退出 1：`src/components/PluginConfigFields.vue (105:32): Error parsing JavaScript expression: Unexpected token (1:8)`。原因是本轮批量编辑将禁用属性误插入箭头表达式，已逐处修正模板属性；没有改编译检查。随后三次 `npm run build` 均为 **479 个模块、退出 0**，依次 1.82、1.50、**1.56 秒**；末次包含互斥分支处理和字段定位生命周期。仅是构建证据，没有新增 Python 行为或运行测试。

一次只读源码定位指定了不存在的路径：`rg: src/len_bot/web/server.py: No such file or directory (os error 2)`；已从实际 web/app.py 读取原 409／config_saved=false 合同，未因此猜测端点或发起网络探测。

交付范围核对发现原 `.gitignore` 的 Python `lib/` 规则也忽略了新增前端 lib/configDraft.js。已仅对前端源码 lib 目录增加例外，让新源码可随同提交；没有放开 dist、依赖、运行配置或数据库。此问题说明本机构建成功也不能独自证明新检出可构建，实际新检出和发布仍未执行。

没有新取得的已登录开发面板；未重复库存查询、制造数据、运行测试／夹具／断言探针／自动截图或故障注入。未实际保存配置、人格、样例、Shadow、群名单或凭据，未运行插件、模型、平台与群发送，未启动生产、提交或推送。前端产物仍未列入 Git 改动。

稳定合同同步架构、运行手册和插件文档。P11 尚未完成：系统／人格等其他表单的冲突处置可达性仍需继续核对；本群与插件的新交互仍待实际页面、同对象并发编辑、已声明外部插件和宽窄屏／键盘确认。核心业务链、A19/A20 文件交付、已启用可选分支及发布前置同样未验收；目标保持 active，不以本批构建替代完整交付。

## 2026-09-20：持续实施 P11 页面读取、保存与登录生命周期

### 本批源码与边界

保留前批所有未提交修改；接续总览、能力和登录页改动，再静态阅读模型、系统与人格设置及原保存接口。定位到配置读取只比较请求序号、保存响应无页面归属核对、人格／连接在读回前更新编辑基线等缺口。这里是源码判断，没有做竞态注入或宣称现场复现。

- **总览与能力**：统计、能力与五类问题列表独立加载，提醒和延期分别查原分类，错误不遮住其他成功内容，未知不补零；历史失败和可选停用项不称当前故障。能力按群／请求者清旧范围，成员独立读取；重复参数明确拒绝，选择账号不冒充工作来源。原任务实体链接携带真实 kind，延期与系统任务进入对应目录。
- **登录与返回**：共享 API 采用前端内存会话世代，旧请求不清掉新登录或回填新会话；401、非 JSON 和会话变化保留 HTTP 状态，不重试。登录清共享运行／场景／时区与位置样本；已核对 login 的业务调用只有登录页。请求 helper 增组件作用域销毁与可选读取范围核对，使用点均在组件初始化范围。404 和导航排除仍落入未知路由的来源；全局退出先提示草稿丢失和已提交操作不取消。
- **三类设置**：读取与操作分别绑定页面、标签及本次世代，新保存使旧读取失效，离页／切页不回填旧结果或继续后续刷新。模型配置与额度账、人格配置与样例列表独立读取。后端只返回保存确认时，模型／人格／连接必须读回保存值才能更新基线；读回失败保留待核对状态，不再次提交该配置。原配置锁、三方合并、凭据版本与权限未改，不建持久重试状态。
- **样例与素材交互**：模板的逐条保存只保留已确认项，首个失败或离页后不继续尚未提交的项；运营素材读取绑定原样例、图片片段和范围，关闭或重选不采用旧列表。保存期间禁用相关草稿操作，普通标签保留配置草稿，弹窗离开先确认。没有实际添加、修改或启停任何人格、样例或素材。

### 编译与未确认

前接续段已完成两次前端构建，末次 478 个模块、1.63 秒、退出 0。本轮三次 `npm run build` 均为 478 个模块、退出 0，依次 1.80、1.72、**1.50 秒**；末次包含最终的页面归属、退出确认、保存值待读回及样例未读取说明。均仅为编译证据，没有新增 Python 行为或把前批 Python 编译作为本批运行验证。

前接续段只读浏览器库存显示：可访问的内嵌浏览器没有标签；另有运行中的外部浏览器，但没有读取其页面。这不能推出用户没有已登录面板。本轮没有新的开发面板地址或已登录页面信息，未重复查询库存或索要同一信息，未新建服务、假登录或业务数据。

没有实际页面、宽窄屏、键盘、登录切换、保存／冲突或资料交付观察；未运行测试、夹具、断言探针、自动截图或故障注入。没有调用模型能力检查、连接检查、Reset、模型／平台或群发送，没有启动生产、改根配置／业务库／人工资料／Shadow／名单，未提交或推送。稳定合同已同步架构和运行手册；前端产物不入 Git。

完整目标保持 active，不把本批源码与编译写成 P11 或整计划验收完成。接续核对余下页面的对象／草稿／失败态与可达导航，核心人工验收、A19/A20 文件交付、已启用可选分支与发布停机／备份／升级前置仍未完成。

## 2026-09-20：持续实施 P10 共享后端与 P01/P02 返回位置

### 本批源码与边界

上一目标轮有源码、文档和编译进展，不是无进展等待。本轮先重读工作树与相关合同，保留全部既有修改；没有尚在运行的命令句柄，也未重启服务。

静态路径确认浏览器／媒体只查 workspace，而能力卡同时认 python_workspace：旧 ID 的 Gateway 可能被媒体忽略，浏览器则可能选择自己的本机试用分支。现沿既有配置模型新增一个窄的 configured_workspace 选择函数，不建配置层或迁移表：唯一启用 ID 优先；都停用时只能有一份已配置项；多启用或多停用配置的歧义明确失败。RootConfig 先逐项解析全局插件参数，再执行原 validate_config，避免依赖校验因 JSON 中的键顺序拿不到已解析模型。根配置、保存锁、凭据世代与参数来源不变。

浏览器、媒体、能力卡及无装载入口的产物面板共用该选择；已有 run_python 注册所有者仍优先决定面板所读实例。文件能力投影遇到歧义不再称具备生成条件。浏览器无共享 Gateway 时保留原本机试用入口，媒体仍拒绝宿主解码。两者实例固定原配置 ID 与 Gateway 参数，在原工作准入、等待锁之后及相应命令入口再次核对；新执行登记后、网络提交前变更则在原日志记 submission_refused／failed，未提交到其他后端。旧执行清理仍用原客户端，未知不重放。没有扩大公共研究工具所有者、群插件开关或 grant，也没有自动重载依赖；运营须沿原配置保存明确重新装载浏览器／媒体。

群工作台原仅在读过消息后保存访问位置，记忆／工作等子页返回时不恢复滚动。现沿已有最多八项的页面内存保存目录和子页的条目锚点、参与者筛选及展开项，消息窗口仍按原规则保存；非消息业务资料返回时正常重读，不缓存设置草稿。读取完后按身份而非列表下标定位，条目不在当前页面时显示说明；用户主动滚动／点击／按键会取消迟到的位置恢复。设置页新增内部 loaded 通知等待实际读取完成，失败不跳离错误；关联检查器尚在读取时也不提前消费滚动位置。登出继续清空，硬刷新或淘汰后只能按 URL 重读。

同一页面的历史维护重试原只比场景 ID，离开后回到同群可能采用旧响应或留下旧轮询。现按操作及页面世代核对，导航／卸载和新重试会停止旧界面跟踪；不取消服务器已接受的维护、不自动重新提交。实际宽窄屏、焦点和状态变化仍需现场观察，不能仅据实现宣称 P01/P02 完成。

稳定合同已同步架构、运行手册和插件文档；下方前批“浏览器／媒体兼容依赖待处理”是当时状态，本批已补源码，现场核对仍未取得。

### 编译与未确认

末次九个 Python 文件（config_store、workspace/config、browser_agent/plugin_core、browser/gateway_service、media_analysis/__init__、media/segment_service、plugins/host、media/files、web/capability_status）以 `uv --cache-dir /private/tmp/lenbot-uv-cache run --no-sync python -m compileall -q` 单独编译退出 0。前端三次 `npm run build` 均退出 0、478 个模块，末次 **1.69 秒**，含最后的关联详情加载时序修改。编译不证明插件导入、配置依赖、运行准入或页面实际正确；没有把源码核对写成运行通过。

仍没有已登录开发面板的新信息，未重复询问或制造登录／业务数据。未做真实后端切换、容器、媒体、页面、键盘或群发送观察；未运行测试、夹具、断言探针、自动截图或故障注入。未动根运行配置、业务库、模型、人格、人工样例、Shadow 或名单，未提交／推送。没有生产启动，构建产物不入 Git。

完整目标继续 active。接续 P11 的剩余页面／加载／失败态与跨对象操作审查，核对上述改动的生命周期遗漏；P09 实际文件交付、A22—A27/A30、真实页面和发布停机／备份／升级前置仍未完成，不以本批编译替代整计划交付。

## 2026-09-20：持续实施 P10 账号面板、宿主与插件巡检

### 本批定位与修改

保留前批全部改动，先补齐 M18 稳定文档与下方记录，再读取账号 connector、平台动作、原审查、配置保存和插件宿主。点赞／收藏的顺序疑点已排除：两者原本声明为 proposal，主工作循环按 proposal_tool_names 顺序执行；没有重复增加 ordered 或改动作调用。固定登录、近期点赞、收藏夹状态及写入端点已与本地接口文档对照，保留 desired_state、当前 grant、工作修订、账号身份和未知不重放规则；没有调用平台或新建审查机制。

- **账号展示**：能力卡分开展示已保存 UID、读取开关、凭据存在性、每日上限、收藏夹和主体授予。只从原 platform_actions 取当前场景范围最近一条动作，连接原工作；不返回凭据、不查平台、不把每日上限叫剩余次数。工作页将确认记录与当前实时状态分开，补记录更新时间及缺目标状态说明。
- **配置交互**：原插件配置／启停在等待响应时可跨导航，随后用 selected/draft 更新另一个对象。现采用操作与选择世代，离页、换对象、返回后不采用旧响应；原服务器锁和凭据世代不变。配置／开放群／诊断标签原只写 URL，现同步实际标签。保存反馈与冲突移入当前详情弹窗；没有重读成功的同插件保存值时不更新基线，保留草稿。离页不等于服务器取消操作。
- **回执合同**：能力卡和可选 Core 的 after_delivery 改为读取完整原回执及 metadata，复用 receipt_delivery_status，避免模拟、未知、矛盾状态或缺消息身份被显示／回填为成功。Core 原逐帧 echo 与回传尝试不重放保持，不增加连接或发送入口。
- **兼容产物入口**：原面板只查 workspace，旧 python_workspace 虽能注册运行但其产物入口可能为空。现按原 run_python 注册所有者读同一实现；无注册入口时只接受一个明确保存的工作空间配置，沿其原 ID 数据目录和后端读取。多配置歧义明确失败，不移动目录、不改配置、不选择备用后端。浏览器／媒体自身仍引用 workspace 的情况单列后续，不能由本项宣称全部兼容。
- **链接媒体缓存**：原缓存字节读取失败被 except/pass 吞掉后重新下载。现同场景缓存不可读或类型不符返回 cached_media_unavailable，在该边界结束；正常无缓存下载保留原字节上限、格式验证和资产登记，没有新增恢复队列或刷新策略。

### M20／M21 逐项结论

下表只表示本批读取的源码入口及处置，不是启用清单、完整运行验证或全部潜在问题已经排除。未配置功能不得为验收而开启。

| 模块／实际 ID | 本批处置与已读边界 | 仍需现场／后续确认 |
|---|---|---|
| 插件宿主 catalog/base/api/models/hooks/host | 保留描述符、冻结调用上下文、角色／范围、同名拒绝、六阶段钩子和所属任务取消；只适配既有工作空间只读所有者 | 实际第三方插件、停用取消及不可确认执行的现场关系 |
| asoul_calendar | 保留 get_live_schedule/on_command：源失败和空日程分开，确定性卡片沿原提交；本批无需改代码 | 已配置来源、时区、字体与卡片真实发送 |
| asoul_dynamics | 保留指定成员／关键词／游标、源快照范围和渲染登记；本批无需改代码 | 当前源协议、新鲜度及实际卡片；源查询不是平台原文全文 |
| bilibili_live_sensor（目录 bilibili_live） | 保留真实新场次与订阅、表达后再次核场次、事实卡与原队列；本批无需改代码 | 同场次实际通知、睡眠延期及订阅撤销 |
| bilibili_content | 保留匿名与账号客户端分离、具体工作／账号动作合同；本批适配账号事实面板，不改平台请求 | 正常账号身份、动作回执、凭据并发编辑；A25 未验收 |
| link_parser | 保留 metadata 与明确下载分开；适配缓存失败不隐式重新下载 | 真实已保存媒体和下载格式；未做故障注入 |
| web_search_tool（目录 web_search） | 保留搜索节选、源无结果与获取失败、read_page 正文／PDF／图片分流；本批无需改代码 | 实际搜索和正文读取、资料依据与范围 |
| group_summary | 保留固定群／时段／截点、来源请求、专用工作和保存报告回读；本批无需改代码 | 批次复用、实际卡片长页和真实交付，报告不冒充原话 |
| workspace / python_workspace | 保留一个实现及两种已有配置 ID；修正产物面板只认新 ID 的缺口，冲突仍拒绝 | 原部署卷、实际产物／file_id；跨 ID 的浏览器／媒体依赖尚需继续核对 |
| browser_agent | 保留 P09 的顺序声明、工作归属、后端和采集覆盖；本批未新增执行变更 | 目标 Gateway 与兼容配置依赖，A24 未验收 |
| media_analysis | 保留已有工作片段／音轨／转写、预算和所属执行；本批无需扩展插件接口 | 已配置 Gateway、原始片段覆盖及兼容配置依赖，A24 未验收 |
| interest_share | 接续 M18 候选和逐群回执适配；公共研究不直接发群 | 实际已启用群的独立资格、语境与发送，A23 未验收 |
| gscore_adapter | 保留精确命令、逐帧 echo、整帧拒绝和持久尝试；补齐完整回执判定 | 实际 Core 版本／逐帧协议、真实消息 ID；A26 未验收 |
| local_plugins/local_clock | 阅读原公共接口样例，保留时区读取、精确命令与有限表达；未改人工样例 | 部署另配外部目录尚未清点或启用，A27 未验收 |

### 编译与未确认

末次四个 Python 文件（web/capability_status、plugins/host、gscore_adapter/plugin_core、link_parser/plugin）以 `uv --cache-dir /private/tmp/lenbot-uv-cache run --no-sync python -m compileall -q` 编译退出 0。前端本批三次 `npm run build` 均退出 0、478 个模块，末次 **1.56 秒**，包含最后的回执身份显示修改。编译只确认语法与打包，不能证明 API、配置竞态、插件加载或页面正确。

没有新的已登录开发面板信息；没有页面、键盘、窄屏、实际账号／Core／下载／文件交付观察。未运行测试、夹具、断言探针、自动截图或故障注入；未启动生产、模型、Gateway、平台调用或群发送，未改运行配置、业务库、人格、人工样例、Shadow 或名单；未提交、未推送。各批改动保留，构建产物不入 Git。

完整目标仍 active。接续核对 workspace 兼容 ID 与 browser/media 的实际后端选择，避免能力卡说明与执行依赖不一致；同时收口 P01/P02 非消息子页返回及 P11 全路径。P09 实际交付、A22—A27/A30 和发布停机／备份／升级前置均未完成，不能以本表或编译替代。

## 2026-09-20：持续实施 P10 公共兴趣选取与发布展示

### 定位与源码

接续已有源码改动，重新阅读 `list_public` 的三类生产调用（心跳种子、资料读取、兴趣分享）、原发送事件、查询和前端详情，补齐稳定文档。原候选先截取 20 项再核匿名来源／群主题／研究意图，可能让前排不合格项长期挡住后面的可用项；现 SQL 先筛主题和用途，按原顺序读取轻量身份，来源核对、回读同修订及有效期之后才计入 limit。来源证明规则与发布前 grant／额度／睡眠复核未放宽，没有新表、缓存或模型调用。

近期表达材料现只采用当前 Bot、同群、原截点内可靠的 live 送达记录，排除模拟、未知和缺身份；最多四条、每条前 300 字符，provenance 只列最终采用项。原“读取四条但覆盖称八条”的文案已对齐。公共兴趣详情从原事件按兴趣／场景／行动列最近 50 个发布行动及回执，不返回群表达正文；只有尝试时保留尚无可靠终态，不写成失败或完成。前端区分已采用与候选、来源证明与事实正确、published_at 与群送达，复用资料覆盖，并保留刷新失败的旧采样和来源返回位置。

### 编译与未确认

此前本小批 `memory/interests.py`、`plugins/builtin/interest_share/plugin.py`、`web/query_service.py` 以 `uv --cache-dir /private/tmp/lenbot-uv-cache run --no-sync python -m compileall -q` 编译退出 0；前端 `npm run build` 退出 0、478 个模块、1.52 秒。本次续接只核对这批源码和补文档，未把旧编译称作新的运行观察。

未运行测试、探针、截图、模型、公共来源读取或逐群发送；没有新的已登录面板信息，A23/A30 尚未人工验收。未改运行配置、业务库、人格、样例或名单，未提交／推送。完整目标仍 active，下一步继续 M19 账号与敏感动作、M20 宿主及 M21 全部插件的逐项结论，P09 实际交付和 P11 发布前置仍待完成。

## 2026-09-20：持续实施 P10 提醒、等待与延期交付

### 定位与源码

前一目标轮已修改 P10 源码与稳定文档，本轮重新读取工作树，补齐中断时未完成的前端模板并编译；不是无进展等待。全部既有未提交修改保留，没有启动服务或重新归档旧计划。

原行动依赖、提醒完成和延期恢复主要按事件类型判送达，可能把模拟或缺平台身份的回执算成真实成功。现通过 receipt_delivery_status 区分 sent/not_sent/rejected/unknown/Shadow/模拟，消息需对应 message_id，文件需对应 file_id，矛盾与未知标记不算成功。适配器称 sent 却缺 ID 时，在原一次尝试后记 unknown，不补 ID、不重试。提醒与延期继续同事务结束，模拟复用原 shadow_observed 状态但单列 delivery_status=simulated；没有新增任务状态表，也未批量回写历史事件或旧状态。

真实等待只在 live 消息的可靠回执事务建立，TTL 仍从送达时刻开始；模拟和不完整回执不更新实际 Bot 发言或确认提问。叫醒 Gate 回读原 prompt_event_id，核对同一请求、真实送达和其后人类答复，不只相信旧 Session 的提示字段。原 Actor 对等待来源的场景／人物核对保留，未增加模型调用或自动发送。

原 Scheduler 先从堆组装 TASK_DUE，再等待写锁认领，期间任务更新仍可能携带旧正文／时间。现保存当前 TaskItem 快照，在原数据库认领事务里重新读取并比较；变化则不提交旧事件，后续仍用原定期同步。未增加版本列、指纹或新的补发队列。心跳和兴趣专属完成入口已阅读，仍直接走原所属调度，不受提醒提案修改路径替代。

deferred_delivery 原混在提醒列表并出现编辑／立即触发按钮。现增加既有任务分类的“延期交付”只读标签，显示原时间、阶段、行动、请求者、回应对象与来源工作／提醒／资产；原提醒 API 与提交事务均拒绝改写延期或系统槽。普通提醒控制保持原确认和事务；跨对象、离开再返回及卸载后不采用旧操作响应。周期 completed 显示“本槽已结束”，模拟显示“模拟回执／观察”，不称实群兑现。事件和关联面板也采用同一回执判定。架构、上下文、运行手册与插件合同已同步。

### 编译与仍待确认

本批 actions/models、actions/delivery_store、actions/queue、events/store、runtime/attention、runtime/gate、scenes/reducer、scheduler/engine、web/query_service、web/routes/cockpit 十个 Python 文件两次以 `uv --cache-dir /private/tmp/lenbot-uv-cache run --no-sync python -m compileall -q` 编译，均退出 0；末次含面板统一回执投影。前端两次 `npm run build` 均退出 0、478 个模块，末次 1.53 秒。Git 状态未列出 dist。只完成源码核对和编译，不证明运行中的认领竞态、事务或页面正确。

没有新的已登录开发面板信息；未实际操作延期页、叫醒、提醒、播放或群发送，A22/A30 不记通过。未运行测试、夹具、断言探针、自动截图或故障注入；未改运行配置、业务库、模型／人格／人工样例、Shadow 或名单，未提交和推送。P09 的实际资料整理、文件上传及目标部署前置仍未完成。

完整目标仍 active。接续核对 P10 公共研究／兴趣来源与逐群发布，再处理账号、可选 Core 和全部插件的保留／适配结论；P01/P02/P11 页面收口及全部正常人工验收仍保留，不能以本批回执判定代替整体验收。

## 2026-09-20：持续实施 P09 执行输入与产物身份

### 定位与源码

中断前的目标轮通过阅读取得了改变下一步的具体证据，属于定位进展，没有尚在等待的运行句柄。本批续接后重新读取相应服务、输入清单、查询、工作页和当前工作树再修改；保留所有前批改动，HEAD 仍为 `6b115aa`。

原 Gateway 工作目录在同一工作全部执行中取最后一项，后来的 browser/media 可能取代 Python 产物身份；历史入口也没有单独核 worker 类型或确认状态。现只选择最新 Python 执行，仍占用或停止未知时不退回旧快照；指定历史身份必须属于同工作、已确认的 Python 执行。宿主工具原会忽略 execution_id，现与面板一样明确拒绝，不静默读取当前目录。以上为静态源码缺口判断，未做混合执行或故障注入复现。

原清单读取把所有 GatewayRefused 都当作“无此执行”，现仅 404 保留该含义，其他拒绝保留原失败。宿主枚举先核已有终止未确认标记，不能在 stat_file 逐项跳过后伪报空目录；运行结果与清单错误分开保存，原 stdout/stderr/termination 不被枚举失败覆盖。Gateway 运行等待到期或返回终止未确认时也不再把可查询的空清单标成已确认产物，保留 artifacts_known=false。宿主恰好达到文件数上限不再误标超出。原目录、文本、下载路由把不可读／未确认状态作为可见错误，不新增重试或回退。

新导出的 manifest 保留 job_revision、文本 source_truncated 与 provenance，仍只导出当前工作登记的资料／获准图片，原 8 项、24 MB、只读控制目录和启动前准入不变。新增明确类型只投影保存清单的来源字段，缺失的旧版本不从当前工作补填，不复制新表或内容指纹。

工作记录页新增按需展开的只读执行详情，使用原 scene/job/execution 身份读取宿主请求及事件，不查询 Gateway 或启动执行。显示原输入来源、字节／范围、失败、停止说明和事件截点；明确清单不证明容器已收到或程序实际读过。脚本、输入正文和媒体字节不返回；格式异常与历史未记录、明确空输入分开，诊断沿已有脱敏函数，原数据库记录不改写。切换工作或执行后不采用旧响应，同对象刷新失败保留带时间的上次内容。

产物页分开 Gateway 的已确认 Python 快照与宿主持续目录，显示原版本及采样时间；Gateway 续读仍绑定同一 execution_id，宿主无不可变身份的文本逐页显示而不跨次拼接。保持原归属、权限、执行后端、取消、真实送达与文件资产边界，没有执行入口或新的运维授权。架构、上下文、运行手册与插件合同已同步。

卡片路径已阅读 HtmlCardRenderer、日程命令及群报告成品链：现有确定性渲染、媒体登记、submit_message 或 PreparedWorkDelivery 与真实回执分开，本批没有替换主题、渲染器或既有图片布局分支，也没有增加模型调用。此结论仅覆盖所读路径；字体、长页、远端附图和实际交付仍未核对，不据此声称 M15 全面验收。

### 编译与后续范围

本批 `execution/inputs.py`、`execution/service.py`、`web/query_service.py`、`web/routes/cockpit.py` 四个文件两次使用 `uv --cache-dir /private/tmp/lenbot-uv-cache run --no-sync python -m compileall -q` 编译，均退出 0；末次含 Gateway 未确认产物的最终修改。前端 `npm run build` 退出 0、478 个模块、1.55 秒，含执行详情与快照／宿主目录区分。Git 状态未列出 dist；编译不等于 API、实际页面、容器或文件交付通过。

没有已登录开发面板的新信息，没有实际展开记录、键盘、窄屏或容器调用观察。未运行测试、夹具、断言探针、自动截图或故障注入；未启动生产、Gateway、真实模型、平台调用或群发送，未改运行配置、业务库、人格、人工样例和名单，未提交或推送。

P09 的真实资料整理→计算→文件→上传／回执、目标部署及媒体／卡片效果仍待获准正常操作，A19—A21/A24/A30 不记通过。下一步推进 P10 的调度／等待／睡眠、兴趣、账号与可选 Core／插件巡检，同时保留上述 P09 运行前置和 P01/P02/P11 页面收口；完整目标仍 active，不能以本批源码和构建替代整计划完成。

## 2026-09-20：持续实施 P09 浏览器与媒体覆盖

### 定位与源码

前一目标轮有文件资产源码进展，本批续接时重新读取工程约束、当前工作树、P09/M13—M15 合同及半成品；HEAD 仍为 `6b115aa`，保留全部既有未提交修改。媒体页曾处于脚本已更新、模板仍引用旧 broken/markBroken 状态，现已补齐模板，不把中断前的计划当成已实施。

静态阅读发现工作附件可能把 WAV 音轨当图片解码，或因 segment 前缀先被判为缺视觉能力。现由原媒体准备器按场景登记 MIME 分开图片与音视频，对话也复用该入口；音视频只保留引用、来源和未听／未看连续画面的说明，不占图片数。旧图片首次读取前无 MIME，仍沿原验证入口。工作清单采用已有 image_manifest 对象格式；恢复时只更新实际图片引用，避免把同清单音轨改成“图片像素未装入”。原图片字节／张数限制、完整交换、实际阅读资格与归档路径保留，未新增缓存层或媒体状态表。

四个浏览器工具原虽有锁，但未向循环声明同响应内的业务顺序，现沿已有 ordered 标记串行。正文观察使用实际快照 URL，源采集截断与宿主仅保存部分 DOM 页都标 partial；宿主源端下一页和 Gateway 已保存 R 的本地续读分开。截图记录请求核对版本而非补造实际像素版本。原 page_ref、逐命令身份、修订校验、未知不重放、准入与部署边界未改，未执行浏览器或截图。

只读媒体文件接口对登记音视频改用原文件读取器，不再套图片字节上限；本地不可读取与来源 HTTP 错误明确展示为媒体错误，不猜失效原因。资料查询从完整已保存正文投影 DOM 版本／截断、实际视频采样帧／音轨和 ASR 区间／分段，未知旧格式明确无法投影。新增覆盖组件与媒体链接，不从当前 JSON 分页残片推测覆盖，不授予模型新阅读范围。

媒体页与消息／引用预览按类型展示；音视频只有打开详情后才可手动播放，不自动播放、不转码。固定目录独立加载、独立错误和采样，数量／上传字节限额取运行配置，不再写死 20 项或 10 MB。详情刷新失败保留旧内容，跨对象保存、上传迟到响应不写进新对象；同页链接和关闭详情保留原列表范围。非运营媒体只读，运营上传仍只接收原图片格式，离开页面不被说成服务器取消。架构、上下文、运行手册与插件合同已同步。

### 编译与仍待确认

本批 `media/service.py`、`cognition/context.py`、`runtime/job_runner.py`、`runtime/work_context.py`、`plugins/builtin/browser_agent/plugin_core.py`、`web/query_service.py`、`web/routes/media.py` 七个文件以 `uv --cache-dir /private/tmp/lenbot-uv-cache run --no-sync python -m compileall -q` 编译，退出 0。前端两次 `npm run build` 均退出 0、476 个模块，末次 1.47 秒，含最终覆盖文案。Git 状态未列出 dist 产物；这些只证明编译，不证明浏览器、媒体或页面行为已验收。

尚未取得前批询问的已登录开发面板，没有实际页面、播放、窄屏、键盘或正常媒体工具调用观察。未运行测试、夹具、断言式探针、自动截图、模拟业务或故障注入；未启动生产、真实模型、Gateway 执行、平台调用或群发送，未改运行配置、业务库、人格、人工样例与名单，未提交、未推送。

完整目标仍 active。P09 的 A19—A21 资料整理到真实文件回执、A24 浏览器／媒体实际读取及 A30 页面核对仍未完成；M13 执行输入／部署与 M15 卡片路径尚需继续核对。后续继续这些真实路径，再推进 P10 调度／兴趣／账号／Core／插件巡检和 P11 全路径收口，不以本批媒体展示替代整计划验收。

## 2026-09-20：持续实施 P09 文件资产与上传事实

### 定位与源码

- 前批 P08 是实际进展。本批重新读取工程约束、工作树与 P09/M13—M16 合同，保留所有已修改内容；没有把编译或文档更新当成整计划完成。
- 沿文件登记→对话候选→提交→尝试→回执阅读发现：原资产只要存在任何旧回执，就可能不再报告另一条无回执尝试的未知；原 `FILE_UPLOADED` 类型也未排除模拟或核平台 file_id。现按同一 action_id 关联持久提交、尝试和最新回执，未关联身份单列，真实成功与另一未知可并存。这里是源码缺口判断，未做故障注入或运行复现。
- `for_job` 复用原事件及审查 Trace，投影资产来源、当前修订、采样时间、上传提交／尝试／回执和逐行动状态；不新建表或队列。模型候选与面板共享状态，当前交付目录排除旧修订、未关联上传审查、过期、已上传、已提交未尝试和未知项；模拟、Shadow 与真实上传分开，平台文件 ID 不以消息 ID 代替。
- 原 relations 将所有带 action_id 的事件都追加为 receipt，含尝试与动作审查。现只把明确消息／文件终态及 Shadow 记为回执，尝试单列；文件行动保留 asset/file 身份，成功缺 file_id 时不显示真实上传。没有改写旧事件或替换原发送事务，原始错误仍可回查。
- 原文件能力投影把 worker 也标成可登记持久资产，且缺申请者时可能称可上传。现按所选场景保存的工作空间配置分开生成和 Gateway 登记条件，运行时协议／开关与申请者授予分列；未选人保留未核对。多请求者对话不再任意选一人的授予代表全部工作。没有启动健康探测或自动切换后端。
- 资产登记在等待已有互斥锁后重新核当前插件与原工作准入，已有资产分支也不能沿等锁前的旧资格返回。原下载检查、格式边界、动作审查、同群额度、单次尝试、未知不重放和显式协议均保留。
- 新 FileAssetsPanel 放在工作资料页，区分登记、已提交无尝试、未上传／拒绝、未知、真实 file_id、模拟和 Shadow；展开可回到原审查、提交、尝试、回执及执行／产物身份。消息与运行记录的文件链接直达同工作指定资产，返回路径保留该定位；原先挤在单段中的资产、审查与上传信息已分开，下载不宣称上传。架构、上下文、运行手册与插件说明已同步。

### 编译与剩余范围

- 本批 media/files、cognition/context、cognition/proposals、web/query_service、web/capability_status 五个 Python 文件两次以 `uv --cache-dir /private/tmp/lenbot-uv-cache run --no-sync python -m compileall -q` 编译，均退出 0；末次包含无行动身份的尝试和配置来源说明。
- 前端两次 `npm run build` 均退出 0、472 个模块，末次 1.57 秒；含文件资产聚焦、消息入口、能力卡与单层返回保持。Git 状态未列出 dist 产物。没有将上述编译写成运行成功。
- 尚未取得前批询问的已登录开发面板，没有实际页面、键盘、窄屏、文件下载或上传观察；未运行测试、夹具、断言式探针、自动截图、模拟业务或故障注入。未发起真实模型、Gateway 执行、平台调用或群发送，没有改运行配置、业务库、人工资料与名单，未提交、未推送。
- P09 仍不等于完成：A19—A21 的获准资料处理、小文件真实上传／回执尚缺；M13 的实际部署与输入链、M14 浏览器及 M15 媒体／卡片分支仍需继续核对，不能用本批文件状态卡代替整体验收。后续继续这些真实路径，再推进 P10/P11；完整目标保持 active。

## 2026-09-20：持续实施 P08 工作连续性与方法来源

### 源码进展

- 前批为实际进展，不是等待或无进展。本批保留全部已有改动，并已修改 P08 源码；过程中中断后先读取工作树，确认已应用内容再继续，没有重复归档旧计划或回退前批修改。
- 原 SkillCandidate 强制 result_ids 非空，与“工具观察或明确纠正”的合同矛盾。现允许仅纠正候选，两种来源不能同时为空；纠正核工作来源、事件身份、Bot 与模拟标记。资料沿原 observation_reads 和区间核对确认实际提供；方法正文、认识／工作投影、历史定位、Bot 发言和模拟内容不独立支持学习。仍由原一次维护判断方法语义，不把来源存在当作纠正必然正确。
- 提交候选、维护调用前与保存方法时复用同一来源检查，保存仍在原事务核候选状态、工作修订、技能版本及所有权。新方法 source_json 记录保存时的工作阅读范围；旧已保存版本不回填。旧待处理候选缺来源或实际提供事实会明确失败，不能补造旧阅读或另购调用。
- 原方法页无法从候选定位实际保存版本，工作页又把 skill_versions 固定记录称作“实际使用”。查询层现按候选／工作／修订／来源场景找精确版本；按同一工作取得的成功 read_skill 记录和实际范围分开固定、保存与提供。候选与方法详情可以回到来源工作、纠正原话、资料范围，仍不称已学会或已正确复用。
- 预算页原以当前默认上限作旧工作用量的分母。现以原冻结字段为主，缺失与明确 null 分开，活动时长与绝对期限分开；当前默认值仅折叠参考。原 model usage API 增可选 job_id 筛选，汇总全部目标版本的原调用账并分页，而不是关系页最多 50 条。输入／输出与缓存／推理子项不重复相加，未完整 usage 保留数量与未知说明。
- 公共研究按已有 has_public_context 身份投影“无需群交付”，不改持久任务状态；工作、群内工作及关联详情均采用该事实，不从系统场景名猜测送达。执行完成和随后各群发布继续分开，未为补发重跑工作。
- 工作附件及正文读取增加当前工作／修订和请求身份核对；切换对象、修订、卸载或执行快照变化后不采用旧响应。续页绑定同一 execution_id，响应快照变化时保留错误而不拼接。首屏读取失败也有可见入口。方法详情关闭保留原列表范围，刷新失败保留带时间的旧内容；旧公开／工作控制请求不把结果写到后来打开的对象。
- 已核对工作恢复、检查点、完整原生交换、单层委派与原预算路径，未接外部黑箱循环、增加审查调用、权限层或表。维护时限疑点未证实：当前 RuntimeConfig 与 WorkBudgetSnapshot 的 job_max_seconds 都是必填正数，不允许 null；没有据不可达条件改动时限逻辑。

### 编译与未确认

- 本批七个 Python 文件两次使用 `uv --cache-dir /private/tmp/lenbot-uv-cache run --no-sync python -m compileall -q` 编译，均退出 0：cognition/jobs、skills/store、skills/learning、runtime/job_store、runtime/job_runner、web/query_service、web/routes/models。末次包含工作输入合同和预算说明的最终修改。
- 前端两次 `npm run build` 均退出 0、470 个模块，末次 1.65 秒；末次含完整方法页、公共研究状态、预算账分页、附件快照核对。Git 状态未列出 dist 产物。这些只证明编译，不是事务、业务或页面验收。
- 中断续接时一次只读进程枚举返回 `zsh:1: operation not permitted: ps`；未据此认定进程结束或重启任何进程。本批后来启动的上述编译命令均已明确返回退出码。
- 仍未取得前批所询问的已登录开发面板，未做实际页面观察或以假登录／假数据代替。候选仅纠正路径、后续方法复用、修订中的迟到响应、全工作调用账、宽窄屏和键盘路径仍待正常人工确认；A15—A18/A29/A30 未记通过。
- 未运行测试、夹具、断言式探针、自动截图、真实模型、工具或群发送；未动运行配置、业务库、人格、样例与名单，未提交或推送。原 P01/P02 返回位置和前批真实业务验收缺口仍在；下一批继续 P09 的执行产物→文件资产→平台真实回执路径，并衔接 P10/P11。完整目标保持 active，P08 不以编译结果记为验收完成。

## 2026-09-20：持续实施 P07 认识修订、历史与索引

### 定位与源码

- 静态阅读发现：词面候选可能等待嵌入/重排后仍沿旧对象返回；即使语义候选有状态过滤，也没有覆盖这份旧词面对象。现对排序后的候选按原场景、主体、类别、时间及有效状态再次读原表，只采用相同修订；变化项移出并保留 partial，不报成查询不存在，也不自动追加调用。这是源码缺口判断，未做运行竞态复现。
- 保存的认识查询在新展示及后续字符/记录回读前核对原表修订和查询后到期；变化后返回 `stale_memory_result`，不换正文、身份或坐标，不自动重查。查询时已过期的历史记录仍可读取，对话投影标明有效性；旧资料在面板保留。原认识事务与 knowledge_revision 仍负责提交边界。
- `memory_retrieval` 使用原 Trace 分开记录候选/修订、返回项、被排除项和语义/重排是否实际采用；不把返回项写成已展示。认识查询定义与输入合同不再暗示语义检索必定开启，旧工具记录不能覆盖当前纠正。
- 索引覆盖原本使用 SQL LIKE 匹配未固定维数的绑定前缀；改为字面前缀，避免标识中的 `_`/`%` 使不同绑定被计入覆盖。没有新指纹、散列、表或自动重建。
- 认识列表与群内记忆默认查询当前有效且未到期项，原 API 的 status 语义不变，旧 status=active 深链仍含原状态有效的到期项；新增可选 validity 由入口明确解析。详情从一次链读取取得选中项和关联版本，显示当前有效链、创建/修订提交，保留原列表范围与筛选。
- 原列表与索引共用 Promise.all，任一索引失败会阻止正常列表采用。现拆开索引卡，独立加载/错误/采样，未知覆盖不补零；先看认识，再看场景整体覆盖。重建目标取已筛选场景而非尚未提交的表单，原确认入口保留；缺绑定/客户端不能返回成功。显式重建结果写原 Trace，历史错误和当前覆盖分开。
- 历史页分开摘要范围、同次维护回执采用的认识操作和冲突候选；当时修订不冒充当前有效状态，旧字段缺失不伪造为全部采用。相关行为同步上下文、架构与运行手册。

### 编译与仍待确认

- 本批 context、retrieval、memory/index、query_service 与 cockpit 五个 Python 文件，以 `uv --cache-dir /private/tmp/lenbot-uv-cache run --no-sync python -m compileall -q` 两次编译均退出 0，末次包含查询定义与提示合同的最终修改。前端两次 `npm run build` 均退出 0、470 个模块；末次包含认识链内的筛选/返回保持，耗时 1.54 秒。均仅为编译证据。
- 没有新增实际页面观察；前批所询问的已登录开发面板仍未提供。认识筛选、修订链、索引独立失败、历史采用清单和窄屏/键盘路径尚未人工验收；没有代造数据或假登录。
- 未运行测试、夹具、断言式探针、自动截图、实际嵌入/重排、真实群聊或重建索引；未动运行配置、业务库、人工资料与名单，未提交或推送。Git 状态未列入 dist 产物。
- P06/P07 均不能以源码完成宣称 A05—A15 通过。下一批继续 P08 的工作目标/累计账/结果状态/方法版本路径；P01/P02 的非消息子页返回及所有实际页面/业务验收仍待收口，完整目标 active。

## 2026-09-20：持续实施 P06 当前互动与行动合同

### 本批源码

- 沿现有对话循环补足输入，不增加规划器、前置调用或持久状态。`input_status` 每项显示说话人、事件类型、本轮来源和完整读取；只是来源定位，未复制原话到运行指令或授予新阅读资格。
- `runtime_facts.participation` 从已有 Session 快照投影相关人物的未过期关注及观察期限，带 session_version；接收新输入和成功 checkpoint 后沿原刷新路径替换。不是全群实时状态，也不证明请求、权限或发言义务。
- 原等待目录增加真实问话位置与创建/到期时间，不改其过期、回应或恢复规则；Bot 问话定位不升级为人类证据。新增投影仍参与最终容量核对。
- 固定合同分组明确直接回应、短查询、范围回忆、计算、必要澄清、长工作与自然结束。强调对象/时间/版本/单位、实际正文/像素与资料缺口；纠正先调整当轮，再按原 refute/supersede 修改持久认识，不以新反向认识覆盖旧有效记录。
- 人格字段未修改；人格段与运行合同分别拼接，插件 include_identity=false 直接省略前者，不再按中文提示词位置截字符串。原工具顺序、消息/文件关系、预算、Actor/Gate 和真实回执边界保留。

### 编译与未确认

- 使用 `uv --cache-dir /private/tmp/lenbot-uv-cache run --no-sync python -m compileall -q src/len_bot/cognition/context.py` 编译，退出 0；这只证明语法编译，不证明群聊效果或缓存收益。
- 本批未新增前端改动，没有重复前批构建。没有运行测试、夹具、断言探针、自动截图、真实模型或消息调用；未动运行配置、业务库、人工人格/样例与名单，未提交或推送。
- A05—A13 的真实接话、潜水、指定照发、按需查证、回忆与纠正后采用仍待正常人工观察；P06 不记为验收完成。继续核对 P07 的有效版本、历史/索引展示及纠正闭合，完整目标仍 active。

## 2026-09-20：持续实施 P05 消息阶段与依据回读

### 本批源码

- 前一目标轮完成 P01/P02 与单条依据卡片的实际源码修改，本批继续推进，不因页面尚不可核对而重复状态说明或标记整目标完成。保留所有前批未提交改动。
- SceneInspector 与 Activity 消息详情接入六阶段简表；完整身份、全部同轮关联、调用和原始字段折叠。只有人类输入及明确行动回执采用消息阶段，系统提交不伪装成新来消息。原有焦点返回保留，窄屏焦点约束补入折叠摘要等控件；实际键盘体验仍未核对。
- 阶段严格按来源、处理清单、工作请求、行动与提交身份收窄；回执不统计同源的其他行动。工作当前状态与答复当时版本分开；旁听、未记录、未完成、发布中断、送达未知、模拟与 Shadow 分开。已入队行动不因同批别的行动出错而被说成失败。明确关联的发送失败/未知回执显示已保存原因，历史失败不覆盖后续回执。
- 静态阅读发现原 relations 主要经提交找对话轮次，提交前失败可能无入口。现沿已存来源/实际阅读身份找到相应 Trace 与调用账，保留场景和数量限制；错误单列为“关联轮次问题”，不猜属于哪条具体回答或是否已恢复。旧 Trace/回执缺少字段时不再擦掉同 action 已从提交取得的明确身份。
- ProvidedReads 接入提交与读取详情，区分旧字段缺失和本次空范围，不把整轮读取等同每条答复依据。
- 单条依据、工作结论/进度/已提供范围均能带原坐标进入资料页。原只读 tool-results 入口增加 end/coordinate_unit；字符与 JSON 记录分别分页，明确的 pending 目录使用原 items。越界、格式不适用、单条超页量均失败，不自动换参。前端分页面保存原坐标，读取失败保留已取页及时间；没有新表、配置源、模型阅读或外部调用。
- 同步架构与运行手册；修正运行手册仍称“双设置入口、工作台未实施”的旧句。计划验收没有勾为通过。

### 编译、失败与未确认

- 首次接入构建在 ProvidedReads 发现缺失括号，失败原文：`[vue/compiler-sfc] Unexpected token, expected "," (8:0)`。已修正该组件并重新构建成功，不绕过编译或修改检查。
- 首次失败修正后，前端三次 `npm run build` 均退出 0、468 个模块；最后一次为 1.43 秒，包含工作范围链接及发送错误原文展示的末轮修改。后端 query_service 与 cockpit 路由在末轮修改后再次用 `uv --cache-dir /private/tmp/lenbot-uv-cache run --no-sync python -m compileall -q` 编译，退出 0。均仅为编译证据；Git 状态未列入 dist 产物。
- 尚未得到前批所询问的已登录开发面板地址；本批没有新页面观察，没有启动生产或代造业务数据。六阶段的真实记录、提交前失败关联、记录/字符范围、窄屏和键盘路径都未做正常人工验收，P05 尚不能记为完成。
- 未运行测试、夹具、断言式探针、自动截图、真实模型调用或消息发送；未动运行配置、业务库或人工资料；未提交、未推送。构建产物不入 Git。
- 后续继续 P06 的当前对象/查证/回忆/自然结束输入合同，并按计划衔接 P07—P11；P01/P02 非消息子页返回、页面验收及全部真实链路仍保留待办。完整目标 active。

## 2026-09-20：持续实施 P01 / P02 群工作台

以下保留前批结束时的状态；阶段与依据回读进展以上方批次为准。

### 本批源码

- 保留前批全部文档和 P03/P04 源码。本批有实际源码进展，不把上轮中断或尚无页面地址记成完成；完整目标继续。
- 一级导航收敛为概览及四个日常区域；群目录统一到 `/scenes`，旧 `/groups` 两类路由只做兼容重定向。移除重复 GroupsView 及其批量即时模式按钮，保留搜索、已保存权限筛选和唯一群设置表单。
- 群工作台主标签为对话、本群记忆、工作、设置；旧注意力/摘要/参与者/偏好/发送标签收进菜单但继续可定位。群头分开显示已保存设置、运行时样本、最近实际发言与未完结工作；不把插件开关或未完结数量标成执行成功/正在执行。
- 群内认识、工作、资料、能力和原始轨迹链接通过单层应用内返回路径保留来源；各相关列表筛选继续携带来源。当前页内存保留最近八个离开位置的消息窗口、原分页截点、采样时间及事件锚点；登出清空，不持久保存聊天正文。硬刷新或淘汰后的全部窗口无法据此恢复，非消息子页完整滚动尚未收口。
- 本群设置保留原 baseline、冲突和未保存离开提示，主动作改为“保存本群设置”。新增折叠的只读能力事实，使用已有 capability_status，不新增 API、权限、配置源或后台动作。
- 阅读组件实现发现标签组默认 `mandatory='force'` 会在诊断深链没有主标签命中时选择首项，已显式改为非强制首项的 `mandatory=true`，避免把旧诊断链接带回对话。这是源码判断，未称为实机复现。

### P05 单条依据展示起步

- 添加单条答复依据组件并接到 SceneInspector、工作详情的行动列表和运行记录的行动列表。类别、未核实缺口、已读原话、资料区间与坐标单位分别显示；工作依据保留当时的修订、完整/部分状态和来源，不把当前工作的新版本替换成当时采用版本。
- 缺字段保留“此表达未记录”，不从请求来源、全部工具记录或工作摘要填造依据。资料链接仍使用原资料读取入口；记录坐标未转换为字符偏移，暂不声称点击即可跳到每个引用区间。
- “触发→实际阅读→处理→资料/工作→提交→发送”的简明阶段组件和原始详情折叠仍未完成，P05 不能以新增卡片算整批完成。没有单条真实新依据记录的页面验收。

### 编译与现场范围

- 已在 `src/len_bot/web/frontend` 执行 `npm run build`；首轮 461 个模块构建通过，完成后续修正与依据卡片后再次构建，463 个模块、退出码 0。只证明本批前端编译，不是页面运行验收。
- 本机 `11307`、`5183` 没有查到监听；浏览器工具返回的内置浏览器无已打开页。已询问可用开发面板地址，尚未取得。没有用假登录、模拟数据、启动生产或代理改向来代替实际页面验收。
- 当前未取得改动后的已登录页面、窄屏/键盘、未保存提示、配置保存及跨对象返回的正常人工观察，A01/A02/A03/A04/A30 仍未通过；不能把构建结果当作 P01/P02 结束。
- 没有运行测试、夹具、断言探针、自动截图、真实模型调用或群发送，没有修改运行配置/业务库。构建产物没有纳入 Git；未提交、未推送。

## 2026-09-20：持续实施 P00 / P03 / P04

以下保留该源码批次结束时的状态；后续前端实施以上方批次为准。

### 已落地源码

- 基线仍为 `6b115aa`，保留前批全部文档改动。P00 已核对输入、提案、工具发现、工作压缩、终结执行和最终容量路径；本批没有重新取得现场页面/真实轮次证据，P00 不记为全量验收完成。
- P03：核心提案工具按固定顺序提供；`respond` 的普通消息/文件形状、关系互斥及引用字段固定，当前目标与回执不再写入 Schema 枚举。无合法工作、提醒、认识、等待或交付目标仍在暂存/终结及原事务处拒绝。
- 对话上下文存在时固定公开 pending/range 读取定义；目录本身不授予原话已读资格。插件角色、能力、渐进发现、显式工具子集与 hook 边界保留。
- 对话、工作、维护均按完整当前目录估算。终结步骤改为保留目录、原定向选择及执行前拒绝非终结调用；压缩后重读账户，并明确同步独立装配请求中的预算与终结提示，不再靠末条消息的位置取预算。历史维护的容量核对移到最终装配阶段，使用更新后的实际请求。
- 本批没有新增表、配置、运行策略或协议回退，没有改变 Actor/Gate、权限、累计预算及真实送达条件。

### P04 答复依据数据链

- 新增小型可选 `TurnMessage.answer_basis` / `AnswerBasis`，沿原 MessageProposal → Actor/Gate 事务 → 提交 JSON → ActionItem → 回执 → relations 查询贯通。业务缺口是单条答复此前无法指向实际已读资料；没有新证据表、真假评分或额外审查调用。
- 人类原话解析为同群、截点内、完整已读的 event_id；工具资料采用非空 ResultSpan，校对实际展示区间和坐标。内部群史/认识/工作目录不能绕过对应来源合同。普通 source 不自动视为依据；general/省略字段也不能证明回答无幻觉。
- 工作依据保留原 job/revision 和结果来源快照，不把工作总结冒充原始证据；紧凑目录不计结果已读。原循环确认返回后才累计工具/工作阅读，Mailbox 的窄投影供原事务核对并随原提交 JSON 持久保存。没有重置预算、重读全部工作材料或伪造来源。
- 新记录可查询依据、缺口、阅读范围及工作版本；旧记录不回填。前端的简明卡片/阶段展示尚未做，查询数据存在不等于 P05 完成。OneBot 仍只编码原消息片段与文件参数。
- 无需数据结构转换；新可选 JSON 字段被旧严格模型拒绝的回退限制已补入运行手册，未动现有数据。

### 实际核对与缺口

- 阅读了相关源码和本轮差异。使用 `uv --cache-dir /private/tmp/lenbot-uv-cache run --no-sync python -m compileall -q` 编译 proposals、agent_loop、social_core、retrieval、job_runner、reflector 六个修改文件，退出码 0。这仅证明 Python 编译通过。
- P04 修改后使用同一 uv 命令编译整个 `src/len_bot`，退出码 0；没有导入运行、真实数据库事务或正常群答复验收。源码阅读核对了嵌套表达共享范围、工作目录/详情区别、OneBot 编码和 relations 原身份链路。
- 未发起新的模型/工具/平台调用；完整目录下的真实终结、各提供商对固定形状的接受情况、缓存和费用变化仍未确认。不能把本批记为 P03 或 A28 的运行验收通过。
- 没有启动服务、修改实际配置/数据库/人格/名单或发送消息；没有运行测试、夹具、探针、截图、压力任务或模型评审。未提交、未推送。
- P03/P04 仅记录源码与编译，A09—A11/A28 的真实链路待获准正常业务确认。下一实现路径为 P01/P02 的唯一导航与群工作台，再衔接 P05 的单条依据/阶段展示；不以后台字段替代用户可见体验。

## 2026-09-20：总计划替换与文档收敛

以下保留该文档批次结束时的状态；其后源码实施以上方批次为准。

### 范围与状态

- 审阅基线为 `6b115aa`；本轮只处理文档，不实施计划中的业务代码。
- 用户已解除旧社会能力总计划的保护，授权与旧文件交付/群配置计划一起移入系统回收站。
- 新总计划覆盖 U01—U10 产品目标、B01—B12 边界、M01—M24 模块、P00—P11 批次、A01—A30 正常人工验收，包含数据/API 兼容、发布与回退。
- 本轮开始前未提交的两节架构与群聊体验评估已被新总计划吸收，原文保留于清理前副本，不再与计划重复维护。
- 不把本轮文档替换算作 P01—P11 实现完成。新 `answer_basis`、统一群工作台和固定工具 Schema 都仍是待实施。

### 回收与保留

旧两份计划已正常移动至：

`/Users/len5010/.Trash/LenBot-docs-20260920-034707`

同目录 `清理前副本/` 保存 README、AGENTS、六份现行文档、发布记录模板及三份部署说明的清理前版本。没有永久删除；原始人格资料、素材索引、业务 SOURCE.md、部署材料和本地插件资料继续保留。

本轮已精简 README 文档入口、AGENTS 首段索引、architecture/context/operations/plugins/serious-issue/iteration，更新发布记录模板，并移除 browser/dev/linux 部署说明中失去入口的旧批次编号。AGENTS 的工程约束正文保持不变；实际运维条件与事务、权限、预算、阅读、回执合同未放宽。

### 已定位的文档漂移

1. `respond` 和部分读取/提案工具仍随当前状态改变 Schema；不是已经完成稳定目录。
2. `anchor_window_start()` 核对本次摘要的 `_summary_complete_ids`，不能用投影偏移或仅有区间目录代替完整原话覆盖。
3. 空的锚定裁剪集合也可能只是窗口头已在格点，不一定是窗口失去锚点。
4. 缓存复用、网关扣额和上游真实成本不同，不能据命中率宣布一次观察只付新消息费用。
5. 维护批次未完成能支持重用批次身份，不证明之前的请求没有计费。
6. 旧文档中的“本批必须迁移”“当前无监听”“已永久排除 TTL/容量”“并发必失败”等不能继续作为无时间边界的操作依据。

### 本轮核对与未执行

已按源码阅读路由、工具/提案类型、窗口锚定、工作压缩、插件能力字段与文档引用，并阅读本轮 Git 差异。全仓 Markdown 检索未再发现指向两份旧计划的引用；新计划为唯一设计入口，发布模板已对应 A01—A30。未运行测试、夹具、断言探针、自动截图、压力任务或模型评审；没有前端代码改动，不运行无关构建。这是文档核对，不是新计划的运行验收。

没有启动服务、发起新的模型/工具/平台调用、发送群消息、修改真实配置/数据库/人格/人工样例/群名单，没有提交或推送。本轮之前的运行记录不代表服务此刻仍在运行。

## 2026-09-20：缓存真实调用的最新证据

### 调用范围

当时取得有限真实模型调用授权。使用合成记录及现有工具定义，没有群聊正文；未启动群收发主进程，未执行返回工具，未改配置或代码，未重启服务。下表 A/B 是该批人工记录标签，不是数据库 call_id；保留原标签，不补造缺失身份。

### 工具表变化

703 条消息、约 139k 输入。A 有 25 个工具；B 仅移除 `refute_memory`、`supersede_memory`，其他消息正文、工具选择及参数不变。

| 调用 | 输入 token | 已报告缓存 token | 条件 |
|---|---:|---:|---|
| A1 | 139233 | 字段缺失 | 首次建立 |
| A2 | 139233 | 134969 | A 原样重复，96.94% |
| B1 | 138850 | 字段缺失 | 只移除两个工具 |
| B2 | 138850 | 134976 | B 原样重复，97.21% |
| A3 | 139233 | 134969 | 恢复 A，旧缓存仍可复用 |
| A4 | 139233 | 134969 | 只修改尾部时间，仍为 96.94% |

六次均 HTTP 200、`finish_reason=tool_calls`。支持工具集合变化存在缓存代价，不能用消息目录稳定排除应用侧变化；各个动态 Schema 字段和终结缩表的独立影响尚未逐项实测。

### 时间间隔与连接故障

A4 返回后等待 360 秒，本机向原入口发完全相同请求，13.334 秒后失败：

```text
ReadError: [Errno 54] Connection reset by peer
```

网关账单与转发访问日志未找到该笔，不计为缓存失效，也没有自动重试。之后从 lbot 本机访问同一网关的 `127.0.0.1:3000/v1/chat/completions`，继续使用原凭据、绑定和请求，未换上游通道：

| 调用 | 条件 | 输入 token | 实际记录 |
|---|---|---:|---|
| A7 | 距 A4 返回约 510 秒 | 139233 | 响应缓存字段缺失，网关缓存 0 |
| A8 | A7 后立即重复 | 139233 | 缓存 134969，96.94% |
| A9 | 同一服务器入口，A8 返回后空闲 310 秒 | 139233 | 网关缓存 0；原始响应未取回 |
| A10 | A9 账单完成约 97 秒后重复 | 139233 | 缓存 134969，96.94% |

A9 的 SSH 输出丢失：

```text
Read from remote host 106.53.150.204: Connection reset by peer
client_loop: send disconnect: Broken pipe
```

服务端仍确认请求成功：访问日志 HTTP 200；账单 `created_at=1789840559`、`use_time=4`、`prompt_tokens=139233`、`completion_tokens=26`、`cache_tokens=0`。A9 不伪称已取得原始响应；原定立即重复未执行，A10 是之后单独发起。

长空档内未在当时的服务端访问日志与通道 4 账单中见到其他模型调用。**其他群灌入大量 token 不是变冷的必要条件。** 证据支持约五分钟级的时间相关失效，不足以确定精确 300 秒 TTL，也不排除不可见上游淘汰。

本批共 10 次成功真实模型请求，另 1 次服务端无记录的传输失败；没有继续压测。运行代码未因此修改。

### 历史 29 轮分组复核

按每群首次模型调用排序，排除各群在窗口内首次和窗口头跨边界：

| 与上轮的工具集合 | 缓存字段 | 次数 | 首次调用开始时间间隔 |
|---|---|---:|---|
| 相同 | 正数 | 17 | 60.24—164.97 秒 |
| 相同 | 缺失 | 3 | 324.73—545.99 秒 |
| 改变 | 缺失 | 2 | 100.20、183.08 秒 |
| 改变 | 正数 | 1 | 134.39 秒 |

两次冷变化分别为移除工作控制工具和移除认识修订工具；后者恢复工具时再次出现正缓存。这是相关分组，不是逐次因果证明：历史没有完整请求正文与完整 Schema，工具名称相同不代表定义相同。

### 网关扣额

通道 4 该批 `cache_ratio=1`。A1/A2 冷热不同，`quota` 均为 5225138；B1/B2 均为 5210775。**该批网关没有缓存扣额折扣**，不是上游实际账单的证明。本轮未改计费参数。

## 2026-09-19：必要历史证据与口径修订

| 观察 | 保留的事实与限制 |
|---|---|
| 真实群流量 | 末两小时 33 次调用、29 轮、5 群；29/29 轮锚定生效，并见窗口头稳定及一次 908→876 的收缩；不等于全部请求命中 |
| 缓存字段 | 22 次正缓存、11 次字段缺失、0 次显式零；首次请求已报告缓存比例 54.28%，全部请求 59.57%。缺失按零汇总不代表取得了上游原始零值 |
| 约 19k 合成前缀 | A/A/B/A 顺序中，A 首次字段缺失、后续 A 为 16353 缓存 token（约 84.1%）；有限短间隔能复用，不排除更长间隔失效 |
| 约 30k 合成并发 | 该次 6 路均报告约 81.8%；只说明那组请求可以并发命中，不推出通用容量结论 |
| 约 40k 前缀留存 | 插入 4 份材料后 A 仍约 92.1%；材料区分度有限，不能据此排除所有负载下驱逐 |
| 不同尺寸 | 6009 输入 token 的 10 次响应均缺失缓存字段；约 30k/40k 曾命中。没有确定尺寸门槛，不能直接解释约 62k 的小群请求 |
| 通道 6 | 历史并发批次 30 次 502（`empty response content`），但另有两次短请求 HTTP 200、正文 OK；间歇故障成因未确认，不宣称“并发必失败/永久不可用” |

独立的传输失败曾包括 `[Errno 32] Broken pipe`、`SSL: UNEXPECTED_EOF_WHILE_READING`、`Connection closed by 106.53.150.204 port 2992`；这些症状不能合并统计为冷请求，也未据此定位到唯一网络层。

已撤销的过度结论：缓存失效只由锚定决定；已排除时间/容量/应用侧变化；上游确定保留 60 分钟；精确尺寸门槛已确定；目录一致等于正文一致；没有显式零就证明全部统计作废。后续只使用上表限定的证据。

## 最近一次已知部署与维护状态（不是本轮复验）

| 链路 | 最近记录 | 仍未确认 |
|---|---|---|
| 离线执行 | 开发 Gateway 曾执行 Python，`state=exited`、`returncode=0`；输出 `uid 65532`、`net blocked: OSError`，产物 `result.txt` 29 字节已登记 | 不证明生产隔离、当前运行状态或正式上传 |
| 公开出口/浏览器/媒体 | 浏览器镜像和 seccomp 曾就位；`public.deployment_verified=false`；请求曾明确拒绝“网络策略 public 尚未完成部署核验” | 出口、域名/资源域、取消、重启与终止结局等现场条件仍需按所属部署材料核对，不因换计划放行 |
| 语义索引 | 面板真实重建曾完成 7 群、共 336 条，并按当时修订与模型代次核对 | 不代表现在的索引覆盖；粗摘要的定位精度、后续召回效果未确认 |
| 维护容量 | 曾失败 `ValueError: 历史维护请求需要 82570 token，可用输入容量为 81808；原区间未推进`；当时调整维护容量并人工重试，两条批次完成 | 当时 7 群无未完成批次，不是当前库状态；此类配置问题没有加入自动恢复集合 |
| 有限维护恢复 | 曾见 `History maintenance resuming blocked batch in group:126300994: batch=b1f04bfa... status=pending error=None`；同批再次失败后阻塞 | 有限恢复已观察过，不能称通用自愈或零成本重试 |
| 插件提交 | 曾发生 `Object of type set is not JSON serializable`；源码已有 `sorted(read_event_ids)` 修正 | 修复后的自主插件真实提交尚未确认；不自行往群注入“时间简报”触发 |
| 兴趣表达 | 曾有 16 次初始容量失败；源码已缩小近期送达资料 | 是否足以解决真实群容量仍未确认，不把源码调整说成恢复成功 |
| 服务连接 | 一次启动中 23 条 conversation_error、27 条失败调用，含 `APIConnectionError: Connection error.` 与 `RemoteProtocolError: Server disconnected without sending a response.` | 只证明当次调用链失败；未把底层根因定案为唯一机器或应用问题 |
| 文件/账号/Core | 各有源码及独立部署/授权合同 | 平台版本、只读文件共享、真实 file_id、账号动作和可选 Core 的完整现场链路各自待确认 |

更早提示装配曾报 `NameError: name 'source' is not defined`，已修正未转义花括号；该例说明编译/导入不能代替实际业务运行。旧副本检查、自动化片段或历史压测均不成为后续可重复运行的许可。

## 尚未完成与下一步

1. P01—P11 已有上文逐批源码／编译与文档记录，但不据此宣布源码无遗漏或整项目已重构。当前优先补齐同版正常运行环境和本次人工验收范围，按顶部目标对照取得真实页面／业务证据；观察暴露的具体缺口再定向修正，不无限扩大静态整理。缓存改善仍需同一实际调用链的 usage，而不是代码形状推算。
2. 答复级 `answer_basis`、消息简明阶段、资料范围定位和群工作台返回位置已有源码；真实接话／沉默、按需查证、纠正后的有效记忆、工作续接、文件回执与页面体验仍待正常人工验收。核心 A01/A02/A04—A13/A16/A17/A28—A30、文件 A19/A20 及实际启用的可选分支不记通过，不凭模型自述、模拟或编译验收。
3. 分批原话续读、最终已读集合重建、观察积压排空与 debounce 的实际体验仍缺足够现场记录；旧统计不能证明全部细节正确。
4. 方法学习目前主要绑定工作候选；普通短对话可保留原话、工具资料和认识修订，不等于已自动发布方法技能。
5. 外部工作循环没有完成适配或部署；本批保留现有循环，未来只考虑明确执行段，不新增第二套调度、压缩和预算。
6. 诊断令牌此前曾被记录为明文暴露，轮换尚未确认；本轮没有读取或修改秘密。
7. 真实配置值只以根文件及面板当前运行投影为准，不在多份文档维护第二份参数表。未来提交、推送、启动、真实调用与发送分别服从当次授权。
