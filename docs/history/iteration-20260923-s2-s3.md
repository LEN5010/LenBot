# 2026-09-23 会话段与插件合同记录

当前状态只见[产品路线](../plan/README.md)，当前交接见[当前任务](../iteration.md)。以下过程与未确认项限定原批次。

## 独立插件接口世代

阶段提交：`a0d07c8`。

### 当前批次

- 分支 `feat/s0-product-contract`，本批基线 `cf3f88d`，开始时工作区干净。
- 用户已确认宿主管理会话段、原话归事件存储；插件接口世代与自身版本分离，身份／范围／预算归宿主。决定见[原记录](plan/s0-02-product-positioning-and-decisions.md)。
- 本批实施独立插件接口世代，不修改真实配置或业务数据，不更改插件自身版本及原工作／出站版本核对。

### 本批实现与核对

现有 version 只能标明插件自身版本，不能判断宿主 API 合同。本批在原 PluginSpec 增加必填 api_version，以整数 1 声明接口世代；原目录发现取得描述符后明确拒绝类型或世代不匹配。缺少字段在描述符构造时失败。核对发生在 Python 包导入之后、插件配置解析及实例创建之前，不是恶意代码隔离。没有新表、重复描述符、配置来源或兼容层。

13 个内置插件与业务时钟均补字面值声明；Host 原状态投影新增 api_version，查询层继续原脱敏路径，插件列表及详情分开显示自身版本和接口世代。所属开发、架构、升级／回退说明同步；公共入口未增删，内部耦合收窄留待后续，稳定支持期未自行决定。

- uv compileall 编译 catalog、host、13 个内置描述符及 local_clock，退出 0。
- 前端 npm run build 退出 0，491 modules，1.59s；日志 `/private/tmp/lenbot-s3-api-build.log`。git diff --check 无格式错误。
- 源码沿 ConfigStore.load → PluginCatalog.discover → RootConfig.model_validate 核对，沿 Host.status_snapshot → query_service.plugins → PluginsView 核对字段；没有执行插件导入来制造运行验收。
- 读取时猜测 `src/len_bot/plugins/spec.py` 得到 `No such file or directory`，实际类型在 catalog.py；两个不存在的文档 glob 分别得到 `zsh:1: no matches found: docs/plan/s3*`、`zsh:1: no matches found: docs/*产品*`，之后读取真实任务卡与盘点。均为路径查找失败，不是业务运行失败。
- 没有同版开发面板，实际页面、外部插件迁移、装载拒绝和正常插件生命周期均待人工验收，不把编译／构建视为运行通过。
- 未新增、修改或运行测试、夹具、断言式探针、自动截图、回放、故障注入或压力任务；未启动服务、读业务库、保存配置、调用模型／平台或发送。提交不含构建产物和运行数据。

### 接续与未确认

1. 继续 S2-01：先沿 scene_sessions、执行 episode、历史压缩与请求装配确认最小持久会话段边界，再实施；不复制整份 transcript，不改变数据保留政策。
2. S3-02 的公共上下文收窄仍须沿实际调用能力拆除内部依赖，不把本批世代字段当作该项已完成。
3. S1-02 仍缺其他提示及插件定义版本、动态材料与工具资料页完整定位；S1-04 仍缺首次／后续请求分类、未记录的入场等待与失败阶段。S1-03／S1-05 源码核对和定向展示已交付；S1-06 与其他同版页面、请求持久化、缓存覆盖和调用状态仍待人工验收。
4. 许可证、字体／素材授权、数据保留、对外支持承诺仍待维护者决定。人物资料、五张常服及 19 张表情仍待人工采用；真实图片关联、成果复用、文件交付和纠正后的采用待真实业务记录。
5. 300k／128k 仍是候选容量，未改根配置；既往诊断令牌轮换与部署条件未复验。仅授权阶段性本地提交，未推送、合并、部署或实发。

## 会话段最小状态设计

阶段提交：`81fda02`。

### 当前批次

- 分支 `feat/s0-product-contract`，本批基线 `a0d07c8`，开始时工作区干净。
- 本轮已本地提交 `a0d07c8`：独立插件接口世代、14 个描述符、面板版本展示及升级说明；源码编译和前端构建完成，真实装载与页面待人工验收，明细见[本日记录](history/iteration-20260923-s2-s3.md)。
- 继续沿已确认宿主所有权方向推进会话段设计；本批仅文档，没有添加尚未接通的类型、表、配置或运行字段。

### 会话段设计与实际核对

[设计与迁移约定](plan/s2-01-conversation-segment.md)区分场景、段、执行与工作。先使用原 scene_sessions.state_json 的可空当前段，保存有序事件／资料范围、摘要引用、稳定编号和必要完整原生交换；不复制原话，不以 traces 代替运行状态，不增加 transcript 表。段更新归 Actor，原提交、观察截点、待处理来源、工作／等待预算保持独立。

核对发现：普通 SocialCognitionCore 每轮新建上下文，未接 exchange_checkpoint；S1 清单没有完整原生响应；TurnReferences.snapshot 混合显示编号和业务资格，不能整份恢复授权；AgentLoop 回调并未覆盖所有成功终结。设计明确这些实际缺口，没有以一个段 ID 宣称持续上下文已实现。

- 源码阅读覆盖 SceneSession、SceneActor、EventStore 的状态保存、SocialCognitionCore、AgentLoop、TurnReferences、ModelProfile、JobStore.save_job_exchange、runtime/work_context 和 history_batches 提交。
- 旧 Session 模型 extra='forbid'：未来段字段写入后，不能声称旧程序无需迁移可直接回退；设计已要求同批备份或另行授权的定向离线转换。本批未写运行状态，因此尚未产生兼容变化。
- 查找时误用路径得到 `rg: src/len_bot/cognition/core.py: IO error for operation on src/len_bot/cognition/core.py: No such file or directory (os error 2)`、`sed: src/len_bot/cognition/work_context.py: No such file or directory`；按实际文件列表转读 social_core.py 和 runtime/work_context.py。属于路径读取失败，不是业务运行失败。
- 本批仅文档，未重新运行编译或前端构建；上一代码提交的构建仍是 491 modules、1.59s。git diff --check 无格式错误。未新增、修改或运行测试、探针、夹具、自动截图、回放或压力任务。
- 没有同版开发面板，未启动服务、读业务库、保存配置、调用模型／平台或发送。本批设计不构成运行验收。

### 接续与未确认

1. S2-01 的固定材料版本依据和完整交换持久表示仍需沿实际渲染补齐，再接 S2-02；先解决普通对话，不让插件表达或工作写主段。压缩交接继续原知识版本和 Actor 互斥。
2. S3-01 已交付待复核；S3-02 仍需按真实能力收窄内部上下文。公开支持／弃用期限未自行决定。
3. S1-02 还缺其他提示及插件定义版本、动态材料与资料页完整定位；S1-04 还缺首次／后续请求分类及部分等待／失败计时。S1-03／05 源码核对已交付，S1-06 与同版页面、请求持久化、缓存覆盖及运行状态待人工验收。
4. 许可证、字体／素材授权、数据保留和公开支持承诺仍待维护者决定。人物资料、五张常服与 19 张表情仍待人工采用；真实图片关联、成果复用、文件交付及纠正后的采用待业务记录。
5. 300k／128k 仍是候选容量，根配置未改；既往诊断令牌轮换与部署条件未复验。授权阶段性本地提交，不授权推送、合并、部署或真实发送。总体工作未全部完成，继续可实施项。

## 当前调用的观察读取

阶段提交：`e113f3b`。

### 当前批次

- 当前分支 `feat/s0-product-contract`，本批基线 `6038cb1`，开始时工作区干净。
- 上轮插件归属和逐请求资料范围均已源码交付，本批先核对 S2 的原生交换保存入口，再推进不依赖交换表示的 S3-02。

### 实际核对与本批实现

S2 源码核对：原工作 archive_trajectory 仅将媒体字节换成资产引用，仍保存完整文字轨迹；直接用于主会话会复制原话和动态正文，不满足原引用存储设计。AgentLoop 成功终结只在 after_finish 返回 continue_run 时保存完整终结交换，其他成功返回不经此回调；普通对话仍未接交换回调。故未直接套用工作检查点、加无消费的段字段或改变已有终结路径，S2 仍需实现，不因缺环境标为完成。

本批 S3-02 解决一个明确缺口：插件读取已保存资料必须直接接触完整 EventStore，并自行传 allowed_scopes。新增现有 PluginCallContext.read_observation 方法，委托 PluginContext 的对应实现；核对 call.plugin 实例归属并复用 Host.validate_call，然后固定按 call.scene_id 读取原观察。无新服务类、表、权限机制或配置。

群报告分析的三个读取点、群报告产物读取、动态卡片资料读取、链接下载解析资料读取共六处改用该入口。原观察不存在／类型不对时的业务处理不变；准入和存储失败原样结束，不补抓或猜别名。动态卡片与链接源端协议、参数和下载逻辑未改。本地读取不登记新观察、不执行工具或授予模型已读资格；工作修订和公共研究来源仍由所属入口核对。

- 只读核对 PluginCallContext 方法合同、Host.validate_call、execute_tool 的 bound_call、工作调用构造和原 read_tool_observation 场景查询；没有运行正常启动或模拟调用。
- uv compileall 编译 plugins/base.py、models.py、group_summary/analysis.py、group_summary/service.py、asoul_dynamics/plugin.py、link_parser/plugin.py，退出 0；git diff --check 无格式错误。
- 本批未遇到命令失败，没有前端修改，不重复构建。上一同版前端构建记录为 491 modules、2.16s；实际页面仍待人工验收。
- 未新增、修改或运行测试、夹具、断言式探针、自动截图、回放、故障注入或压力任务；未启动服务、读业务库、保存配置、调用模型／平台或发送。

### 接续与未确认

1. S3-02 已有调用绑定的观察读取，其他 EventStore、Runtime、媒体及工作内部依赖仍需沿真实业务收窄；不删尚有消费者的内部入口或声称恶意代码隔离。真实有效调用、停用后的拒绝、同场景缺失和各业务正常使用待人工验收。
2. S2-01／02 的原话／资料引用与原生协议片段需要分开保存，固定材料版本、恢复后的稳定编号和当前阅读资格不能混用；由 Actor 保存，不套工作检查点形成第二份聊天正文。
3. S1-02 其他提示、插件动态定义及部分专用入口仍有缺档；S1-04 首次／后续分类和部分等待计时待补。S3-01 与 S1-03／05 保持待复核，实际持久化、页面和运行状态待人工验收。
4. 许可证、字体／素材授权、数据保留、公开支持承诺仍待维护者决定；人物资料、五张常服与 19 张表情待人工采用。真实图片关联、成果复用、文件交付及纠正后的采用待业务记录。
5. 300k／128k 仍为候选，根配置未改；既往令牌轮换与部署条件未复验。仅授权阶段性本地提交，不推送、合并、部署或真实发送。总体目标保持进行中。

## 群报告的调用绑定读取

阶段提交：`bb82044`。

### 当前批次

- 分支 `feat/s0-product-contract`，本批基线 `e113f3b`，开始时工作区干净。
- 上批已交付当次调用的已保存观察读取；本批继续 S3-02，移除群报告服务对象持有完整 EventStore 的依赖。

### 本批实现与核对

原服务需要原话窗口、所属插件资源分页、工作状态和媒体可用性，却因此取得完整存储对象。新增当次调用的 read_source、read_group_messages、list_observations、read_work／list_work、media_available，复用原 Host 准入和存储查询；不允许作者自选场景、插件版本或原话截点。read_source 直接返回准入已读来源，未额外重查。已有准入调用方忽略返回值的行为保持不变。

工作原始记录含预算和检查点，不能作为公开读取结果。本批在原 plugins/work.py 增加窄 PluginWorkSnapshot，只投影身份、修订、状态、operation、请求者和参数模型副本；按当前场景、本插件自身版本及已声明专用工作过滤。没有新表、重复状态、缓存、配置或控制入口。api.py 导出现有 Event 与该快照，接口世代保持 1。

群报告服务构造只接收配置，分析入口、原话读取、资源分页、历史产物与状态读取均改为当前调用方法；报告创建的同范围工作查找及查询来源也迁移。快照的 Bot 身份和事件截点不一致明确结束，不能切换范围继续。原分页顺序、半开日期、资料解析、来源引用、工作修订与发送合同保留。工作准备／阅读采用／续页回调仍有内部 store 或 job 合同，未宣称全部公共化。

- 源码核对全部 GroupSummaryService 构造与 source_messages 消费者；服务中剩余 job 下标只在供内部工作续页调用的静态 current_continuation，未误改为公开快照。
- 核对 Host.validate_call 的工具／handler 两个成功出口、现有忽略返回值的调用方、PluginCallContext 工作来源构造，以及原观察、群原话、工作、媒体查询范围与排序。
- uv compileall 编译 plugins/base.py、models.py、work.py、api.py、host.py 及 group_summary 的 service.py、plugin.py、analysis.py，退出 0；git diff --check 无格式错误。
- 本批未遇到命令失败；没有前端改动，不重复构建。上一前端构建记录为 491 modules、2.16s。无同版开发面板，运行及页面仍待人工验收。
- 未新增、修改或运行测试、夹具、断言式探针、自动截图、回放、故障注入或压力任务；未启动服务、读业务库、保存配置、调用模型／平台或发送。

### 接续与未确认

1. S3-02 继续工作准备、阅读采用和续页回调的内部存储合同，以及其他插件的媒体／工作／运行时依赖。当前新增读取仍待正常调用、停用拒绝、固定原话范围、历史报告与专用工作快照人工验收。
2. S2 的原话／资料引用与原生协议片段还未接通持久化，不能直接套用保留完整文字轨迹的工作检查点。固定材料、稳定编号和本次阅读资格仍需分开实现；段由 Actor 拥有。
3. S1-02 其他提示、插件动态定义及部分专用入口仍有缺档；S1-04 首次／后续分类和部分等待计时待补。S3-01 与 S1-03／05 保持待复核，各同版页面和真实调用持久化待人工验收。
4. 许可证、字体／素材授权、数据保留、公开支持承诺仍待维护者决定；人物资料、五张常服与 19 张表情待人工采用。真实图片关联、成果复用、文件交付及纠正后的采用待业务记录。
5. 300k／128k 仍为候选，根配置未改；既往令牌轮换与部署条件未复验。仅授权阶段性本地提交，不推送、合并、部署或真实发送。总体目标保持进行中。

## 专用工作回调与接口世代二

阶段提交：`8518c07`。

### 当前批次

- 分支 `feat/s0-product-contract`，本批基线 `bb82044`，开始时工作区干净。
- 继续 S3-02：专用工作回调不再接完整 EventStore 或 job 字典，原 JobStore 仍拥有持久事务与预算。
- 回调签名是不兼容变化，因此宿主接口升到世代 2；13 个内置插件与业务时钟同步字面值声明。旧世代明确拒绝，不增加兼容分支，不改插件自身版本、根配置或业务身份。

### 本批实现与核对

原 new_progress 获得整个 EventStore；只为群统计需求新增窄 PluginWorkPreparation，含固定 scene_id 和原统计查询的读取函数。时间区间、源截点与 Bot 账号仍来自插件已解析业务快照，不由宿主猜参数。准备回调只返回进度，由原宿主模型校验后保存。

原 adopt_reads 取得存储与整份工作。本批由原事务在工作修订、资料所属、范围总长和页引用核对之后读取原观察，将范围／资料对及已解析参数、进度交给同步回调计算；异常沿原事务回滚。原较旧修订阅读登记、报告源范围判读与累计计数规则保留，不清预算或重做外部行为。

续页回调改为 PluginWorkSnapshot；needs_model 改为已解析参数、进度、目标和 tuple 约束。前者不见原生检查点，后者不见预算存储；恢复可行性查询与真正执行两处调用同步迁移，仍由原宿主检查额度／期限。新增公开已有 ResultPresentation 与准备类型，不建新的观察或执行账。

- 源码核对全部 new_progress、adopt_reads、continuation、needs_model 调用点；仓库唯一专用实现为群报告。检查原写锁、事务更新／回滚位置及旧修订范围准入均未搬动。
- uv compileall 编译公共 work/api/catalog、13 个内置描述符、local_clock 描述符、群报告 work/service、runtime/job_store、job_runner、agent_runtime，退出 0；git diff --check 无格式错误。
- 当前开发、架构、运行手册及本地插件说明更新为世代 2，并给出四类回调迁移表；历史批次中的世代 1 保留原时点，不篡改为当时已采用世代 2。
- 本批未遇到命令失败，没有前端修改；现有页面读取实际 api_version，不写死世代。沿用此前 491 modules、2.16s 的同版前端构建，不把这当作本批运行通过。
- 未新增、修改或运行测试、夹具、断言式探针、自动截图、回放、故障注入或压力任务；未启动服务、读业务库、保存配置、调用模型／平台或发送。外部插件回调迁移、实际装载拒绝及正常报告准备／读取／采用／恢复均待人工验收。

### 接续与未确认

1. S3-02 继续执行协作、媒体与公开错误合同的内部依赖；四类工作回调已收窄，不宣称其他插件已全部迁移或同进程代码已隔离。世代 2 上线需另行授权停机、备份和同版升级，旧回调不能只改声明数字。
2. S2 的原话／资料引用与原生协议片段尚未接通持久化，不能直接套用保留完整文字轨迹的工作检查点。固定材料、稳定编号和本次阅读资格需分开实现；段由 Actor 拥有。
3. S1-02 其他提示、插件动态定义及部分专用入口仍有缺档；S1-04 首次／后续分类和部分等待计时待补。S3-01 与 S1-03／05 保持待复核，各同版页面和真实调用持久化待人工验收。
4. 许可证、字体／素材授权、数据保留、公开支持承诺仍待维护者决定；人物资料、五张常服与 19 张表情待人工采用。真实图片关联、成果复用、文件交付及纠正后的采用待业务记录。
5. 300k／128k 仍为候选，根配置未改；既往令牌轮换与部署条件未复验。仅授权阶段性本地提交，不推送、合并、部署或真实发送。总体目标保持进行中。

## 公共工作次数与控制异常

阶段提交：`4e78ca5`。

### 当前批次

- 分支 `feat/s0-product-contract`，本批基线 `8518c07`，开始时工作区干净。
- 上批已提交四类专用工作回调收窄与接口世代 2，旧世代明确拒绝。本批继续 S3-02，消除群报告执行代码对内部预算算法和工作异常的直接导入。

### 本批实现与核对

群报告只为计算两处剩余调用次数而导入 cognition.budget。本批在现有 PluginWorkContext 增加 remaining_model_calls，复用既有 budget 回调与 count_remaining，返回 None／非负次数；不预约、不扣减、不重算限额。两处业务调度判断迁移到该方法，原批次及合并条件不变。

公共 api 直接导出已有 JobChanged、JobBudgetExhausted 和 project_onebot_text，未创建包装异常或复制算法；群报告的分析、服务和工作合同仅从公共入口取得这些符号。取消、工作变更的传播、预算耗尽时进度说明及宿主捕获对象保持原类型身份。CQ 投影仍只形成文字／媒体类型标记，不变成原话坐标或像素依据。

- 源码核对 count_remaining 的 None／零／正数语义、两处预算判定、原异常定义及报告异常传播；按群报告 Python 文件实际 import 和属性访问核对，cognition/runtime/events、event_store、_runtime、_host 的直接依赖已无匹配。
- 同次搜索仍看到 render.py 导入 cards.tokens 和 cards.layout；这是明确剩余内部版式／主题依赖，未把整个目录宣称为可独立发行，也未处理未授权字体或素材。
- uv compileall 编译 plugins/work.py、api.py 与 group_summary 的 analysis.py、service.py、work.py，退出 0；git diff --check 无格式错误。未执行动态导入、断言或业务调用。
- 本批未遇到命令失败，没有前端改动，不重复构建；此前同版前端记录仍为 491 modules、2.16s。无同版开发面板，实际业务与页面待人工验收。
- 未新增、修改或运行测试、夹具、断言式探针、自动截图、回放、故障注入或压力任务；未启动服务、读业务库、保存配置、调用模型／平台或发送。

### 接续与未确认

1. S3-02 继续其他插件的媒体／工作／运行时内部依赖；S3 参考目录的版式资源与公共错误／取消行为需继续核对。现有读取、世代拒绝、工作准备／采用／恢复和执行异常仍待人工验收。
2. S2 的原话／资料引用与原生协议片段尚未接通持久化，不能直接套保留完整文字轨迹的工作检查点。固定材料、稳定编号与本次阅读资格仍需分开实现；段由 Actor 拥有。
3. S1-02 其他提示、插件动态定义及部分专用入口仍有缺档；S1-04 首次／后续分类和部分等待计时待补。S3-01 与 S1-03／05 保持待复核，各同版页面和真实调用持久化待人工验收。
4. 许可证、字体／素材授权、数据保留、公开支持承诺仍待维护者决定；人物资料、五张常服与 19 张表情待人工采用。真实图片关联、成果复用、文件交付及纠正后的采用待业务记录。
5. 300k／128k 仍为候选，根配置未改；既往令牌轮换与部署条件未复验。仅授权阶段性本地提交，不推送、合并、部署或真实发送。总体目标保持进行中。
