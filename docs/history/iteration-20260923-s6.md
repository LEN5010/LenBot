# 2026-09-23 候选验收与发布准备记录

任务状态只见[产品路线](../plan/README.md)，当前交接见[当前任务](../iteration.md)。以下过程与未确认项限定原批次。

## 同版候选证据与未确认表

阶段提交：`6a193af`。

### 当前批次

- 分支 feat/s0-product-contract，基线 91ad528，开始时工作区干净；上一阶段支持边界与首个验收方向已本地提交，属于实际进展。
- S6-01：同一候选版本的验收材料结构。原模板已有发布身份、业务与恢复记录，缺少候选范围、源码／静态／人工／平台分栏及未确认表；直接补原模板，不另建报告系统、任务状态表或采集器。

### 本批交付与核对

发布观察模板增加候选提交与同版前端／插件／文档、模型渠道、非秘密配置基线和已有修订。配置编辑实际为原基线比较，不补造全局 revision 或配置指纹。更换源码／插件／前端／镜像另记版本，配置变更标时间段，历史不同版结果只作参考。

按原 A01—A30 分配候选证据栏目，核心体验、工作材料与控制、执行／文件、工具／完整账、自然失败与可选业务均保留。群报告记录时间截点、实际读取覆盖、工作与报告表达；文件交付另串 execution／产物／资产／行动／尝试／平台 file_id，不假定报告图片自动成为可下载文件，也不把不同工作拼接。诊断导出只是受限关联投影，原内容、执行细节和平台回执不能用附件替代。

未确认表区分候选选择、源码缺口、同版业务与政策决定；模板准备完成不冒称已经取得候选验收材料。原计划的核心范围仍完整保留，S1／S2 未完成源码不改写为只缺运行环境。路线状态只在 plan/README.md，修正部署说明与模板原“阶段状态归 iteration”的说法。

- 阅读 S6-01／02 任务卡、原计划第 11 节、发布模板、部署配方、现有变更模板与 CI；核对 ConfigEdit、事件诊断投影、工作执行／文件资产查询及群报告实际图片字段。
- 发现 Linux 文档原要求取得 data.file_id 后才填 deployment_verified，与 FileUploadConfig 及运行手册冲突；修正文档为核对版本／只读挂载后确认部署，授权后的正常上传另取真实回执。未改源码或真实开关。
- 源码定位出现 `rg: src/len_bot/web/services: No such file or directory (os error 2)` 与 `rg: src/len_bot/cognition/toolkit.py: No such file or directory (os error 2)`；随后按文件清单、路由真实导入与现有 query_service 定位，没有据空路径增加实现。
- git diff --check 无格式错误。本批仅文档，未运行编译、构建、依赖安装、测试、断言探针、截图、回放、故障注入、服务或平台操作；未读取业务库或真实根配置。无本批业务失败原文或候选现场回执。

### 待决定与接续

1. 接续 S6-02 的既有构建与复核入口：现有 CI 使用浮动 uv 且未锁定同步，与本地／容器锁文件路径不一致，沿原流程定向收口，不新增检查系统。
2. S6-01 仅材料结构已准备；候选提交、精确 Linux／镜像／模型／平台版本与现场授权待确认，S4-06／S5-06 的依赖未被模板替代。
3. S1 材料／耗时、S2 完整活动段／未登记依赖仍有源码缺口；S2 额外原生字段保存、S5-03 分类保留、许可证／素材授权和支持承诺仍待决定。
4. S3、S4-01—05、S5-02／04／05 待同版人工复核；不推送、合并、部署或实发，总体目标继续。

## 锁文件构建与最小复核入口

阶段提交：`9f10681`。

### 当前批次

- 分支 feat/s0-product-contract，基线 6a193af，开始时工作区干净；S6-01 候选证据模板已本地提交，属于实际进展，真实候选材料尚未取得。
- S6-02：固定既有构建与差异复核入口，直接修改原 CI 三项配置，未新增脚本、扫描器或检查系统。

### 本批交付与核对

原 CI 的 uv 从 latest 改为现有本地／Linux 配方使用的 0.12.13；依赖同步添加 --locked，后续编译使用 --no-sync，防止编译步骤重新同步依赖。保留 Python 3.13、Node 22、npm ci、npm run build 与既有触发范围，不改依赖锁文件、构建内容或运行配置。托管 runner、Action 主版本标签、补丁版本与基础镜像仍未成为不可变环境，不将此改动描述为字节一致构建。

贡献入口补充原 CI 对应的最小编译、前端构建、差异与暂存复核命令，列明接口迁移／样例、配置与数据影响以及未自然发生的失败分支如何记录。发布模板只增加实际版本、目录／命令、退出码与证据范围，不另建任务表。未安装依赖时如实写现有环境；本地构建、远端 CI、wheel／镜像与正常业务各自确认。

本批实际执行：

- 阅读既有 CI、Linux Dockerfile、安装入口、前端 package.json、忽略规则、插件兼容清单与变更模板；本次只修改 CI 和文档，没有公共 API、配置字段、数据库或页面行为变化。
- uv 0.12.13；现有项目 Python 3.13.13。项目根执行 uv --cache-dir /private/tmp/lenbot-uv-cache run --no-sync python -m compileall -q src/len_bot，退出 0；只编译，不导入启动业务。
- 本机 Node v26.8.2、npm 11.19.1，与配方 Node 22 不同。在 src/len_bot/web/frontend 执行 npm run build，退出 0；Vite 6.4.3，491 个模块，1.64s。日志位于 /private/tmp/lenbot-s6-02-build.log；仅证明本机现有依赖下构建成功，不是 Linux／Node 22 或候选现场通过。
- git diff --check 无格式错误；构建产物被忽略，不进 Git。原 CI 锁文件安装路径仅源码复核，未本地安装依赖、触发远端 CI、构建／解包 wheel 或容器镜像。没有前端源码变动或本批实际页面观察。
- 本批无编译／构建失败或业务失败原文。局部 AGENTS.md 文件检索无匹配返回 1，不是运行失败；适用约束沿根文件。未新增、修改或运行测试、夹具、断言式探针、回放、故障注入、截图或覆盖率任务；未读取业务库或真实根配置、启动服务、调用模型／平台或实发。

### 待决定与接续

1. 接续 S6-03 的版本化交付流程和发布说明准备，沿现有贡献、兼容与发布观察入口整理；候选提交、版本号、分支冻结、合并／标签／发布分别由维护者确认，不能把当前开发 HEAD 自动标成稳定候选。
2. S6-01 的具体候选和 S4-06／S5-06 现场依赖未完成；S6-02 入口改动与本机编译构建已完成，远端 CI、精确候选环境及维护者复核仍待进行。
3. S1 请求材料／耗时、S2 完整活动段／未登记依赖仍有源码缺口；额外原生字段保存、分类保留、许可证／素材授权、精确支持组合与公开承诺仍待决定。
4. S3、S4-01—05、S5-02／04／05 待同版人工验收；继续不依赖未决事项的工作，不推送、合并、部署或实发，总体目标进行中。
