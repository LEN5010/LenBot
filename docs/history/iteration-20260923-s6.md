# 2026-09-23 候选验收与发布准备记录

任务状态只见[产品路线](../plan/README.md)，当前交接见[当前任务](../iteration.md)。以下过程与未确认项限定原批次。

## 同版候选证据与未确认表

阶段提交：`6a193af`。

### 当前批次

- 分支 feat/s0-product-contract，基线 91ad528，开始时工作区干净；上一阶段支持边界与首个验收方向已本地提交，属于实际进展。
- S6-01：同一候选版本的验收材料结构。原模板已有发布身份、业务与恢复记录，缺少候选范围、源码／静态／人工／平台分栏及未确认表；直接补原模板，不另建报告系统、任务状态表或采集器。

### 本批交付与核对

发布观察模板增加候选提交与同版前端／插件／文档、模型渠道、非秘密配置基线和已有修订。配置编辑实际为原基线比较，不补造全局 revision 或配置指纹。更换源码／插件／前端／镜像另记版本，配置变更标时间段，历史不同版结果只作参考。

按原 A01—A30 分配候选证据栏目，核心体验、工作材料与控制、执行／文件、工具／完整账、自然失败与可选业务均保留。群报告记录时间截点、实际读取覆盖、工作与报告表达；文件交付另串 execution／产物／资产／行动／尝试／平台 file_id，不假定报告图片自动成为可下载文件，也不把不同工作拼接。诊断导出只是受限关联投影，原内容、执行细节和平台回执不能用附件替代。

未确认表区分候选选择、源码缺口、同版业务与政策决定；模板准备完成不冒称已经取得候选验收材料。原计划的核心范围仍完整保留，S1／S2 未完成源码不改写为只缺运行环境。路线状态只在 plan/README.md，修正部署说明与模板原“阶段状态归 iteration”的说法。

- 阅读 S6-01／02 任务卡、原计划第 11 节、发布模板、部署配方、现有变更模板与 CI；核对 ConfigEdit、事件诊断投影、工作执行／文件资产查询及群报告实际图片字段。
- 发现 Linux 文档原要求取得 data.file_id 后才填 deployment_verified，与 FileUploadConfig 及运行手册冲突；修正文档为核对版本／只读挂载后确认部署，授权后的正常上传另取真实回执。未改源码或真实开关。
- 源码定位出现 `rg: src/len_bot/web/services: No such file or directory (os error 2)` 与 `rg: src/len_bot/cognition/toolkit.py: No such file or directory (os error 2)`；随后按文件清单、路由真实导入与现有 query_service 定位，没有据空路径增加实现。
- git diff --check 无格式错误。本批仅文档，未运行编译、构建、依赖安装、测试、断言探针、截图、回放、故障注入、服务或平台操作；未读取业务库或真实根配置。无本批业务失败原文或候选现场回执。

### 待决定与接续

1. 接续 S6-02 的既有构建与复核入口：现有 CI 使用浮动 uv 且未锁定同步，与本地／容器锁文件路径不一致，沿原流程定向收口，不新增检查系统。
2. S6-01 仅材料结构已准备；候选提交、精确 Linux／镜像／模型／平台版本与现场授权待确认，S4-06／S5-06 的依赖未被模板替代。
3. S1 材料／耗时、S2 完整活动段／未登记依赖仍有源码缺口；S2 额外原生字段保存、S5-03 分类保留、许可证／素材授权和支持承诺仍待决定。
4. S3、S4-01—05、S5-02／04／05 待同版人工复核；不推送、合并、部署或实发，总体目标继续。

## 锁文件构建与最小复核入口

阶段提交：`9f10681`。

### 当前批次

- 分支 feat/s0-product-contract，基线 6a193af，开始时工作区干净；S6-01 候选证据模板已本地提交，属于实际进展，真实候选材料尚未取得。
- S6-02：固定既有构建与差异复核入口，直接修改原 CI 三项配置，未新增脚本、扫描器或检查系统。

### 本批交付与核对

原 CI 的 uv 从 latest 改为现有本地／Linux 配方使用的 0.12.13；依赖同步添加 --locked，后续编译使用 --no-sync，防止编译步骤重新同步依赖。保留 Python 3.13、Node 22、npm ci、npm run build 与既有触发范围，不改依赖锁文件、构建内容或运行配置。托管 runner、Action 主版本标签、补丁版本与基础镜像仍未成为不可变环境，不将此改动描述为字节一致构建。

贡献入口补充原 CI 对应的最小编译、前端构建、差异与暂存复核命令，列明接口迁移／样例、配置与数据影响以及未自然发生的失败分支如何记录。发布模板只增加实际版本、目录／命令、退出码与证据范围，不另建任务表。未安装依赖时如实写现有环境；本地构建、远端 CI、wheel／镜像与正常业务各自确认。

本批实际执行：

- 阅读既有 CI、Linux Dockerfile、安装入口、前端 package.json、忽略规则、插件兼容清单与变更模板；本次只修改 CI 和文档，没有公共 API、配置字段、数据库或页面行为变化。
- uv 0.12.13；现有项目 Python 3.13.13。项目根执行 uv --cache-dir /private/tmp/lenbot-uv-cache run --no-sync python -m compileall -q src/len_bot，退出 0；只编译，不导入启动业务。
- 本机 Node v26.8.2、npm 11.19.1，与配方 Node 22 不同。在 src/len_bot/web/frontend 执行 npm run build，退出 0；Vite 6.4.3，491 个模块，1.64s。日志位于 /private/tmp/lenbot-s6-02-build.log；仅证明本机现有依赖下构建成功，不是 Linux／Node 22 或候选现场通过。
- git diff --check 无格式错误；构建产物被忽略，不进 Git。原 CI 锁文件安装路径仅源码复核，未本地安装依赖、触发远端 CI、构建／解包 wheel 或容器镜像。没有前端源码变动或本批实际页面观察。
- 本批无编译／构建失败或业务失败原文。局部 AGENTS.md 文件检索无匹配返回 1，不是运行失败；适用约束沿根文件。未新增、修改或运行测试、夹具、断言式探针、回放、故障注入、截图或覆盖率任务；未读取业务库或真实根配置、启动服务、调用模型／平台或实发。

### 待决定与接续

1. 接续 S6-03 的版本化交付流程和发布说明准备，沿现有贡献、兼容与发布观察入口整理；候选提交、版本号、分支冻结、合并／标签／发布分别由维护者确认，不能把当前开发 HEAD 自动标成稳定候选。
2. S6-01 的具体候选和 S4-06／S5-06 现场依赖未完成；S6-02 入口改动与本机编译构建已完成，远端 CI、精确候选环境及维护者复核仍待进行。
3. S1 请求材料／耗时、S2 完整活动段／未登记依赖仍有源码缺口；额外原生字段保存、分类保留、许可证／素材授权、精确支持组合与公开承诺仍待决定。
4. S3、S4-01—05、S5-02／04／05 待同版人工验收；继续不依赖未决事项的工作，不推送、合并、部署或实发，总体目标进行中。

## 候选流程与未发布变更

阶段提交：`d7d2395`。

### 当前批次

- 分支 feat/s0-product-contract，基线 9f10681，开始时工作区干净；上一阶段构建入口和证据记录已提交，属于实际进展。
- S6-03：版本化交付流程与未发布变更说明。原贡献入口已有复核步骤、发布模板已有证据位置，但没有本轮面向升级者的变更摘要；补一份未发布说明，流程仍归贡献入口，不新建发布工具或第二套任务表。

### 本批交付与核对

贡献入口区分开发提交、候选冻结、缺陷修复、合入后重新对齐、版本标记与独立部署授权。候选新增修复或合入形成新提交后，旧构建／现场材料只能作参考；不修改旧标签来掩盖差异，也不建立长期多开发主线。主包、前端包、插件自身版本和接口世代不是同一个版本号。

新增 release-notes.md，以 ad41a5a 到 9f10681 的产品化改造为明确比较范围，摘要记录调用诊断、原话窗口、插件合同、工作采用、运营回执和文档构建入口。版本、候选、标签、产物与发布日期均未确定，没有把现有主包 0.1.0／前端 0.3.0 当作本次发布版本。列明插件世代 2、同版前后端、Session JSON 回退及更早配置迁移的实际限制；S1／S2 源码缺口与政策未决仍保留。

- 阅读 S6-03 任务卡、当前路线／行为入口、比较范围内的 Git 提交、原版本文件、插件 API 与兼容清单、config_saved／control_accepted 路由、SceneSession／ConversationSegment 及原事件存储写入。
- Git 标签列表只有既有备份用途标签，本批未创建、移动或删除标签；没有选定候选、变更版本元数据、创建分支、推送、合并或发布。读取标签名称不代表它们是发行版本。
- 源码定位曾出现 `rg: src/len_bot/scene/models.py: No such file or directory (os error 2)`；按实际文件清单改读 scenes/models.py，未据不存在路径补实现。
- git diff --check 无格式错误。本批仅文档，未重复运行编译／构建或执行测试、夹具、探针、截图、回放、故障注入、依赖安装、服务、转换或平台调用；未读取业务库与真实根配置。无本批业务失败或现场证据，上一批 Node 26 构建不移称本版 Linux 验收。

### 待决定与接续

1. 接续 S6-04 原升级／回退路径的源码与文档核对。真实升级观察需获准环境；不因此跳过能独立整理的回退判定，亦不执行转换或恢复旧全库。
2. S6-03 流程与草稿已准备，候选、版本号、合入、标签与发布仍待维护者确认；S6-01 同版材料与 S6-02 远端 CI／精确环境未完成。
3. S1 请求材料／耗时、S2 完整活动段／未登记依赖仍有源码缺口。额外原生字段保存、分类保留、许可证／素材授权、精确支持组合与公开承诺仍待决定，继续不受影响的任务。
4. 既有 S3、S4、S5 源码及页面待人工复核项保持；只做阶段性本地提交，不推送、合并、部署或实发，总体目标进行中。

## 升级回退与新增事实保留

阶段提交：`f259a42`。

### 当前批次

- 分支 feat/s0-product-contract，基线 d7d2395，开始时工作区干净；候选流程与未发布说明已本地提交，属于实际进展。
- S6-04：原升级、备份与回退限制的定向核对。复用运行手册和现场模板，不新增恢复脚本、状态库、配置层或通用迁移器。

### 本批交付与核对

运行手册补充升级／回退判定，按当次授权、在途与未知执行、目标对现存数据的兼容、备份后的新增事实分别决定下一步。只有具体目标能读取全部当前记录才考虑仅代码回退；恢复升级前备份必须不丢失任何后续业务事实。修正会话段一节容易被理解为无条件恢复旧全库的说法，把同一条件放到字段回退说明旁。

停止新入场不等于添加全局冻结按钮；原群、工作、插件与发送配置只控制各自范围。全实例仍走正常停机；部分退出、队列取消、管理器超时或进程消失不能证明外部执行已停止。原发送尝试缺可靠终态保留 unknown，明确未尝试阶段才沿原恢复合同；工作身份、版本、预算、deadline 与已发生副作用不能因升级重新生成。

原观察配置离线命令添加 --no-sync，沿已安装同版依赖，不在转换时隐式同步。说明不带 --write 也会读取真实配置并导入插件，不是可随意运行的无数据探针；发现失败和冲突仍在原边界结束。本批没有运行该脚本，也没有编造活动段降版命令。

- 阅读 main.run_app 的面板／适配器／运行时停止顺序、AgentRuntime.stop／_stop_conversation_workers、JobRunner.stop、ActionQueue.stop 与 recover_deferred_delivery_tasks；核对原执行服务的未知状态及 Gateway shutdown 入口。只描述已有源码，不声称实际停机经过这些分支。
- 阅读 scripts/migrate_observation_config.py 的唯一根配置、原键转换、插件发现、严格解析和写回路径；核对 Session 严格字段、原升级与数据保留说明、Linux 停机超时和同批库／文件范围。
- git diff --check 无格式错误；本批仅文档，未运行编译、构建、测试、夹具、探针、自动截图、回放、故障注入或覆盖率任务。没有停机、备份、转换、升级、恢复、服务启动、模型／平台调用或实发；未读取业务库和真实根配置。
- 本批无源码定位失败或实际业务失败原文；未取得同版升级／恢复现场记录。S6-04 保持实施中，不将回退判定文档视作升级验收。

### 待决定与接续

1. 接续 S6-05，同版插件升级与公开接口的旧→新说明沿现有兼容清单核对，优先找源码与清单的具体差异，不另写第二份公共 API。
2. S6-01 的候选现场材料、S6-02 的远端 CI／精确环境、S6-03 候选版本／合入／标签／发布和 S6-04 正常升级观察仍待相应确认与授权。
3. S1 材料／耗时、S2 完整活动段／未登记依赖仍有源码缺口；额外原生字段保存、分类期限、许可证／素材授权、精确支持组合与公开承诺仍待决定。
4. S3、S4、S5 中源码及页面待人工复核项保持；继续独立任务，仅阶段性本地提交，不推送、合并、部署或实发，总体目标进行中。

## 插件升级与宿主调用对齐

阶段提交：`36919e7`。

### 当前批次

- 分支 feat/s0-product-contract，基线 f259a42，开始时工作区干净；上一阶段升级回退说明已本地提交，属于实际进展。
- S6-05：复用现有插件兼容清单补同版升级判读，不另建公共 API、描述符清单、加载探针或兼容实现。

### 本批交付与核对

将兼容清单的核对范围从原 8730431 对齐到 f259a42，保留原基线与逐插件适配事实。Git 差异表明公共 api.py／catalog.py／models.py／work.py、全部内置业务及 local_clock 在这段范围没有变化；PluginHost 仅补工具声明的面板投影。宿主的工作交付身份和配置结果处理则有后续变化，新增入口表提醒不能用插件代码未变代替宿主同版证据。

旧→新判读直接链接既有世代 2 四回调表、默认顺序、公共读取／资源与加载合同，不复制新的实现。补明原 PluginOrigin.plugin_version、注册操作与场景资格仍约束旧工作；api_version 匹配不表示旧工作自动可继续，不能改历史插件归属绕过拒绝。三条参考各自列出正常现场证据范围，未取得外部作者独立迁移记录，不拿内置源码对齐冒充。

- 阅读 PluginSpec／PluginCatalog、PluginCallContext、PluginWorkSpec／Context、公共导出、13 个内置描述符与 local_clock 的 API／自身版本及配置应用声明；仍是接口世代 2，未修改描述符或业务版本。
- 阅读三条参考的实际公共导入、群报告字体要求、直播随包字体和时钟说明；没有据可导入内部模块宣布新公共入口，也未改模板、字体或真实配置。
- 对照 PluginHost.work_spec／origin_issue／work_issue、工具面板投影，以及 runtime.plugin_interactions 的成品交付 call 与工具箱上下文；相关工作修订、发起主体、原回执和 unknown 规则不变。
- git diff --check 无格式错误。本批仅文档，未运行编译、构建、依赖安装、测试、夹具、断言式探针、自动截图、回放、故障注入、服务或插件发现。未读取业务库／真实根配置、启用插件、调用模型／平台或实发。
- 本批无源码定位失败或实际业务失败原文。候选尚未确定，没有同版加载、外部插件迁移或现场业务回执，S6-05 保持实施中。

### 待决定与接续

1. S6-06 的实际发布／暂缓决定依赖候选、现场材料与维护者确认，不能由本清单代签。先回到仍可实施的 S1 请求材料／耗时缺口，避免只准备发布文档而遗漏核心源码。
2. S6-01 同版现场材料、S6-02 远端 CI／精确环境、S6-03 候选版本／标签／发布、S6-04 正常升级以及 S6-05 外部迁移仍待相应输入或授权。
3. S2 完整活动段／未登记依赖仍有源码缺口；额外原生字段保存、分类保留、许可证／素材授权、精确支持组合和公开承诺仍待决定。S7 独立使用者与外部作者实际记录尚未取得。
4. 既有源码与页面待人工复核项保持；仅阶段性本地提交，不推送、合并、部署或实发，总体目标进行中。
