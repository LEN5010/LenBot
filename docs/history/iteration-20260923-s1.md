# 2026-09-23 S1 实施与复核记录

按批次保留源码核对与未确认项；当前状态见[路线入口](../plan/README.md)，当前交接见[当前任务](../iteration.md)。历史中的授权与未提交说明限定原批次。

## 事件诊断预览与下载

阶段提交：`dc919e8`。以下保留当批记录。

### 当前批次

- 分支 `feat/s0-product-contract`，基线 `13e8fdf`，开始时工作区干净。
- 用户授权持续推进可实施任务并阶段性本地提交，不授权推送、合并、部署或真实发送。前批提交构成实际进展，本轮沿现有查询补 S1-06 的事件诊断材料，不重建审计系统。
- 没有可用同版开发面板；不另起服务或用假数据代替验收。

### 实现与边界

原事件关联查询已有身份、状态和上限，但完整对象包含正文和动态材料。本批在 RuntimeQueryService 增加定向 event_diagnostics 投影，使用既有 relations，只选择事件、提交、轨迹、调用、工作、行动和工具资料的固定身份、状态、用量与回执计时字段。

新 GET 路由沿原 Cookie 登录，按 scene_id/event_id 定位，找不到返回 404，响应禁止缓存。没有正文、人格、参数、错误原文、请求快照、配置、媒体或凭据；仍有业务编号，不承诺匿名。生成不落库、不执行模型／工具／平台，不增加已读资格；多次查询的起止及原关联 limits/truncated 一并保存，不称原子快照或完整历史。

事件详情增加显式读取、预览与下载。新读取和切换对象清空旧结果；登录状态变化沿原客户端丢弃旧响应；过时异步响应不覆盖新对象。下载仅使用本次预览，不重新读取或外发。说明和操作方法已同步架构、运行手册。

### 实际核对、失败与未确认

- uv compileall 编译 query_service.py 与 routes/cockpit.py，退出 0；前端 npm run build 退出 0，491 个模块、1.72 秒，日志 /private/tmp/lenbot-s1-diagnostics-build.log。
- 源码阅读覆盖原登录依赖、同场景存在性查询、各类截断、SourceOutcome 真正字段及导出字段选择；git diff --check 无格式错误。
- 文档补丁初次使用不存在的 `### 接入与能力` 标题，返回 `Failed to find expected lines`；随后读取实际标题与段落重新定位，没有因此改变文档结构或业务源码。
- 实际 SQL、鉴权响应、文件下载、对象切换、截断及窄屏页面未人工验收；没有读取业务数据库或执行接口请求。本次没有运行测试、夹具、断言式探针、自动截图、回放、故障注入或压力任务，没有修改真实配置或启动服务。构建产物不入 Git。

## 来源处理与提交送达判读

阶段提交：`84c9d38`。以下保留本批实现与核对。

### 当前批次

- 分支 `feat/s0-product-contract`，本批起点 `dc919e8`，工作区当时干净；上批已提交事件诊断预览与下载。
- 持续推进授权包括阶段性本地提交，不包括推送、合并、部署、真实模型／平台调用或发送。当前阶段补 S1-03 的具体展示缺口，核对并保留 S1-05 已有实现。

### 交付与源码依据

| 边界 | 已核对的真实路径 | 本批处理 |
|---|---|---|
| 来源处理 | cognition/models.py 的 SourceOutcome；events/store.py 同事务保存 source_outcomes；scenes/reducer.py 按处理来源移除 pending_wakes | 保留五种来源状态，消息详情补原 unfinished；不因读过或整轮沉默改变本条结局 |
| 失败与拒绝 | agent_loop.py 保存终结参数失败、新输入冲突、预算异常等；agent_runtime.py 保存异常类型与阶段；GateDecision.record 保存 accepted/reason | 查询投影原 error_type、gate_accepted、gate_reason；页面按原类型显示，缺错误原文的 conversation_error 也保留问题入口 |
| 提交与发布 | scenes/actor.py 已有 checkpoint 返回 not_repeated；event_store 同事务提交；RuntimeGate.publish_committed 限原 pending 状态，失败保留持久提交 | 未改运行代码；补状态判读表和运营步骤，不重建事务或发布队列 |
| 发送与未知 | actions/delivery_store.py 尝试先落库；delivery_fact 无终态时返回 unknown；ActionQueue 先读事实且不自动重放；receipt_delivery_status 核对平台消息／文件 ID | 未改既有实现，不用事件名称冒充真实送达；保持模拟／Shadow 与线上分离 |

新增展示只说明本次候选或关联轨迹，不能覆盖先前已提交 checkpoint。工具错误统计从原 tool_outcomes 读取，属于整轨迹而非本条独占。解释和未完成项只展示已保存文本，不生成心理过程，不扫描错误正文猜分类。诊断下载继续不含错误原文，仅补原 Gate 接受状态。

### 核对结果与未确认

- uv compileall 编译本批 query_service.py，退出 0；前端 npm run build 退出 0，491 个模块、1.65 秒，日志 /private/tmp/lenbot-s1-outcomes-build.log。
- git diff --check 无格式错误。本批没有新增定位或编译失败，也没有运行业务故障观察。
- S1-03 本阶段展示源码完成；S1-05 现有实现已核对并保留，未为重复完成而改代码。两项记待复核，不记已验收。
- 没有可用同版面板，实际 Gate 拒绝、失败后继续、发布中断、发送未知及重复提交均待人工验收。未运行测试、夹具、断言式探针、自动截图、回放、故障注入、压力任务；未读业务库、改真实配置或启动服务。阶段代码与文档一起本地提交，构建产物不入 Git。

## 核心读取与工作工具定义快照

阶段提交：`cf3f88d`。以下保留当批记录。

### 当前批次

- 当前分支 `feat/s0-product-contract`，本批基线 `84c9d38`，开始时工作区干净。
- 已连续本地提交：`dc919e8` 事件诊断预览／下载；`84c9d38` 来源未完成项、Gate 拒绝及异常类型展示，并核对保留原提交／发布／送达边界。
- 本批继续 S1-02：将已有私有组件快照机制接入核心读取、工作及方法维护工具，未新增模块、表、公共类型或配置参数。

### 本批实现与核对

原 v2 清单只留存核心提案工具，读取／工作工具虽有名字仍无法定位完整定义。本批复用 _RecordedToolDefinition：RetrievalToolkit 先按原逻辑装入读取页数值默认值与上限，再记录核心定义，之后才拼入插件工具；工作运行器在方法目录上限确定后记录方法工具，其他固定工作工具与方法维护工具在声明处记录。

当次工具描述、Schema、顺序、发现子集及运行资格不变；不快照工具参数、工作目标、候选正文或插件配置。JSON 估算不计附属属性，网关仍在传输前转回普通字典；最终定义变化沿原 changed_after_declaration 保持缺档。配置仅有原 Schema 已公开的数值限额进入快照，不复制完整配置。没有读取真实配置或业务库。

- uv compileall 编译 tools/retrieval.py、runtime/job_runner.py、skills/learning.py，退出 0；git diff --check 无格式错误。
- 源码阅读核对原配置模型生成、发现过滤、工作定义复制及维护网关调用；rg 查找这些常量被下标修改的位置无匹配、退出 1，是只读搜索结果而非运行失败。
- 路线补丁因同文件段落顺序错误返回 `Failed to find expected lines`，读取实际段落后按文件顺序重新应用；没有为此更改任务语义。
- 本批没有修改前端，沿用 `84c9d38` 同版前端的成功构建；没有新页面验收。真实序列化、快照持久化及实际调用仍待人工验收，不把编译当业务通过。
- 未新增、修改或运行测试、夹具、断言式探针、自动截图、回放、故障注入、压力任务；未启动服务、保存配置、调用模型／平台或发送消息。仅提交代码与所属文档，不含构建产物和运行数据。

### 已确认方向与接续

2026-09-23 用户明确采纳：会话段由宿主管理，原话仍以事件存储为唯一权威；插件接口世代与插件自身版本分开，身份、范围与预算仍归宿主。决定已写回[原决策记录](plan/s0-02-product-positioning-and-decisions.md)，S2-01／S3-01 可按最小改动实施；不是已验收，不调整真实配置或保留期限。

1. S1-02 尚缺其他提示及插件定义版本、动态材料与工具资料页完整定位；不扩大为整条群聊请求永久复制。
2. S1-04 尚缺首次／后续请求分类、未记录的入场等待与失败阶段；同版缓存覆盖和取消／晚到 usage 待人工核对。
3. S1-03／S1-05 源码核对与定向展示已交付，保持待复核；S1-06 诊断预览／下载已有源码，尚待同版页面、下载、状态及关联验收。
4. 继续 S3-01 的独立接口世代及 S2-01 的会话段最小持久方案；不新增恶意插件沙箱，不把接口收窄宣称安全隔离。
5. 许可证、字体与素材授权、数据保留和对外支持承诺仍待维护者决定；相关任务暂停决定部分，其他可实施代码继续。
6. 人物资料、五张常服、19 张表情仍待人工采用；真实图片关联、研究成果复用、文件交付与纠正后的采用待业务记录。容量 300k／128k 仍为候选，未改根配置；既往诊断令牌轮换及部署条件未复验，见既有历史与素材交接。

## 插件工具归属版本

阶段提交：`8cd7a8e`。

### 当前批次

- 当前分支 `feat/s0-product-contract`，本批基线 `81fda02`，开始时工作区干净。
- 上轮接口世代和会话段设计均已提交。本批继续 S1-02 的插件工具版本依据，为后续持续上下文补材料定位，不宣称完整恢复已实现。

### 本批实现与核对

原清单只能保存插件工具名字，无法确定当时所属插件和接口世代。新增一个宿主私有的字典附属类型，沿 PluginHost.get_tool_definitions 绑定实际工具所属插件 ID、已加载实例版本和 catalog 已准入接口世代；不新增公共描述符、数据库表或配置字段。

最终网关登记格式升至 v3，保存归属字段与 origin_recorded／changed_after_declaration，插件动态 Schema 始终不落库；只有原固定核心定义继续保存完整快照。私有声明副本只供内存比较，附属属性不进入 JSON 估算或传输。面板区分归属记录和完整定义留存，兼容读取已有 v1/v2。自身版本相同并不证明动态 Schema 恒定，不把本批来源定位替代 S2 的固定材料依据。

- 源码核对工具发现、deferred 过滤、Hook 子集、深复制及网关转普通字典路径；没有改变定义内容、顺序、权限或执行入口。
- uv compileall 编译 request_record.py 与 plugins/host.py，退出 0；前端 npm run build 退出 0，491 modules、1.59s，日志 `/private/tmp/lenbot-s1-plugin-origin-build.log`。
- git diff --check 无格式错误。本批未遇到命令失败；未执行插件导入、序列化探针或业务调用，编译／构建不等于运行验收。
- 没有同版开发面板，实际调用持久化和页面仍待人工验收。未新增、修改或运行测试、夹具、断言式探针、自动截图、回放、故障注入或压力任务；未启动服务、读业务库、保存配置、调用模型／平台或发送。

### 接续与未确认

1. 继续最终工具资料页的定位范围；同一轮的已读轨迹不能替代每次最终请求中实际保留的范围。仍不复制动态正文。
2. S2-01 的固定材料版本和完整交换表示需继续接通，先解决普通对话，段归 Actor，不让工作或插件表达写主段；设计见[原约定](plan/s2-01-conversation-segment.md)。
3. S3-01 已交付待复核，S3-02 仍需按能力收窄内部上下文。S1-04 的首次／后续请求分类和缺失等待计时仍待补；各同版页面、持久化和运行事实继续待人工验收。
4. 许可证、字体／素材授权、数据保留、公开支持承诺仍待维护者决定；人物资料、五张常服与 19 张表情仍待人工采用。真实图片关联、成果复用、文件交付及纠正后的采用待业务记录。
5. 300k／128k 仍为候选，根配置未改；既往令牌轮换与部署条件未复验。仅授权阶段性本地提交，不推送、合并、部署或真实发送。总体目标保持进行中。

## 最终请求中的资料页定位

阶段提交：`6038cb1`。

### 当前批次

- 当前分支 `feat/s0-product-contract`，本批基线 `8cd7a8e`，开始时工作区干净。
- 上批已提交插件工具归属版本记录，v3 不复制动态 Schema。本批继续 S1-02：将原资料呈现判定接到每次最终请求清单，而不是用整轮累计阅读范围代替。

### 本批实现与核对

复用原私有 _RequestLocation 增加可空 tool_presentations；普通对话、独立插件表达在 model_messages 清理私有字段前逐消息调用原 read_presentations，普通工作在最终请求副本附同一定位旁路。原判定核对原资料正文或已登记宿主投影、坐标范围和引用名，不从单纯复制的坐标推断已提供。没有新表、公共类型、配置或解析协议。

每次调用保存实际 result_id、坐标单位、start/end/total 及已匹配 evidence_ref；同一页在不同位置分别记录。不调用 adopt_presentations，不用旧累计账回填，模型响应后原确认入口保持不变。工作未登记的省略标记保持 null，不因附带资料定位变成“未省略”。面板沿原资料页路由查看范围，同时说明未匹配不代表工具未执行、登记不代表模型已收到。缺字段／null 与空列表区分，不回填旧记录。

- 源码核对最终裁剪与 Hook 顺序、read_presentations 原文／投影判定、model_messages 清理、工作请求副本与原轨迹分离、网关登记后移除旁路、原资料页带范围导航。工作检查点和实际已读账没有新增字段。
- uv compileall 编译 request_record、context、social_core、plugin_interactions、job_runner，退出 0；git diff --check 无格式错误。
- 前端构建两次均退出 0：第一次 491 modules、1.67s；补齐工作入口和页面说明后最终 491 modules、2.16s，日志 `/private/tmp/lenbot-s1-request-pages-build.log`。
- 本批未遇到命令失败；没有执行导入或序列化探针。逐位置资料判定额外遍历一次保留消息，未跑耗时探针或据此宣称性能无影响。
- 没有同版开发面板，实际调用持久化、范围导航及页面仍待人工验收。未新增、修改或运行测试、夹具、断言式探针、自动截图、回放、故障注入或压力任务；未启动服务、读业务库、保存配置、调用模型／平台或发送。

### 接续与未确认

1. S1-02 已补插件归属及对话／独立插件／普通工作资料页范围，但其他提示版本、插件完整动态定义、部分专用入口与动态消息正文仍缺档；不宣称完整原生交换可恢复。
2. 继续 S2-01 固定材料依据与完整交换持久表示；段由 Actor 保存，事件原话仍唯一权威，压缩沿原知识版本互斥。此前[设计](plan/s2-01-conversation-segment.md)尚未写入运行字段。
3. S3-01 已交付待复核，S3-02 需按能力收窄内部上下文。S1-04 首次／后续请求分类和缺失等待计时仍待补；各同版页面、调用持久化与状态继续待人工验收。
4. 许可证、字体／素材授权、数据保留、公开支持承诺仍待维护者决定；人物资料、五张常服与 19 张表情仍待人工采用。真实图片关联、成果复用、文件交付及纠正后的采用待业务记录。
5. 300k／128k 仍为候选，根配置未改；既往令牌轮换与部署条件未复验。仅授权阶段性本地提交，不推送、合并、部署或真实发送。总体目标保持进行中。

## 绑定实例请求顺序与分组用量

阶段提交：`e27987c`。

### 当前批次

- 分支 `feat/s0-product-contract`，本批基线 `d267d58`，开始时工作区干净。
- S2 完整原生续接所需的额外片段保存边界已向维护者提出简洁选择；尚未收到回答，因此暂不持久化新的原生响应／必要回复片段，继续不依赖此决定的 S1-04。

### 本批实现与核对

原记录没有确定的首次／后续请求范围，不能仅凭相近时间推断。本批在实际 ModelGateway 实例建立随机绑定身份，在 complete 的同步准备入口分配序号，随原调用材料清单登记。计数不进模型请求、不改传输重试或预算；未登记准备可留下序号缺口，外层未进入网关的容量拒绝不计入。

query_service.model_usage 通过既有 trace 主键和同场景／call_id 定位顺序字段，使用原完整筛选统计首次、后续和未知；没有新表、列、索引或业务状态。平均输入只用有效报告，缓存沿原配对和加权分母；旧清单不猜顺序。调用详情、调用账与工作预算显示明确范围和限制，不把绑定实例等同群首问、工作首次或会话段首轮，不把准备次数等同费用。

- 源码核对网关普通调用、WorkGateway 外层容量检查、循环／维护入口及原 begin_model_call 原子登记；序号在任何 await 之前分配，不复用供应商重试序号。
- 阅读核对 SQL 使用原 trace 主键、kind、ref_id、scene_id 关联，复用原筛选参数和 usage 配对条件；没有执行 SQL 探针或打开业务库，实际查询仍待人工验收。
- uv compileall 编译 cognition/gateway.py、web/query_service.py，退出 0；前端 npm run build 退出 0，491 modules、1.67s，日志 `/private/tmp/lenbot-s1-request-order-build.log`；git diff --check 无格式错误。
- rg 查找调用表 latency_ms/duration_ms 无匹配，没有据此捏造延迟列；本批未遇到执行错误。无同版开发面板，分组统计、请求序号和实际页面仍待人工验收。
- 未新增、修改或运行测试、夹具、断言式探针、自动截图、回放、故障注入或压力任务；未启动服务、读业务库、保存配置、调用模型／平台或发送。

### 待决定与接续

1. 待维护者选择：允许在当前活动段保存无法从原事件／资料还原的原生响应字段与必要回复片段，或仅存引用并暂缓完整原生续接。原话、可定位资料继续只存引用，不新增长期归档或保留期限。已确认的宿主所有权不变，本次只厘清额外保存内容。
2. S2 当前已有原话窗口引用与 R/J 编号预留，但不保存完整工具交换；其他编号、提案句柄、窗外范围、基础摘要和压缩交接仍待实现。相关保存决定未确认不妨碍继续其他源码工作。
3. S1-04 仍缺部分等待／失败计时和实际运行核对；S1-02 其他提示、插件动态定义及部分专用入口仍有缺档。S3-02 继续其他插件内部依赖，群报告仍用 cards 版式；各同版页面与实际业务待人工验收。
4. 许可证、字体／素材授权、长期数据保留、公开支持承诺仍待维护者决定；人物资料、五张常服与 19 张表情待人工采用。真实图片关联、成果复用、文件交付及纠正后的采用待业务记录。
5. 300k／128k 仍为候选，根配置未改；既往令牌轮换与部署条件未复验。仅授权阶段性本地提交，不推送、合并、部署或真实发送。总体目标保持进行中。

## 对话槽位等待与取消边界

阶段提交：`dde1e57`。

### 当前批次

- 分支 `feat/s0-product-contract`，本批基线 `e27987c`，开始时工作区干净。
- 继续 S1-04／06：普通对话等待共享执行槽位发生在处理 episode 之前，原轮次耗时不能解释这段等待。本批记录这一个实际边界，不扩建调度或计时平台。

### 本批实现与核对

沿原 _cognition_semaphore 的 async with 记录等待区间。取得槽位后保存 acquired；在取得前取消或失败保存 cancelled／failed、异常类型和已等待时间，再传播原错误。复用原 traces，kind 为 conversation_wait，ref_id 使用真实 Stimulus.id，明确关联 source_event_ids；没有虚构 episode、调用、已读或来源处理结果。

关系查询按原话来源找到等待轨迹，但不把 Stimulus 编号加入 episode 集合。消息进展和阶段耗时组件分别显示等待状态及毫秒，等待问题保留在处理问题列表；调用归属仍只来自真实轮次。槽位取得还不代表来源／预算／租约准入，不把多来源批次耗时当单条独占，也不与外层或发送计时相加。

新增取消时诊断写入使 save_trace 的取消事务边界必须明确。本批在原写锁内补异常／取消时 rollback 并原样抛出；成功记录流程不变。等待失败记录本身失败时记录日志并继续抛原等待错误，不吞错开展业务，不自动重试。

- 源码核对槽位等待、成功记录、取消传播、真实 Stimulus 身份、关系种子、episode 集合排除与 MessageProgress 调用关联；等待计时不含先前批次合并、Actor 获取／来源筛选、日志写入或后续 Actor 租约等待。
- 首次按文件名查找 stimulus 没有匹配、组合命令退出 1；随后沿 agent_runtime 的实际 import 定位 events/models.py。是查找结果，不是业务运行失败。
- uv compileall 编译 runtime/agent_runtime.py、web/query_service.py、events/store.py，退出 0；前端 npm run build 退出 0，491 modules、1.68s，日志 `/private/tmp/lenbot-s1-slot-wait-build.log`；git diff --check 无格式错误。
- 没有同版开发面板，槽位等待、取消写入、回滚、来源关联和实际页面均待人工验收；没有把编译／构建或源码阅读写成运行通过。
- 未新增、修改或运行测试、夹具、断言式探针、自动截图、回放、故障注入或压力任务；未启动服务、读业务库、保存配置、调用模型／平台或发送。

### 待决定与接续

1. S2 活动段中不可由引用还原的原生响应字段／必要回复片段保存边界仍待维护者回答；未重复提问或自行扩大保存。现有宿主所有权、原话引用和 R/J 预留不变。
2. S1-04 其他等待／失败计时仍未全面接通，S1-02 其他提示、插件动态定义及部分专用入口仍缺档；同版调用记录、统计与页面待人工验收。
3. S2 仍缺完整交换、其他编号／提案句柄、窗外范围、基础摘要与压缩交接；S3-02 继续其他插件内部依赖，群报告仍用 cards 版式。继续不受保存决定影响的源码工作。
4. 许可证、字体／素材授权、长期数据保留、公开支持承诺仍待维护者决定；人物资料、五张常服与 19 张表情待人工采用。真实图片关联、成果复用、文件交付及纠正后的采用待业务记录。
5. 300k／128k 仍为候选，根配置未改；既往令牌轮换与部署条件未复验。仅授权阶段性本地提交，不推送、合并、部署或真实发送。总体目标保持进行中。

## 工作压缩固定合同与定义快照

阶段提交：`f0b9fd4`。

### 当前批次

- 分支 feat/s0-product-contract，基线 36919e7，开始时工作区干净；S6-05 兼容对齐已本地提交，属于实际进展。
- 回到 S1-02：工作压缩调用的固定合同与工具定义仍未登记快照。本批沿原 WorkCompressor 请求和既有记录类型补齐，不因发布准备而跳过核心源码缺口。

### 本批交付与核对

WorkCompressor 在原 summarize_work_segment 定义处使用已有 _RecordedToolDefinition，组件为 core.work_compression.summarize_work_segment，修订 1，Schema 仍来自实际解析的 WorkSegment。原区间选定并确认来源后，在调用前用既有 _RequestLocation／_PromptComponent 标记固定 system 合同 work_compression.contract，修订 1。没有复制第二份提示文本、改变提示内容或增加组件存储表。

提示定位在窗口估算之后附加，原网关先取出私有定位再估算／传输；工具附属属性不参与字典 JSON。沿原 prepare_request_record 比较最终内容，匹配才留快照，再与调用登记在原事务内保存。失败仍按原压缩／调用边界结束，没有新请求、重试、预算维度或降级。原 UI 已可显示格式 v3 的组件和工具快照，不修改页面。

本批只多保留源码固定合同和固定 Schema；动态目标、约束、归档交换、媒体与模型回复未复制进新快照，原压缩区间与结果仍归工作记录。没有扩展待确认的活动段原生回复保存范围，也没有为旧调用补造记录。上下文文档同步组件、修订、估算与可还原范围限制。

- 阅读请求记录、ModelGateway 的复制／剥离／估算顺序、begin_model_call 的原事务、WorkCompressor 的区间选择／来源核对／计数与响应解析，以及现有 RequestRecordDetails 组件显示入口。
- 核对核心提案、读取、普通工作与方法维护定义已使用原快照类型，不重复接入；动态插件 return_result Schema 与其他提示仍有缺口，未顺手扩大保存。当前定位只针对无动态插值的压缩 system 文本。
- 源码检索出现 `rg: src/len_bot/cognition/compaction.py: No such file or directory (os error 2)`；之后按工作运行器真实导入定位 runtime/work_context.py，没有新建猜测路径。
- uv --cache-dir /private/tmp/lenbot-uv-cache run --no-sync python -m compileall -q src/len_bot/runtime/work_context.py 退出 0；git diff --check 无格式错误。无前端源码变化，未重跑前端构建。编译不证明新调用记录或页面现场通过。
- 无本批业务失败原文。未运行测试、夹具、断言探针、回放、故障注入、自动截图、覆盖率、依赖安装或服务；未读取业务库／真实根配置、调用模型／平台或实发。

### 待决定与接续

1. 继续 S1 请求材料／耗时的真实缺口，优先复用现有记录和状态；对动态内容仅凭位置不能还原的范围继续明确说明，不擅自复制私人正文。
2. S2 完整活动段／未登记依赖仍有源码工作；额外原生字段保存、分类保留、许可证／素材授权、精确支持组合及公开承诺待决定。
3. S6 各项候选、同版业务、远端 CI、升级、外部插件迁移与发布依赖未完成；S7 独立使用者／作者记录未取得，不将模板或静态清单作为实际闭环。
4. 已有 S3、S4、S5 源码／页面待人工复核项保持；仅阶段性本地提交，不推送、合并、部署或实发，总体目标继续。

## 维护与动作审查固定合同

阶段提交：`a4e3e89`。

### 当前批次

- 分支 feat/s0-product-contract，基线 f0b9fd4，开始时工作区干净；上一阶段压缩固定合同已本地提交，属于实际进展。
- S1-02：历史维护、方法维护与动作审查的固定提示及历史工具定义缺档，复用原请求记录，不新增存储、模型或执行路径。

### 本批交付与核对

LLMReflector 的 finish_history_maintenance 与 query_memory 使用已有 _RecordedToolDefinition，Schema 仍来自 ReflectionOutput／MemoryLookup；未提供认识存储时仍不注册查询工具。原 finalize_request 容量核对之后返回消息列表与固定首消息的副本，附 history_maintenance.contract 定位；原轨迹不被写入侧带信息，不改变下一轮估算。无新回调层、工具或请求。

maintain_candidates 在原输入与预算核对后为固定 system 文本附 skill_maintenance.contract；原 save_skill／skip_skill 已有定义快照，不重复改写。ActionReviewer.approve 在原上下文核对后附 action_review.contract，覆盖源码固定说明及原来已在该文本内的 ReviewDecision Schema。审查工具仍为空，绑定、权限、预算、期限、动作结论与重用规则不变，没有新增审查模型。

组件与定义初始修订均为 1；提示／Schema 改变由维护者递增，原网关剥离定位并比较最终内容后保存匹配快照。沿原调用登记事务和既有界面呈现，不批量回填旧调用。动态批次原话、既有认识、方法候选、工作要求、动作参数及原生回复不进入这些新增快照，待确认的活动段保存边界没有被扩张。

- 阅读三条真实组装路径、AgentLoop 的最终回调顺序、固定模型 Schema 和原窗口／预算检查；历史固定 system 文本没有运行值插入，动作的实际参数仅在 user 消息中，本批不保存它。
- 原 _PromptComponent／_RequestLocation 与 _RecordedToolDefinition 不新增格式版本；原估算先完成，私有信息在网关计量／传输前移除；历史工具附属属性不参与 JSON。请求内容、工具名、参数模型和解析不变。
- uv --cache-dir /private/tmp/lenbot-uv-cache run --no-sync python -m compileall -q src/len_bot/skills/learning.py src/len_bot/cognition/action_review.py src/len_bot/memory/reflector.py 退出 0；git diff --check 无格式错误。未修改前端，不重跑前端构建；实际调用记录与页面显示仍待同版人工观察。
- 本批无源码路径定位失败、编译失败或业务失败原文。未运行测试、夹具、断言探针、自动截图、回放、故障注入、覆盖率、依赖安装、服务或模型／平台调用；未读取真实配置／业务库或实发。

### 待决定与接续

1. 接续 S1 剩余请求定位与耗时缺口，动态内容无可还原载体时继续明确缺档，不用静态组件快照宣称完整请求可重建。
2. S2 完整活动段／未登记依赖仍有源码工作；额外原生字段保存、分类期限、许可证／素材授权、精确支持组合与公开承诺仍待决定。
3. S6 的候选、同版现场、远端 CI、升级、外部插件迁移与发布依赖未完成，S7 独立使用者／作者记录尚未取得；原 S3／S4／S5 待人工复核项保持。
4. 仅阶段性本地提交，不推送、合并、部署或实发，总体目标进行中。

## 普通工作最终像素位置

阶段提交：`6329648`。

### 当前批次

- 分支 feat/s0-product-contract，基线 a4e3e89，开始时工作区干净；上一阶段维护固定合同已提交，属于实际进展。
- S1-02：普通工作最终请求中已知图片资产未进入逐位置记录。原窗口只返回 current_pixel_assets 集合，不能对应每条消息的具体位置；在原工作上下文模块补局部映射函数，不新增表、状态或媒体读取路径。

### 本批交付与核对

新增 request_image_assets 复用 _image_contexts 对原工作清单的读取，将 image_manifest 的图片序号转换为 content 的实际块位置。只消费当前位置的 image_url 和已同步的 block_index，不用 URL、文件名、全轮资产集合或未保留目录项猜映射。它不改窗口内容、清单、资产、已读状态、模型资格或容量限制。

InformationJobRunner 原 finalize_request 在来源核对、图片同步和容量检查之后，对发送副本逐消息填写已有 _RequestLocation.image_assets；tool_presentations 同时保留。原网关继续剥离私有信息，将位置和资产 ID 写到既有请求记录。被移出或 included_elsewhere 的目录项不会被登记成本位置像素；不保存 base64、读取文件或发起下载，旧记录不回填。

- 阅读工作图片清单生成、同步时的序号重排／移出／异处保留、原 finalize_request、prepare_request_record 的图像位置投影，以及现有 RequestRecordDetails 的图像缺定位计数。两种序号不同，不能直接把 block_index 当作 content 索引。
- 局部函数的业务缺口是最终请求逐位置关联，原集合无法表达；未改变 synchronize_image_window 返回合同、工作归属或重启恢复路径，也未增加新的图片协议。
- 源码初查包含不存在的 tools/media.py，输出被截断；随后按实际工作模块及导入定位，不据缺失路径新增文件或推导能力。无本批实际业务失败原文。
- uv --cache-dir /private/tmp/lenbot-uv-cache run --no-sync python -m compileall -q src/len_bot/runtime/work_context.py src/len_bot/runtime/job_runner.py 退出 0；git diff --check 无格式错误。无前端改动，未重跑前端构建；实际像素位置记录与页面计数仍待同版人工观察。
- 未运行测试、夹具、断言探针、自动截图、回放、故障注入、覆盖率、依赖安装、服务或模型／平台调用；未读取真实配置／业务库、图片字节或实发。

### 待决定与接续

1. 继续 S1 的请求定位展示与耗时缺口；当前图片逐位置数据已登记，原界面主要展示图片数量和缺定位数量，详细可读性仍可沿现有组件完善。
2. S2 完整活动段／未登记依赖仍有源码工作；额外原生字段保存、分类期限、许可证／素材授权、精确支持组合与公开承诺仍待决定。
3. S6 候选／现场／远端 CI／升级／外部迁移／发布及 S7 独立使用者／作者记录未完成；原 S3／S4／S5 待人工复核项保持，不以静态记录代替现场。
4. 仅阶段性本地提交，不推送、合并、部署或实发，总体目标继续。

## 调用内逐消息图像定位展示

阶段提交：`6b085d2`。

### 当前批次

- 分支 feat/s0-product-contract，基线 6329648，开始时工作区干净；上一阶段工作请求像素定位已本地提交，属于实际进展。
- S1-06：原调用材料表只有图像数量和缺定位数量，需展开完整定位对象才能辨认资产。本批在原组件按消息直接展示已有位置，不新增页面、请求或媒体预览系统。

### 本批交付与核对

RequestRecordDetails 在原每批 25 条消息的表格内增加原生 details／summary，逐图像列出 content 块位置、图像类型与资产 ID。消息和内容块均从 1 开始显示，保存字段仍从 0 开始；缺资产 ID 明确显示未记录，不从当前资料反推。完整定位字段入口与原统计保留，展开不会请求媒体或自动显示像素。

长资产编号使用可换行 code，不截掉关键身份，不增加复制权限或自建媒体链接；沿现有 Vue／Vuetify、表格和主题，保留原生键盘操作与焦点，不使用新增动画或仅悬停交互。图像关联不表示模型接收、资产当前可读或平台取得文件，说明在有图像时显示。

- 阅读现有组件、请求记录的 images／part_index／asset_id 字段和原 EntityLink 边界；这里只呈现已保存身份，不新增后端字段或请求。
- 使用本地界面资料检索“渐进展开／长标识符”，命中长 token 换行和内容截断建议；采用完整换行和原生折叠，不建新设计系统。按可访问性、交互与性能清单进行源码核对；主题对比、窄屏、长编号、键盘与实际交互仍需现场观察，不能由源码推导通过。
- 最终 npm run build 在 src/len_bot/web/frontend 退出 0：Vite 6.4.3、491 个模块、1.64s；本机 Node v26.8.2、npm 11.19.1，非 Linux／Node 22 配方验收。日志 /private/tmp/lenbot-request-images-build.log，产物不进 Git；git diff --check 无格式错误。
- 当前没有获准的同版面板页面，本批未启动服务或用静态假页面代替实际检查；实际改动页面保持待人工验收。无 Python 变更，未重复编译后端。
- 本批无源码定位、构建或实际业务失败原文。未新增、修改或运行测试、夹具、断言探针、截图、回放、故障注入、覆盖率或依赖安装；未读取真实配置／业务库、加载图片字节、调用模型／平台或实发。

### 待决定与接续

1. 继续 S1 剩余请求定位与分阶段耗时缺口；本批只改善已保存图片身份的可读性，不代表完整请求或整条业务时间线已经验收。
2. S2 完整活动段／未登记依赖仍有源码工作；额外原生字段保存、分类期限、许可证／素材授权、精确支持组合与公开承诺仍待决定。
3. S6 的候选／现场／远端 CI／升级／外部迁移／发布与 S7 独立使用者／作者记录未完成；已有 S3／S4／S5 待人工复核项保持。
4. 仅阶段性本地提交，不推送、合并、部署或实发，总体目标继续。

## 插件槽位等待与取消耗时

阶段提交：`4347f47`。

### 当前批次

- 分支 feat/s0-product-contract，基线 6b085d2，开始时工作区干净；上一阶段调用定位展示已提交，属于实际进展。
- S1-04：独立插件表达与插件等待恢复的共享对话槽位等待缺档。原普通对话已有单独等待记录，本批复用插件父执行审计与阶段展示，不另建计时表或排队系统。

### 本批交付与核对

_model_slot 仅在实际申请共享信号量时向原 execution.audit.model_slot_waits 追加等待项，用单调时钟记录取得前耗时和 acquired／cancelled／failed。一次父执行可能多次进入，故用本次原审计内的列表，不覆盖前一次等待；没有新业务状态或独立 trace 类型。取得后执行失败保留 acquired，不把模型时间计入等待，也不改变异常传播或信号量释放。

model_slot_owned 分支保持原样，不等待也不补零；该值表示已有执行容量，不由本次计时重新授予。等待计时不覆盖前面的 agent_lock、资料准备、工作槽位和模型调用，未登记的其他等待仍未知。原父处理／恢复 finally 保存审计，未额外写库或调用模型。

RuntimeQueryService 沿原嵌套审计收集 model_slot_waits，并投影 cognition_slot_wait_state；TraceTimings 复用已有等待时长，显示取得、等待取消或失败，不由等待结局推导后续业务成功。原对话等待也使用真实已存 state；无状态的旧记录不反推。列表是分段展示，不用其顺序重建全局时间线或相加为总延迟。

- 阅读 _model_slot 的两个真实调用方、父执行容量、独立表达及恢复审计保存、原对话等待和 AgentLoop／网关响应计时；确认插件排队取消此前尚未建立子运行审计，故将等待记录留在原父执行中。
- 阅读查询服务 _trace_runs 的既有 runs／agents 展开及耗时字段投影；新增等待叶项没有工具／模型步骤，不填其他阶段零值。沿现有 Vue 组件和文字状态展示，不新建图表或视觉系统。
- uv --cache-dir /private/tmp/lenbot-uv-cache run --no-sync python -m compileall -q src/len_bot/runtime/plugin_interactions.py src/len_bot/web/query_service.py 退出 0；npm run build 在原前端目录退出 0，491 个模块、1.71s。日志 /private/tmp/lenbot-plugin-slot-build.log；git diff --check 无格式错误，产物不进 Git。
- 源码查找出现 `rg: src/len_bot/web/frontend/src/components/ModelCallDetails.vue: No such file or directory (os error 2)`，随后按实际文件清单改读 TraceTimings.vue／TraceDetails.vue 的既有路径；未新增猜测组件。无本批运行失败原文。
- 无获准同版面板，实际改动页面及正常等待／取消仍待人工验收；未为验证创建服务或制造等待场景。未运行测试、夹具、断言探针、自动截图、回放、故障注入、覆盖率、依赖安装或真实调用；未读取业务库／根配置、启动服务或实发。

### 待决定与接续

1. 继续 S1 的其他锁／工作排队与失败请求耗时缺口，先核对原记录再补实际边界；不把本批插件槽位计时说成全部排队已完成。
2. S1 其他请求材料、S2 完整活动段／未登记依赖仍有源码工作；额外原生字段保存、分类保留、许可证／素材授权、精确支持组合与公开承诺待决定。
3. S6 候选／现场／远端 CI／升级／外部迁移／发布及 S7 独立使用者／作者记录仍未完成；既有 S3／S4／S5 待人工复核项保持。
4. 仅阶段性本地提交，不推送、合并、部署或实发，总体目标继续。

## 客户端解析与中断整体耗时

阶段提交：`bd90e41`。

### 当前批次

- 分支 feat/s0-product-contract，基线 4347f47，开始时工作区干净；上一阶段插件等待计时已本地提交，属于实际进展。
- S1-04：原 HTTP 尝试已有 duration_ms、成功响应已有 latency_ms，但客户端跨尝试等待及失败／取消前的整体时长未单列。沿原传输记录补一个时长，不新建计时表、监控系统或调用。

### 本批交付与核对

ModelGateway 在原调用登记后的计时起点，分别于响应解析完成及异常／取消到达时记录 transport.client_elapsed_ms，随后才进入原 shield 结算。这使跨尝试等待、客户端内部重试间隔与本地解析被同一单调时长覆盖，不把逐 HTTP 尝试求和当总值；原成功 latency_ms、调用账起止和 HTTP 时长仍保持各自口径。

新增时长不含前置准入／登记、后续结算、执行槽位等待或消息发送；失败和取消仍保留原 usage、error_type、终态及费用不确定性，不证明上游停止。原 begin／end 调用事务与传输 trace 身份不变，没有增加重试或吞错逻辑。旧记录、登记前失败及未经过此网关的入口不补数值。

ActivityView 的原调用详情单列“客户端请求至解析／中断”，使用现有 formatDurationMs；未知显示未记录，不用当前时间或调用起止反推。原查询服务已经按 call_id／scene_id 读取完整受控传输对象，本批无需另加 API 或读取路径。

- 阅读 RecordedHttpClient 的请求／响应钩子与逐尝试时长、ModelGateway 的 body／usage／响应解析与取消结算，以及 end_model_call 原事务；成功与失败均在结算前取时，结算等待不混入客户端耗时。
- 核对查询服务 model_call／_call 原传输返回及实际 ActivityView 调用面板；MessageProgress 原调用账时间仍是另一口径，未批量改写指标或当成首字延迟。
- 源码定位出现 `rg: src/len_bot/web/frontend/src/views/CallsView.vue: IO error for operation on src/len_bot/web/frontend/src/views/CallsView.vue: No such file or directory (os error 2)`；随后按组件引用定位 ActivityView.vue，未新增猜测页面。
- uv --cache-dir /private/tmp/lenbot-uv-cache run --no-sync python -m compileall -q src/len_bot/cognition/gateway.py 退出 0；原前端目录 npm run build 退出 0，491 个模块、1.65s，日志 /private/tmp/lenbot-client-elapsed-build.log。git diff --check 无格式错误，产物不进 Git。
- 无获准同版面板或本批实际失败／取消记录，页面和真实时长待人工验收；未启动服务、故意中断请求或制造错误。未运行测试、夹具、断言探针、截图、回放、故障注入、覆盖率、依赖安装或真实模型／平台调用；未读取业务库／根配置或实发。

### 待决定与接续

1. 继续 S1 的其他锁／工作排队和登记前阶段缺口，不把网关客户端时长当作所有等待已覆盖；请求材料与完整时间线仍待完善和同版人工观察。
2. S2 完整活动段／未登记依赖仍有源码工作；额外原生字段保存、分类期限、许可证／素材授权、精确支持组合和公开承诺待决定。
3. S6 的候选／现场／远端 CI／升级／外部迁移／发布及 S7 独立使用者／作者记录未完成，原 S3／S4／S5 待人工复核项保持。
4. 仅阶段性本地提交，不推送、合并、部署或实发，总体目标继续。

## 请求准备失败与取消计时

阶段提交：`2618d5a`。

### 当前批次

- 分支 feat/s0-product-contract，基线 bd90e41，开始时工作区干净；上一阶段客户端整体耗时已本地提交，属于实际进展。
- S1-04：AgentLoop 准备失败／取消早于原 steps 建立时没有准备耗时。本批沿原审计补退出记录，不新增模型步骤、调用、计时表或测试路径。

### 本批交付与核对

每步准备起点仍在原预算状态读取前；若循环退出时本步尚未建立 step，且准备确实已开始，则记录 request_preparation_failure 的 step、state、elapsed_ms、error_type。预算状态、装配、Hook、最终核对与额度取得期间的失败／取消归此记录，正常步骤、网关／工具执行失败仍沿原路径。没有进入准备时不补零，准备失败不添加假的模型 call_id 或 steps，也不修改次数。

原循环初始化重置本次准备失败字段，与既有步骤和终结原因重置同步，避免复用审计对象时把上次失败当成本次。已持久化的历史审计不回填或修改。只有导致本次循环退出的准备需要这一个字段，故不新增列表或独立状态机。

查询服务沿原执行段投影，TraceTimings 单列准备失败／取消的步骤、耗时和异常类型；明确尚未进入本步主模型请求，但准备可能已经调用压缩等嵌套模型，不据此判断无调用／费用。字段仍是阶段时长，不相加成总延迟。

- 阅读 AgentLoop 的准备、步骤建立、调用计数、外层异常和复用审计初始化；核对原 steps 的工作呈现、调用关联与页面消费者，不改变这些消费者的步骤意义。
- 同时阅读工作 _run_scene／_learn_scene 的 _slots 等待；它们在运行审计建立前发生，仍是后续待处理缺口，本批未改工作排序或提前认领任务。
- uv --cache-dir /private/tmp/lenbot-uv-cache run --no-sync python -m compileall -q src/len_bot/cognition/agent_loop.py src/len_bot/web/query_service.py 退出 0；原前端目录 npm run build 退出 0，491 个模块、1.72s，日志 /private/tmp/lenbot-preparation-failure-build.log。git diff --check 无格式错误，产物不进 Git。
- 无本批源码定位或构建失败原文，也无真实准备失败／取消记录。没有获准同版面板，实际改动页面及自然失败行为保持待人工验收；未运行服务、测试、夹具、断言探针、自动截图、回放、故障注入、覆盖率或依赖安装，未读取真实配置／业务库或实发。

### 待决定与接续

1. 继续 S1 其他锁／工作排队计时及请求材料缺口，不将本次循环内准备记录扩大成全部登记前阶段已覆盖。
2. S2 完整活动段／未登记依赖仍有源码工作；额外原生字段保存、分类期限、许可证／素材授权、精确支持组合与公开承诺待决定。
3. S6 候选／现场／远端 CI／升级／外部迁移／发布及 S7 独立使用者／作者记录仍未完成；原 S3／S4／S5 待人工复核项保持。
4. 仅阶段性本地提交，不推送、合并、部署或实发，总体目标继续。

## 信息工作槽位等待记录

阶段提交：`7a5db6e`。

### 当前批次

- 分支 feat/s0-product-contract，基线 2618d5a，开始时工作区干净；上一阶段请求准备退出计时已提交，属于实际进展。其后两次自动续转在任何工具命令前中断；本批重新核对 Git，未沿不存在的进程或半成品重启工作。
- S1-04：信息工作在 _slots 前还没有运行审计，等待取消无处留存。仅在现有 traces 按原 job 身份增加工作等待种类，不新增表、工作状态、提前认领或测试路径。

### 本批交付与核对

InformationJobRunner 在原 list_jobs 选定 processing 工作之后、共享槽位之前开始单调计时；取得时写 agent_job_wait（acquired），取得前取消或异常写 cancelled／failed。以当时原 job_id 和 revision 保存来源，不将版本变化后实际执行的结论倒填旧选择；_active_jobs、_run_job、工作预算和 Gate 均维持原顺序。取得槽位只说明容量可用，不代表工作已处理。

既有 agent_job 审计在 _run_job 内更晚才建立，无法保存排队取消，因此复用 traces 表增加这个有原工作关联的种类，而非把等待伪造成执行步骤。取得后工作异常不会改写等待为失败；等待记录写入失败时不静默继续工作。取消时若审计写入也失败，留下原日志并仍传播原取消，不按已取得处理。

查询投影单列 work_slot_wait_ms／state，页面沿已有 TraceTimings 显示工作槽位耗时和取得／取消／失败；与对话槽位分开。选中工作前的目录读取、同群前一工作、共享槽位内的方法维护、模型与外部执行不包含在此计时；不能当成从工作创建到执行的总排队。旧轨迹不回填。

- 阅读 job_runner._run_scene／_learn_scene 与 _run_job 的审计初始化时点、list_jobs 返回字段、EventStore.save_trace 写入语义、原 _trace 投影和 TraceDetails／TraceTimings 页面入口。
- uv --cache-dir /private/tmp/lenbot-uv-cache run --no-sync python -m compileall -q src/len_bot/runtime/job_runner.py src/len_bot/web/query_service.py 退出 0；原前端目录 npm run build 退出 0，491 个模块、1.75s，日志 /private/tmp/lenbot-job-slot-build.log。git diff --check 无格式错误，构建产物不进 Git。
- 无获准同版面板、真实工作排队或取消记录；实际轨迹和页面仍待人工验收。未运行测试、夹具、断言探针、自动截图、回放、故障注入、覆盖率、依赖安装、服务或真实模型／平台调用；未读取真实配置／业务库或实发。
- 本批无源码定位、编译构建或实际业务失败原文。

### 待决定与接续

1. S1-04 还缺方法维护自身等待、选中工作之前与其他锁等待的区分；S1 请求材料与审计时间线同版观察仍不完整，继续逐一核对真实路径。
2. S2 完整活动段／未登记依赖仍有源码工作；额外原生字段保存、分类期限、许可证／素材授权、精确支持组合和公开承诺待决定。
3. S6 候选／现场／远端 CI／升级／外部迁移／发布及 S7 独立使用者／作者记录未完成；原 S3／S4／S5 待人工复核项保持。
4. 仅阶段性本地提交，不推送、合并、部署或实发，总体目标继续。

## 场景级方法维护槽位等待

阶段提交：`5241384`。

### 当前批次

- 分支 feat/s0-product-contract，基线 7a5db6e，开始时工作区干净；上一阶段信息工作槽位等待已提交，属于实际进展。
- S1-04：同一工作槽位还服务方法维护，原 _learn_scene 在选定具体候选前等待，不能将它误归给来源工作。本批只补场景级等待记录，不增加候选身份、预算或新状态表。

### 本批交付与核对

InformationJobRunner._learn_scene 在进入原共享 _slots 前开始单调计时，取得后保存 skill_maintenance_wait 的 acquired 状态；取得前取消／失败分别记录 cancelled／failed。审计只属于当前场景调度，ref_id 为 scene_id，没有猜测或临时创建 candidate_id／job_id。原 maintain_candidates 循环、脏标记、预算、请求、实际采用与资源释放逻辑保持不变。取得槽位不证明有候选被处理。

等待审计沿原 traces 表和保存方法，不新增表、恢复路径或调用。记录失败不会被当成功继续维护；取消时若记录也失败，日志留下失败，原取消仍传播。取得槽位后的维护错误不记为等待失败。新时长不包括之后的候选筛选、模型调用、工作预算或结算。

查询服务在原阶段投影中加入 maintenance_slot_wait_ms／state，已有阶段组件以独立文案展示，并在轨迹类型目录标为“方法维护槽位等待”。这不把场景级等待接到具体候选或同轮用户消息，也不把几个时间求和成总延迟。

- 阅读 _start_learning／_learn_scene、maintain_candidates 的候选选取顺序、原 _slots 共享、EventStore.save_trace、阶段查询和界面路径；确认选取候选确实在取得槽位之后。
- uv --cache-dir /private/tmp/lenbot-uv-cache run --no-sync python -m compileall -q src/len_bot/runtime/job_runner.py src/len_bot/web/query_service.py 退出 0；原前端目录 npm run build 退出 0，491 个模块、1.70s，日志 /private/tmp/lenbot-maintenance-slot-build.log。git diff --check 无格式错误，构建产物不进 Git。
- 无获准同版面板或自然发生的等待／取消；实际轨迹、页面、窄屏与键盘交互待人工验收。未运行测试、夹具、断言探针、自动截图、回放、故障注入、覆盖率、依赖安装、服务或真实模型／平台调用；未读取真实配置／业务库或实发。
- 本批无源码定位、编译构建或实际业务失败原文。静态计时实现不能证明时延改善、费用变化或业务完成。

### 待决定与接续

1. S1-04 还缺信息工作选中前及部分锁等待的可观察范围；没有原身份时不造 per-job 排队记录。接续 S2 可独立于活动原生字段决定的来源与未完事项核对，避免只为补指标无限扩大审计。
2. S2 完整活动段／未登记依赖仍有源码工作；额外原生字段保存、分类期限、许可证／素材授权、精确支持组合与公开承诺仍待决定。
3. S6 候选／现场／远端 CI／升级／外部迁移／发布及 S7 独立使用者／作者记录未完成；原 S3／S4／S5 待人工复核项保持。
4. 仅阶段性本地提交，不推送、合并、部署或实发，总体目标继续。

## 插件结果模式的固定通知版本

阶段提交：`2f8c14d`。

### 当前批次

- 分支 feat/s0-product-contract，基线 5241384，开始时工作区干净；上一阶段方法维护槽位等待已本地提交，属于实际进展。
- S1-02：独立插件 result_only 的源码固定通知缺少当次请求组件定位。只复用原消息与 _PromptComponent，动态输出 Schema 仍留在明确缺档范围。

### 本批交付与核对

plugin_interactions 在原 result_only developer 消息上标记 plugin_agent.result_only_notice，修订 1，内容直接引用既有 RESULT_ONLY_NOTICE 常量；不复制另一份提示文本。context.model_messages 在原最终裁剪后将 _prompt_components 交给既有 _RequestLocation，剥离私有字段；ModelGateway 原 request_record 比较最终内容，匹配才在原调用事务内保存固定片段。合法 Hook 修改或移除片段不能将旧声明冒充本次材料。

respond 分支不追加这条通知，也不因此扩大插件发送资格。动态插件指令、return_result 的插件输出 Schema、模型回复和媒体字节不进入新增快照；不替旧调用补造版本，组件变更后由维护者递增修订。无表、协议、配置、预算、工具或行为路径变化。

- 阅读插件 Agent 的 input_mode、output_mode、result_only 通知、prepare_request／Context.model_messages、请求记录对私有组件的处理和网关原调用登记边界。只有固定常量进入快照；该工作不涉及用户尚未确认的原生续接字段保存。
- uv --cache-dir /private/tmp/lenbot-uv-cache run --no-sync python -m compileall -q src/len_bot/runtime/plugin_interactions.py 退出 0；git diff --check 无格式错误。无前端改动，未重复构建；无获准同版环境，实际请求记录与页面仍待人工验收。
- 无本批源码定位、编译或实际业务失败原文。未运行测试、夹具、断言探针、自动截图、回放、故障注入、覆盖率、依赖安装、服务或真实模型／平台调用；未读取真实配置／业务库或实发。

### 待决定与接续

1. S1-02 仍有动态定义、其他提示／材料与完整请求可还原范围的缺口，静态片段不能替代它们；S1-04 工作选中前、部分锁与同版体验仍需核对。
2. S2 完整活动段／未登记依赖与 S2-03 压缩交接仍未完成；额外原生字段保存边界、分类保留、许可证／素材授权、精确支持组合及公开承诺待决定，不据本批固定提示推导决定。
3. S6 候选／现场／远端 CI／升级／外部迁移／发布及 S7 独立使用者／作者记录未完成，原 S3／S4／S5 待人工复核项保持。
4. 仅阶段性本地提交，不推送、合并、部署或实发，总体目标继续。

## 插件子调用串行锁等待

阶段提交：`e3c76f9`。

### 当前批次

- 分支 feat/s0-product-contract，基线 2f8c14d，开始时工作区干净；上一阶段插件固定通知已本地提交，属于实际进展。
- S1-04：同一父执行的插件 Agent 先等待原 agent_lock，再可能等待共享模型槽位；原审计只记后者，串行锁等待取消缺档。复用父执行 audit 与阶段投影，不新增持久表或通用排队层。

### 本批交付与核对

_plugin_interactions._agent_lock 沿原 contextmanager 包裹既有 execution.agent_lock：每次等待向父 audit.agent_lock_waits 追加 waiting 条目，取得更新 acquired 与单调时长；取得前取消／失败更新 cancelled／failed 与异常类型后仍传播。锁体内的模型、工具、提交异常不改写锁等待状态；原锁获取、持有与释放顺序不变。子运行审计建立在锁之后，故早于该时点的取消只能归父执行，不能生成假子调用。

查询服务沿原父子运行收集新增等待叶项，TraceTimings 显示插件子调用串行锁的真实等待及状态，与共享模型槽位分开。两段可能属于同一插件调用，却不代表整个调用的独占总时长；未记录不补零，不给等锁过程补模型 call_id 或请求数。不存在相应业务来源时不根据等待值推断权限或送达。

- 阅读 run_agent 原 budget、agent_lock、_model_slot、子 audit 构造与父审计保存，以及 _trace_runs 嵌套展开和现有阶段组件；只修改实际锁持有处与现有只读投影。
- uv --cache-dir /private/tmp/lenbot-uv-cache run --no-sync python -m compileall -q src/len_bot/runtime/plugin_interactions.py src/len_bot/web/query_service.py 退出 0；原前端目录 npm run build 退出 0，491 个模块、1.85s，日志 /private/tmp/lenbot-agent-lock-build.log。git diff --check 无格式错误，构建产物不进 Git。
- 无获准同版面板，也未在本批实际等待或取消；代码与构建不当运行验收。未运行测试、夹具、断言探针、自动截图、回放、故障注入、覆盖率、依赖安装、服务或真实模型／平台调用；未读取真实配置／业务库或实发。
- 本批无源码定位、编译构建或实际业务失败原文。

### 待决定与接续

1. S1-04 仍有工作选中前与其他所属锁等待以及同版人工证据缺口；S1-02 动态定义／材料范围仍需来源事实。不要无限加时钟来宣称完整互动延迟。
2. S2 当前 source_window_only，完整活动交换与 S2-03 压缩交接未完成；额外原生字段保存、分类期限、许可证／素材授权、精确支持组合与公开承诺仍待维护者决定。
3. S6 候选／现场／远端 CI／升级／外部迁移／发布及 S7 独立使用者／作者记录未完成，原 S3／S4／S5 待人工复核项保持。
4. 仅阶段性本地提交，不推送、合并、部署或实发，总体目标继续。

## 上段资料定位的逐请求清单

阶段提交：`c282533`。

### 当前批次

- 分支 feat/s0-product-contract，基线 7b999cf，开始时工作区干净；上一阶段已存资料定位续读已本地提交。
- S1-02：上段资料目录虽有可见 R 编号，原逐请求清单只记该消息的角色、类别和长度。段状态下一轮会改写，不能用当前段或整轮最终引用表反推某次请求实际保留了哪些 R。复用原请求位置旁路，只补定位字段，不复制动态正文或建立新表。

### 本批交付与核对

在 saved_result_locators 消息保留仅本次装配所需的私有目录文字及 R→资料 ID 对照；最终请求裁剪和合法 Hook 后，只有目录正文完全未变且未省略时，才把可用 R 放入本轮可解析表。进入模型网关前由原 model_messages 把私有旁路移走，并在现有 RequestLocation 中给出 retained、omitted 或 changed_after_declaration。格式 v4 的原调用登记逐消息只记录保留的 R、原工具名、装配时可用性与可用项的 result_id；省略／改变则无编号清单。旧格式不补造，资料正文、调用参数、像素和旧阅读资格不复制。

调用详情页面沿原滚动表显示上段编号与旧资料链接，并明确这是目录装配时位置，不代表正文已读、当前仍有效或模型已收到。旧 v1—v3 页仍按原格式显示，v4 仍可看原固定提示、工具定义和图像块；未改变其他状态投影或权限。该版本提升的是逐请求定位解释范围，不是完整请求还原或 S2 原生交换续接。

- 阅读 Segment 目录构造、AgentLoop 最终 Hook／裁剪、Context.model_messages、ModelGateway.prepare_request_record、原调用详情组件以及工具资料二次读取边界；改动仅在目录声明、原清单结构和现有展示组件。
- uv --cache-dir /private/tmp/lenbot-uv-cache run --no-sync python -m compileall -q src/len_bot/cognition/context.py src/len_bot/cognition/request_record.py src/len_bot/cognition/social_core.py 退出 0；原前端目录 npm run build 退出 0，491 个模块、最终 1.71s，日志 /private/tmp/lenbot-request-locator-build.log；git diff --check 无格式错误。构建产物不进 Git。
- 没有获准的同版服务与面板，实际请求记录、真实旧资料续读、列表页面与窄屏仍待人工验收；编译和构建不当作运行通过。未运行测试、夹具、断言探针、自动截图、回放、故障注入、覆盖率、依赖安装、服务或真实模型／平台调用；未读取真实配置／业务库或实发。
- 本批无源码定位、编译构建或实际业务失败原文。

### 待决定与接续

1. S1-02 动态插件定义、其他动态材料和完整请求可还原范围仍缺；S1-04 尚有未登记的等待，S1-03／05／06 的同版人工观察未完成。不以格式 v4 视为完整请求存档。
2. S2 原生交换、固定材料基线和压缩交接仍未完成；额外原生字段及必要回复片段保存边界待维护者确认。本批不新增这些字段。
3. 已选 Linux 容器群报告与文件交付方向、私下报告邮箱为 Git 作者邮箱；精确支持版本与发布承诺、分类保留期限及许可证／素材授权未定。S3／S4／S5 待同版人工验收，S6／S7 现场与外部记录仍需后续授权。
4. 仅阶段性本地提交，不推送、合并、部署或实发，总体目标继续。

## 当前身份与阶段关系

阶段提交：`3a822ea`。

### 当前批次

- 分支 feat/s0-product-contract，基线 9d9daf1，开始时工作区干净；上一阶段 S0-01／S0-05 基线索引与唯一文档入口已本地转待复核。
- S1-01：原身份关系与只读关联查询已有，但当前架构合同没有一张可直接使用的身份／阶段表。复用现有字段与查询，不新增 episode 表、追踪 ID 或第二套事件系统。

### 本批交付与核对

在架构主链下增加现有 event/source、episode/checkpoint、call/tool/result、job/revision、action/commit 和回执／平台 ID 的归属表。分别说明真实连接与不能推断的状态：来源覆盖不是结论证据，调用登记不证明发送，工具结果保存不授已读，提交／入队不等于送达，旧工作版本不能取得当前成果资格。answer_basis 仍是逐答复依据，不从 source/covers 自动产生；会话段不替代执行、工作或行动身份。表只总结已有源码和查询，不写新业务状态。

源码核对：普通 episode 在 Runtime 生成或等待恢复沿用；ModelGateway 原 model_calls 取得 call_id；原生 tool_call_id 随观察保存；Gate 分配 action_ids 并同序写提交 payload，ActionQueue 的发送尝试和最终事件回指 action_id，sent 还需对应平台 message_id／file_id。RuntimeQueryService.relations 按已存身份查询与扩展，不使用时间相近或正文相似。旧对象缺身份时不猜造关系。上述是静态路径核对，尚无同版真实关系链或页面操作记录，S1-01 仅转待复核。

- 阅读 Runtime 普通轮次、Actor 提交、Gateway 调用登记、工具观察、Gate 行动分配、ActionQueue 回执和 RuntimeQueryService.relations 的真实路径；对照原 S1 基线报告，不沿其旧行号推断新代码。
- 本批仅架构合同与路线状态文档改变，git diff --check 无格式错误；未修改 Python 或前端，不重复编译构建。没有同版服务，关联查询与回执待人工验收。
- 初次编辑时工具返回 `apply_patch verification failed: Failed to find expected lines in /Users/len5010/code/len_bot/docs/architecture.md`；改用实际小节标题定位后完成。该错误是文档补丁上下文不匹配，不是业务运行失败。本批无编译构建或实际业务失败原文。
- 未运行测试、夹具、断言探针、自动截图、回放、故障注入、覆盖率、依赖安装、服务、模型／平台调用或实发；未读取真实配置和业务库。

### 待决定与接续

1. S1-01 的同版普通回复／确定性插件／工作交付关联仍待获准人工观察；S1-02 动态定义与材料、S1-04 未登记等待、S1-06 单条可读时间线仍未全面验收，不把表格当实链通过。
2. S2 完整原生交换、固定材料基线与压缩交接有源码缺口；额外原生字段及必要回复片段保存边界待维护者答复。S0-02／04／06 的保留、授权、精确支持组合与公开承诺未决定。
3. S3／S4／S5 的同版现场、S5-03 分类期限、S6 候选与远端 CI／升级、S7 外部使用者／作者闭环未完成；没有生产或发布授权。
4. 仅阶段性本地提交，不推送、合并、部署或实发，总体目标继续。

## 工作目录读取与选择耗时

阶段提交：`d545976`。

### 当前批次

- 分支 feat/s0-product-contract，基线 a7dfebe，开始时工作区干净；上一阶段 S2-02／05 旧插件资料当前归属复核已本地提交，仍待同版人工验收。
- S1-04：工作执行槽位等待已有按 job 记录；其前的 `list_jobs(scene_id)` 读取、解码及选取发生在 job 执行审计开始前，不能从既有槽位等待或运行总耗时反推。只在选中真实 processing 工作后随原 `agent_job_wait` 登记场景调度阶段，不新增计时表、trace 种类或假工作身份。

### 本批交付与核对

`InformationJobRunner._run_scene` 从本次场景目录读取前计时，选出首个 processing 工作后记 `work_selection_ms`，与之后独立计量的 `work_slot_wait_ms` 同存原等待 trace。取得槽位及取得前取消／失败均保存此已完成的选择阶段；没有候选、目录读取中断或失败时没有可归属 job，不写假记录或零值。原 job_id／revision、槽位获取、预算、执行、提交和发送边界不变。

`RuntimeQueryService._trace` 沿原只读计时投影暴露该字段，TraceTimings 在原等待段单列“本群工作目录读取与选择”，说明它不代表所选工作创建后的总排队时长。旧记录缺字段仍为未记录；不将目录、槽位、同群前一工作和模型耗时相加称总延迟。

- 阅读 `_run_scene` 的目录筛选、原 `agent_job_wait` 持久记录、查询投影和 TraceDetails／MessageProgress 对阶段组件的引用。首次在前端工作目录调用 Python 编译，输出原文 `Can't list 'src/len_bot/runtime/job_runner.py'`、`Can't list 'src/len_bot/web/query_service.py'`，尽管命令退出 0，不能算编译通过；随后在仓库根目录按真实路径重跑 uv compileall 两文件，退出 0。
- 前端目录 `npm run build` 退出 0，491 个模块、1.81 秒，日志 `/private/tmp/lenbot-work-selection-build.log`；`git diff --check` 退出 0。构建产物未纳入 Git。源码核对组件已被实际轨迹详情引用；没有获准同版面板，页面显示、真实目录等待及取消分支待人工观察，不把静态路径或构建当运行通过。
- 未运行测试、夹具、断言探针、自动截图、回放、故障注入、覆盖率、依赖安装、服务、模型／平台调用或实发；未读取真实配置和业务库。本批没有业务运行失败原文。

### 待决定与接续

1. S1-04 仍缺其他所属锁等待、没有候选时的场景级排队判读和同版用量／延迟验收；不为凑齐单一总耗时追加无归属计数或把缺值当零。S1-02 动态定义／材料与 S1-06 实链页面也未全面验收。
2. S2 仍为 `source_window_only` 增量，原生续接字段与必要回复片段的保存边界待维护者答复；完整压缩交接、旧摘要未登记依赖与自由表达仍有缺口。S3／S4／S5 主要待同版现场，尤其首选 Linux 容器群报告及文件交付回执未验收。
3. S5-03 分类期限、许可证／素材授权、精确支持版本、S6 候选与远端 CI／升级、S7 外部使用者／作者闭环仍未完成；没有生产或发布授权。
4. 仅阶段性本地提交，不推送、合并、部署或实发，总体目标继续。

## 逐请求历史摘要批次位置

阶段提交：`1dd3e2a`。

### 当前批次

- 分支 feat/s0-product-contract，基线 d545976，开始时工作区干净；上一阶段 S1-04 工作目录读取与选择耗时已本地提交，实际页面和等待仍待同版人工观察。
- S1-02：历史摘要会进入普通对话请求，但原调用清单只有 `history_summary` 消息类别和长度，无法回答该次最终保留了哪个批次、何版本与哪个覆盖范围。复用现有摘要装配和请求位置旁路，只登记身份／版本／范围，不复制摘要正文或把调用记录当活动段。

### 本批交付与核对

历史摘要装配时在私有旁路保留当次已通过本群范围、来源可用性及容量选择的批次 ID、generation_version 和 rowid／offset 覆盖。最终请求若仍保留完全相同的摘要目录，调用记录格式 v5 才登记这些项目；容量省略或来源失效／合法改写时分别登记 omitted、changed_after_declaration，不把已失效原目录当作实际输入。没有批次但保留覆盖目录时明确为空，旧 v1—v4 缺字段仍为未登记。原批次正文、原话及模型请求正文不复制，已有已读和历史修订核对不变。

调用详情沿原消息表展示保留批次的 ID、版本与范围，并解释定位不等于已读原话或之后仍有效。原 v1—v4 页面继续按原格式显示；v5 保持固定提示、工具定义、上段 R 定位和图像块的已有展示。不新建表、摘要缓存、恢复路径或保留期限。

- 源码核对 list_history_batches 的同群、已完成、来源可用与截点筛选，摘要最终失效替换、Context.model_messages 私有字段清理、Gateway.prepare_request_record 和原调用详情组件。uv compileall 编译 context.py、request_record.py、retrieval.py 退出 0；前端目录 npm run build 退出 0，491 个模块、1.84 秒，日志 `/private/tmp/lenbot-summary-refs-build.log`；git diff --check 退出 0，构建产物不入 Git。
- 没有获准同版服务，实际摘要失效、请求登记、列表滚动与窄屏页面待人工验收；编译、构建及源码路径核对不等于运行通过。本批无命令或业务失败原文。未运行测试、夹具、断言探针、自动截图、回放、故障注入、覆盖率、依赖安装、服务、模型／平台调用或实发；未读取真实配置和业务库。

### 待决定与接续

1. S1-02 仍缺动态插件完整定义、其他提示与材料来源；本次 v5 不能逐字还原请求，也不是 S2 固定摘要基线。S1-04 其他锁等待、S1-06 实链与同版页面仍待核对。
2. S2 仍为 `source_window_only` 增量，完整原生交换、活动摘要基线与压缩交接未完成；额外原生字段及必要回复片段保存边界待维护者答复。S3／S4／S5 现场尤其 Linux 容器群报告和文件交付仍待人工证据。
3. S5-03 分类期限、许可证／素材授权、精确支持版本、S6 候选及远端 CI／升级、S7 外部使用者／作者闭环仍未完成；没有生产或发布授权。
4. 仅阶段性本地提交，不推送、合并、部署或实发，总体目标继续。

## 原话与历史维护批次的只读关联

阶段提交：`7488f73`。

### 当前批次

- 分支 feat/s0-product-contract，基线 1dd3e2a，开始时工作区干净；上一阶段 S1-02 逐请求历史摘要批次版本／范围已本地提交，实际请求登记和页面待同版人工验收。
- S1-06／S2-05：原 `relations(scene_id,event_id=...)` 的 batch_id 专用分支可从批次回看来源，但从一条原话进入普通关联时 `batches` 固定为空。运营者无法沿原事件身份定位它参加过的维护批次。仅反查 history_batches 已保存的 source_event_ids_json，不以范围重叠、正文相似或时间相近补关系。

### 本批交付与核对

原只读 `relations` 在事件根入口按同群、精确 source_event_id 取最多 50 个历史批次，登记截断；返回 ID、状态、generation_version、rowid／offset 范围、即时来源可用性和 offset_basis，不返回摘要正文。其他工作／轮次／行动根入口不猜间接批次；原 batch_id 入口及其来源、调用、维护轨迹行为不变。失败或来源失效的旧批次也可用于审计，但不能作为新请求材料。

单条消息阶段页在既有六阶段之外折叠显示这些直接来源批次，状态与版本分开；参与批次不表示摘要完成、进入模型请求、认识采用或原话已读。事件诊断格式 2 沿同一关联限额增加批次定位，不复制摘要正文，继续保留读取起止、截断和非原子快照说明。新增是只读关系和现有页面投影，没有表、索引、业务状态或重试入口。

- 阅读 history_batches 的原 source_event_ids_json、同群查询与来源可用性条件、`RuntimeQueryService.relations` 的批次专用分支和通用上限、MessageProgress 的原身份收窄，以及事件诊断下载字段。uv compileall 编译 query_service.py 退出 0；前端目录 npm run build 最终退出 0，491 个模块、1.64 秒，日志 `/private/tmp/lenbot-source-history-build.log`；git diff --check 退出 0，构建产物不入 Git。
- 当前没有获准的同版服务与面板，实际 SQL 时延、列表截断、原话关联、诊断文件、手机布局仍待人工正常操作；源码路径和构建不当业务通过。本批无命令或业务失败原文。未运行测试、夹具、断言探针、自动截图、回放、故障注入、覆盖率、依赖安装、服务、模型／平台调用或实发；未读取真实配置和业务库。

### 待决定与接续

1. S1-06 源码已能从原话读处理、调用、工作、行动、回执和直接历史维护来源，但 S1-02 动态定义／材料、S1-04 剩余阶段及同版实链／页面仍未全面复核；保持实施中，不把一条关系扩展判为整个审计时间线已验收。
2. S2 仍为 `source_window_only` 增量，原生交换、活动摘要基线与压缩交接未完成；必要原生回复片段保存边界待维护者答复。S3／S4／S5 现场尤其 Linux 容器群报告和文件交付仍待人工证据。
3. S5-03 分类期限、许可证／素材授权、精确支持版本、S6 候选及远端 CI／升级、S7 外部使用者／作者闭环仍未完成；没有生产或发布授权。
4. 仅阶段性本地提交，不推送、合并、部署或实发，总体目标继续。

## 单条消息审计入口源码复核

阶段提交：`f45b151`。

### 当前批次

- 分支 `feat/s0-product-contract`，基线 `7488f73`，开始时工作区干净；上一批原话直达历史维护来源批次已本地提交，实际查询与页面待同版人工核对。
- S1-06：按任务卡复核单条群消息审计入口。原 `MessageProgress` 已沿明确来源与行动身份收窄六阶段、轮次调用、逐次请求材料、工作、提交与回执；`SceneInspector` 和事件详情保留原始记录；`EventDiagnostics` 从原鉴权路由读取并预览同份只读材料后下载，不再次执行。缺失关联不反推无执行。

### 本批交付与核对

补齐两个源头消息可读性缺口：若已取得关联但没有精确以该原话为来源的历史维护批次，明确提示“本次关联未找到”，不把空列表解释成全群无维护；关联截断提示将新增批次类别显示为“历史批次”，不泄露内部键名。复用原查询与组件，没有新状态、接口、持久化或业务副作用。

源码核对原 `/events/{event_id}/diagnostics` 路由的登录约束和 `no-store`、查询服务的显式字段投影、前端预览与下载同一份记录，以及群消息／回执的身份收窄。按 S1-06 自身源码交付转入待复核，不宣称依赖的 S1-02 请求材料清单与 S1-04 阶段计时已完成，也不将 S1-03／05 的静态状态判读当作真实发送验收。

- 前端目录 `npm run build` 退出 0，491 个模块、1.68 秒，日志 `/private/tmp/lenbot-s1-timeline-review-build.log`；`git diff --check` 退出 0。只改前端和文档，未重复 Python 编译，构建产物不入 Git。
- 首次补丁因行内空格上下文与实际文件不同，返回 `apply_patch verification failed: Failed to find expected lines in /Users/len5010/code/len_bot/src/len_bot/web/frontend/src/components/SceneInspector.vue`；读取真实行后补丁成功。不是业务运行失败。整理文档时一次同文件删除与新建合并补丁因工具不接受重复目标而失败，改为单次文件写入，无业务影响。
- 没有获准同版服务与面板，实际群消息、调用归属、缺档提示、截断、鉴权、诊断下载和窄屏布局仍待人工正常操作；源码与构建不当作业务通过。未运行测试、夹具、断言探针、自动截图、回放、故障注入、覆盖率、依赖安装、服务、模型／平台调用或实发；未读取真实配置和业务库。

### 待决定与接续

1. S1-06 待同版人工从一条群消息核对读取机会、处理、调用／依据、工作、提交／发布、回执和诊断文件；未获记录前不标已验收。S1-02 动态定义／材料及 S1-04 其他等待仍各自实施中，S1-03／05 的状态分支仍待人工复核。
2. S2 仍为 `source_window_only` 增量；原生交换、活动摘要基线与压缩交接未完成，额外原生响应与必要回复片段的保存边界待维护者答复。S3／S4／S5 现场尤其已选 Linux 容器群报告和文件交付仍待人工证据。
3. S5-03 分类期限、许可证／素材授权、精确支持版本、S6 候选与远端 CI／升级、S7 外部使用者／作者闭环仍未完成；没有生产或发布授权。
4. 仅阶段性本地提交，不推送、合并、部署或实发，总体目标继续。

## 本群发布串行锁等待

阶段提交：`0d4dfb0`。

### 当前批次

- 分支 `feat/s0-product-contract`，基线 `f45b151`，开始时工作区干净；上一批 S1-06 单条消息审计源码已转待复核，尚无同版页面或业务链验收。
- S1-04：原 `timings_ms.publication` 包含本群按提交顺序发布前等待与准备，却不能解释具体是否卡在发布串行锁。沿原 `RuntimeGate.publish_committed` 和持久提交身份，单列申请该锁至取得／取消／失败的已耗时间；不借此补造发送状态或总延迟。

### 本批交付与核对

`PublicationRecord` 在原每群发布串行锁申请开始后计时；取得后记 `acquired`，取得前取消或失败记相应状态和已耗毫秒，原异常、已提交行动身份、队列和发布状态不变。已取得后发生的准备／调度／入队问题仍由原发布阶段与回执解释，不倒写成锁等待失败。即时取得也可能记录正值，不能据此断言实际争用。

只读轨迹投影从原对话 checkpoint 或插件执行 `commits` 的各次持久提交记录逐项提取等待，旧记录返回空列表；阶段页在原段落内列出每次等待与对应提交，不与外层 `publication` 累计或发送时长相加。未在业务库新增列、表、trace 类型或配置。适用原直接和分阶段提交路径，不对未形成提交的候选生成锁计时。

- 源码核对 `publish_committed` 的等待／异常传播、对话 checkpoint 发布后刷新、插件提交审计保存、查询服务原轨迹分段与详情页 `TraceTimings` 的使用路径。`uv --cache-dir /private/tmp/lenbot-uv-cache run --no-sync python -m compileall -q` 编译 `gate.py`、`query_service.py` 退出 0；前端目录 `npm run build` 退出 0，491 个模块、1.67 秒，日志 `/private/tmp/lenbot-publication-lock-build.log`；构建产物不入 Git。
- 无同版获准服务与面板，实际锁争用、取得前取消、轨迹投影及页面显示待人工正常操作；编译、构建和源码核对不当作业务通过。本批没有命令或业务失败原文。未运行测试、夹具、断言探针、自动截图、回放、故障注入、覆盖率、依赖安装、服务、模型／平台调用或实发；未读取真实配置和业务库。

### 待决定与接续

1. S1-04 仍未形成完整无重叠总延迟：其他等待、未选定候选和同版用量／计时仍待复核。原缓存率、覆盖、请求分组、工作与维护计时继续按各自已存事实解释，不把本批锁计时当任务完成。
2. S1-02 动态定义／材料仍缺；S1-06 已交付源码待同版实链与页面复核。S2 仅 `source_window_only`，原生交换、摘要基线与压缩交接未完成，额外字段保存边界待维护者答复。
3. 已选 Linux 容器群报告和文件交付仍需正常现场回执；S5-03 分类期限、许可证／素材授权、精确支持版本、S6 远端 CI／升级、S7 外部使用者／作者闭环仍未完成。没有生产或发布授权。
4. 仅阶段性本地提交，不推送、合并、部署或实发，总体目标继续。

## 普通工作固定提示依据

阶段提交：`147fc31`。

### 当前批次

- 分支 `feat/s0-product-contract`，基线 `0d4dfb0`，开始时工作区干净；上一批 S1-04 发布串行锁等待已本地提交，实际争用、取消与页面仍待同版人工观察。
- S1-02：普通工作每次最终请求中的固定 system 合同原来只有角色和长度，没有当次固定文案的修订依据；直接从现在的源码回填旧调用会把之后改过的合同误当原文。仅对当前声明的固定工作合同及公共研究固定补充句登记原 `_PromptComponent`，不复制目标、来源、资料或整个原生请求。

### 本批交付与核对

`InformationJobRunner._context` 在组装两个固定片段处分别声明 `work.contract` 和可选 `work.public_research_notice`，均为修订 1。声明通过该私有方法的返回值交给原工作 `finalize_request`，只附在最终发送副本的首条 system 消息；不把私有定位写进工作 checkpoint、工具观察、容量估算或供应商请求。原请求登记逐片段核对最终文本；被合法改动就标记 `changed_after_declaration`，不保存旧片段冒充实际内容。无公共研究时不登记对应通知，动态 work_facts 与目标／约束不进入快照。

本批只增加请求记录已有 `prompt_components` 列表的内容，不改格式号、表、工作权限、模型绑定、工具定义或出站消息。旧格式与旧 v5 调用无此组件时仍表示未登记，不能据现在源码推断其当时版本；这也不是 S2 完整活动段或原生交换续接。

- 源码核对 `_context` 仅由普通工作执行路径调用、恢复检查点保留当前首两条消息、`finalize_request` 在图片窗口与合法 Hook 后复制请求、`prepare_request_record` 逐片段比对并在传输前移除私有旁路，以及调用详情现有固定组件展开。`uv --cache-dir /private/tmp/lenbot-uv-cache run --no-sync python -m compileall -q src/len_bot/runtime/job_runner.py` 退出 0；未改前端，不重复构建。`git diff --check` 退出 0，构建产物不入 Git。
- 没有获准同版服务，实际普通工作与公共研究调用登记、合法修改及详情页面待人工正常操作；源码和编译不当业务通过。本批没有命令或业务失败原文。未运行测试、夹具、断言探针、自动截图、回放、故障注入、覆盖率、依赖安装、服务、模型／平台调用或实发；未读取真实配置和业务库。

### 待决定与接续

1. S1-02 仍缺动态插件完整定义、人格配置／插件指令及其他动态材料的当次版本或精确来源；本批固定片段不能逐字重建请求。S1-04 其他等待与同版用量／计时、S1-06 真实页面链仍待复核。
2. S2 仅 `source_window_only`，原生交换、活动摘要基线与压缩交接未完成，额外原生响应与必要回复片段的保存边界待维护者答复；本批审计快照不是段内恢复材料。
3. 已选 Linux 容器群报告和文件交付仍需正常现场回执；S5-03 分类期限、许可证／素材授权、精确支持版本、S6 远端 CI／升级、S7 外部使用者／作者闭环仍未完成。没有生产或发布授权。
4. 仅阶段性本地提交，不推送、合并、部署或实发，总体目标继续。

## 对话条件固定提示片段

阶段提交：`c14e9bd`。

### 当前批次

- 分支 `feat/s0-product-contract`，基线 `147fc31`，开始时工作区干净；上一批 S1-02 普通工作固定 system 合同已本地提交，实际请求登记待同版人工核对。
- 继续 S1-02：普通对话的固定合同已有逐调用快照，但条件启用的本群表情偏好固定句与插件入口固定引导句未单独登记。它们是源码固定文字；周围的人格配置、偏好选项与插件 instructions 仍为动态材料。本批只接通固定句的准确位置和修订，不增加提示版本系统。

### 本批交付与核对

`ConversationContext` 在原系统消息拼接位置分别声明 `conversation.sticker_preference_notice`、`conversation.plugin_entry_notice`，初始修订均为 1。仅条件实际成立才附加对应 `_PromptComponent`；原 `conversation.contract` 修订 2 与身份前缀保持不变。网关在最终装配与合法 Hook 后沿原 `prepare_request_record` 比对每一片段，匹配才留存当次文字和字符范围；改变时标记缺档。不复制插件动态指令、人设配置或整条请求，也不改变消息正文、工具、能力或运行配置。

调用详情原固定组件面板改为说明条件偏好提示可能单独留存，仍明确人格和插件动态指令缺档。旧 v5 无这些组件表示未登记，不回填；S2 活动段固定材料与原生续接仍未因此完成。

- 源码核对系统文字由身份前缀、固定合同、条件偏好和插件引导／动态指令依次拼接，原 `model_messages` 只将私有组件带至最终请求副本，调用登记前逐片段比较并移除。`uv --cache-dir /private/tmp/lenbot-uv-cache run --no-sync python -m compileall -q src/len_bot/cognition/context.py` 退出 0；前端目录 `npm run build` 退出 0，491 个模块、1.68 秒，日志 `/private/tmp/lenbot-conversation-notices-build.log`；`git diff --check` 退出 0，构建产物不入 Git。
- 没有获准同版服务与面板，实际条件启用、合法 Hook 改写、调用快照和详情页待人工正常操作；编译构建及源码路径不当业务通过。本批没有命令或业务失败原文。未运行测试、夹具、断言探针、自动截图、回放、故障注入、覆盖率、依赖安装、服务、模型／平台调用或实发；未读取真实配置和业务库。

### 待决定与接续

1. S1-02 仍缺插件动态完整定义、人格配置／插件 instructions 的当次版本或精确来源，以及其他动态材料；固定句不能逐字重建完整请求。S1-04 同版用量／计时与其他等待、S1-06 真实页面链仍待复核。
2. S2 仍仅 `source_window_only`；原生交换、活动摘要基线与压缩交接未完成，额外原生响应与必要回复片段的保存边界待维护者答复。这些审计快照不替代段内恢复状态。
3. Linux 容器群报告及文件交付、数据保留期限、代码与素材授权、精确支持版本、远端 CI／升级、外部使用者／作者闭环仍待相应决定或正常现场证据；无生产或发布授权。
4. 仅阶段性本地提交，不推送、合并、部署或实发，总体目标继续。

## 调用用量与阶段计时源码复核

阶段提交：`c9203c0`。

### 当前批次

- 分支 `feat/s0-product-contract`，基线 `c14e9bd`，开始时工作区干净；上一批 S1-02 对话条件固定提示片段已本地提交，实际调用和页面待同版人工复核。
- S1-04 源码审阅：本批不加新的计时器。对照任务卡检查调用账、缓存覆盖、首次／后续分类、工作预占和各阶段展示，判断现有源码是否具备转入复核的范围；不把“能显示字段”当作真实运行通过。

### 本批交付与核对

`ModelGateway` 的调用登记与响应／异常结算使用原 call_id；响应解析前已取到的 usage 即使协议解析失败也传给 `end_model_call`，取消或异常缺少可用 usage 时不写成零。`ModelCallStoreMixin` 对工作预占按每个调用自身的输入估算、输出上限和已报告用量结算；原账保留实报与估算两栏，在途仍占用。供应商在客户端取消后是否继续执行或收费不可由本地代码确定，账目明确保留未知而不是声称已停计费。

`RuntimeQueryService.model_usage` 在完整筛选后的账本而非当前页上统计输入、输出、失败／取消／未确认、缓存报告覆盖；缓存命中按有报告输入 token 加权，首次／后续只认网关绑定实例的已登记序号，旧调用归未知。`TraceTimings` 与调用、消息详情分别列出已保存的来源装配、请求准备、模型客户端／步骤、工具、提交、发布、本群串行锁、执行槽位、工作目录和发送排队／适配器时长。阶段彼此可能嵌套、并行或按不同身份归属，缺失不按零，也不合成单条消息总延迟；发送仍查原行动回执。

据此将 S1-04 的**源码交付范围**转为待复核：任务要求的主要口径已有真实字段、只读投影与页面入口。未把所有内部等待都拆开；取消后的供应商实际账单、缓存字段形状、旧调用、同版聚合 SQL 和页面仍需人工正常记录，不能标为已验收。无业务代码或运行配置改动。

- 只读核对 `cognition/gateway.py`、`cognition/call_store.py`、`web/query_service.py`、`actions/queue.py`，以及 `TraceTimings`、`CacheUsageSummary`、`ActivityView`、`MessageProgress` 的实际引用；对照[请求计时口径](context.md#7-缓存与诊断边界)，没有依赖旧盘点行号。`git diff --check` 退出 0；本批只有文档，不重复编译构建。
- 查找配置实现时误查不存在的路径，错误原文为 `rg: src/len_bot/runtime/config.py: No such file or directory (os error 2)`；随后从实际文件列表定位 `src/len_bot/config_store.py`。是只读路径错误，不是业务运行失败。
- 没有获准同版服务、面板或供应商账单；未运行测试、夹具、断言探针、自动截图、回放、故障注入、覆盖率、依赖安装、服务、模型／平台调用或实发；未读取真实配置和业务库。

### 待决定与接续

1. S1-04 待同版正常操作核对取消、晚到或缺失 usage、分组 SQL、缓存覆盖和阶段页面，并与真实回执分别对照；在此之前不能标已验收。少量无独立身份的内部等待不补造字段或总时长。
2. S1-02 动态插件定义、人格／插件指令版本与其他动态材料仍实施中。S2 仅 `source_window_only`，原生交换、活动摘要基线与压缩交接未完成，额外原生响应及必要回复片段的保存边界待维护者答复；继续不依赖该决定的源码工作。
3. Linux 容器群报告与文件交付、数据保留期限、代码／素材授权、精确支持版本、远端 CI／升级及外部使用者闭环仍待决定或现场证据；无生产或发布授权。
4. 仅阶段性本地提交，不推送、合并、部署或实发，总体目标继续。

## 路线现状与原顺序纠偏

阶段提交：`32323cb`。

### 当前批次

- 分支 `feat/s0-product-contract`，基线 `c9203c0`，开始时工作区干净；上一批 S1-04 调用用量与阶段计时按源码交付转待复核，供应商账单和同版页面尚无人工记录。
- 清理路线入口的两处时态冲突，不重写路线书或修改任务卡历史：S2-01 的原话窗口已由 Actor 保存到当前场景状态；原“前期实施顺序”是当时的依赖，不是 S2／S3 尚未开始的当前指令。

### 本批交付与核对

[产品路线](plan/README.md) 的 S2-01 底稿说明改为“原话窗口已有运行字段、完整交换仍未实现”，与状态行和[会话段设计](plan/s2-01-conversation-segment.md)一致。原推进顺序标为历史依赖；指出 S1-02 实施中、S2-01 部分接通、S3-01 待复核，当前任务优先由状态表与本页决定。S1-04 源码核对链接从临时当前页改为已归档的本日历史锚点，后续批次替换当前页不会使依据错指。

- 阅读当前 `ConversationSegment` 严格类型、`SceneActor._save_conversation_segment`、`EventStore.save_conversation_segment`、路线状态与设计开头，确认只有原话窗口引用和部分稳定编号已落地，不能称完整主段。`git diff --check` 退出 0；本批仅文档，不重复编译或前端构建。
- 无同版获准服务与面板，运行状态格式、旧数据回退及页面仍待人工正常核对；静态源码不当验收。本批无命令或业务失败原文。未运行测试、夹具、断言探针、自动截图、回放、故障注入、覆盖率、依赖安装、服务、模型／平台调用或实发；未读取真实配置和业务库。

### 待决定与接续

1. S1-02 的动态定义／提示来源仍实施中；S1-04／06 等待同版复核。S2 当前仅 `source_window_only`，原生交换、活动摘要基线与压缩交接未完成；额外原生响应与必要回复片段的保存边界待维护者答复，不通过旧路线文字假装已经决定。
2. S3／S4 多项源码待复核；已选 Linux 容器群报告及文件交付、S5-03 分类期限、许可证／素材授权、精确支持版本、S6 现场与远端 CI／升级、S7 外部使用者闭环仍待决定或正常证据。
3. 仅阶段性本地提交，不推送、合并、部署或实发，总体目标继续。
