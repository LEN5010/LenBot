# 通用 Agent 实施记录

## VNext 替换式重构进行中

已获准采用 BotCF / `gemini-3.8-flash` 的 conversation、work 两条独立路由，并在最终切换时停机备份、完整 Reset。配置、登录、OneBot、人工样例和运营表情库保留。开发期间不启动中间状态的旧管线。

阶段 1：新增冻结配置的 ModelGateway 与统一 AgentLoop，原生终结工具替代模型文本转发；完整保留供应商续接数据和并行工具回执顺序。19 项隔离底座检查通过。后续阶段继续替换对话/工作、事实与认识、原生媒体及面板，完成后单次上线。

面向实施者与运营者。基线 4da367d；分支 codex/general-agent-capabilities。当前能力包括信息型工具工作和图片/表情，实发群名单初始为 `group:126300994`，全局 Shadow 默认开启；实际运行沿用运营保存的配置。不包含代码执行、外部写操作、浏览器、MCP 或站点专用业务流程。

08:45–08:50 的实际运行已完成[接话、角色与表情审计](research/20260906-live-run-audit.md)。记录参与判断失误、自创游戏参与意愿进入工作认识、格式修复负担、零表情检索和数学错误；审计本身只保存证据与修改建议。

## 2026-09-06 表情检索修复与停机

08:28:53 和 08:29:06 的生产记录表明，模型已经主动调用 `search_media`，查询“委屈 哼”“可爱 不服”“无奈 摊手 可爱”，三次均返回 `no_results`。此前检索要求每个词同时匹配描述或标签，组合词把已有素材排除了；当时没有生成图片消息提案。

修复为任一关键词命中即可进入候选，按不同关键词的命中数量降序排列，再以原有时间和 ID 稳定排序。SQL 场景范围、运营素材和启停条件继续应用于全部候选，模型结合描述选择图片。

使用生产库只读复查上述三组原始查询，分别返回 5、5、7 张候选。“嘉然 开心”优先返回同时命中两个词的素材。6 项现有媒体检查通过，其中补充验证部分命中排序、跨群隔离、停用素材和完全无匹配的情况。按运营要求结束本轮时停止 len_bot，确认原 PID 退出、11307 端口释放；修复在下次启动时加载。

## 2026-09-06 表达、上下文与表情库

根据 Reset 后实际群聊中接话偏离原意、角色资料混入现实判断、图片观察未延续的问题，调整 Social Core 的提示组织：当前意图决定回应内容，人格通过态度和措辞体现；现实资料按来源及适用时间核对。按运营确认保存核心人格与六条日常表达样例，说明见 [嘉然表达与表情库](persona/diana/dialogue-and-stickers.md)。本轮提示采用正向描述，列明应如何理解和表达。

上下文移除当前输入在近期原话中的重复副本，省略空状态及 schema 展示元数据，将输出契约放入稳定系统前缀。保留事件身份、引用和截点；已提交的本场景图片观察按资产复用，附带原问题、范围和来源。空昵称/群名片按空值记录，偏好称呼保持独立。

导入 115 张嘉然原图，按 20 种主情绪分类为 global-safe 运营素材，排除“糖糖与小然熊的游园会”。组合关键词检索已接入现有媒体工具，模型按语境决定是否附图。Reset 保留运营表情及其来源/编辑记录，清理聊天产生的媒体与观察。

相关现有检查共 56 项通过，前端构建与差异检查通过。115 张素材经过生产 MediaService 读取及原文件字节校验，文字和图片混排通过本地 OneBot 编码检查。前端页面由运营自行查看，实际表情发送和聊天自然度继续在真实群中观察。

已在服务停止状态备份数据库和媒体并完成导入，2026-09-06 08:23 重启后 OneBot 连接正常。升级前的 20 条事件逐项保留，新增 115 条素材记录事件和一条运营设置事件；会话、任务、记忆、模型与发送配置经备份核对保持，已获准的人格字段和六条样例应用到保存配置。此次升级没有执行生产 Reset。

## 当前实施方向

2026-09-06：删除新旧日常评测操作流程，包括回放页、效果页、试运行评分页、面板模拟消息、人工打标、报告导入、回复观察窗口、批量评测脚本及其专用固定案例。内部 ReplayLab 保留给现有确定性测试使用。

实发群名单与全局 Shadow 是唯一运营发送控制，不再要求消息数量、候选数量、评分、视觉验证或源码对应报告。修改源码、模型或人格不清空名单，不把既有任务批量改为待核对。现有提供商、模型、人格、账号配置和原始事件继续保留；不会自动开启实发。

下一步在真实群聊中发现具体问题，再沿原话、工具结果、Gate 和发送回执定位，修复后回到实机确认。只运行相关检查，不默认新增测试集或完整回归。运行方式见 [运行手册](operations.md)。

当次清理的相关检查、前端构建和隔离面板操作已通过。实机停机备份、迁移并重启后 OneBot 连接正常；当时 Shadow 为 true，实发群名单仅 group:126300994。原 356 条事件以及 Session、任务、记忆、模型和人格与备份逐项一致。这是此前清理时的状态记录。

## 能力与默认值

工具返回类型化状态、来源/时间与持久引用，长结果分页；核心及常用网页工具直接可见，其余按需发现。同工作复用结果，独立只读调用有界并发，模型尝试与工具执行计入预算。

工作由 Social Core 提案、Gate 提交，执行器只使用只读工具。默认每群 1、全局 2，16 个模型步骤、24 次工具、300 秒、64K 上下文，修订不重置预算。结果回流后按最新语境表达，不固定播报。中断保留观察并待核对恢复。

图片按 scene 保存，vision 独立配置，未配置不猜测。表情由运营维护；消息使用文字/图片段，最多三条，后续有限等待；失败或不确定停止本组，不重试未知发送。

## 历史实施

以下记录当时的实现与检查，不是当前准入条件；已删除功能以本页“当前实施方向”为准。

| 阶段 | 提交 | 保留的结果 |
|---|---|---|
| 1 当前文档 | 7e87b0e | 当前架构、领域术语和文档入口 |
| 2 通用工具 | 2f24b5a | 结构化观察、网页读取、资料分页和工具发现 |
| 3 交错回放 | 1b03210 | 生产链路的内部测试工具；日常回放和批量案例已删除 |
| 4 独立工作 | 166902e | 查询中修订/取消、预算、恢复、进展及工作面板 |
| 5 媒体投递 | b1405c0 | 图片资产、scope、混排、发送节奏和媒体面板 |
| 6 互动质量 | 47d186c | 原意/来源提示、表达样例预览、续接工具回执修复；评分设施已删除 |
| 7 真实验收 | 未单独提交 | 曾实现报告门槛和样本计数，现已撤销；保留简单发送配置和基础日志 |

### 通用工具与工作

2026-09-06 的真实读取取得 Python asyncio 文档、Trafilatura API 文档和普通 GitHub issue 页面正文。首次抽测发现压缩体二次解码，已修复，没有增加站点判断。搜索结果与正文不代表模型已经正确综合。观察与待投递事件同事务保存，派生内容不能充作独立记忆证据。

工作详情 agent_jobs 与 tasks 使用同一 ID，任务表是调度/送达权威。创建、修订、取消、恢复与 Session、记忆和确认消息在 Gate 同事务提交；执行器持久保存目标版本、已消耗预算及资料引用。后台步骤不推进社会读取截点，进展和结果才回到社会理解。只有就绪结果才能关联交付，发送前检查取消和版本。

工作页查看、修订、停止、恢复及资料分页已接线，HTTP 读取经 QueryService。历史检查覆盖事务失败、旧版本结果、重复完成、在途补充/取消与重启；实际对话中的目标理解继续在群里核对。

### 媒体与表达

OneBot 原始 segments 保留，入站图片与 source Event 同事务登记为 scene-scoped media_assets。图片按需获取，限 10MB/20MP，以 Pillow 校验并哈希缓存；不读取任意本地路径。运营素材可显式选择 global-safe，群内图不自动晋升。

inspect_image 使用独立 routing.vision。视觉解释属于 model 证据，保留原图事件；动图只分析首帧并标注。视觉调用与格式修复计入认知模型额度。ActionQueue 每群保序，全局最多 4 个投递并发，后续片段按长度等待 0.6–2 秒，发送前复查资产、工作版本和 Shadow。

Social Core 加强当前目标、最新约束与来源/未知的区分，继续输出稀疏状态。diana-v3 通过已有预览流程追加四组表达样例，保留人工编辑和启停。发现最终续接工具观察未计入截点后，已修正为确认自己读过的连续观察，不能越过未读输入；这一真实修复继续保留。

### 已知失败与历史证据

此前模型对照出现纠错、来源依据与未读链接却声称看过的问题。首轮报告还发现缺视觉路由错误传播到文字场景；相关代码已修复，原始 initial-failure 文件保留。供应商 429 限流保持失败记录，没有以其他模型替代结果。

[视觉探测](evaluation/runs/vision-provider-probe.json)对当时 L 目录中的 Flash、Flash-0731、Pro、Pro-0813 发送真实数字 PNG，四个均返回 400 且明确不支持图片。这是当前已知能力限制，使用图片理解需要配置可用视觉型号，不作为文字聊天准入条件。

已提交的真实模型运行和首轮失败保留在 `docs/evaluation/runs/`；本轮未提交的原始运行和早期启动记录移至本地忽略目录 `.runtime/evaluation-history/`。无用的脚本基线、空白评分表、准入摘要、重复汇总和源码归档已删除，历史报告不再要求持续生成或导入。曾通过的 197 项回归及前端检查只描述阶段 7 当时状态，不代表本次清理后的检查结果。

早期上机前已在 `.backups` 保存 SQLite 备份并通过 integrity_check；当时核对原 315 条事件和提供商、人格配置一致，随后连接真实 OneBot 并保持 Shadow。该次记录的空名单和启用截点已被当前简单发送配置取代。
