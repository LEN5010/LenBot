# 实施记录

## 2026-09-08：新工作确认消息误关联历史任务

实群 `group:126300994` 在 04:32:25 与 04:36:39 的确认消息均记录为 `MESSAGE_SEND_FAILED / not_sent`，报错为 `Reply refers to an obsolete or cancelled job`。两轮实际创建的工作分别是 `job_13dc7dab5c3557dcae00`、`job_80ae72a91b9858dfac67`，发送动作却都关联到已完成的 `job_e994b42a60905ca586ed`。原始失败事件分别为 `b665e0ee-f0bb-4926-a477-14093e83edf8`、`82c7a8ed-5a58-4d3d-a4c7-a58e6d7aff56`。

原因是 Gate 在提交后按当轮临时编号 `S1` 搜索全部场景任务，并取首个匹配项；不同轮次会复用这个编号。现改为按事务已经写入任务的唯一 `ack_action_id` 定位本次确认所对应的工作，保留原有工作版本和发送前校验。隔离测试覆盖历史同编号工作仍待执行、已完成、已取消，以及重复确认复用同一工作；修复前分别出现错误关联或发送被拒，修复后均通过。

相关工作、任务生命周期、投递协议、发送配置和场景事务检查共 49 项通过。仅修改本地源码并使用隔离库验证，未重启运行服务、补发消息或改写生产记录。同期间 04:33:25、04:37:40 另有对话模型 `APITimeoutError`，04:36:22 工作结果保留压缩请求超时；这些请求失败与确认消息串号是独立原因，本次修复不声称解决供应商超时。

## 2026-09-08：控制面板重构（提交供运营验证）

在 `885d370efde586a3d7984946d57acad47d2f35b5` 上替换控制面板布局与导航。沿用 Vue/Vite 和 FastAPI 托管，使用 Vuetify 统一控件、Vue Router hash history 统一对象定位；旧手写导航、全局按钮/表格布局样式，以及重复的技能与调用账入口退出。场景、工作、提醒、认识和素材采用有界列表与全文详情，配置草稿独立保存；结构、功能去向和依赖版本见[本次前端改造记录](research/20260908-frontend-rebuild.md)。

查询层补充服务端分页、对象读取与真实引用关联；修改接口的实际消费者同步更新，不猜测数组/对象两种返回形状。页面分别显示执行、交付、模型请求、Shadow 和模拟事实。凭据留空保留，技能与工作控制提交准确版本；阅读与刷新不产生模型请求，主动能力检查明确两次请求及可能费用。Agent 认知、预算、权限和发送配置沿用现行契约。

当前前端构建通过；相关 API/权限检查 **30 项通过**，后续 **18 项**场景与关联边界复验通过，两批含重叠，不累计为独立用例数。临时表单检查确认空白凭据、草稿保留、图文顺序、素材范围、技能版本与单次提交。隔离库的 49 张素材实际分页为 48＋1，指定技能版本可读，外场景读取本地版本返回 404。

已检查主要桌面页面并记录四类布局缺陷的同源对照；收尾构建及 53 项相关检查通过。按运营要求停止继续浏览器检查，先提交当前版本供其自行验证，手机、缩放及完整操作路径未判通过。生产按要求保持停止；本次未执行 Reset、启动生产、调用真实模型或修改生产配置，也不据此声明实群效果或费用已验收。已有检查、截图位置与剩余验证范围见上述记录。

## 2026-09-08：审查缺陷修复

在 master 的 `ad770951c14fc0a3bef0ca285d941394c2c4902e` 上修复三处具体缺陷，沿用[ADR-0045](adr/0045-attention-maintenance-and-durable-work.md)与现有运行链路。

- 任务恢复先匹配本次认领的待投递 TASK_DUE，再决定是否进入核对；原事件可处理一次，旧触发不能抢占新认领。未知送达仍不自动重发。
- 工作图片以实际请求中的像素去重和计算六张窗口；压缩后可重新读取原图，第七张可换入，换出的来源定位与覆盖记录保留。
- 对话 Context 统一计算消息、工具定义、图片和输出预留。原文先于可选目录与摘要；待处理目录有界分页，未读来源不确认。持久目录保存事件 ID，回读时生成本轮引用，避免跨轮 M 引用错配。

本地 Python 3.13.13 执行现有 CI 测试命令 `uv run --offline pytest -v`，**246 项全部通过**。复用任务、工作媒体和运行时夹具，覆盖未投递认领重启、压缩后实际 PNG 像素回读与第七张换入，以及 1,000 条唤醒下逐步读取、未读取消阻止旧工作提交。没有删除原失败检查或放松发送与新鲜度边界。GitHub 的旧提交运行仍是 1 failed / 241 passed；本次本地通过不等于远端 CI 已通过。

修复前发现服务运行中，已正常停止并于 01:51 备份至本地 `.backups/review-fix-20260908-015106`，含 5,623 条事件、324 个媒体文件与修改前源码，数据库完整性为 ok。本次修复保持停机，未执行 Reset、重启生产或更换模型与发送配置，前端未改动。当前部署、实群行为、usage 和费用的独立结论见[审查修复验证记录](research/20260908-review-fix-validation.md)。

## 2026-09-08：注意力、统一维护与持久工作

开发分支 `codex/attention-work-skills` 已合入 master，提交为 `ad770951c14fc0a3bef0ca285d941394c2c4902e`，评审/开发基线为 `659eff4dd40b7a72c30e215ce8e6f382bc07db8a`。本节记录该次实现及当时的验证状态，后续修复见上节。现行契约见[ADR-0045](adr/0045-attention-maintenance-and-durable-work.md)，下面的 2026-09-06 部分保留为历史，不能用其中旧消费游标、work 反思或取消行为解释当前代码。

| 范围 | 当前实现与核对 |
|---|---|
| 契约与结构 | 四类进度分开，显式离线迁移、保全指纹和未完成启动阻断；不执行 Reset |
| 成本与上下文 | 每次真实请求一条持久账，区分用途、未知 usage、取消与估算；稳定参考前置，动态时间与事实后置 |
| 历史维护 | 显式 maintenance；新增原文 token 分块，一次输出摘要与稀疏认识，来源/认识/覆盖原子提交 |
| 注意力与续接 | 持久唤醒、固定窗口抽样、关键词合并、连续互动/工作输入；整条原话或字符范围实际读取，晚到终结拒绝在原预算内续接 |
| 工作恢复与压缩 | 简短进度、固定绑定、完整原生交换、图片引用和原预算恢复；归档正文后按完整旧区间压缩，失败不丢原始资料 |
| 程序性技能 | 稳定短目录、按需正文、来源/版本、候选维护及纠正修订闭环；精确版本发布与人工内容保护 |
| 运营入口 | 三角色就绪与调用账、注意力参数、场景覆盖/失败重试、工作进度/压缩/检查点和技能来源版本；实际页面已检查 |

交叉审查修复了扫描状态内存/数据库不一致、超大原文无法续读、引用对象关注错误、恢复图片重复、发送失败误重做 completed 工作、技能维护零时间余额仍请求、并发候选遗漏及修订时实际耗时丢失。旧 last_cognized 队列、每条新消息取消对话、固定 30 条 work 反思和重复运行器调用累计已退出；老字段只保留在一次性迁移识别中。

数据库副本演练保留 5,090 条事件、3 个场景、307 个媒体文件，以及全部原业务/配置表内容；3 个旧未处理来源仍 pending，没有伪造摘要覆盖。注入半途失败后普通启动被阻断。原生产数据库、模型、人格、Shadow 和群名单没有执行本次切换。

用户明确授权以 BotCF / gemini-3.8-flash / low 做 maintenance 的隔离供应商验证；work 沿用 high。7 次真实请求全部返回成功：history 1、work 5、compression 1。工作在同一预算内压缩、续接、回读原资料并 completed；供应商输入 45,519、输出 5,107 tokens，推理 3,056 已包含于输出，不再次相加。未核实价格和生产前后对照，费用与节省比例不判通过。

验证明细与材料来源见[本次隔离验证](research/20260908-attention-work-validation.md)，旧调用账基线见[记录快照](evaluation/20260908-recorded-cost-baseline.json)。本次覆盖 176 项相关现有及必要业务边界检查，逐项通过；其中旧引用测试夹具补充 RuntimeConfig 后，相关 38 项复查通过。前端构建和六个实际页面已核对，编译、48 个受影响 Markdown 文件引用及 git diff --check 通过。稳定人格与人工参考前缀在时间和输入改变时保持相同，参考仍以 user 角色提供；这不证明供应商缓存命中。

当时本分支尚未部署；这不是对后续运行状态的判断。真实群聊的自然度、持续高负载下的回应时机、实际送达与前后成本仍须依原群原话核对。本次供应商没有返回私有签名字段，因此只证明返回的原生工具协议能压缩续接；签名原样保留由本地夹具验证。旧工作若已消耗预算却没有可证明的模型绑定，明确拒绝猜测恢复，已存结果和原始资料保留。

## 2026-09-06：VNext 历史切换

2026-09-06，分支 `codex/general-agent-capabilities`。当前实现见[架构](architecture.md)，切换与回滚方式见[运行手册](operations.md)。

## 五阶段替换

| 阶段 | 提交 | 实施结果 |
|---|---|---|
| 1 模型与循环底座 | `aac08e3` | 固定模型绑定、无损原生续接、统一 AgentLoop、conversation/work 独立路由 |
| 2 对话与工作 | `08eef39` | 窄 finish_turn、当轮提案账本、原话上下文、独立只读工作和受限算术 |
| 3 事实与认识 | `26eefb6` | 单一事实 SceneSession、证据账本、只提案反思、Actor/Gate 和运营干预 |
| 4 图文与运营 | `47a3884` | 原生图片、固定目录、六条图文参考、短角色卡与对应面板 |
| 5 清理与切换 | 本提交 | 删除旧世界状态、整份 JSON 管线、最终草稿续接和旧专用测试；补齐引用、暂存撤回和真实切换记录 |

第三、四阶段是替换过程中的中间结构。完成全部阶段后只运行新引擎，不提供双管线兼容或启动时自动擦除旧库。

## 最后整合与验证

独立审查发现并修复了分页前授予隐藏引用、暂存提案无法撤回、移出图片不能重新读取、相同像素覆盖来源、仅剩终结步骤还吸收新请求，以及历史 Shadow 候选污染新真人消息的问题。

`discard_proposal` 只撤回本轮尚未提交的意向；已提交工作仍使用有版本的控制。相对提醒由事务提交时间计算。对话观察至少保留两个模型步骤和一次工具额度，终结后不再吸收；认识版本变化中止旧轮次。

最终相关检查合跑 **140 项通过**，涵盖原生协议、工作、认识、SceneActor 事务、Reset、媒体、模型/预设/面板、工具、发送和提醒。其他已迁移的认证、插件、SSRF 和调度相关检查通过。前端正式构建通过，全部 196 项测试可收集；没有以此替代实际群聊观察。Markdown 引用、编译和 `git diff --check` 已检查。

实际 BotCF 核对与完整失败说明见[接口核对记录](research/20260906-vnext-checks.md)。格式修正后，抵触反馈的单图、图片数字识别和别人之间的对话均可一次调用结束。糖果原题存在抽取策略假设差别：任意摸取保证数为 29，允许按形状挑选有 21 颗策略。最后明确随机摸取条件的模型验证因协议失败及服务方额度不足未完成；没有把它写成通过，也没有硬编码答案。

## 数据切换

切换前已停止旧运行时，备份位于本地 `.backups/vnext-before-20260906-093524`，对应旧代码 `907e19d`。正式切换前再次确认旧进程和监听均已停止，并在 `.backups/vnext-cutover-20260906-104826` 保存数据库与完整媒体，SQLite integrity_check 通过。

隔离副本演练通过：清理旧对话、双 Session、自动认识、任务、工作、工具资料、回执等待和聊天媒体；115 张运营原图、115 条来源、登录及其他配置逐项保持。固定目录从原有 20 类情绪各选一张，应用六条 diana-v4 参考。Shadow 和两条实发群名单按原保存值恢复。

**正式切换于 2026-09-06 10:49 完成。** 已执行获准完整 Reset，旧场景结构删除；旧原话、认识、工作、任务、工具资料、OpenLoop 和四张聊天图片清理。115 张运营图片及 115 条来源逐项保留，20 项目录与六条样例就绪，未清空或替换登录、OneBot 及其他运营配置。

新进程 PID 9123，面板 `/` 返回 200、受保护 `/api/auth/me` 未登录返回 401；OneBot 已连接 `127.0.0.1:13001` 并确认原账号。启动后待投递事件为零。保存的 Shadow 仍为 false，实发群为 `group:1014123451` 与 `group:126300994`，没有自动更换名单。

conversation 与 work 已保存为 BotCF / `gemini-3.8-flash`，分别 low/high。切换时 BotCF 额度不足，最后一项模型核对当时未完成；14:41 起的真实运行已取得成功模型响应与真实发送回执，不再把早前额度错误当作当前不可用状态。切换核对未进行额外的真实群聊测试发送或伪造送达。

## 首轮实群反馈修正

2026-09-06 下午根据实际原话、用户截图、事件和回执审查，详见[本次运行审查](research/20260906-vnext-live-followup.md)。修正了普通图片类型导致运营表情显示偏大、下一轮看不到在途表达而重答旧话、人物 @ 与短引用脱节，以及强制收尾仍暴露其他工具的问题。

表情沿用运营原图，以明确的 `表情包` 标签选择 QQ 自定义表情类型；模型结合最近几次回复安排图文，保留单图和混排。对陌生概念、外部事实与当前信息更早建立后台查询工作，工作结果核对来源、对象、日期和适用范围。没有新增硬性发图比例、关键词触发或逐消息自动检索。

相关协议、对话、工作、发送与媒体检查共 79 项通过。已获准表达、最新回执和新群友输入保持不同事实；阻塞发送、截点外回执、unknown、跨场景与同批部分送达都有定向验证。编译、受影响的 Markdown 引用及 `git diff --check` 通过，前端未改动。

匿名化隔离核对中，在途重复案例改为接新消息并只发文字；更早的六条参考和与生产对齐的两条带图参考下都没有重发旧句。主动搜索案例完整建立工作、执行 8 个模型步骤和 7 次工具调用，再由对话提交带履约关联的结果；全程 Shadow，无 OneBot 连接。首次搜索核对的非法末步撤回保留为失败，补明确收尾提示后才完成；仍观察到一次未先创建工作就填 ack_ref 的参数修正，不能声称模型协议已无失败。

修正于 **15:37 上线**。停止 PID 9123 后，将数据库和媒体备份至 `.backups/live-followup-20260906-153704`，未执行 Reset；新进程 PID 24018 已连接原 OneBot 端点和账号。260 条原始事件逐条哈希不变，115 张运营原图逐张校验，现有两条样例和全部动态配置不变；SQLite integrity_check 为 ok，面板返回 200、未登录受保护接口返回 401。

## 工作截断与失败后的误答修正

针对 15:42–15:43 的新失败，详见[工作失败审查](research/20260906-work-failure-review.md)。工作单次输出配置此前未接线且写死为 4096，现统一使用 work_output_tokens，默认 16384。总工作预算及原模型路由保持，服务方 length 仍视为不完整输出，不发送残稿。

工作终结只要求结论、资料引用和未决项；完成/部分完成状态由运行时确定，消除“已完成且有未决项”的重复填表矛盾。工作执行结局与回应阶段分开，失败不能履约，可在原预算内恢复；原始资料保留。提交冲突的意向仅向下一轮说明未生效，仍需根据新输入重新提案，不放松新鲜度检查。

运行记录正确汇总工作 runs，并优先显示真实错误；没有提交的候选说明不再冒充“已经提交”。相关 59 项检查、编译、Markdown 引用和前端构建通过，页面纯函数已用实际截图 trace 验证。

隔离核对保留全部先前失败。16K 下的糖果工作完成完整推导，没有截断；最后一次 Qwen 查询读取官方介绍与评测资料，建单、工作终结和带履约关联的表达全部完成，没有参数修正。没有向真实群聊发送测试消息；模型回答仍须结合来源、条件和后续真实互动判断。

修正于 **16:11 上线**。停止 PID 24018 后备份至 `.backups/live-followup-20260906-161106`，新进程 PID 27029 已连接原 OneBot 账号。436 条原始事件逐条哈希保持，115 张运营原图及现有两条样例保持，全部动态配置不变；新增恢复事件正常追加。数据库完整性通过，面板返回 200、未登录受保护接口返回 401，未执行 Reset。

## 连续接话与新表情库

运营提供五张其他 Bot 的连续群聊截图，以及本地 `Diana/表情包` 新素材。对可见承接方式的分析见[连续接话观察](research/20260906-conversation-continuity.md)。本轮仅把参与提示具体化为多人分别回应、保留尚在继续的问题和沿前文细节接话，不推测对方的底层配置，也不复制其现实活动表述。

新包 49 张全部完成格式、哈希与视觉核对，包含 25 张 GIF、24 张静态图；建立动作、台词与用途索引，并选 20 张常用图进入固定目录。替换演练在隔离库中删除旧 115 条运营资产和旧运行文件、新增 49 条及来源事件，重绑两个图样例；其他媒体、配置和原始事件逐项保持。GIF 传输检查确认最大 9.29MB/37 帧文件和庆祝动图均保留原字节，使用 QQ 表情类型。

相关对话/发送上下文的 18 项检查通过；目录缩略图、语义检索、完整动画与替换后的引用可用性已核对。没有新增自动配图比例或关键词触发，也未自动恢复此前删除的人工样例。

**16:47 正式替换完成。** 停机备份位于 `.backups/live-followup-20260906-164735`。删除旧 115 条运营资产及 115 个无其他引用的旧运行文件，新增 49 张和来源记录；固定目录 20 项。切换前 149 条事件及 1 条聊天媒体全部保留，原目录 49 个文件逐一哈希不变，运行配置不变。准备期间控制面板曾执行 Reset，切换前样例库也已经为空；本次替换未执行 Reset 或恢复已删除样例。新进程 PID 29865 已连接原 OneBot，健康检查与数据库完整性通过。

## 停机与后续规划

运营于 **2026-09-06 17:20** 要求先停机，再规划模型输出与上下文精简。PID 29865 已正常退出，OneBot 适配器与 Runtime 完成关闭，面板端口已无监听；当前保持停机。

随后按运营“开始修改”落实[接口与上下文精简计划](research/20260906-conversation-interface-plan.md)。修改前离线备份为 `.backups/conversation-interface-20260906-172952`。模型片段改为单字段 text/image，提案入口转换为原内部结构；原生续接、事务、权限和回执语义保持。空运行事实初次省略，已提供状态的消失明确更新；无管理目标时工具从 22 个减至 13 个，查询或暂存后按真实对象展开。

新增可编辑的呼唤昵称，默认“然比、小然”。@、已读 Bot 引用、私聊和昵称匹配只提供输入线索，同一 Core 以旁听为默认判断参与，不添加词表硬拦截或未读输入旁路。表情目录保留，文字、单图与混排独立于参与频率选择；人工样例和运行记录页适配新模型格式，存储格式不变。

相关 **93 项检查**、前端构建与实际设置/运行记录页面核对通过。仅使用合成输入的当前模型核对中，昵称回应、普通群聊沉默和单图提案均一步完成，无参数修正；这不代表真实群聊效果已验证。与修改前备份逐项核对，生产 13 张业务/配置表及 54 个媒体文件保持，SQLite integrity_check 为 ok；没有 Reset、恢复人工样例或重启实发。测试面板也已关闭。

## 历史问题与保留证据

运营随后恢复测试并于 **18:06:51** 再次 Reset。[本次 Reset 后审查](research/20260906-post-reset-run-review.md)确认：普通分享的旁听已有改善，但主群六轮因只填写 ack_ref、没有实际创建工作而失败；Qwen 查询也未建单。糖果工作进入 work 链路后仍产生错误构造及盲摸数字，已用独立枚举保留反例。最后一条结果处于待处理状态，审查时程序已不在运行；本轮是只读定位，没有再次修改代码或恢复运行。

## 创建回执与有限枚举修复

运营要求开始修复，并明确少补测试、直接上机验证。本轮新建工作与提醒移除模型自拟的 `proposal_ref`，统一返回工具生成的暂存引用；只有存在有效的新建提案时，`finish_turn` 才开放 `ack_ref`，并枚举实际引用。确认消息与对应提案仍原子提交，撤回后引用立即失效。普通聊天仍可一步结束；需要确认新建事项时，先创建、再依据回执收尾。

前次协议失败即使未形成工作提案，也能在对应输入尚未消费时以失败记录提供；与提交冲突一样明确没有提交提案和消息，不自动创建工作、重试或清除未读。记录按实际读取截点与场景筛选。

独立工作增加受限 `finite_check`：明确变量整数范围、筛选条件、计数或极值目标，完整枚举并返回见证。新工具本地核对了 201600 种糖果计数状态：盲摸失败上界 28；旧“10 圆＋11 星”存在 6 个失败构造，“9 圆＋12 星”在相同模型内无反例。这些是工具自身验证，不是新模型已经正确解题的证据。工作指令要求可枚举问题核对最终结论与边界，建模假设和未验证项仍保留。

只运行受影响的创建确认、原子回滚、撤回和失败上下文检查，10 项通过；另对新工具的真实题目枚举、代码表达式拒绝和组合数上限做直接检查。未增加场景集或重跑全套回归，未修改前端。

**19:14:40 已按原配置恢复运行。** 停机备份为 `.backups/work-repair-20260906-190826`，包含数据库、52 个媒体文件及修改前源码；启动前原始事件、运营配置、人工样例和媒体记录逐项不变，SQLite 完整性通过，没有 Reset。面板监听与原 OneBot 连接已恢复，保存的 Shadow=false 和两个实发群保持。启动恢复了旧待处理工作，随后新群消息使旧结果交付遭遇新鲜度冲突，候选被拒绝；这不算新解题链路通过。新工作建单与枚举结果仍需以本次实群输入、工具记录和真实回执核对。

实机继续发现 `trc_c81dea7d7a3d`：重启将已有结果的工作一律改为 review_required，造成执行已完成却不能履约。已修复恢复逻辑，使持久化结果保留 result_ready；无结果的中断工作仍要求显式恢复，未知发送仍不重试。补查受影响的重启与失败不得履约边界，5 项通过。再次正常停机，备份至 `.backups/work-recovery-20260906-191930` 后重启；历史错误工作结果原样保留，不因代码修改自动重算。

第二次部署于 **19:20:52** 接回原链路。**19:21:16** 主群取得真实 MESSAGE_SENT（事件 rowid 331，协议 message_id 1336200486），旧待交付工作转为 completed，确认重启恢复、Gate 与实际传输已通。该条是旧工作的交付，不能据此声称新建单和模型枚举解题已经实群验证；本次已请求运营在原群重发新题继续核对。面板返回 200，生产保持运行。

运营于 **19:24:57** 再次 Reset。[本次搜索审查](research/20260906-search-run-review.md)确认：Qwen 与 Astra 均已两步建单，无协议修复；但三项工作七次网页搜索全部被 DuckDuckGo 验证码挡住。Qwen 绕路耗尽 16 步且读取端遗漏指标图片；Astra 工作没有 OpenAI 来源，却将检索失败和 Google 同名项目写成“OpenAI 从未发布”。使用现有 `read_page` 可成功读取 GPT-6 Astra 官方模型页。本轮只读定位，程序保持运行，搜索与证据问题尚未修复。

## 搜索、图表与来源检查修复

后续按运营要求开始修复，停机备份至 `.backups/search-repair-20260906-200225`。网页搜索改为 Bing 公开 RSS，实际查询 OpenAI Astra 和 Qwen3Guard 均返回相应官方来源。不可用的搜索服务在当前工作内停止重复调用；保留用户指定对象，确认消息只确认查证安排。

`read_page` 保留网页图表链接并支持PDF文字层；新增 `read_web_media` 读取图表原图或一页PDF，将图片与工具观察原子登记在本场景。PDF通过可终止子进程处理，依赖锁定 pypdfium2 5.13.0。本机已读取 Qwen 指标图、28页技术报告文本和第3页原图，实际查看确认可读。首次实读发现空alt属性和PDF页对象关闭方式问题，已修正并重查。

工作进展与终结检查引用来源的可用性：外部查询只取得失败、摘要或本地聊天时不能当作完整查证。Trace 增加资料状态与错误码，分开显示调用结束和资料可用性。相关现有检查及两条必要的隔离/证据边界检查共10项通过，随后复查受影响的PDF与来源检查；前端构建通过。当前继续进行模型与实际页面核对，实机结果在取得后补记。

隔离库使用当前保存的模型连接复测两条原问题：Qwen 取得 GitHub、Qwen 博客和 arXiv 正文，继续读取长资料并提交完整结果；Astra 通过 Bing 找到 OpenAI 发布页和模型页，站点正文读取失败时转向了其他来源，没有再声称 OpenAI 未发布。两条均在 Shadow 下完成，无真实发送。运行记录页面实际显示 `调用结束` 与 `资料已读取`，对 `body_unavailable` 显示红色“服务或格式不可用”和错误码；Qwen 图表原图与 PDF 第 3 页在隔离场景已进入原生图片链路。生产进程保持运行，后续仍需在原群验证自然交互与发送效果。

**2026-09-07 00:03 实机审查**见[最新运行记录](research/20260907-latest-live-review.md)。部署后成功对话 317 轮，其中 284 轮沉默、33 条真实发送，无发送失败；新增两项工作最终交付。但游戏结果完成后拖延约30分44秒才插入无关话题，期间六次对话因严格新鲜度冲突失败。13次网页搜索虽均为ok，至少8次内容明显跑题，原始RSS复现也不可靠；已有来源检查无法证明具体结论被来源支持。少回应表情包的明确要求已保存并影响后续旁听，另有一次反思协议错误。本轮只读诊断，程序与配置保持运行状态。

针对该审查的首轮修正：对话模型在新的人类输入到达时立即取消未完成轮次，输入保留并由下一轮合并，避免长调用完成后再以过时结果重试；新建工作同轮消息必须绑定实际暂存回执并只表达查证安排。Bing RSS 结果按原查询词项筛选，`site:` 域名约束在本地复核，偏题结果降为无结果而不进入证据。对话上下文改为只提供表情文字目录，发送图片前必须先读取所选像素，避免每轮注入整张目录图。相关对话、提案、媒体和搜索检查已通过；本次停机备份位于 `.backups/plan-execution-20260907-045844`，当前服务按要求保持停机，尚未用新代码重新启动实机。

[旧实群审计](research/20260906-live-run-audit.md)记录了角色发挥盖过语境、抵触反馈被当作继续调侃、没有依据的 MC 参与声明、格式修复和数学错误。原话、时间与事件/trace ID 保留；其中糖果答案的适用假设在后续核对中已补充。

此前 115 张运营表情按原图哈希导入，组合词搜索改为任一词匹配并按命中数排序，来源及素材仍保留。本次把按需视觉模型调用替换为原生图像输入；旧[视觉探测失败](evaluation/runs/vision-provider-probe.json)仅描述当时 L 提供商的接口能力，不是 BotCF 的能力结论。

旧评分、固定批量案例、报告导入和源码准入流程已退出日常操作。已提交的真实模型失败保留在 `docs/evaluation/runs`；本次完整匿名化轨迹、演练库和一次性切换脚本保存在本地 `.runtime`，不进入 Git。后续仍以真实群聊的具体问题推进修改。
