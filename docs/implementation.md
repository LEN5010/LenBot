# VNext 实施记录

2026-09-06，分支 `codex/general-agent-capabilities`。当前实现见[架构](architecture.md)，切换与回滚方式见[运行手册](operations.md)。

## 五阶段替换

| 阶段 | 提交 | 实施结果 |
|---|---|---|
| 1 模型与循环底座 | `aac08e3` | 固定模型绑定、无损原生续接、统一 AgentLoop、conversation/work 独立路由 |
| 2 对话与工作 | `08eef39` | 窄 finish_turn、当轮提案账本、原话上下文、独立只读工作和受限算术 |
| 3 事实与认识 | `26eefb6` | 单一事实 SceneSession、证据账本、只提案反思、Actor/Gate 和运营干预 |
| 4 图文与运营 | `47a3884` | 原生图片、固定目录、六条图文参考、短角色卡与对应面板 |
| 5 清理与切换 | 本提交 | 删除旧世界状态、整份 JSON 管线、最终草稿续接和旧专用测试；补齐引用、暂存撤回和真实切换记录 |

第三、四阶段是替换过程中的中间结构。完成全部阶段后只运行新引擎，不提供双管线兼容或启动时自动擦除旧库。

## 最后整合与验证

独立审查发现并修复了分页前授予隐藏引用、暂存提案无法撤回、移出图片不能重新读取、相同像素覆盖来源、仅剩终结步骤还吸收新请求，以及历史 Shadow 候选污染新真人消息的问题。

`discard_proposal` 只撤回本轮尚未提交的意向；已提交工作仍使用有版本的控制。相对提醒由事务提交时间计算。对话观察至少保留两个模型步骤和一次工具额度，终结后不再吸收；认识版本变化中止旧轮次。

最终相关检查合跑 **140 项通过**，涵盖原生协议、工作、认识、SceneActor 事务、Reset、媒体、模型/预设/面板、工具、发送和提醒。其他已迁移的认证、插件、SSRF 和调度相关检查通过。前端正式构建通过，全部 196 项测试可收集；没有以此替代实际群聊观察。Markdown 引用、编译和 `git diff --check` 已检查。

实际 BotCF 核对与完整失败说明见[接口核对记录](research/20260906-vnext-checks.md)。格式修正后，抵触反馈的单图、图片数字识别和别人之间的对话均可一次调用结束。糖果原题存在抽取策略假设差别：任意摸取保证数为 29，允许按形状挑选有 21 颗策略。最后明确随机摸取条件的模型验证因协议失败及服务方额度不足未完成；没有把它写成通过，也没有硬编码答案。

## 数据切换

切换前已停止旧运行时，备份位于本地 `.backups/vnext-before-20260906-093524`，对应旧代码 `907e19d`。正式切换前再次确认旧进程和监听均已停止，并在 `.backups/vnext-cutover-20260906-104826` 保存数据库与完整媒体，SQLite integrity_check 通过。

隔离副本演练通过：清理旧对话、双 Session、自动认识、任务、工作、工具资料、回执等待和聊天媒体；115 张运营原图、115 条来源、登录及其他配置逐项保持。固定目录从原有 20 类情绪各选一张，应用六条 diana-v4 参考。Shadow 和两条实发群名单按原保存值恢复。

**正式切换于 2026-09-06 10:49 完成。** 已执行获准完整 Reset，旧场景结构删除；旧原话、认识、工作、任务、工具资料、OpenLoop 和四张聊天图片清理。115 张运营图片及 115 条来源逐项保留，20 项目录与六条样例就绪，未清空或替换登录、OneBot 及其他运营配置。

新进程 PID 9123，面板 `/` 返回 200、受保护 `/api/auth/me` 未登录返回 401；OneBot 已连接 `127.0.0.1:13001` 并确认原账号。启动后待投递事件为零。保存的 Shadow 仍为 false，实发群为 `group:1014123451` 与 `group:126300994`，没有自动更换名单。

conversation 与 work 已保存为 BotCF / `gemini-3.8-flash`，分别 low/high。切换时 BotCF 额度不足，最后一项模型核对当时未完成；14:41 起的真实运行已取得成功模型响应与真实发送回执，不再把早前额度错误当作当前不可用状态。切换核对未进行额外的真实群聊测试发送或伪造送达。

## 首轮实群反馈修正

2026-09-06 下午根据实际原话、用户截图、事件和回执审查，详见[本次运行审查](research/20260906-vnext-live-followup.md)。修正了普通图片类型导致运营表情显示偏大、下一轮看不到在途表达而重答旧话、人物 @ 与短引用脱节，以及强制收尾仍暴露其他工具的问题。

表情沿用运营原图，以明确的 `表情包` 标签选择 QQ 自定义表情类型；模型结合最近几次回复安排图文，保留单图和混排。对陌生概念、外部事实与当前信息更早建立后台查询工作，工作结果核对来源、对象、日期和适用范围。没有新增硬性发图比例、关键词触发或逐消息自动检索。

相关协议、对话、工作、发送与媒体检查共 79 项通过。已获准表达、最新回执和新群友输入保持不同事实；阻塞发送、截点外回执、unknown、跨场景与同批部分送达都有定向验证。编译、受影响的 Markdown 引用及 `git diff --check` 通过，前端未改动。

匿名化隔离核对中，在途重复案例改为接新消息并只发文字；更早的六条参考和与生产对齐的两条带图参考下都没有重发旧句。主动搜索案例完整建立工作、执行 8 个模型步骤和 7 次工具调用，再由对话提交带履约关联的结果；全程 Shadow，无 OneBot 连接。首次搜索核对的非法末步撤回保留为失败，补明确收尾提示后才完成；仍观察到一次未先创建工作就填 ack_ref 的参数修正，不能声称模型协议已无失败。

修正于 **15:37 上线**。停止 PID 9123 后，将数据库和媒体备份至 `.backups/live-followup-20260906-153704`，未执行 Reset；新进程 PID 24018 已连接原 OneBot 端点和账号。260 条原始事件逐条哈希不变，115 张运营原图逐张校验，现有两条样例和全部动态配置不变；SQLite integrity_check 为 ok，面板返回 200、未登录受保护接口返回 401。

## 工作截断与失败后的误答修正

针对 15:42–15:43 的新失败，详见[工作失败审查](research/20260906-work-failure-review.md)。工作单次输出配置此前未接线且写死为 4096，现统一使用 work_output_tokens，默认 16384。总工作预算及原模型路由保持，服务方 length 仍视为不完整输出，不发送残稿。

工作终结只要求结论、资料引用和未决项；完成/部分完成状态由运行时确定，消除“已完成且有未决项”的重复填表矛盾。工作执行结局与回应阶段分开，失败不能履约，可在原预算内恢复；原始资料保留。提交冲突的意向仅向下一轮说明未生效，仍需根据新输入重新提案，不放松新鲜度检查。

运行记录正确汇总工作 runs，并优先显示真实错误；没有提交的候选说明不再冒充“已经提交”。相关 59 项检查、编译、Markdown 引用和前端构建通过，页面纯函数已用实际截图 trace 验证。

隔离核对保留全部先前失败。16K 下的糖果工作完成完整推导，没有截断；最后一次 Qwen 查询读取官方介绍与评测资料，建单、工作终结和带履约关联的表达全部完成，没有参数修正。没有向真实群聊发送测试消息；模型回答仍须结合来源、条件和后续真实互动判断。

修正于 **16:11 上线**。停止 PID 24018 后备份至 `.backups/live-followup-20260906-161106`，新进程 PID 27029 已连接原 OneBot 账号。436 条原始事件逐条哈希保持，115 张运营原图及现有两条样例保持，全部动态配置不变；新增恢复事件正常追加。数据库完整性通过，面板返回 200、未登录受保护接口返回 401，未执行 Reset。

## 连续接话与新表情库

运营提供五张其他 Bot 的连续群聊截图，以及本地 `Diana/表情包` 新素材。对可见承接方式的分析见[连续接话观察](research/20260906-conversation-continuity.md)。本轮仅把参与提示具体化为多人分别回应、保留尚在继续的问题和沿前文细节接话，不推测对方的底层配置，也不复制其现实活动表述。

新包 49 张全部完成格式、哈希与视觉核对，包含 25 张 GIF、24 张静态图；建立动作、台词与用途索引，并选 20 张常用图进入固定目录。替换演练在隔离库中删除旧 115 条运营资产和旧运行文件、新增 49 条及来源事件，重绑两个图样例；其他媒体、配置和原始事件逐项保持。GIF 传输检查确认最大 9.29MB/37 帧文件和庆祝动图均保留原字节，使用 QQ 表情类型。

相关对话/发送上下文的 18 项检查通过；目录缩略图、语义检索、完整动画与替换后的引用可用性已核对。没有新增自动配图比例或关键词触发，也未自动恢复此前删除的人工样例。

**16:47 正式替换完成。** 停机备份位于 `.backups/live-followup-20260906-164735`。删除旧 115 条运营资产及 115 个无其他引用的旧运行文件，新增 49 张和来源记录；固定目录 20 项。切换前 149 条事件及 1 条聊天媒体全部保留，原目录 49 个文件逐一哈希不变，运行配置不变。准备期间控制面板曾执行 Reset，切换前样例库也已经为空；本次替换未执行 Reset 或恢复已删除样例。新进程 PID 29865 已连接原 OneBot，健康检查与数据库完整性通过。

## 历史问题与保留证据

[旧实群审计](research/20260906-live-run-audit.md)记录了角色发挥盖过语境、抵触反馈被当作继续调侃、没有依据的 MC 参与声明、格式修复和数学错误。原话、时间与事件/trace ID 保留；其中糖果答案的适用假设在后续核对中已补充。

此前 115 张运营表情按原图哈希导入，组合词搜索改为任一词匹配并按命中数排序，来源及素材仍保留。本次把按需视觉模型调用替换为原生图像输入；旧[视觉探测失败](evaluation/runs/vision-provider-probe.json)仅描述当时 L 提供商的接口能力，不是 BotCF 的能力结论。

旧评分、固定批量案例、报告导入和源码准入流程已退出日常操作。已提交的真实模型失败保留在 `docs/evaluation/runs`；本次完整匿名化轨迹、演练库和一次性切换脚本保存在本地 `.runtime`，不进入 Git。后续仍以真实群聊的具体问题推进修改。
