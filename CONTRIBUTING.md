# 参与 LenBot 开发

面向核心贡献者；插件作者先走[最小参考插件](docs/plugin-examples.md#一读取与确定性命令业务时钟)。工程要求以[AGENTS.md](AGENTS.md)为准，本页只把现有阅读、修改和交付流程串起来，不新增贡献协议、支持期限或发布授权。

## 先对齐版本与问题

1. 阅读[当前任务](docs/iteration.md)与[路线入口](docs/plan/README.md)，记下实际 Git 提交和工作区状态。历史报告里的行号和结论只属于其标明的版本，不是当前源码依据。
2. 用[任务模板](.github/ISSUE_TEMPLATE/task.md)描述一项用户结果：当前在哪一步断开、已有能力是什么、为何不是重复实现。普通缺陷使用[缺陷模板](.github/ISSUE_TEMPLATE/bug.md)。模板也可在本地整理，不意味着已经提交公开问题。
3. 沿真实业务入口定位所属源码，再读[主链与所有权](docs/architecture.md#主链与所有权)。涉及请求材料读[上下文](docs/context.md)，涉及插件读[接口世代与迁移](docs/plugins.md#接口世代-2-与迁移)，涉及起停或数据读[运行手册](docs/operations.md)和[数据生命周期](docs/data-lifecycle.md)。

不要求修改一个局部问题前通读全部历史计划。计划字段不代表已经存在的接口；新增类型、表或状态前先说明现有结构为何不足。

## 实施一个局部变更

- 复用原事件、作用域、版本、事务、预算与回执边界；失败留在所属阶段，不猜参数、吞错继续或自动换协议。
- 只改本轮业务所需的路径。群配置、模型、人格、样例和真实发送名单不因为开发或教程而改变。
- 依赖安装与初次启动只使用[唯一初始化步骤](docs/operations.md#初始化与启动)。有未授权的服务／平台副作用时不执行；没有获准环境就记录待人工验收，继续独立源码工作。
- 遵守仓库禁止测试、夹具、探针、自动截图及覆盖率任务的要求。代码变更可用 uv 对修改文件做编译；前端变更按工程约束在原目录执行 npm run build，构建产物不提交。编译／构建和实际行为分开记录。
- 行为变化同步所属当前合同，不靠当前任务或历史日志替代稳定文档。新增／改名的页面、响应字段和插件示例必须与同一提交的源码对应。

## 提交前写清楚什么

按[现有变更模板](.github/PULL_REQUEST_TEMPLATE.md)记录：实际改变的符号、配置／数据／接口影响、源码阅读、编译构建、获准正常操作及未确认项。不要把未经观察的分支写成通过；完整原始数据与秘密不进入报告。

代码与对应文档同批，构建产物、运行数据和凭据不进 Git。当前批次写 docs/iteration.md，结束后归档 docs/history；任务状态只在 docs/plan/README.md 维护，不另建进度表。阶段提交、推送、合并、启动和真实发送分别服从当次授权，不因提交成功就部署。

若改动涉及旧数据，说明原事件与业务身份如何保留、哪个旧版本不能读取，以及已发生的费用或平台动作无法由回退撤销。具体备份、转换与恢复命令仍归运行手册，不另写通用迁移器或校验系统。

## 复核与发布

维护者复核源码与对应文档，再在获准同版环境按真实操作验收。源码完成、构建成功、待验收、已验收和已发布不是同一个结论。许可证、素材授权、支持组合与公开发布范围仍按[维护者决策清点](docs/plan/s0-04-06-license-and-support.md)确认，不由一次代码贡献推导。
