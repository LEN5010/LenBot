# 来源

统计口径与话题组织参考 `astrbot_plugin_qq_group_daily_analysis`（`ce320dc`）的 `statistics_calculator.py` 和 `single_daily_report_service.py`：程序统计消息与参与人数，模型按事件和话题整理。本实现直接读取 LenBot 固定范围的原始事件，不迁移独立日报模型、跨群流水线、用户排名或人格点评。

上游作者：Helian Nuits。许可证原文保存在同目录 `LICENSE.upstream`。
