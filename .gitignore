# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# The built control plane stays out of Git.  Vite content-hashes every asset,
# so committing it turned each frontend change into a delete+add churn of ~40
# files that buried the real diff.  It is produced by `npm run build` for local
# runs and by the Node stage in deploy/linux/Dockerfile for deploys; the bare
# `dist/` rule above already covers it, and this anchored rule records intent.
/src/len_bot/web/static/dist/

# Virtual Environment
.venv/
env/
venv/
ENV/

# Testing
.pytest_cache/
.coverage
htmlcov/

# Databases
*.db
*.db-journal
*.db-wal
*.db-shm
/media/

# IDE / OS
.DS_Store
.vscode/
.idea/

# Node / frontend tooling
node_modules/
.vite/

# ZCode session artifacts
.zcode/

# Local trial state and recovery copies
/.backups/
/.runtime/
/lenbot.config.json
/.lenbot-config-*.json
/gateway.config.json
