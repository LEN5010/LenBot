# LenBot 工程约束

产品行为见 [产品文档](docs/product.md)，数据与执行边界见 [架构](docs/architecture.md)，起停、配置和备份见 [运行手册](docs/operations.md)，本轮状态只记在 [当前任务](docs/iteration.md)。已确认的产品要求决定目标；未实现的目标只能记录为待办，不能当作已有能力。

## 修改方式

- 先沿真实路径定位问题：收到消息、读取资料、建立工作、提交表达和保存回执。删除不必要的概念，只改完成本轮业务所需的最短路径。
- 新增层、表、状态或兼容路径前，说明它解决的具体业务缺口；保留现有框架、数据库、事务、版本、权限、登录和回执边界。
- 外部协议在入口解析一次，内部使用明确类型。失败在所属边界记录并结束，不猜字段、不吞错后继续、不自动修参数、不绕过既有权限。
- 社会 Agent 的阶段目标、D01—D12 裁决和验收证据以 [`docs/LenBot_社会Agent_完整实施计划_7a4152d.md`](docs/LenBot_社会Agent_完整实施计划_7a4152d.md) 为准；计划条款是待实现合同，不能当作当前能力或权限。前期只做契约、配置模型和文档收口，保留现有事件、事务、Gate、OneBot 回执与根配置边界。
- 运行参数统一来自项目根目录 `lenbot.config.json`；样例只供人工初始化，不参与运行合并。禁止用 env、dotenv、CLI 或数据库覆盖根配置；运行中由面板保存，手工改配置前先停机。

## 禁止事项

- 不新增、修改或运行测试、夹具、断言式探针、自动截图、覆盖率任务；不修改检查来制造通过结果。
- 不新增通用 checksum、内容指纹、迁移哈希清单、配置签名、备份校验、guardrail、风险评分、审查模型、权限平台、自愈或降级系统；本项目已有的 workspace/browser 执行边界只做其明确缺口的定向修复。
- 不自动逐条检索，不把角色资料、摘要、Bot 发言或模拟结果当作真实证据；具体读取、认识、工作和发送契约遵循产品与架构文档。

## 开发与交付

- 使用 uv。代码行为变化要同步所属文档；本轮实际观察、失败原文和未确认项写入 `docs/iteration.md`，不把静态检查写成运行通过。
- 只做阅读、正常编译或构建，以及已获准环境中的正常启动和人工操作。涉及前端时，在 `src/len_bot/web/frontend` 执行 `npm run build`，检查实际改动页面；不生成自动截图。
- 升级前停机，按 [运行手册](docs/operations.md) 做普通备份；必要结构转换离线执行并保留原始事件与业务身份。
- 本轮不擅自 Reset、启动生产、替换模型/人格/人工样例/Shadow 或实发群名单。提交、推送、生产启动和真实发送分别服从用户当前授权。
- 提交包含同批代码、必要前端构建产物和文档，并说明行为变化、实际核对范围及未确认项；不重写 Git 历史。
