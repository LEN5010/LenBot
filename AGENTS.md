# LenBot 工程约束

产品行为归[产品文档](docs/product.md)，数据与执行边界归[架构](docs/architecture.md)，起停与配置归[运行手册](docs/operations.md)，本轮状态只记在[当前任务](docs/iteration.md)。已确认的产品要求决定目标，代码反映实际实现；尚未实现的目标写入当前任务，不能冒充现有能力。

## 修改方式

先明确用户操作路径，沿收到消息、取资料、建工作或发送的真实处理链定位阻碍，删除多余概念，再修改最短正常路径。新增一层、一张表或一种状态之前，说明缺少它会使哪条本轮业务无法完成。保留原框架、数据库与前端，不按文件长度拆分，不以行数下降证明效果。

配置和外部协议在入口解析一次，内部使用明确类型。失败在负责该边界的位置记录并结束，不猜字段、不吞错后成功、不自动修参数，不增加兼容开关、旧新双写、替代路径或降级系统。已有版本、场景权限、事务、登录与真实回执边界继续履行各自业务职责。

运行参数统一进入项目根目录 `lenbot.config.json`；样例只供人工初始化，不参与运行合并。禁止 env、dotenv、CLI 或数据库覆盖根文件，不把协议字段、工具名、算法常数和普通 CSS 值机械参数化。运行中由面板保存；手工改文件先停机。

## 禁止事项

- 禁止新增、修改或运行测试、测试夹具、断言式临时探针、自动截图和覆盖率任务，无例外。既有测试不决定业务契约，不为它保留兼容方法，也不修改检查来制造通过结果。
- 禁止自建 checksum、内容指纹、迁移哈希清单、配置签名或备份校验流水线；不把一种摘要算法替换成另一种。既有密码派生、协议签名、Git 对象和依赖锁定不在删除范围。
- 禁止新增 guardrail、风险评分、审查模型、通用权限或沙箱平台、自愈及降级系统；不借此扩张已有网络工具权限。
- 不自动每消息检索，不持久化模型生成的兴趣或工作世界，不把角色资料、摘要、Bot 发言或模拟结果当作真实事实依据。具体读取与发送契约见架构。

## 开发与交付

使用 uv，代码与对应现行文档一起更新。只做阅读、正常编译或构建，以及获准环境中的正常启动和人工操作。涉及前端时，在 `src/len_bot/web/frontend` 执行 `npm run build`，检查实际改动页面；不生成自动截图。检查 `git diff --check` 与受影响的 Markdown 引用。

群聊问题记录原话、时间及有关事件、工具资料和发送回执，修改后回到同一真实链路核对。没有固定消息数、评分或批量模型对照要求。没有实际观察到的启动、送达、模型效果或费用不能写成通过。

升级前停机，普通备份指定数据库、媒体与实际配置；必要结构转换显式离线执行，保留原始事件与业务身份。本轮不授权 Reset，不自动启动生产，不自动更换模型、人格、人工样例、Shadow 或实发群名单。管理命令的具体步骤归运行手册。

现行规则只在所属文档维护，其他地方链接。有效内容迁出后可以删除旧文档和失效脚本；真实失败原文保留在 Git 历史及生产记录，不重写提交历史，不另建默认检索的历史归档目录。只记录一份简短当前状态，说明改动、实际看到的结果与未确认项。

提交应包含同批代码、必要前端构建产物与文档，说明行为变化和实际核对范围；提交、推送及生产启动分别按用户当前授权执行，不把一个动作视作其余动作的授权。
