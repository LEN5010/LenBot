# len_bot 工程约束

当前实现与阶段状态见 [架构](docs/architecture.md) 和 [实施记录](docs/implementation.md)。VNext 决策见 [ADR-0044](docs/adr/0044-native-conversation-and-evidence-ledger.md)；被替代 ADR 只记录历史，不是当前实现要求。

## 不变量

1. Event → Runtime State：事件追加且不可变；Session、任务、OpenLoop、认识由事件或获准提案更新。
2. 插件和适配器提供感知与工具，不直接调用认知或发送。Social Core 只有提案权，表达必须经过 Gate。
3. SQLite 保存持续事实；模型上下文是临时的，不能成为永久会话或唯一状态来源。
4. Reflection 仅提出有证据的认识修订和待核对事项，不创建工作、任务或发送。
5. 兴趣、话题和参与意愿属于当轮解释，不持久化为工作世界，也不自动成为任务。
6. `SceneActor` 是事实 `SceneSession` 单写者，不等待模型；验证 episode lease、实际读取截点与 `knowledge_revision`。
7. 普通聊天按实际截点提交；任务、工作控制、履约和 OpenLoop 严格新鲜。未读输入保留，不能用清空 mailbox 掩盖。运营提交不消费未认知的人类消息。
8. 认识、工作状态、回执和行动提案原子提交，冲突回滚且不能发送确认。原始证据和被撤销认识保留。
9. 历史、工具观察和媒体在 SQL/存储层强制场景隔离；global-safe 素材由运营显式发布，不靠提示词限制越权。
10. 不自动每消息 RAG；Social Core 判断回忆、查询、参与和沉默，禁止用关键词、随机性、固定套话替代社会判断。
11. sent/not_sent/rejected/unknown 明确区分；真实 `MESSAGE_SENT` 才证明送达、激活 OpenLoop 或确认履约。Shadow 与模拟事实分开，不确定发送不重试。
12. 实发群名单与全局 Shadow 是唯一运营发送控制。默认开启 Shadow，初始实发群名单为 `group:126300994`；保存值优先，名单外不实发。源码、模型或人格变化不触发重新验收、清空名单或批量隔离任务。内部模拟不能伪装真实群聊。
13. 账号昵称、群名片是事件事实；偏好称呼和明确反馈经有证据的认识提案创建、撤销或替代。昵称/名片字段缺省不清空，同名不合并。

显式 Reset 是删除全部会话数据的管理命令：先停止旧认知、反思、工作和投递，再清理数据库与缓存；保持运行配置、人工表达样例和运营表情库（含素材来源记录）。普通事件处理不能借此绕过追加写入约束。

## 认知与配置

唯一社会认知入口为 `SocialCognitionCore`。对话通过原生工具读取和暂存提案，`finish_turn` 提交零至三条文字/图片消息，空消息列表表示沉默；普通模型正文不会发送。默认预算为三个模型步骤、六次工具执行、24K 文本上下文、4096 输出 tokens。参数修正最多一次并计入预算；工具步骤之间可读取新增输入，终结后不再吸收请求。

联网查询、解题、计算、核实和资料整理经 `start_work` 交给后台 `InformationJobRunner`；前台继续接收新话。默认工作预算为十六个模型步骤、二十四次工具、300 秒、64K 上下文，修订保留已用预算和资料。工作只有只读执行权，结果回到 Social Core 决定表达。

`conversation` 与 `work` 分别显式配置，Reflection 使用 `work`。`ModelGateway` 在一次运行内固定提供商、模型和推理强度；没有默认提供商、自动路由迁移、同轮型号切换、跨型号 fallback 或独立视觉路由。未配置模型时继续保存输入，不发起认知。

近期原话和原图按实际范围提供，媒体窗口与未覆盖部分明确记录。运营样例按固定顺序携带；人格预设经面板显式预览应用并保留人工编辑，启动不覆盖。角色资料、模型摘要和 Bot 自己说过的话不能证明群友事实或 Bot 的现实能力。

## 模块边界

- events/store：事件、持久化和事务；scenes：事实 reducer 与单写 Actor。
- cognition：临时上下文、原生模型循环和提案；runtime：生命周期、Gate、工作执行、预算和指标。
- scheduler/actions：持久认领、调度、传输与回执。
- memory：单一认识账本及只提案的反思，无执行权。
- plugins/tools：工具发现、只读能力和隔离检索；media：受控原图与运营素材；adapters：协议归一化与获准传输。
- web：所有读取经过 RuntimeQueryService，干预使用事件/提案接口，路由不直接读取私有容器或数据库。
- testing：复用生产链路的隔离回放，模拟不能伪装成真实成功。

## 开发流程

使用 uv，代码与对应文档一同更新。从具体问题追踪完整处理链路，按最短路径修改；不添加需求外的兼容层、补丁流程、兜底或降级。

群聊效果主要在真实群聊中判断：记录有问题的原话和时间，查看相关事件、工具结果与发送回执，修改后回到同一实机链路确认。没有固定消息数、评分或批量模型对照要求，不把开发评测做成日常操作流程。

仅运行与改动有关的现有检查；确有必要保护事务、权限或发送边界时才补确定性测试，不默认新增场景集或运行完整回归。涉及前端时在 `src/len_bot/web/frontend` 执行 `npm run build` 并检查实际改动页面。检查 `git diff --check` 与受影响的 Markdown 引用。

升级前停机备份，测试使用隔离库。仅按运营批准的实发群名单和 Shadow 配置发送，不自动更换生产提供商、模型或人格。过时且有效信息已迁出的文档可以删除，真实失败证据保留为历史记录。
